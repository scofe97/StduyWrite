spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&currentSchema=querydsl_practice
    username: postgres.vhirdstmzdzfkvofycfc
    password: 47Ivx19vxSO8HniV
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: false
        default_batch_fetch_size: 100
        jdbc:
          lob:
            non_contextual_creation: true

decorator:
  datasource:
    datasource-proxy:
      logging: slf4j
      query:
        enable-logging: false
      slow-query:
        enable-logging: false

logging:
  level:
    root: warn
    org.hibernate.SQL: debug
    org.hibernate.tool.schema: warn
    org.hibernate.orm.jdbc.bind: trace
    com.runnershigh: info
  pattern:
    console: "%d{HH:mm:ss.SSS} %-5level %logger{0} - %msg%n"
