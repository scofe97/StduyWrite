.DS_Store
.obsidian/
.makemd/
.space/
Tags/
.cursor/
.idea/

# 세션 작업 산출물 — 노트가 아니다
.omo/
.sbh/

# 개인/회사 민감 — 공개 저장소에 올리지 않는다
.omc/
이력서/
resume/
write/_company/
write/_notebooklm/
project/Devops Tps/
project/redpanda-playground/
project/Go-CLI/
issue/
journal/

# 도식 생성기 실행 산출물 (재생성됨)
__pycache__/
*.pyc

# 코드 그래프 도구 캐시 (세션 로컬)
.codegraph/

# 도식 렌더 산출물 — dd-render.sh 가 SVG 를 PNG 로 뽑아 눈으로 확인하는 자리다.
#   문서는 SVG 를 참조하고 mkdocs 도 SVG 를 배포하므로 PNG 는 저장소에 둘 이유가 없다.
#   한 장이 수백 KB 라 도식을 고칠 때마다 바이너리 diff 가 쌓인다. 필요하면 다시 렌더한다.
**/_assets/shots/
**/_assets/qa-*/
