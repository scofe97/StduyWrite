# Ch09. INVESTIGATE — 데이터 저장소 선택과 관리

탐구 질문들을 직접 조사하고 답변을 기록한다.
정답보다 "왜 그렇게 동작하는가"를 설명하는 데 집중할 것.

---

## Q1. CAP 정리와 실제 시스템 선택

**질문**: CAP 정리에서 CP와 AP 중 무엇을 선택해야 하는가? 실제 서비스에서 어떤 기준으로 결정하는가?

**탐구 방향**:
- 네트워크 파티션이 실제로 얼마나 자주 발생하는가?
- "Consistency vs Availability" 트레이드오프가 비즈니스에 미치는 영향은?
- DynamoDB, Cassandra, PostgreSQL 각각이 CAP 어디에 위치하는가?
- 은행 계좌 이체와 소셜 피드 좋아요 수에서 각각 어느 쪽을 선택해야 하는가?

**나의 답변**:

_(여기에 작성)_

---

## Q2. 스키마 마이그레이션 무중단 적용 전략

**질문**: 수백만 행이 있는 테이블에 컬럼을 추가하거나 인덱스를 생성할 때 서비스 중단 없이 적용하려면 어떻게 해야 하는가?

**탐구 방향**:
- `ALTER TABLE ADD COLUMN`이 테이블 락을 거는 조건은 무엇인가?
- PostgreSQL의 `CREATE INDEX CONCURRENTLY`는 어떤 원리로 락 없이 동작하는가?
- Expand-Contract 패턴(컬럼 추가 → 듀얼 쓰기 → 구 컬럼 제거)이란 무엇인가?
- Knex.js에서 무중단 마이그레이션을 구현할 때 주의할 점은?

**나의 답변**:

_(여기에 작성)_

---

## Q3. Redis 캐시 무효화 전략 (TTL vs Event-Driven)

**질문**: TTL 기반 만료와 이벤트 기반 무효화 중 어느 방식이 더 적합한 상황은 각각 언제인가?

**탐구 방향**:
- TTL 기반 무효화의 단점: 갱신 전 최대 TTL 시간만큼 낡은 데이터 제공
- 이벤트 기반(Pub/Sub, 메시지 큐) 무효화의 복잡성 비용은?
- Cache Stampede 문제(동시에 많은 요청이 캐시 미스로 DB를 향하는 현상)를 어떻게 완화하는가?
- Redis의 `WAIT` 명령과 `MULTI/EXEC` 트랜잭션이 캐시 무효화 원자성에 어떻게 기여하는가?

**나의 답변**:

_(여기에 작성)_

---

## Q4. S3 버저닝과 라이프사이클 정책

**질문**: S3 버저닝을 활성화하면 어떤 이점이 생기고, 라이프사이클 정책과 어떻게 조합해야 비용을 통제할 수 있는가?

**탐구 방향**:
- 버저닝 활성화 시 동일 키에 덮어쓰면 어떻게 되는가? (버전 스택)
- 삭제 마커(Delete Marker)란 무엇이고, 실제 데이터는 언제 지워지는가?
- 이전 버전(non-current versions)에 라이프사이클을 적용해 비용을 줄이는 방법은?
- MFA Delete 설정이 왜 랜섬웨어 방어에 도움이 되는가?

**나의 답변**:

_(여기에 작성)_

---

## Q5. 백업 RTO/RPO 설계

**질문**: RTO와 RPO 목표치를 결정하는 요소는 무엇이고, 목표에 맞는 백업 전략은 어떻게 설계하는가?

**탐구 방향**:
- RTO 1시간 / RPO 15분을 달성하려면 어떤 백업 주기와 복구 절차가 필요한가?
- 논리적 백업(pg_dump)과 물리적 백업(pg_basebackup + WAL 아카이빙)의 차이는?
- Point-in-Time Recovery(PITR)는 어떤 원리로 특정 시점으로 복구하는가?
- 백업 검증(복원 테스트)을 자동화하는 방법은?

**나의 답변**:

_(여기에 작성)_

---

## Q6. Polyglot Persistence의 장단점

**질문**: 여러 저장소를 조합하는 Polyglot Persistence는 어떤 상황에서 이점이 있고, 어떤 복잡성을 감수해야 하는가?

**탐구 방향**:
- 단일 DB 대비 Polyglot Persistence가 운영 복잡도를 얼마나 높이는가?
- 데이터 일관성: Redis 캐시와 PostgreSQL 원본이 불일치하면 어떻게 감지하고 해소하는가?
- 서비스 초기(스타트업)에 Polyglot Persistence를 바로 도입하는 것이 올바른 선택인가?
- 분산 트랜잭션 없이 여러 저장소에 걸친 데이터 정합성을 보장하는 패턴은? (Saga, Outbox)

**나의 답변**:

_(여기에 작성)_

---

## 실습 체크리스트

- [ ] Knex.js로 로컬 PostgreSQL에 마이그레이션 실행 후 `knex_migrations` 테이블 확인
- [ ] Redis에 캐시 키를 저장하고 TTL이 만료되는 과정을 `TTL` 명령으로 관찰
- [ ] S3 버킷에 라이프사이클 정책 적용 후 `aws s3api get-bucket-lifecycle-configuration`으로 확인
- [ ] `backup-postgres.sh` 스크립트를 로컬에서 실행하고 덤프 파일이 생성되는지 검증
- [ ] Cache-Aside 패턴에서 캐시 미스와 히트가 로그에 어떻게 찍히는지 확인
