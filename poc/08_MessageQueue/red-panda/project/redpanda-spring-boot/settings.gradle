// I4: Schema Registry Gradle Plugin이 Confluent 저장소의 의존성을 필요로 함
pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://packages.confluent.io/maven/' }
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = 'redpanda-spring-boot'
