# Ch13 Source/Sink & EIP 패턴 실습 목록

appendix-source-sink-connector.md + 13-messaging-patterns-impl.md 문서 기반 — **개념 단위**로 구성

---

## 실습 항목 (8개)

| # | 개념 | 배우는 것 | 문서 위치 | 상태 |
|---|------|----------|----------|------|
| 1 | Event Envelope | CloudEvents 헤더(ce-type, ce-source, ce-id) ProducerInterceptor 자동 주입, Consumer @Header 추출 | 17 line 90~247 | |
| 2 | Dual Write → Outbox 진화 | TransactionSynchronization으로 순서 보장 → Outbox 엔티티 + @TransactionalEventListener → CDC 연동 | 16 line 61~315 | |
| 3 | Event Gateway | REST→Kafka Bridge(EventGatewayController), 이벤트 검증/변환, Rate Limiting(@RateLimiter) 적용 | 16 line 316~457 | |
| 4 | Event Router | @KafkaHandler 다형성 라우팅, KStream.split() 스트림 라우팅, @SendTo 기반 라우팅 — 3가지 비교 | 17 line 248~438 | |
| 5 | Claim Check | S3 대용량 메시지 오프로드, 500KB 임계값 기준 자동 분기, S3StorageService + ClaimCheckProducer/Consumer | 17 line 686~837 | |
| 6 | Event Splitter + Translator | 1→N 분할(OrderSplitter: 주문→아이템별), 스키마 변환(Avro 진화 + Kafka Streams 명시적 변환) | 17 line 439~568 | |
| 7 | Sink Consumer | S3 배치 업로드(배치 리스너 + 주기적 flush), Elasticsearch Bulk API(BulkRequest 배치 색인) | 16 line 458~635 | |
| 8 | 패턴 통합 파이프라인 | Filter→Router→Multi-branch 전체 흐름, Kafka Streams Topology로 파이프라인 구성, TopologyTestDriver 테스트 | 17 line 977~1231 | |

---

## 진행 순서 및 이유

| 순서 | 실습 | 이유 |
|------|------|------|
| 1 | Event Envelope | 모든 이벤트의 메타데이터 표준 — 나머지 패턴의 기반 |
| 2 | Dual Write → Outbox 진화 | Source 패턴의 핵심 — DB 변경을 이벤트로 안전하게 발행 |
| 3 | Event Gateway | 외부 요청을 Kafka로 진입시키는 관문 — REST→이벤트 변환 |
| 4 | Event Router | 이벤트를 조건에 따라 분기 — 파이프라인의 교차로 |
| 5 | Claim Check | 대용량 메시지 처리 — 실무에서 자주 만나는 제약 해결 |
| 6 | Event Splitter + Translator | 이벤트 분할과 변환 — 다른 도메인/스키마 간 연결 |
| 7 | Sink Consumer | 이벤트를 외부 시스템에 적재 — 파이프라인의 종착점 |
| 8 | 패턴 통합 파이프라인 | 1~7 패턴을 조합한 전체 흐름 — 종합 실습 |

---

## 각 실습의 구현 범위

### 1. Event Envelope
- ProducerInterceptor 구현: onSend()에서 CloudEvents 헤더 자동 주입
  - ce-type, ce-source, ce-id, ce-time, ce-specversion
- interceptor.classes 속성에 FQCN 등록 (또는 ProducerPostProcessor)
- Consumer에서 @Header("ce-type") 등으로 메타데이터 추출
- 이벤트 타입별 라우팅에 ce-type 헤더 활용

### 2. Dual Write → Outbox 진화
- 단계 1: 단순 Dual Write (DB 저장 + KafkaTemplate.send) — 문제점 확인
- 단계 2: TransactionSynchronization.afterCommit()으로 순서 보장
- 단계 3: OutboxEvent 엔티티 + @TransactionalEventListener로 Outbox 패턴
- 단계 4: (선택) Debezium CDC 설정으로 폴링 없는 Outbox

### 3. Event Gateway
- EventGatewayController: REST 엔드포인트 → Kafka 이벤트 발행
- 이벤트 검증: @Valid + CloudEvents 헤더 매핑
- 이벤트 변환: REST DTO → Avro/도메인 이벤트
- Rate Limiting: @RateLimiter (Resilience4j) 또는 Bucket4j 적용

### 4. Event Router
- 방식 1: @KafkaListener + @KafkaHandler — 타입별 다형성 라우팅
- 방식 2: KStream.split() — 스트림 기반 조건부 라우팅 (Predicate)
- 방식 3: @SendTo — 반환값 기반 정적 라우팅
- 세 방식의 적합 시점 비교 문서

### 5. Claim Check
- S3StorageService: putObject/getObject 래퍼 (AWS SDK v2)
- ClaimCheckProducer: 메시지 크기 > 500KB → S3 업로드 + 참조 키만 Kafka로 전송
- ClaimCheckConsumer: 참조 키 수신 → S3에서 원본 다운로드 → 처리
- build.gradle S3 의존성 설정

### 6. Event Splitter + Translator
- Splitter: 주문 이벤트 → 주문 항목별 개별 이벤트로 분할 (1→N)
- Translator: Avro 스키마 진화(호환 변환) 활용한 자동 변환
- Kafka Streams 명시적 변환: mapValues()로 도메인 간 스키마 변환
- @KafkaListener 기반 변환 서비스 (간단한 경우)

### 7. Sink Consumer
- S3 Sink: 배치 리스너(BatchMessageListener) + 주기적 flush (시간/건수 기준)
- Elasticsearch Sink: BulkRequest 배치 색인 + 실패 항목 재시도
- 배치 모드 ContainerFactory 설정 (ConcurrentKafkaListenerContainerFactory.setBatchListener)
- 외부 시스템 장애 시 재시도/DLQ 처리

### 8. 패턴 통합 파이프라인
- 전체 흐름: REST 수신(Gateway) → 검증(Filter) → 라우팅(Router) → 변환(Translator) → 적재(Sink)
- Kafka Streams Topology로 파이프라인 구성
- TopologyTestDriver로 단위 테스트
- Testcontainers + Redpanda 통합 테스트

---

## 기존 챕터와의 관계

- **Ch03 SAGA**: Outbox 패턴(실습 2)은 SAGA 이벤트 발행의 안전한 대안
- **Ch08 트랜잭션**: Dual Write 문제(실습 2)는 Ch08 DB+Kafka 원자성의 실전 적용
- **Ch13 파이프라인**: Router(실습 4)는 Filter-Branch 패턴의 EIP 관점 구현
- **Ch06 직렬화**: Avro 스키마 진화(실습 6)는 Ch06 Avro 설정의 심화 활용
