# 기본 설정 파일
# 환경변수로 오버라이드 가능 (APP_ 접두사)
# 예: APP_SERVER_PORT=9090

server:
  host: "localhost"
  port: 8080

database:
  host: "localhost"
  port: 5432
  name: "myapp"
  user: "postgres"
  password: "secret"

log:
  level: "info"
  format: "console"  # console or json
