version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries/"
    schema: "db/schema.sql"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_json_tags: true
        emit_empty_slices: true
