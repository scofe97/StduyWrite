services:
  lab:
    build: .
    volumes:
      - .:/app
    cap_add:
      - SYS_PTRACE  # strace 사용을 위해 필요
    security_opt:
      - seccomp:unconfined  # strace 전체 기능 사용
    stdin_open: true
    tty: true
    working_dir: /app
