# sqlc generate 명령어로 이 디렉토리에 코드가 생성됩니다.
# sqlc.yaml 설정에 따라 db 패키지가 생성됩니다.
#
# 사용법:
# 1. sqlc 설치: go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest
# 2. 코드 생성: sqlc generate
#
# 생성되는 파일:
# - db.go: 데이터베이스 연결 인터페이스
# - models.go: 테이블에 대응하는 Go 구조체
# - posts.sql.go: posts.sql 쿼리에 대응하는 함수
