# 개발 환경 설정
# config.yaml 위에 병합됨

server:
  port: 3000  # 개발 환경에서는 3000 포트 사용

database:
  host: "localhost"
  name: "myapp_dev"  # 개발용 DB

log:
  level: "debug"  # 개발 환경에서는 debug 레벨
  format: "console"
