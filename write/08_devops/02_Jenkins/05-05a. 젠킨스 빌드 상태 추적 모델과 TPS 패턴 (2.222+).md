---
title: 젠킨스 빌드 상태 추적 모델과 TPS 패턴 (2.222+)
tags: []
status: draft
related: []
updated: 2026-04-19
---

# 젠킨스 빌드 상태 추적 모델과 TPS 패턴 (2.222+)
---
> 이 문서는 Jenkins 빌드 상태 추적에서 TPS가 사용하는 모델과 운영 해석 포인트를 설명하는 이론 문서다.
>
> - DB와 Jenkins 경로 매칭, 상태 매핑, SyncJob을 다룬다.
> - Blue Ocean/Workflow API 해석과 현대화 판단은 `05-05b`에서 별도로 다룬다.
> - 순수 상태 조회 endpoint는 `05-05`에서 별도로 다룬다.

## 1. DB와 Jenkins 파이프라인 매칭

> TPS와 Jenkins 사이의 파이프라인 식별은 폴더 경로 기반이다.

TPS DB에 저장된 필드와 Jenkins 경로는 보통 이렇게 대응된다:

```text
TPS DB 레코드                   Jenkins URL 경로
taskCd  = ACME-001              /job/ACME-001
envrnCd = DEV                   /job/ACME-001/job/DEV
bizNm   = payment-build         /job/ACME-001/job/DEV/job/payment-build
```

- 이 변환을 담당하는 것이 `PipelineStructVo`다. `taskCd`, `envrnCd`, `bizNm`으로 Jenkins 폴더 경로를 구성하고, `bizNm`이 비어 있으면 트리거 파이프라인으로 해석한다.

### 1-1. 빌드 번호 매칭

빌드 실행 시에는 경로뿐 아니라 `buildNumber`까지 맞춰야 한다. 문제는 Jenkins 빌드 트리거가 비동기라는 점이다.

TPS는 보통 다음 순서를 쓴다:

1. 빌드 트리거
2. `queueId` 확보
3. `buildNumber` 확보
4. `struct + buildNumber`를 DB에 저장

이 정보가 있어야 이후 상태 조회와 로그 조회가 같은 빌드를 가리킬 수 있다.

### 1-2. VO 구조 요약

TPS에서 상태 추적까지 포함해 자주 등장하는 VO는 다음과 같다:

| VO | 역할 | 주요 필드 |
|------|------|------|
| `JenkinsToolVo` | Jenkins 서버 접속 정보 | `url`, `id`, `password`, `basicAuth` |
| `PipelineStructVo` | 파이프라인 폴더 경로 | `taskCd`, `envrnCd`, `bizNm` |
| `JenkinsJobVo` | 파이프라인 전체 정의 | `pipelineStructVo`, `jenkinsFile`, `jenkinsJobParamVoList` |



## 2. Jenkins 상태를 TPS 상태로 어떻게 바꾸는가

> Jenkins는 세분화된 상태를 쓰고, TPS는 더 적은 상태 코드로 단순화한다.

### 2-1. Jenkins는 상태를 한 값으로만 표현하지 않는다

Jenkins는 "현재 상태"를 하나의 enum으로만 주지 않는다. 조회하는 API와 시점에 따라 서로 다른 상태 신호를 준다.

대표적으로 나뉘는 축은 다음과 같다:

| 구분 | 어디서 보나 | 상태 예시 | 의미 |
|------|------|------|------|
| 큐 상태 | `/queue/api/json` | `WaitingItem`, `BlockedItem`, `BuildableItem` | 아직 빌드 번호가 생기기 전 대기 상태 |
| Job 메타 상태 | `/{pipelineStruct}/api/json` | `inQueue`, `buildable`, `color=blue_anime` | Job 레벨의 빠른 상태 |
| 코어 빌드 상태 | `/{pipelineStruct}/{buildNumber}/api/json` | `building=true`, `result=SUCCESS` | 빌드 전체의 실행/종료 상태 |
| Workflow 전체 상태 | `wfapi/describe` | `SUCCESS`, `FAILED`, `PAUSED_PENDING_INPUT` | 파이프라인 전체 관점의 상태 |
| 스테이지 상태 | `wfapi/describe.stages[]` | `IN_PROGRESS`, `NOT_EXECUTED`, `ABORTED` | 개별 stage 관점의 상태 |

즉 Jenkins 상태를 해석할 때는 "어느 API의 어떤 필드인가"를 먼저 구분해야 한다.

### 2-2. 실제로 어떤 상황일 때 그런 상태가 되는가

자주 보는 상태를 상황별로 풀면 다음과 같다:

| 상태 신호 | 언제 그렇게 되는가 |
|------|------|
| `queue: WaitingItem` | Quiet Period를 기다리거나, 큐에 들어갔지만 아직 실행 준비 전일 때 |
| `queue: BlockedItem` | 이전 빌드, 동시 실행 제한, 자원 부족 등으로 시작이 막혔을 때 |
| `queue: BuildableItem` | 실행 조건은 충족했지만 executor나 agent를 아직 못 잡았을 때 |
| `building=true` | 빌드가 실제 시작되어 아직 끝나지 않았을 때 |
| `result=null` | 빌드가 아직 종료되지 않았을 때 |
| `result=SUCCESS` | 스크립트와 post 처리까지 정상 종료됐을 때 |
| `result=FAILURE` | `error`, shell exit code 비정상 종료, 예외 등으로 실패했을 때 |
| `result=UNSTABLE` | 테스트 실패나 품질 경고처럼 완전 실패는 아니지만 불안정 판정일 때 |
| `result=ABORTED` | 사용자가 stop을 누르거나 timeout, 강제 중단 정책이 적용됐을 때 |
| `wfapi status=IN_PROGRESS` | 파이프라인이 현재 실행 중일 때 |
| `wfapi status=PAUSED_PENDING_INPUT` | `input` 스텝에서 승인/입력 대기 중일 때 |
| `wfapi status=NOT_EXECUTED` | `when`, 이전 stage 실패, 분기 미진입 등으로 해당 stage가 실행되지 않았을 때 |
| `color=blue_anime`, `red_anime` | Job 레벨에서 최근 결과 색상 + 현재 실행 중 표시가 같이 붙을 때 |

중요한 점은 `result`와 `wfapi status`가 서로 다른 층위라는 것이다.

- `result`는 빌드 전체 종료 결과다.
- `wfapi status`는 파이프라인 전체 진행 상태나 stage 상태를 더 잘게 보여준다.
- `color`는 대시보드용 빠른 신호에 가깝다.

그래서 TPS처럼 운영 시스템이 상태를 수집할 때는 보통 다음 순서로 해석하는 편이 낫다:

1. 큐 단계에서는 queue API를 본다.
2. 실행 여부와 최종 결과는 코어 build API를 본다.
3. 승인 대기나 stage 진행률은 `wfapi/describe`를 본다.

대표적인 매핑은 다음과 같다:

| Jenkins 상태 | TPS 상태 | 의미 |
|------|------|------|
| `IN_PROGRESS` | `EXCN` | 실행 중 |
| `PAUSED_PENDING_INPUT` | `EXCN` | 승인 대기지만 아직 끝나지 않음 |
| `SUCCESS` | `CMPTN` | 완료 |
| `FAILED` | `FAIL` | 실패 |
| `FAILURE` | `FAIL` | 실패 |
| `UNSTABLE` | `FAIL` | 불안정 |
| `ABORTED` | `RJCT` | 사용자 중지 |
| 기타 | `WAIT` | 대기 또는 미분류 |

### 2-3. 왜 `PAUSED_PENDING_INPUT`도 `EXCN`인가

TPS 관점에서 승인 대기는 "종료되지 않은 실행"이다. 그래서 사용자는 실행 중으로 보되, 승인 버튼 노출 여부는 별도 로직으로 분리하는 편이 상태 해석을 단순하게 만든다.

### 2-4. `sttsCdConverter()` 같은 매핑 함수의 의미

매핑 로직은 한 곳에 모으는 편이 맞다. 그래야:

- 폴링 잡
- 승인 대기 체크
- 로그 수집 트리거

이 세 흐름이 같은 상태 해석을 공유할 수 있다.



## 3. PipelineSyncJob과 폴링 전략

> Jenkins는 중간 상태 변화를 외부 시스템에 세밀하게 푸시하지 않기 때문에, TPS는 폴링 잡을 둔다.

`PipelineSyncJob`은 보통 다음 역할을 한다:

- DB에서 실행 중(`EXCN`) 파이프라인 조회
- `wfapi/describe` 호출
- Jenkins 상태를 TPS 상태로 변환
- DB 상태 갱신

10초 같은 짧은 주기를 쓰는 이유는 실행 중인 빌드에만 제한적으로 적용하기 때문이다. 빌드가 끝나면 더 이상 폴링 대상이 아니므로 부하가 제어된다.

### 3-1. 로그 적재와의 관계

로그 수집 작업은 상태 동기화에 의존할 수 있다. 

- 예를 들어 상태가 여전히 `EXCN`이면, 실제 Jenkins에서는 끝났어도 로그 적재 작업이 아직 수집 대상으로 보지 않을 수 있다.
- 즉 상태 동기화는 로그 적재의 선행 조건이 될 수 있다.



## 4. 추천 상태 추적 순서

빌드 트리거 이후 상태 추적은 보통 이렇게 본다:

1. `queueId` 확보
2. `buildNumber` 확보
3. `GET /{pipelineStruct}/{buildNumber}/api/json`으로 `building`, `result` 확인
4. `GET /{pipelineStruct}/{buildNumber}/wfapi/describe`로 스테이지 상태 확인
5. 완료 후 로그 적재나 후속 상태 전이 수행

즉 코어 API는 "빌드 전체 상태", Workflow API는 "파이프라인/스테이지 상태"라는 역할 분리가 핵심이다.



## 5. 문서 구분

이 영역 문서는 다음처럼 보면 된다:

- `05-04`: 빌드를 어떻게 실행하는가
- `05-05`: 실행된 빌드 상태를 어떤 API로 조회하는가
- `05-05a`: 그 상태를 TPS에서 어떻게 저장하고 매핑하는가
- `05-05b`: Blue Ocean과 Workflow API를 현재 Jenkins 관점에서 어떻게 해석하는가
- `05-06`: 완료된 빌드 로그를 어떻게 조회하고 적재하는가



## 6. 참고 링크

- `05-05. 젠킨스 빌드 상태 추적 API 스펙`
- `05-05b. 젠킨스 상태 추적 API 현대화와 Blue Ocean 해석.md`
