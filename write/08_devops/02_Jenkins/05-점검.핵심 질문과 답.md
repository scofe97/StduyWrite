# 5단계 점검 — Jenkins API 핵심 질문

---

> 이 단계를 마쳤다면 다음 질문에 답할 수 있어야 한다.
> 다루는 문서: 05-01~05-09. Jenkins REST API 구조와 연동, 인증 모델, 파이프라인 CRUD, 빌드 트리거·Queue 추적, 상태 추적(wfapi), 로그 조회, 크레덴셜 관리, 쿼리 최적화, 배포 승인과 운영 관리

## Q1: Jenkins REST API 인증에서 비밀번호 대신 API Token을 사용하는 이유는?

API Token이 비밀번호보다 나은 이유:

- **개별 폐기 가능**: 토큰 유출 시 해당 토큰만 폐기하면 되므로 영향 범위가 최소화된다.
- **계정 비밀번호와 독립**: Jenkins 계정 비밀번호를 변경해도 토큰 기반 스크립트는 깨지지 않는다.
- **사용 기록 추적 가능**: 토큰별 API 호출 기록을 남길 수 있다.

| 전달 방식 | 형식 |
|-----------|------|
| HTTP 헤더 | `Authorization: Basic base64(username:apiToken)` |
| curl 축약 | `-u admin:API_TOKEN` |

TLS 없이 사용하면 토큰이 평문으로 노출되므로 HTTPS 적용은 필수다.

### 심화

토큰을 환경변수가 아닌 Jenkinsfile 평문에 하드코딩하면 어떤 문제가 발생하는가?



## Q2: Jenkins CSRF Protection이 동작하는 원리는? crumb는 왜 필요한가?

CSRF 공격 시나리오:

- 악의적인 웹 페이지가 `<form action="http://jenkins/job/deploy/build" method="POST">`를 삽입하면, Jenkins에 로그인된 사용자의 브라우저가 세션 쿠키를 자동으로 첨부해 빌드를 트리거할 수 있다.

crumb 방어 흐름:

1. `GET /crumbIssuer/api/json` → `{"crumb": "abc123", "crumbRequestField": "Jenkins-Crumb"}` 수신
2. 이후 모든 POST 요청에 `Jenkins-Crumb: abc123` 헤더 포함
3. crumb는 사용자 세션에 바인딩 → 다른 세션의 crumb로는 요청이 거부됨

### 심화

crumb 만료 시 자동화 스크립트에서 어떻게 처리하는가?



## Q3: `tree` 파라미터와 `depth` 파라미터의 차이는?

| 파라미터 | 동작 방식 | 특징 |
|----------|-----------|------|
| `depth=N` | 응답 JSON 중첩 깊이를 숫자로 제어 | 예측하기 쉬움, 불필요한 필드도 포함 |
| `tree=필드명` | 반환할 필드를 명시적으로 지정 | 정밀 제어, 응답 크기 최소화 |

- `?tree=builds[number,result]{0,5}` 형태로 필드명과 배열 범위를 함께 지정할 수 있다.
- 대량 빌드 목록 조회나 모니터링 폴링에는 `tree`를 우선한다. Controller 직렬화 부하도 낮춘다.

### 심화

빌드 결과를 polling할 때 exponential backoff를 써야 하는 이유는?



## Q4: 파라미터 빌드 트리거 후 Queue Item → Build Number를 어떻게 추적하는가?

빌드는 즉시 실행되지 않고 큐에 등록되므로 4단계 추적이 필요하다:

1. **crumb 발급**: `GET /crumbIssuer/api/json`
2. **빌드 트리거**: `POST /job/my-pipeline/buildWithParameters` → 응답 `201 Created`의 `Location` 헤더에 큐 아이템 URL이 담긴다.
3. **빌드 번호 확인**: 큐 아이템 URL에 `api/json` 추가 → `.executable.number`가 null이 아닐 때까지 폴링
4. **빌드 결과 조회**: `/job/my-pipeline/{BUILD_NUM}/api/json?tree=result,duration`

- `result`가 null이면 아직 실행 중이다.
- `SUCCESS`, `FAILURE`, `ABORTED` 중 하나가 채워지면 완료된 것이다.

### 심화

Queue에서 빌드가 영원히 빠지지 않는 원인 3가지는?



## Q5: Generic Webhook Trigger와 SCM Webhook은 어떤 상황에서 각각 선택하는가?

| 구분 | SCM Webhook | Generic Webhook Trigger |
|------|-------------|------------------------|
| 용도 | GitHub/GitLab 코드 변경 이벤트 전용 | 비-SCM 이벤트 (Slack, AlertManager, JIRA 등) |
| 엔드포인트 | `/github-webhook/` | `/generic-webhook-trigger/invoke` |
| 파싱 | 플러그인이 자동 파싱 | JSONPath로 직접 파싱, 환경 변수 추출 |
| 인증 | GitHub Secret Token으로 payload 서명 검증 | URL의 `token` 파라미터 (URL에 노출, 상대적으로 취약) |

- SCM Webhook + Multibranch Pipeline 조합: 브랜치 자동 감지와 PR 빌드가 기본 제공된다.
- Generic Webhook: 어떤 시스템과도 연동 가능하지만 설정이 복잡하다.

### 심화

Generic Webhook Trigger에서 동일 이벤트가 중복 전달될 때 어떻게 멱등성을 보장하는가?



## Q6: `/build`와 `/buildWithParameters`의 차이는?

| 엔드포인트 | 대상 Job | 동작 |
|------------|----------|------|
| `/build` | 파라미터 없는 Job | 즉시 실행 |
| `/buildWithParameters` | 파라미터가 정의된 Job | `--data-urlencode 'KEY=VALUE'` 형태로 파라미터 전달 |

- 파라미터가 있는 Job에 `/build`를 호출하면 기본값으로 실행되거나 `400 Bad Request`가 반환된다.

### 심화

Jenkins API로 배포 승인(Input step)을 프로그래밍 방식으로 처리하려면 어떤 엔드포인트를 사용하는가?
