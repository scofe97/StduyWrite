---
title: MySQL Operator 점검
tags: [kubernetes, mysql, operator, innodb-cluster, review]
status: final
source:
  - ../../../../poc/03_CloudNative/02-kubernetes/learning/08-mysql-operator/INVESTIGATE.md@8ac9e97
related:
  - ../06-02.MySQL Operator.md
  - ../06-01.Operator 패턴.md
updated: 2026-04-19
---

# MySQL Operator 점검

> 본 장의 심화 점검 질문이다. LEARN에서 다룬 개념의 경계와 운영 환경에서 주의할 판단 포인트를 Q&A 형태로 정리했다.



## 점검 질문
> MySQL Operator 운영 포인트를 장애와 복구 관점에서 다시 점검한다.

### Q1. Group Replication이 전통적인 비동기 복제와 어떻게 다른가?

전통적인 MySQL 비동기 복제는 Primary가 binlog를 Secondary로 전송하고 Secondary가 비동기로 적용하는 구조다. Primary가 죽으면 Secondary가 어디까지 적용했는지 확실히 알 수 없어 데이터 손실 가능성이 있고, 수동 개입 없이 승격이 불가능하다.

Group Replication은 Paxos 기반 합의 프로토콜로 이 문제를 해결한다. 트랜잭션이 Primary에서 실행되면 변경 내용이 그룹에 브로드캐스트되고, 과반수(Quorum)가 승인하면 커밋된다. 이후 모든 멤버가 동일한 순서로 트랜잭션을 적용하므로 일관성이 보장된다.

충돌 감지도 내장되어 있다. Multi-Primary 모드에서 같은 행을 수정하면 First-Commit-Wins 정책으로 먼저 커밋된 트랜잭션이 승리하고 나머지는 롤백된다. Single-Primary 모드는 하나의 Primary만 쓰기를 받으므로 이런 충돌이 발생하지 않는다.

3개 클러스터 기준으로 2개가 살아있으면 Quorum이 유지되어 계속 쓰기가 가능하다. 2개가 죽으면 Quorum이 깨지고 남은 1개는 읽기 전용으로 전환된다. 이는 스플릿 브레인(네트워크 파티션으로 두 그룹이 각각 Primary를 선출하는 상황)을 방지하기 위한 설계다.

심화. 5개 클러스터는 2개 장애를 허용하고 3개 클러스터는 1개 장애를 허용한다. Flow Control은 느린 멤버가 복제를 따라가지 못할 때 전체 처리량을 제한하는 메커니즘으로, 과도한 지연 누적을 방지하지만 일시적인 쓰기 성능 저하를 유발한다.

### Q2. MySQL Router가 필요한 이유는 무엇인가?

Group Replication에서 Primary는 언제든 바뀔 수 있다. 애플리케이션이 Pod에 직접 연결하면 "현재 Primary가 누구인가?"를 계속 추적해야 하는데, 이는 애플리케이션의 책임이 아니다.

MySQL Router는 이 추적을 대신 처리한다. 6446 포트(Read-Write)로 연결하면 Router가 현재 Primary로만 라우팅하고, Primary가 바뀌면 자동으로 새 Primary로 전환한다. 6447 포트(Read-Only)로 연결하면 Primary와 Secondary 간 라운드 로빈으로 분산하여 읽기 부하를 줄인다.

연결 풀 관리 효과도 있다. 애플리케이션이 수백 개의 연결을 열어도 Router는 MySQL에 적은 수의 연결만 유지할 수 있어 `max_connections` 제한을 완화한다. Primary 장애 시에는 새 Primary 선출을 기다렸다가 자동으로 재연결하므로, 애플리케이션은 연결 재시도 로직만 구현하면 된다.

심화. Router는 Stateless이므로 여러 개 배포해도 문제없고 Service LoadBalancer가 분산을 담당한다. 6447 포트의 읽기 분산에서 Replication Lag가 있으면 사용자가 방금 입력한 데이터를 못 볼 수 있다. 이를 해결하려면 쓰기 후 일정 시간은 6446으로 유도하거나(세션 스티키니스) 동기 복제를 사용한다.

### Q3. Primary Pod 삭제 시 누가 새 Primary를 선출하는가?

Primary Pod가 죽으면 나머지 Secondary들이 Group Replication 프로토콜로 새 Primary를 선출한다. 이 과정은 MySQL 자체 기능이고, Operator는 개입하지 않는다. 보통 5~10초 내에 완료된다.

Operator는 그 이후의 복구 작업을 담당한다. Pod가 삭제되었음을 감지하고 StatefulSet Controller와 협력하여 새 Pod를 생성한다. 새 Pod가 시작되면 MySQL Shell을 호출하여 이 Pod를 클러스터에 추가한다. 이 과정에서 데이터 동기화(Clone 또는 Incremental Recovery)가 발생하며, 해당 Pod는 Secondary로 합류한다.

MySQL Router는 `performance_schema.replication_group_members`를 주기적으로 폴링하여 현재 Primary를 추적하므로, Primary가 바뀌면 자동으로 새 Primary로 연결을 전환한다. Operator 개입 없이 동작한다.

Quorum이 깨져서 클러스터 전체가 읽기 전용이 된 경우에는 Operator가 `force-quorum-using-partition-of` 명령을 실행하여 클러스터를 재구성한다. 이 상황에서는 수동 개입이 필요할 수 있다.

심화. Primary 선출 중(5~10초) 쓰기 트랜잭션이 들어오면 실패하므로 애플리케이션은 재시도 로직이 필요하다. StatefulSet Pod가 재생성될 때 PV는 Pod 이름과 바인딩되어 있어 데이터가 보존된다.

### Q4. InnoDBCluster CR의 주요 spec 필드는 무엇인가?

`instances`는 MySQL 인스턴스 개수이며 StatefulSet의 replicas로 변환된다. HA를 위해 최소 3이 권장되고, 스플릿 브레인 방지를 위해 홀수(3, 5, 7)로 설정해야 한다.

`router.instances`는 MySQL Router 개수이며 Deployment의 replicas로 변환된다. Router는 Stateless이므로 SPOF 방지를 위해 2~3으로 설정하는 것이 좋다.

`version`은 MySQL 버전이다. 프로덕션에서는 `latest` 대신 `8.0.35`처럼 명시적 버전을 지정해야 한다.

`tlsUseSelfSigned`는 자체 서명 인증서 사용 여부다. `true`로 설정하면 Operator가 자동으로 인증서를 생성하고 인스턴스 간 통신이 TLS로 암호화된다. 프로덕션에서는 `false`로 설정하고 실제 CA 인증서를 제공해야 한다.

`datadirVolumeClaimTemplate`는 PVC 템플릿으로 StatefulSet의 `volumeClaimTemplates`로 변환된다. 프로덕션에서는 SSD 기반 StorageClass를 사용하고 용량은 데이터 크기의 2~3배로 여유 있게 설정한다.

`podSpec.resources`는 Pod CPU/메모리 제한이다. InnoDB Buffer Pool은 메모리의 70~80%로 설정되므로 메모리가 2Gi면 Buffer Pool은 약 1.5Gi다.

심화. `instances`를 3에서 5로 변경하면 Operator가 Rolling Update로 Pod를 추가하므로 클러스터 재시작이 필요 없다. `tlsUseSelfSigned: true`로 설정해도 Router가 TLS를 종료하므로 애플리케이션은 평문으로 Router에 연결할 수 있다.

### Q5. StatefulSet 기반 MySQL과 Operator 기반 MySQL의 차이는?

StatefulSet만 사용하는 경우 MySQL의 Replication 설정, Primary/Secondary 구성, 장애 복구를 모두 수동으로 처리해야 한다. 각 Pod에 접속하여 `CHANGE MASTER TO` 명령을 실행하는 식이다.

Operator는 InnoDBCluster CR을 감시하고 필요한 모든 리소스(StatefulSet, Service, Secret, ConfigMap)를 생성한다. Day-2 Operation이 핵심 차이다. Primary Pod가 죽으면 Operator가 즉시 감지하고 MySQL Shell을 호출하여 클러스터를 재구성한다. 백업 스케줄, 모니터링 메트릭, TLS 인증서 갱신도 자동으로 처리된다.

선언적 관리 철학에서도 차이가 있다. StatefulSet 방식은 "어떻게(How)"를 명시하는 명령형이고, Operator 방식은 "무엇을(What)" 원하는지 선언하면 상태가 틀어질 때 Operator가 자동으로 재조정(Reconcile)한다.

Operator를 사용하면 MySQL 관리는 쉬워지지만 Operator 자체를 관리해야 하는 복잡도가 추가된다. 그러나 소규모 팀이거나 여러 DB를 관리해야 한다면 Operator가 훨씬 효율적이다.

심화. Operator가 죽어도 기존 MySQL 클러스터는 계속 동작하지만, 장애 복구나 스케일 아웃은 불가능하다.

### Q6. mysqlsh로 클러스터 상태를 어떻게 확인하는가?

```bash
# Pod 내부에서 접속
kubectl exec -it mycluster-0 -- mysqlsh --uri root@localhost:3306

# 클러스터 객체 획득 및 상태 확인
var cluster = dba.getCluster()
cluster.status()
```

`cluster.status()` 출력에서 `defaultReplicaSet.primary`가 현재 Primary 주소를 나타내고, `topology` 섹션의 각 멤버의 `status: "ONLINE"`과 `replicationLag`로 복제 상태를 확인한다.

SQL로 직접 확인하려면:

```sql
SELECT MEMBER_HOST, MEMBER_ROLE, MEMBER_STATE
FROM performance_schema.replication_group_members;
```

`status`가 `OK`이고 모든 멤버가 `ONLINE`이면 Quorum이 정상이다. `NO_QUORUM`이나 `OFFLINE` 멤버가 있으면 장애 상황이다.

심화. `cluster.status({extended: 1})`을 실행하면 네트워크 통신량, 트랜잭션 처리량 등 추가 정보를 볼 수 있다. `ERROR` 상태의 인스턴스는 `cluster.rejoinInstance()`로 복구할 수 있지만, 데이터 불일치가 심한 경우에는 전체 재구성이 필요할 수 있다.

### Q7. minikube에서 MySQL HA 테스트의 한계는?

minikube는 단일 노드 클러스터라서 3개 MySQL Pod가 모두 같은 물리 머신에서 동작한다. 프로덕션에서 `podAntiAffinity`로 Pod를 다른 노드에 분산시키는 구성은 minikube에서 의미가 없다.

노드 장애 테스트를 할 수 없다. 프로덕션에서 "노드 하나가 통째로 죽으면 어떻게 되는가?"를 검증해야 하는데, minikube는 노드가 하나뿐이므로 이 시나리오를 재현할 수 없다.

네트워크 파티션 시뮬레이션도 어렵다. `iptables`로 Pod 간 트래픽을 차단하는 방법이 있지만 복잡하고 불완전하다. 스토리지 장애나 I/O 오류 시나리오도 마찬가지다.

그럼에도 minikube는 "기본 동작 확인"에 충분하다. Operator 설치, CR 작성, 클러스터 생성, Router 연결, Pod 삭제 복구 같은 Happy Path를 빠르게 검증하는 용도로 적합하다.

심화. kind나 k3d로 멀티 노드 환경을 로컬에서 시뮬레이션할 수 있지만 리소스 소모가 크다. Chaos Mesh나 Litmus 같은 도구를 설치하면 네트워크 파티션, Pod 장애를 더 정교하게 시뮬레이션할 수 있다.

---



## 관련 문서

- [MySQL Operator](../06-02.MySQL%20Operator.md) — 본 실습의 개념 편
- [gcp 스킬](../../../../.claude/skills/gcp/SKILL.md) — 실습 대상 클러스터의 운영 문서
