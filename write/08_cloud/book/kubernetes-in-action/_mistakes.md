---
title: Kubernetes in Action 오답 노트
tags: [kubernetes, mistakes, review]
status: in_progress
updated: 2026-08-12
---

# Kubernetes in Action 오답 노트

## 2026-08-12 — 단위와 판정 주체를 놓침 (17-01 DaemonSet 4-Phase · Phase 1 통과 · Phase 4 5축 중 2문항 막힘)

- **무엇**: 17-01 DaemonSet 기초를 `learning-session` 4-Phase로 진행했습니다. kind 클러스터가 없어 Phase 3은 생략하고 실습 연결을 뺀 5축으로 검증했습니다. Phase 1은 다섯 문항 모두 자력 또는 되물음 한 번으로 통과했고, Phase 4에서 두 문항이 막혔습니다. 막힌 둘의 뿌리가 같아 축으로 묶어 기록합니다.

- **★ 뿌리 A — 세는 대상의 단위를 놓쳤습니다**: 두 오개념이 한 뿌리에서 나왔습니다.
  - **자기 답 (Phase 1 Q1)**: "레플리카가 없다는 건 DaemonSet이 무조건 **1개만** 올라간다는 의미 아닌가"로 답했습니다. 되물음("노드 5개면 파드 몇 개일까요") 한 번에 **스스로 5개로 교정**했습니다.
  - **자기 답 (Phase 4 Q1)**: `DESIRED 2 / CURRENT 3` 출력에서 `CURRENT`를 "실제 올라간 **파드 수**"로 읽고 왜 3인지 막혔습니다. "여기선 노드 수가 되나요"라고 되물었습니다.
  - **정답**: DaemonSet status 6필드(`currentNumberScheduled`·`desiredNumberScheduled`·`numberAvailable`·`numberMisscheduled`·`numberReady`·`updatedNumberScheduled`)는 이름이 파드를 세는 것처럼 생겼지만 **전부 노드 수**입니다. `numberReady`는 "ready 파드 수"가 아니라 "ready daemon 파드가 도는 **노드** 수"입니다. 관심사가 "파드 몇 개 돌리나"가 아니라 "**몇 개 노드를 커버하나**"이기 때문입니다. `DESIRED 2 / CURRENT 3`이 모순이 아닌 이유는 두 숫자의 **단위가 갈리는 순간**이 있기 때문입니다 — 업데이트 중 한 노드에서 옛 파드가 `Terminating`으로 사라지는 동안 새 파드가 만들어져 총 3개가 됩니다. `AVAILABLE 2`가 그대로인 것이 단서입니다(겹친 파드가 커버 노드를 늘려 주지 않음).
  - **원인 추정**: `replicas`가 없다는 사실에서 "숫자를 안 적는다 → 하나뿐이다"로 건너뛰었습니다. 실제 함의는 "숫자를 **클러스터 상태에서 파생**시킨다"입니다. 같은 건너뛰기가 status 필드에서 재발했습니다 — 필드 **이름**(파드처럼 생김)을 단위의 근거로 삼고 정의문("파드가 하나 이상 도는 노드 수")을 안 읽었습니다.

- **★ 뿌리 B — 판정하는 주체를 놓쳤습니다**: 세 오개념이 "무엇이 이 결정을 내리는가"를 잘못 짚은 것입니다.
  - **자기 답**: (1) control plane에 파드가 안 뜨는 이유를 "마스터 노드에는 **kube-system과 관련된 파드만** 올라가는 걸로 안다"로 답했습니다. (2) control plane 회피를 "**예외 대상**"이라 불렀습니다. (3) "데몬셋 말고는 보통 메타데이터에 **노드 이름을 그대로 적는 걸로 안다**"고 했습니다.
  - **정답**: (1) `kube-system`은 **네임스페이스**이고 네임스페이스는 스케줄링에 영향을 주지 않습니다. kube-proxy가 kube-system에 있는 건 맞지만 control plane에 뜨는 이유는 네임스페이스가 아니라 매니페스트에 직접 적은 `tolerations: [{operator: Exists}]`입니다. (2) 하드코딩된 예외 규칙이 아니라 **taint/toleration이라는 범용 장치의 한 사례**입니다. 노드에 표시(taint)를 붙이고 파드가 감당을 선언(toleration)하면 통과시키는 구조라, control plane도 특별 대우가 아닙니다. (3) `nodeName` 직접 지정은 일반적 방법이 **아닙니다**. 정석은 nodeSelector·nodeAffinity로 조건을 주고 스케줄러가 고르는 것이고, nodeName 직접 지정은 스케줄러를 **의도적으로 우회**하는 특수 용례(static pod, 디버깅)입니다. DaemonSet 컨트롤러도 옛 버전에서 쓰다 버렸습니다.
  - **원인 추정**: "무엇이 이 파드를 여기 있게 했나"를 물을 때, 눈에 보이는 표식(네임스페이스 이름·필드에 박힌 노드 이름)을 원인으로 삼았습니다. 실제 판정자는 taint 매칭과 스케줄러이고, 표식은 결과일 뿐입니다. (1)이 07-02 label selector 오답(2026-07-14)과 같은 계통입니다 — 인접하고 익숙한 개념으로 빈칸을 메우는 패턴.

- **주어가 생략된 채 외운 것 하나**: "노드당 하나"의 주어를 **클러스터 전체**로 읽어, "노드에 daemon 파드가 하나만 있어야 하나 / 데몬셋 종류가 많을 텐데"라고 두 번 되물었습니다. 생략된 주어는 항상 **"그 DaemonSet의"** 입니다 — 같은 DaemonSet의 파드는 노드당 하나이고, 서로 다른 DaemonSet(kube-proxy·CNI·로그 수집기)의 파드는 한 노드에 여러 개 공존합니다. lock 충돌도 같은 범위입니다(Fluent Bit 둘은 충돌, Fluent Bit + node-exporter는 무관). 문서 11곳 전부에서 이 주어가 생략돼 있어 **문서 보강 대상**으로 잡았습니다(§1 진입부).

- **살아남은 축 — 자력으로 도달한 것들**: 다음 복습에서 건너뛸 수 있습니다.
  - **maxSurge 0의 이유를 스스로 추론**했습니다. "노드당 1개라는 성질을 지녀서 2개를 지니면 안 된다는 건가 (2개 이상이면 중복으로 문제가 생기는 이슈가 있는가)" — lock 충돌이라는 결론까지 되물음 형태로 도달했습니다.
  - **OnDelete의 안전성을 16장과 연결**했습니다. "배포 진행 이후 다른 파드를 망가뜨리거나 하는 경우에 멈춰야 하는데 멈추지 못한다"로 답했고, 컨트롤러의 판정 대상(daemon 자신의 readiness probe)과 실제 피해 대상(그 노드의 다른 파드)이 어긋난다는 급소를 짚었습니다. "수작업은 실수가 생기고 오래 걸린다"는 비용까지 덧붙여 OnDelete가 기본값이 아닌 이유도 함께 봤습니다.
  - **kubelet 순환 의존을 되물음이 열쇠가 되어 도달**했습니다. "kubelet도 데몬 파드의 종류인가"라는 물음 자체가 답의 입구였습니다 — kubelet은 daemon 파드가 아니라 **파드를 실행하는 주체**라, DaemonSet으로 배포하면 kubelet 파드를 띄울 kubelet이 필요한 순환이 생깁니다. 자동 toleration이 끊어 주는 CNI 교착("네트워크가 있어야 노드 ready, 노드 ready여야 CNI 뜸")과 같은 계통이고, kubelet 쪽 고리는 끊을 방법이 없어 **아예 파드 밖으로 빼낸** 것입니다.
  - **스케줄러 위임의 이득을 힌트 1단계로 도달**했습니다. "너의 힌트를 보면 자원도 보는 거 같다" — nodeName을 박으면 자원 검증이 생략돼 문제를 노드까지 배달한 뒤 터진다는 지점입니다.

- **⛔ 트레이드오프 결론은 맞고 근거가 틀렸습니다**: arch 혼재 클러스터에서 "그냥 B(multiarch 단일 DaemonSet)가 낫지 않나"는 방향이 맞았지만, 근거로 댄 "**A는 노드 추가될 때마다 매번 작업해야 할 것 같다**"가 틀렸습니다. A에서도 nodeSelector가 arch label을 걸고 있어 노드 추가 시 컨트롤러가 알아서 파드를 만듭니다(Node 오브젝트 감시 → label 변화에 reconciliation). A의 비용은 수작업이 아니라 **매니페스트 두 벌 유지**입니다. 판단 기준은 **아키텍처별로 달라져야 하는 것이 이미지뿐인지** — 이미지만 다르면 B, `resources.requests` 처럼 다른 필드도 달라야 하면 Pod 템플릿이 하나뿐이라 A입니다. Phase 1에서 잡은 "개수가 클러스터 상태에서 파생된다"를 A에도 적용하지 못한 것이 원인입니다.

- **참고 챕터**: 17-01 §1(reconciliation·워크로드 종류)·§2(status는 노드 수·control plane taint·자동 toleration·nodeAffinity)·§4(maxSurge 0·OnDelete). taint/toleration 개념본은 `../../kubernetes/05_scheduling/05-01`.
- **다음 편 예고로 미룬 것**: DaemonSet과 Service의 관계를 물었습니다. daemon 파드도 label이 있어 Service로 묶을 수는 있지만 노드 에이전트에는 보통 불필요하고(데이터를 내보내는 쪽), node-exporter처럼 긁혀야 하는 경우엔 ClusterIP로 로드밸런싱하면 **어느 노드의 메트릭인지 알 수 없어** 목적이 깨집니다. `hostNetwork`·`hostPort` 로 노드 IP에 직접 노출하는 방식은 **17-02**가 다룹니다.
- **재방문 트리거**: 다음 복습에서 (1) `DESIRED 2 / CURRENT 3` 출력을 다시 던져 두 숫자의 **단위**로 모순이 아님을 설명, (2) "노드당 하나"를 **주어를 붙여** 말하고 한 워커 노드에 daemon 파드가 여러 개 도는 예를 들기, (3) control plane 회피를 네임스페이스 언급 없이 taint/toleration으로만 설명, (4) arch 혼재에서 A/B 판단 기준을 `resources.requests` 를 근거로 대기. maxSurge·OnDelete·kubelet 순환은 자력 도달했으니 **제외**합니다.

## 2026-07-28 — 보강했는데 인출이 거의 안 됨 → Phase 1 재실행 결정 (12-02 즉석 복습 · 평균 1.6)

- **무엇**: 2026-07-27 12-02 항목이 예고한 **재방문 트리거 (1)~(5)** 를 다섯 문항으로 재측정했습니다. 91행이 "개념 자체의 재검증은 다음 복습 몫"이라 미뤄 둔 회차입니다. 즉석 모드라 `_review/` 회차 파일은 만들지 않았습니다.

| 축 | 문항 | quality | 어제 트리거 |
|---|---|---|---|
| 정의 | 암호 해제 지점 + 인증서 소유자 | 3 | (1) |
| 메커니즘 | passthrough에서 `pathType` 동작 여부 | 2 | (2) |
| 적용 | 프록시 설정을 어디에 적나 + ConfigMap 차이 | 1 | (3) |
| 함정 | 인증서·host 불일치의 실패 시점과 검증 주체 | 1 | (4) |
| 구조 | `ingressClassName`이 가리키는 것 + 한 단계의 이유 | 1 | (5) |

- **자기 답**: (1) 두 방식을 합쳐 **"암호도 파드에서 풀리고 인증서도 파드가 쥔다"** 로 답했다가, "파드가 HTTPS를 모르는데 풀 수 있나"라는 되물음에 **스스로 갈라냈습니다**("termination은 L7에서, passthrough는 파드에서"). 이어 "프록시와 L7 서버가 같은 말인가 / 컨트롤러는 중계기인가"를 물었습니다. (2) 결론은 **자력 정답**("적용되지 않을 것 같다")이고 이유도 방향이 맞았으나("파드에서 검증하면 L7이 의미가 없다"), **SNI에 의한 호스트 분기 예외가 비어 있었고** "그럼 L7 프록시의 라우팅은 어떻게 되나"를 스스로 물었습니다. **SNI가 뭐냐고 두 번째로 되물었습니다**(첫 번째는 2026-07-27 13-03). (3) **"잘 모르겠습니다"** — 미답. (4) 시점을 **"프록시가 검증하면 apply에서, 파드면 그냥 넘어간다"** 로 갈래를 나눠 답했고, 이유는 "검증 대상이 달라서"로 방향만 짚었습니다. (5) `ingressClassName`이 **"컨트롤러로 기억한다"** 오답, 한 단계를 두는 이유는 **"여러 컨트롤러를 달 수 있어서"** 로 답했습니다.
- **정답**: (1) 해제 지점과 인증서 소유자가 **함께 움직입니다** — 쥔 쪽이 푸는 쪽입니다(termination=프록시, passthrough=파드). 문서 용어는 "L7 서버"가 아니라 **프록시**이고(12-01 §1 "이 편에서는 L7 로드밸런서를 프록시라 부릅니다"), 프록시는 **서버이면서 동시에 클라이언트**라 "서버"로만 부르면 그 이중성이 가려집니다. **컨트롤러는 중계기가 아니라 관리자**입니다 — 트래픽 경로에 아예 없고 설정만 만듭니다. (2) **동작하지 않습니다.** TLS가 감추는 것이 하필 프록시가 읽어야 할 그 부분이라 `17 03 03 ...` 암호문만 보입니다. 다만 **호스트 분기만 예외**로, SNI가 핸드셰이크 중(암호화 시작 **전**)에 평문으로 오가기 때문입니다. 표현도 "L7이 의미 없다"가 아니라 **"L7 중간자의 눈을 가린다"** 입니다 — 중간자는 서 있고 SNI로 호스트는 가릅니다. (3) **annotation**이고 `spec`이 아니라 **`metadata` 아래**입니다. ConfigMap과는 **읽는 주체**가 갈립니다(annotation=컨트롤러가 API에서 / ConfigMap=앱이 볼륨·환경변수로). 단 컨트롤러 **전역** 설정은 ConfigMap이 맞아, 완전한 오답이 아니라 **층을 하나 잘못 짚은 것**입니다. (4) **갈래를 나눌 필요가 없습니다** — termination이든 passthrough든 **쿠버네티스가 검증을 안 하는 건 마찬가지**라 apply·get·컨트롤러 로그·설정 반영이 전부 초록불이고, **클라이언트 접속에서 처음** 터집니다(`ERR_CERT_COMMON_NAME_INVALID`). "대상"이 아니라 **주체**가 다른 것이고, 검증하는 쪽이 클러스터 안에 없고 **브라우저**에 있습니다. (5) **컨트롤러가 아니라 IngressClass 오브젝트**를 가리킵니다. 한 단계의 이유는 "여러 컨트롤러"가 아니라 정반대로 — **컨트롤러는 하나인데 쓰임을 둘로 갈라야 할 때**(ALB 하나로 `alb-public`/`alb-internal`) 이름이 같아 직접 적으면 구별할 수 없기 때문입니다.
- **★ 12-01과 갈렸습니다 — 보강이 통한 편과 안 통한 편**: 어제 12-01 재측정은 같은 조건(보강 완료 후 첫 복습)에서 **2.8**이었는데 12-02는 **1.6**입니다. 편의 구조가 다릅니다 — 12-01은 **L4/L7 한 축**으로 여러 문항이 꿰여 그 축 하나를 잡으면 방어가 되지만, 12-02는 TLS·annotation·IngressClass가 **서로 독립된 세 덩어리**라 축 하나로 막히지 않습니다. Q3·Q4·Q5가 나란히 1점인 것이 그 구조의 결과입니다.
- **⛔ 무너진 축 — 오브젝트를 지목하는 문항이 전멸했습니다**: Q3(annotation)·Q5(IngressClass) 둘 다 **"무엇이라 부르는가"를 묻는 순간** 막혔습니다. 어제 진단이 이걸 정확히 예고했습니다 — "인접하고 익숙한 개념으로 빈칸을 메우는 패턴 / **이 결정은 어느 층의 일인가라는 배치 감각**이 다음 과제". 다만 양상이 바뀌었습니다: 어제는 **오답**(annotation 자리에 ConfigMap 2회)이었는데 오늘은 **미답**입니다. 틀린 답이 사라진 것은 진전이지만 맞는 답이 들어서지 못했습니다.
- **살아남은 축**: L4/L7은 오늘도 작동했습니다 — Q1을 되물음 한 번에 스스로 갈라냈고 Q2 결론도 맞혔습니다. 어제 "L4/L7 축은 세 번 다 정확히 인출"이 유지됩니다.
- **Q4가 특히 아깝습니다**: 요소 단위 규칙(`*.example.com`이 `foo.api.example.com`을 못 덮음)은 **어제 quality 4로 맞힌 것**인데, 인증서 맥락으로 옮기니 연결되지 않았습니다. §1이 "**DNS 매칭·Ingress host 매칭·인증서 SAN 검증 셋을 따로 외울 필요가 없다**"고 묶어 둔 바로 그 지점입니다. 축은 섰는데 **적용 범위가 아직 좁습니다.**
- **자료 공백이 아닙니다**: 학습자 요청으로 SVG 2종을 렌더해 확인했습니다. `12-02-termination-vs-passthrough-visibility.svg`는 Q1·Q2의 답(해제 지점·인증서 소유자·암호문 바이트·SNI 예외 박스)을, `12-02-cert-host-mismatch-silent-failure.svg`는 Q4의 (a)(b)를 그 순서로 담고 **하필 출제한 `foo.api.example.com` 반례까지** 그려져 있습니다. 12-01과 같은 구도 — 자료는 갖춰졌고 인출이 안 됩니다.
- **★ 결정 — Phase 1 재실행**: 복습으로 메울 수준이 아니라고 학습자가 판단했습니다. 평균 1.6에 다섯 중 셋이 1점이면 **복습의 전제인 "한 번 학습된 상태"가 성립하지 않아**, 같은 회차를 반복해도 같은 자리에서 막힙니다. `learning-session` 으로 12-02 Phase 1을 다시 돌린 뒤 재측정합니다. **다음 복습 트리거는 Phase 1 이후에 다시 잡습니다** — 오늘 트리거를 그대로 물려주면 재학습 전 측정치와 섞입니다.
- **~~미해결~~ 보강 4건 → 2026-07-28 반영 완료** (`cc3a2ebc`): ① §1 SNI *존재 이유* — 닭과 달걀 구조(인증서를 내밀어야 암호화가 시작되는데 어느 인증서인지 먼저 알아야 하고, 그 답인 `Host` 헤더는 암호화 뒤라 늦음)와 12-01 봉투 비유(SNI = 겉면의 수취인 이름) ② §1 진입부 **TLS 자체 소개** — 보장 셋(암호화·무결성·신원 확인)과 신원 확인이 인증서의 몫이라 `--cert`·`--key` 를 둘 다 요구한다는 것 ③④ SVG 두 장 재작도 — 25자 이상 산문을 12→5개·13→5개로 줄이고, 추상 라벨 나열을 컴포넌트 축(cert-mismatch)과 가로축 흐름 대비(termination-vs-passthrough)로 바꿨습니다. 399 → 417줄.
  - SVG 에서 뺀 산문 13건 중 **12건은 본문에 이미 있었고**, 12-01 대비 한 문장("커널이 안 읽어서 / 여기서는 읽으려 해도 암호문이어서")만 §1 에 흡수했습니다. 사실이 사라진 것은 없습니다.
  - ⚠️ **렌더 눈 확인에서 잡힌 것**: 자물쇠 이모지(🔓/🔒)가 rsvg 에서 단색 실루엣으로 나와 **열림·잠김이 구분되지 않았습니다.** ▲(푸는 지점)·✗(못 푸는 지점)으로 교체했습니다. 대비 스크립트로는 잡히지 않는 종류라, TODO 가 경고한 "렌더 후 눈 확인"이 실제로 값을 했습니다.

### 같은 날 이어서 — Phase 1 재실행 (층 배치 축)

- **방식 전환**: 복습에서 원문 정답을 이미 열어 **"읽기 전 예측"의 전제가 깨진 상태**였습니다. 그대로 예측시키면 방금 본 답을 되뇌는 것이라 측정이 되지 않으므로, 문서 재예측 대신 **세 배치축**(오브젝트=누가 읽는가 · 시점=어디서 검증되나 · 소유=누가 무엇을 하나)을 세우는 쪽으로 Phase 1 을 재구성했습니다. 어제·오늘 진단이 공통으로 지목한 **"이 결정은 어느 층의 일인가"** 가 그 축입니다.
- **★ 출구 게이트 통과 — 근거는 정답률이 아니라 자기 정정 4회**: ① annotation 을 "프록시가 읽는다"→ **"컨트롤러가 읽어서 프록시를 만드니 컨트롤러"** 로 스스로 고침 ② ConfigMap 에서 **"파드도 되는 거 아닌가"** 를 먼저 의심(§2 의 앱/컨트롤러전역 이중성을 스스로 감지) ③ Secret 을 **"통과면 파드, 아니면 Ingress"** 로 — Q1 의 *쥔 쪽이 푸는 쪽* 축을 적용해 재구성 ④ annotation 무시 사례를 **"어피니티는 접속 가부가 아니라 어느 파드냐의 설정"** 이라는 자기 논리로 판정. 낮 복습에서 Q3·Q5 가 **미답**이던 것과 비교하면 같은 내용에 **접근 경로가 생겼습니다.**
- **세운 축 셋**: (가) **API 오브젝트는 거의 다 컨트롤러가 읽습니다** — 프록시는 API 를 모르고 설정 파일만, 앱은 볼륨·환경변수로 받은 것만 압니다. ConfigMap 만 읽는 주체가 갈립니다(앱 / 컨트롤러 전역). (나) **API 서버는 문법 검사기이지 의미 검사기가 아닙니다** — 등록된 kind 인가·타입이 맞는가까지만 보고, 그 이름의 IngressClass 가 존재하는지·인증서가 host 와 맞는지는 안 봅니다. 그래서 **판단 주체가 없으면 에러 대신 침묵**이 옵니다. (다) **프록시가 복호화와 전달을 겸합니다** — 트래픽 경로에 있어야 풀 수 있으므로, 경로 밖의 컨트롤러는 애초에 못 합니다(어제 "푸는 주체는 컨트롤러" 오답이 여기서 정리).
- **★ 세 가지 다른 침묵**(시점 축의 수확): 같은 "조용한 실패"도 걸리는 지점이 다릅니다 — **없는 클래스**는 ② 컨트롤러 단계에서 *아무도 집어가지 않아* ADDRESS 가 안 뜨고(터질 것이 만들어지지 않음), **인증서 불일치**는 ③ 까지 정상 동작하고 ④ 브라우저가 거부하며(검증 주체가 클러스터 밖), **안 맞는 annotation** 은 **어느 시점에도 안 걸리고** 기능만 꺼진 채 서비스는 정상입니다. 13-01~13-03 의 "조용히 실패" 6회 패턴과 같은 축입니다.
- **틀린 채 넘어간 것 하나**: "실제로 만들어지는 건 로드밸런서와 L7 프록시"로 답해 **둘이 나란히 생긴다**고 봤습니다. 순서는 **프록시가 먼저이고 LB 는 그 프록시(도 파드다)를 외부에 내보내려고 딸려 오는 것**입니다(12-01 §1). 이어 LB·Ingress 개수 관계를 물어 **N 대 1**(여러 Ingress 가 프록시 앞 LB 하나를 공유)임을 확인했고, 클라우드 관리형(GKE·ALB)은 Ingress 마다 LB 가 붙어 §5 의 "규칙을 한 오브젝트에 모으기"가 곧 비용 절감이 된다는 대비까지 이었습니다.
- **남은 빈틈 둘**(다음 Phase 4 에서 확인): ① **용어 고정** — L7 로드밸런서를 "L7 서버"·"L7" 로 부르는 습관이 남아 있습니다. 문서 용어는 **프록시**이고, 프록시는 *서버이면서 동시에 클라이언트* 라 "서버"로만 부르면 그 이중성이 가려집니다. 컨트롤러와 헷갈리는 뿌리일 수 있습니다. ② **IngressClass 의 한 칸** — "무엇을(Ingress) / 누가(IngressClass)" 는 잡혔으나 *왜 굳이 한 칸을 두는지*(같은 컨트롤러를 다른 파라미터로) 는 아직 인출로 확인되지 않았습니다.
- **다음**: Phase 2 는 건너뜁니다(문서 399줄·SVG 5장 이미 존재, durable 승격 완료). Phase 3 은 어제 판단대로 13장 환경 구축과 합칩니다. **보강 4항목 반영 후 Phase 4** 로 갑니다 — 학습 문서가 고쳐진 뒤 검증하는 것이 순서입니다.
- ⚠️ **게이트 수치 정정**: TODO 작성 중 문서 줄수를 390 으로 적었으나 실측은 **399** 였습니다(세션 내내 근거 없이 390 으로 알고 있었음). 게이트는 다음 세션이 "이미 처리됐나"를 판정하는 장치라 값이 틀리면 그대로 오작동하므로, TODO 다섯 군데를 전부 399 로 고치고 게이트 6개 값(399·0·0·2·12·13)이 실측과 일치함을 재확인했습니다.
- **참고 챕터**: 12-02 §1(TLS·SNI·인증서 불일치)·§2(annotation·ConfigMap 층)·§3(IngressClass·개수 관계). "**쥔 쪽이 푸는 쪽 / passthrough는 눈을 가리되 SNI로 호스트는 봄 / 프록시 설정은 metadata의 annotation, 전역은 ConfigMap / 인증서 검증 주체는 브라우저 / ingressClassName은 컨트롤러가 아니라 IngressClass**" 한 줄.

## 2026-07-28 — 보강 후 재측정: 문서는 고쳐졌고 인출은 절반만 (12-01 즉석 복습 · 평균 2.8)

- **무엇**: 2026-07-27 항목(아래 "결론은 맞고 근거가 다른 모델에서 나옴")이 예고한 **재방문 트리거 7개 중 (2)(3)(5)(6)(7)** 을 다섯 문항으로 재측정했습니다. 78행이 "개념 자체의 재검증은 다음 복습 몫"이라 미뤄 둔 그 회차입니다. 즉석 모드라 `_review/` 회차 파일은 만들지 않았습니다.

| 축 | 문항 | quality | 어제 트리거 |
|---|---|---|---|
| 정의 | 컨트롤러 vs L7 프록시의 입출력 | 2 | (2) |
| 동기 | LB가 여전히 필요한 이유 + IP 비용 | 3 | — |
| 메커니즘 | 파드 직접 전달의 의미 + 명단 출처 | 3 | (3) |
| 적용 | Exact/Prefix 우선순위와 순서 무관성 | 2 | (5) |
| 함정 | 와일드카드 요소 개수 고정 | 4 | (6) |

- **자기 답**: (1) 컨트롤러와 프록시를 합쳐 "HTTP 요청을 받아 전달할 IP를 뱉는다"로 답했고, 이어 "Ingress가 LB와 컨트롤러를 만들어주나"로 **생성 순서를 뒤집었습니다.** (2) "공인 IP를 앞에 둬야 해서 LB가 필요하고, path 라우팅이 되니 단일 진입점" — 방향은 맞으나 근거 문장을 원문에서 못 짚었습니다. (3) EndpointSlice 명단·weight 조절을 **자력 정답**했고 "iptables 말고 다른 방법은 없나"를 스스로 물었습니다. (4) `/api` Prefix + `/api/health` Exact에서 **`main`으로 간다**고 답하고 "순서를 뒤집으면 결과가 달라질 것 같다"고 했습니다 — "일반 규칙 + 예외" 패턴은 모름. (5) `*.example.com`에 셋 중 `kiada.example.com`만 걸림을 **자력 정답**하고 "내가 지정한 `.`의 개수만큼만 연다"로 원리까지 재구성했습니다.
- **정답**: (1) 컨트롤러는 **오브젝트를 입력받아 설정을 출력**하고(HTTP 요청은 한 건도 안 봄), 프록시는 **설정+HTTP 요청을 입력받아 파드 IP로 출력**합니다. 순서는 반대로 — **컨트롤러가 먼저 설치돼 있어야**(§2 클러스터 애드온) Ingress 오브젝트를 감시해 프록시를 세웁니다. Ingress 오브젝트는 선언일 뿐 아무것도 만들지 않으며, ADDRESS가 안 뜨는 증상의 원인이 이것입니다(§3). (4) **Exact가 이기므로 `healthcheck`로 가고, 순서를 뒤집어도 같습니다.** 기준은 순서가 아니라 **구체성**이고, 그 덕에 "`/api` 전부를 보내면서 `/api/health`만 빼는" **일반 규칙 + 예외 패턴이 성립**합니다(§4).
- **★ 어제와 갈린 지점 — 진전과 잔존이 갈라졌습니다**:
  - **해소된 것 둘.** 어제 세 번 반복됐다던 "결론 맞고 근거 틀림"이 **와일드카드에서 끊겼습니다** — 어제는 `foo.kiada.example.com`이 매칭된다고 오답했는데 오늘은 셋 다 맞히고 "개수 고정"까지 스스로 말했습니다(quality 4, 이번 최고점). 파드 직접 전달도 어제 "모르겠음"(미학습)에서 오늘 자력 정답으로 올라섰습니다. **보강한 SVG 4종이 실제로 인출을 바꾼 축입니다.**
  - **⛔ 남은 것 둘.** 어제 (5)로 예고한 **선언 순서 모델이 그대로 재현됐습니다** — 반례 YAML을 직접 주고 물었는데도 "순서를 뒤집으면 달라질 것 같다"로 답했습니다. 어제 "조건이 조금 바뀌면 무너진다"고 적어 둔 예측이 하루 만에 그대로 맞았습니다. 컨트롤러/프록시 합치기도 (2) 트리거에서 재발했습니다.
  - **공통 성격**: 남은 둘 다 **문서가 명시적으로 경고해 둔 자리**입니다 — §1 "흔히 한 덩어리로 부르지만 둘은 별개", §4 "먼저 걸린 것이 이기는 방식이 아닙니다". 경고문을 읽는 것과 반례 앞에서 그 경고를 꺼내 쓰는 것이 별개라는 증거입니다.
- **원인 추정**: **문서 보강으로 해결되지 않는 종류의 실패**입니다. 어제 10항목을 반영해 268→432줄·SVG 9장이 됐고, 오늘 확인하니 `12-01-ingress-controller-role.svg`는 "컨트롤러는 트래픽을 나르지 않습니다 / 2와 3은 별개"를, `12-01-lb-plus-proxy-stacking.svg`는 "LB 1개 + 프록시 + 규칙 3개"를 **이미 정확히 담고 있습니다.** 즉 자료 공백이 아니라 **인출 실패**이며, 대응은 보강이 아니라 재인출입니다. 특히 순서 모델은 방화벽·`if-else`로 굳은 직관이라 서술을 더 얹는 것으로는 안 밀리고, **반례를 직접 풀게 하는 방식**이라야 합니다.
- **⛔ 내 실수 하나 (진행자 측)**: 세션 중 `ls _assets | grep '^12-01'`이 빈 결과를 내자 **"SVG 8개 전부 부재"로 단정**하고 보강 건을 두 개 적립했습니다. Python 전수 조회 결과 **9개 전부 실재**했고(168개 중), Drive 폴더의 지연 응답을 부재로 읽은 것이었습니다. `structure-verification.md §1`(구조의 1차 자료는 디스크, 전수로 확인)의 위반이고, 그 오진 위에서 "SVG가 없어 Q2를 못 짚었다"는 **틀린 진단까지 얹었습니다.** 앞으로 Drive 경로의 파일 존재 판정은 `ls` 1회가 아니라 Python 전수로 확정합니다.
- **별건 발견**: 전수 조회 중 12-01과 무관한 실제 누락 2건 — `05-02.Pod 생성과 상호작용.md`의 `05-02-access-methods-client-ip.svg`, `README.md`의 zero-width space 섞인 깨진 참조. 이번 승인 범위 밖이라 손대지 않았습니다.
- **참고 챕터**: 12-01 §1(세 요소·컨트롤러 역할)·§2(컨트롤러 설치)·§3(ADDRESS 미표시 진단)·§4(pathType 우선순위). "**컨트롤러는 오브젝트→설정, 프록시는 설정+요청→파드 IP / 컨트롤러가 먼저 있고 Ingress는 선언일 뿐 / 겹치면 순서 아닌 구체성, 그래서 일반+예외가 된다**" 한 줄.
- **재방문 트리거**: 다음 복습에서 **둘만** 재측정합니다(해소된 축은 뺍니다). (1) Ingress YAML을 `apply` 한 직후, HTTP 요청이 오기 전에 **일하는 주체와 그 입출력**을 적기 — 이어서 "ADDRESS가 안 뜨면 무엇을 의심하나"까지. (2) Exact/Prefix가 겹치는 YAML을 **순서를 뒤집은 두 벌로** 주고 각각 어디로 가는지 답한 뒤, "왜 그렇게 정해졌는가"를 **예외 패턴** 축으로 설명하기. 2연속 실패이므로 세 번째로 막히면 `STATE.md` 재학습 등록 대상입니다.

## 2026-07-28 — 13장 세 편 보강 완료 (문서 작업 기록)

- **무엇**: `_todo-13-01/02/03-보강.md` 13항목을 전부 반영하고 TODO 세 개를 삭제했습니다. 착수 전 게이트로 세 편 모두 미착수임을 확인했습니다(13-01 0/16/244줄 · 13-02 0/34/242줄 · 13-03 0/21/285줄).
- **커밋**:

| 편 | 커밋 | 줄수 | 신규 SVG |
|---|---|---|---|
| 13-01 | `affc5824` | 244 → 333 | `13-01-schema-shape-not-behavior` · `13-01-status-layers-hide-problem` |
| 13-02 | `76666728` | 242 → 352 | `13-02-ref-failure-directions` · `13-02-filter-kinds-and-mirror-trap` |
| 13-03 | `c0936bde` | 285 → 384 | `13-03-network-vs-reference-layers` · `13-03-three-failure-modes` |

- **교차 참조 셋을 한 번에 설계**: ① `ResolvedRefs` 3연결(13-01 listener 조건 → 13-02 위/아래 방향 대응 → 13-03 `RefNotPermitted`) ② **"조용히 실패 → status에 드러남"** 축은 13-03 §2의 세 양상 대비표를 허브로 삼고 13-01 `attachedRoutes: 0`·12-02 기본 IngressClass 부재를 그 표에 묶었습니다 ③ 크로스 네임스페이스 정본은 13-03 §3이고 나머지 두 편은 링크만.
- **미학습 축 대비**: 13-03 §4에 `parentRefs` 재해석("부모 Gateway"가 아니라 **트래픽 진입점**이고 Gateway는 그 한 형태)을 명시하고, 왜 Service가 진입점이 되는지를 11-01과 이었습니다. 13-02 기능(weight·mirror)의 east/west 적용표도 붙였습니다 — 다음 복습 첫 문항의 답이 문서에 있습니다.
- **내 실수 하나**: 착수 게이트를 돌릴 때 13-01 TODO의 게이트 블록을 읽지 않고 패턴을 임의로 지어내 "9건 잡힘 = 이미 완료"로 오판했습니다. TODO에 적힌 정확한 패턴으로 다시 돌리니 0건이었습니다. **게이트는 TODO에 적힌 것을 그대로 실행해야 합니다** — 지어낸 패턴은 기존 본문의 일반 용어를 잡아 위양성을 냅니다.
- **검증**: SVG 6장 rsvg 렌더 + 눈 확인(매트릭스 행 겹침·이모지 글리프·값 붙음 등 5건 수정), 편별 §16 grep 전 항목 통과, 벽 단락은 세 편 모두 baseline 유지(신규 4곳 전부 해소), 링크·각주 0 broken.

## 2026-07-27 — 네트워크 통신과 오브젝트 참조를 한 층으로 봄 (13-03 Phase 1 · 조용히 실패 5·6회차 · ⛔ 게이트 미통과)

- **자기 답**: 13-03 네 축을 예측·채점으로 돌린 결과. (1) TLS 두 방식은 "termination은 프록시에서 멈추고 passthrough는 통과"를 **자력 정답**했으나, **"TCP·UDP·gRPC는 프록시 서버를 안 쓰는 프로토콜"** 이라 오답했습니다. 세션 중 "SNI가 뭐였지?"를 물었습니다(12-02에서 다룬 개념). (2) 크로스 네임스페이스에서 `payments` 의 실제 존재하는 서비스를 백엔드로 적었을 때 **"흐르지 않을까요? 네임스페이스가 달라도 통신은 되잖아"** 로 답했습니다. (3) Istio 미지원 `UDPRoute` 를 만들면 **"apply 가 실패할 것 같다"**, 확인법은 "support level 로 명시한 거 아님?"으로 답했습니다. (4) GAMMA east/west 는 **두 번 "모르겠음"** — 미학습.
- **정답**: (1) **프록시는 어느 프로토콜에서든 있습니다.** 갈리는 것은 "프록시를 쓰는가"가 아니라 **"프록시가 내용을 이해하는가"** 입니다 — HTTP(termination)는 경로·헤더까지, TLS passthrough는 **SNI 호스트명만**, TCP는 **아무것도** 못 봅니다. termination→**HTTPRoute**(listener `protocol: HTTPS` = "풀면 HTTP다"), passthrough→**TLSRoute**(listener `protocol: TLS` = "TLS인 것만 안다"). SNI 는 **핸드셰이크가 암호화 전에 일어나므로 평문**이라 유일한 단서입니다(한 IP 여러 도메인에서 어느 인증서를 내밀지 알아야 하고, 인증서 전송 시점엔 암호화가 없으며 `Host` 헤더는 너무 늦음). (2) **흐르지 않습니다.** 두 층이 다릅니다 — **파드 간 네트워크 통신은 네임스페이스를 넘고**(11-01 flat 네트워크, FQDN 호출은 그냥 됨) **오브젝트 참조는 기본적으로 못 넘습니다.** 막는 이유는 위협 모델 — 안 막으면 아무 네임스페이스에서 HTTPRoute 로 **남의 내부 전용 서비스를 소유자 모르게 외부 노출**할 수 있습니다. 그래서 허가 주체가 소유자이고, `ReferenceGrant` 를 **참조 대상(서비스)의 네임스페이스**에 만듭니다. 없으면 `Accepted: True` / `ResolvedRefs: False (RefNotPermitted)`. (3) **`apply` 가 성공합니다.** `apply` 를 받는 건 **API 서버**이고 CRD 설치 여부와 스키마만 봅니다 — **Istio 가 UDPRoute 를 안 본다는 사실을 API 서버는 모릅니다.** status 를 채우는 주체가 컨트롤러이므로, 아무도 안 보면 **채울 사람이 없어 status 가 빕니다.** support level 은 *명세에 적힌 규범*이고 "지금 이 컨트롤러가 실제로 지원하는가"는 **status 로 확인**합니다. (4) `parentRefs` 의 뜻은 **"부모 Gateway"가 아니라 "이 규칙이 적용될 트래픽 진입점"** 이고 Gateway 는 그 한 형태일 뿐입니다. east/west 는 거기에 **Service** 를 넣습니다(클라이언트가 부르는 변하지 않는 대표 이름). 호출하는 쪽 코드는 안 바뀌고, 13-02 의 weight·RequestMirror 가 그대로 내부 카나리·내부 미러링이 됩니다 — 이것이 GAMMA 의 해법.
- **★ 세 가지 실패 양상이 다르다** (이번 세션의 핵심 수확):

| 상황 | `apply` | status |
|---|---|---|
| 스키마 오류·없는 필드 | **실패** | — |
| 참조 거부(ReferenceGrant 없음) | 성공 | `ResolvedRefs: False` + reason |
| 구현 미지원(Istio + UDPRoute) | 성공 | **비어 있음** |

  세 번째가 가장 어렵습니다 — 에러도 거부 메시지도 없고 **아무 일도 일어나지 않습니다.** 13-01 `attachedRoutes: 0` · 12-02 기본 IngressClass 부재와 **같은 결**이고 진단법도 같습니다("빈지/0인지 보라").
- **원인 추정**: "조용히 실패한다" 축이 **5·6회차**로 늘었습니다(12-02 인증서·12-02 annotation·13-01 미지원 필드·13-02 참조 오타에 이어 (2)와 (3)). (2)는 특히 **가장 교활한 케이스** — 오타도 없고 서비스도 실제로 존재해 YAML 만으로는 못 찾고 status 를 봐야만 압니다. 뿌리는 이번엔 명령형 감각이 아니라 **층 혼동**입니다 — "통신이 되니 참조도 될 것"으로 네트워크 층과 API 오브젝트 층을 한 덩어리로 봤습니다(12-02의 ConfigMap↔annotation 혼동과 같은 결). (1)의 "프록시를 안 쓴다"도 같은 결로, **프로토콜 계층과 프록시 유무를 한 축으로** 묶은 것입니다. (4)는 노출 부족 — `parentRefs` 를 13-02에서 "부모 Gateway"로만 익혀 그 이름에 갇혔습니다.
- **⛔ Phase 1 게이트 미통과**: §4(GAMMA)가 미학습이라 닫지 못했습니다. 다음 복습 **첫 문항**으로 둡니다.
- **참고 챕터**: 13-03 §1(TLS 두 방식)·§2(TCP/UDP/gRPC 지원 확인)·§3(크로스 네임스페이스)·§4(GAMMA). 보강 TODO 는 `_todo-13-03-보강.md`(5항목). 13장 세 편에 **교차 참조 셋**(`ResolvedRefs` 3연결 · "조용히 실패" 축 · 크로스 네임스페이스 정본)이 있어 **묶어서 보강**할 것. "**프록시는 늘 있고 갈리는 건 내용 이해 여부 / 네트워크는 네임스페이스를 넘고 오브젝트 참조는 못 넘는다 / 거부는 status에 이유가 찍히고 미지원은 status가 빈다 / parentRefs = 규칙이 적용될 트래픽**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) ★ **"east/west 에는 Gateway 가 없는데 HTTPRoute 를 어떻게 쓰나"** — 미학습 축이므로 최우선, `parentRefs` 의 의미까지, (2) 실제 존재하는 서비스를 다른 네임스페이스에서 백엔드로 적으면 흐르는가와 **왜 막아 두는가**(위협 모델), (3) `ReferenceGrant` 를 **어느 네임스페이스에** 만드는지와 그 이유, (4) 미지원 Route 를 만들면 `apply` 가 성공하는지와 **status 가 어떤 모양인지**(거부와의 차이), (5) termination/passthrough 에 각각 어느 Route 와 어느 listener 프로토콜을 쓰는지, (6) "TCP 는 프록시를 안 쓰나"에 답하기.

## 2026-07-27 — "필수값이니 실패할 것" (13-02 Phase 1 · 조용히 실패 4회차)

- **자기 답**: 13-02 네 축을 예측·채점으로 돌린 결과. (1) `parentRefs`/`backendRefs` 는 이름을 못 맞혔으나 **방향이 둘**이라는 구조는 잡았습니다("selectGateway"). 왜 클래스가 아니라 Gateway 인스턴스를 지목하는지는 "모르겠음". (2) ★ `parentRefs` 에 존재하지 않는 Gateway 이름(오타)을 적으면 어떻게 되는지 물었을 때 **"안 되지 않을까요? 부모는 필수값으로 보이는데"** 로 답하고, 결과를 어디서 보는지는 "모르겠음". (3) weight 분할은 **(a)(b)(c) 전부 자력 통과** — 9:1 분리, 생략 시 균등, 그리고 (c)에서 **"99대 1로 해야겠지? 그런데 그러면 파드를 100개를 띄워야 함"** 을 힌트 하나로 스스로 도출했습니다. (4) matches 안이 AND 인 것은 **자력 정답**했으나, `rules` 를 여럿 두는 것을 **"그거는 그냥 문법 에러 아니야? rule을 여러번 선언이 되?"** 로 봤습니다. (5) 필터 여섯 중 성격이 다른 것을 `ExtensionRef`·`RequestMirror` **두 후보로 좁혔으나**(정답 포함) 이유는 "모르겠다".
- **정답**: (1) `parentRefs`(위=Gateway) / `backendRefs`(아래=Service)입니다. `parent` 라 부르는 것은 Route 가 Gateway 에 **매달리는** 관계라서이고(13-01 `attachedRoutes` 와 같은 비유), 클래스가 아니라 **인스턴스**를 지목하는 이유는 **Gateway 하나 = 프록시 하나**여서 "istio 클래스"라고만 하면 셋 다 해당돼 어느 입구인지 특정할 수 없기 때문입니다. 층이 나뉜 것 — `gatewayClassName`=누가 처리하나(Ingress `ingressClassName` 과 같은 층) / `parentRefs`=어느 입구에 붙나. (2) **`apply` 가 성공합니다.** 스키마 검증은 `name` 이 문자열인지만 보고 **그 이름의 Gateway 가 있는지는 다른 오브젝트 조회가 필요해 모릅니다 — 필수값 검사도 "모양"에 속합니다.** 결과는 Route status 의 `parents[].conditions` 에 `Accepted: False / NoMatchingParent` 로 남고, **`Accepted` 는 위쪽(부모)·`ResolvedRefs` 는 아래쪽(백엔드)** 을 봅니다(두 방향 참조가 각자 조건을 가짐). 같은 사건이 양쪽에 다르게 기록됩니다 — Gateway 는 `attachedRoutes: 0`("붙은 게 없다"), Route 는 `Accepted: False`("붙을 곳을 못 찾았다"). (3) 자력 통과. 파드 개수 방식의 문제는 **① 해상도가 파드 수에 묶임(0.1%면 1000개) ② 비율과 용량이 한 손잡이에 묶임**(용량 증설이 트래픽 비율을 바꿔 버림)이고, weight 는 둘을 분리합니다. (4) `rules`·`matches`·`backendRefs` 모두 **배열**이라 여럿이 정상입니다. **3층 결합** — matches 항목 **안**은 AND, matches **항목끼리는 OR**, rules 끼리는 **명세가 정한 구체성 우선**(path 길이 → method 유무 → header 수 → queryParam 수, 동률이면 생성 시각 → 이름)이고 **YAML 순서는 무관**합니다. `matches` 생략은 기본값이 채워져 catch-all 이 됩니다. (5) **`RequestMirror`** — 나머지 다섯은 요청 하나를 목적지 하나로 다루는데 이것만 **흐름을 복제**해 백엔드 둘이 동시에 처리하고 복사본 응답은 버립니다. `ExtensionRef` 가 결이 다른 건 *설정을 어디 두는가*이고 동작은 여전히 하나의 흐름입니다. `RequestRedirect` 는 게이트웨이가 **직접 응답**해 백엔드로 안 보내니 `backendRefs` 가 불필요합니다.
- **원인 추정**: (2)가 **"조용히 실패한다" 4회차**입니다 — 12-02 인증서 host 불일치("에러 예상"), 12-02 annotation("ConfigMap"), 13-01 미지원 필드("예외가 터진다")에 이어. 이번에는 근거가 **"필수값이니 검증될 것"** 으로 한 겹 더 구체화됐는데, 여기에 빠진 구분이 **필수값 검사도 "모양"에 속한다**는 것입니다 — 값의 존재만 보고 그 값이 가리키는 대상의 존재는 보지 않습니다. 뿌리는 여전히 명령형 API 감각. (4)는 다른 결 — **배열 중첩 구조를 못 봄**(복수형 이름이 단서인데 단일 값으로 읽음)이고, 12-01에서 `paths` 배열을 다뤘음에도 `rules` 층에서 다시 걸렸습니다. 반면 (3)의 자력 통과는 11-01 canary 서술과 이어진 좋은 인출이고, (5)에서 후보를 둘로 좁힌 것도 이름만으로 성격을 추론한 유효한 시도입니다.
- **참고 챕터**: 13-02 §1(parentRefs·status)·§2(weight)·§3(matches)·§4(필터). 보강 TODO 는 `_todo-13-02-보강.md`(4항목 + 🚩 사실 확인 1건). "**필수값 검사도 모양만 본다 → 참조 오타는 apply 를 통과하고 status 에 남는다(Accepted=위/ResolvedRefs=아래) / matches 안 AND · 항목끼리 OR · rules 는 구체성 우선 / RequestMirror 만 흐름을 복제**" 한 줄.
- **🚩 문서 사실 확인 완료 (2026-07-28)**: 13-02 221행의 "순차 평가"가 부정확함을 공식 타입 정의([httproute_types.go](https://github.com/kubernetes-sigs/gateway-api/blob/main/apis/v1/httproute_types.go))로 확인하고 승인 후 수정했습니다. 명세는 **구체성 우선**(Exact → 긴 PathPrefix → method → header·queryParam 수)이고, Route 간 동률이면 생성시각·이름, **그래도 동률일 때만 리스트 순서**입니다. 즉 선언 순서는 무관한 게 아니라 **맨 마지막 순위**입니다 — TODO에 적힌 "YAML 순서 무관"도 과한 서술이었습니다. 판정 범위가 한 Route 안이 아니라 그 Gateway에 붙은 **모든 Route를 통틀어**("Across all rules specified on applicable Routes")라는 점도 새로 확인했습니다.
- **재방문 트리거**: 다음 복습에서 (1) **"참조에 오타를 내면 apply 가 성공하나"에 예·아니오와 이유로 답하기** — 4회차 축이므로 최우선, (2) `Accepted` 와 `ResolvedRefs` 가 각각 **어느 방향**을 보는지, (3) 1% 카나리를 파드 개수로 하면 파드가 몇 개 필요한지와 weight 가 분리해 주는 두 가지, (4) matches 안·matches 항목끼리·rules 끼리의 **결합 방식 셋**을 각각 답하기, (5) 필터 여섯 중 흐름을 복제하는 것과 백엔드가 필요 없는 것을 고르고 이유 대기, (6) **미러링 시 부수효과가 두 번 일어나는 문제**(문서에 없던 내용).

## 2026-07-27 — ★ "조용히 실패한다"를 세 편에 걸쳐 놓침 (13-01 Phase 1 · 공통 축)

- **자기 답**: 13-01 네 축을 예측·채점으로 돌린 결과. (1) Gateway/Route 경계는 **자력 정답**("게이트웨이는 호스트를 담당하고 라우트는 paths를 받을 것 같다")이나, 쪼갠 *이유*를 **"어차피 호스트와 패스는 거의 동일하지 않나?"** 로 못 봤습니다. (2) release channel·support level은 (a) "모르겠음 — 인터페이스를 만들어야 할 텐데", (b) **"필드는 바뀌면 예외가 발생할 거고, 구현체가 없는 건 안 되는 걸 호출하니 예외가 터지는 거 아닌가"** 로 오답 — 완전 미달 축입니다. (3) Istio 생성물은 LoadBalancer를 **자력 정답**했으나, 프록시가 **"하나의 공유 아닐까? 입구가 여러 개일 수는 없으니"** 로 오답. (4) 404 진단에서 "라우트가 없는 거 아닌가"를 **자력 정답**했으나, 볼 곳을 "게이트웨이 내부 이벤트"로 답해 층을 못 짚었습니다.
- **정답**: (1) 호스트와 경로는 내용이 비슷해 보이지만 **결정 주체와 변경 빈도가 다릅니다** — 호스트·인증서·IP는 플랫폼 팀이 정하고 거의 안 바뀌며, 경로→서비스는 앱 개발자가 배포마다 바꿉니다. 한 오브젝트에 있으면 **RBAC이 오브젝트 단위라 "이 경로만 수정" 권한을 줄 수 없어**, 개발자에게 권한을 주면 남의 경로·TLS를 깨뜨릴 수 있고 인프라 팀이 대신하면 배포가 그 팀 처리량에 묶입니다. 쪼개면 RBAC 경계가 조직 경계와 맞습니다. (2) **스키마는 모양을 강제하지만 행동은 강제하지 못합니다** — 지원 안 하는 필드도 CRD 스키마에 있으니 `apply`가 성공하고 컨트롤러가 **조용히 무시**합니다(예외가 아님). 그래서 표시가 둘 필요합니다 — **channel**(standard/experimental = 시간 축, API가 바뀔까)과 **support level**(core/extended/implementation-specific = 구현 축, 다들 처리할까). 둘은 독립이고 **`standard`+`extended`가 함정**입니다("standard니까 안전"이 아니라 이 구현이 지원하는지 따로 확인). `extended`의 "지원하면 동작이 같다"가 Ingress annotation과의 결정적 차이 — 규격이 이름·의미를 고정합니다. (3) **Gateway마다 전용 프록시가 그 네임스페이스에 생깁니다.** 입구는 여러 개일 수 있고, 오히려 그게 설계 의도입니다. 공유하지 않는 이유는 **성능 격리·장애 범위·관측 분리** — §1의 RBAC(선언) 경계에 **런타임 경계**가 더해집니다. 대가로 12-01과 **반대 방향 트레이드오프**(Ingress는 LB 하나 공유로 IP 절감, Gateway API는 Gateway마다 LB)가 생기고, 해소책은 Gateway를 "격리가 필요한 단위"마다만 만들고 **Route가 다른 네임스페이스의 Gateway를 참조**하게 하는 것입니다(→ 13-03 `ReferenceGrant`). (4) status **3층** 중 listener 층을 봅니다 — `attachedRoutes`가 0인지. `PROGRAMMED: True`는 "설정이 생성돼 **접근 가능해졌다**"이지 "라우팅이 완성됐다"가 아닙니다(**입구는 열렸고 안내판이 없는 상태**). `kubectl get gtw`가 보여주는 전체 conditions가 listener 층 문제를 **가립니다.**
- **원인 추정**: ★ **오늘 세 편에 걸친 공통 축이 드러났습니다 — "조용히 실패한다"를 반복해서 놓칩니다.** 12-02 인증서 host 불일치를 "못 쓰는 거 아니야?"(에러 예상), 12-02 annotation 오배치를 ConfigMap이라 답, 13-01 미지원 필드를 "예외가 터진다"로. 실제로는 **`apply`가 성공하고 컨트롤러가 무시하고 런타임에 조용히 다르게 동작**합니다. 뿌리는 **명령형 API 감각**입니다 — "잘못 부르면 에러가 난다"를 기대하는데, 쿠버네티스는 선언적이라 "접수했고 맞출 수 있으면 맞춘다"가 기본이고 **못 맞추면 예외가 아니라 status에 남깁니다.** 그래서 status 읽기가 진단의 핵심이 되고 (4)의 `attachedRoutes`가 그 예입니다. 13-01 고유 오답 둘((1) 분리 이유, (3) 프록시 공유)은 다른 결 — 문서에 *사실*은 있으나 *이유*가 없어 자기 모델로 메운 것으로, 12-01의 "결론 맞고 근거 틀림"과 같은 유형입니다.
- **2026-07-27 최종 집계 — 다섯 편 / 여섯 번**: 같은 축이 13-02(`parentRefs` 오타를 "필수값이니 실패할 것")와 13-03(크로스 네임스페이스 참조 거부 / Istio 미지원 Route)에서 재발했습니다. 이 항목의 "세 편"은 **다섯 편·여섯 회**로 읽습니다 — 12-01·12-02·13-01·13-02·13-03. 13-03에서 **세 가지 실패 양상이 서로 다르다**는 것이 정리됐습니다(스키마 오류=apply 실패 / 거부=status에 이유 / 미지원=status가 빔). 상세는 13-03 항목.
- **참고 챕터**: 13-01 §1(Route 분리)·§2(channel·level)·§4(전용 프록시)·§5(status 3층). 보강 TODO는 `_todo-13-01-보강.md`(4항목, 전부 시각화감). "**스키마는 모양만 강제하고 행동은 못 한다 → channel(시간)·level(구현) 두 축 / Gateway마다 전용 프록시(격리) / PROGRAMMED True 여도 listener의 attachedRoutes가 0이면 404**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) **"지원하지 않는 필드를 쓰면 어떻게 되는가"에 한 문장으로 답하기** — 이 축이 오늘의 핵심이므로 최우선, (2) channel과 level이 각각 무엇을 보장하는지와 `standard`+`extended` 조합이 왜 함정인지, (3) Gateway를 셋 만들면 프록시가 몇 개 생기고 왜 공유하지 않는지, (4) `PROGRAMMED: True`인데 404일 때 어느 층의 어느 숫자를 보는지, (5) Gateway/Route를 쪼갠 이유를 **RBAC** 관점으로 답하기.

## 2026-07-27 — 묻는 "층"을 잘못 짚음 (12-02 Phase 1)

- **자기 답**: 12-02 세 축을 예측·채점으로 돌린 결과. (1) TLS termination/passthrough에서 **"중단도 동일 지점 아닌가요?"** 로 답해 두 방식이 같은 곳에서 암호를 푼다고 봤습니다(termination 논리 자체 — "백엔드는 몰라도 된다, 내부 통신에선 필요 없다" — 는 자력 정답). 암호를 푸는 주체를 "인그레스 컨트롤러"라 했습니다. "표준? 그런 게 있는가?"로 표준 개념 자체를 몰랐습니다. (2) Secret은 자력 정답이나 참조 방법을 **"config로 가져가는 거 아니야?"** 로 오답. 인증서·host 불일치는 "못 쓰는 거 아니야?"로 방향만. (3) annotation을 못 떠올려 **"ConfigMap에서 다룬다?"** 로 오답(두 번째 ConfigMap 오답). spec 필드로 안 들어간 이유를 **"Ingress에서 하기에는 무거운 작업이기 때문"** 으로 답했습니다. (4) 쿠키 어피니티는 **자력 통과**(L4/L7 축 + NAT 뒤 동일 IP 구분 불가). 단 "세션 어피니티에 LOCAL/CLUSTER 정책 상관없이 L7을 쓸 수 있단 건가?"로 층을 섞어 물었습니다. (5) IngressClass에서 (a) **`rules`로 담당 API를 적기**, (b) 컨트롤러 이름 직접, (c) **"2개 중 아무거나 로드밸런싱"** 으로 셋 다 오답. 이어 "Ingress 1개를 컨트롤러 2개가 나눠 처리하는 것"으로 전제를 오해했습니다.
- **정답**: (1) **암호가 풀리는 지점이 다른 것이 두 방식의 정의**입니다 — termination은 프록시가(인증서를 프록시가 쥠), passthrough는 파드가(인증서를 파드가 쥠) 풉니다. 푸는 주체는 컨트롤러가 아니라 **프록시**입니다(12-01의 컨트롤러=설정 생성 / 프록시=트래픽 처리 구분). **"표준"은 쿠버네티스 API에 정식 필드로 있는가**를 뜻하고, termination은 `spec.tls`가 정식 필드라 어느 컨트롤러든 통하지만 passthrough는 필드가 없어 annotation + 컨트롤러 플래그에 의존합니다. **passthrough가 "L7 라우팅 포기"인 이유**: TLS가 암호화하는 대상이 바로 프록시가 읽어야 할 `GET /quote` · `Host:` 부분이라, 프록시 눈에는 암호문만 보여 `pathType` 매칭이 작동할 수 없습니다. 분기에 쓸 수 있는 건 핸드셰이크의 **SNI**(암호화 전이라 평문)뿐이므로 호스트 단위까지가 한계입니다. (2) `spec.tls[].secretName`으로 참조합니다 — ConfigMap이 아니고, 12-01에서 확인한 spec 필드 넷 중 하나입니다. host 불일치는 **오브젝트 생성이 성공하고 런타임에 브라우저가 거부**합니다(`ERR_CERT_COMMON_NAME_INVALID`) — 검증 주체가 서버가 아니라 **클라이언트**라 배포 파이프라인은 초록불인데 사용자만 막힙니다. (3) **annotation**입니다(07-03). spec 필드로 못 들어간 이유는 무게가 아니라 **의미를 API가 보장할 수 없어서** — `rateLimit`의 단위·기준·초과 응답이 구현마다 다르고 AWS ALB엔 개념 자체가 없습니다. annotation/ConfigMap은 층이 갈립니다: **Ingress별 프록시 설정 = annotation(컨트롤러가 API에서 읽음) / 컨트롤러 전역 설정 = ConfigMap**. (4) 어피니티와 `externalTrafficPolicy`는 **다른 구간의 일**입니다 — 프록시는 cluster IP를 건너뛰므로 프록시→백엔드 구간에 Service가 개입하지 않습니다. 단 LB→프록시 구간에 `Cluster` 정책이면 SNAT이 걸려 **프록시가 보는 클라이언트 IP가 노드 IP로 오염**되므로 컨트롤러 LB Service에 `Local`을 거는 것이 관행입니다(유일한 교차점). (5) **`ingressClassName`** 이고, 컨트롤러가 아니라 **IngressClass를 가리킵니다** — 한 단계를 두는 이유는 같은 컨트롤러를 다른 파라미터로 여러 번 쓰기 위해서(ALB `internet-facing` vs `internal`)입니다. 미지정 시 기본 클래스가 받고, 기본이 없으면 **아무도 처리하지 않습니다**(로드밸런싱이 아님). **Ingress 1개를 처리하는 컨트롤러는 항상 하나**이고, 컨트롤러 여럿은 트래픽이 아니라 **Ingress 집합(일감)을 나눠 갖는 것**입니다.
- **원인 추정**: 12-01의 "결론 맞고 근거 틀림"과 **다른 결** — 여기서는 **묻는 층·오브젝트를 잘못 짚습니다.** annotation 자리에 ConfigMap 2회, 소유권(누가 처리하나) 문제에 `rules`(라우팅), 어피니티 층에 `externalTrafficPolicy`, 컨트롤러 개수를 Ingress 개수와 혼동. 인접하고 익숙한 개념으로 빈칸을 메우는 패턴입니다. 반대로 **L4/L7 축은 세 번 다 정확히 인출**했습니다(termination 논리·쿠키 가능 이유·NAT 구분 불가) — 11-01부터 반복해 온 축이라 굳었습니다. 즉 개별 사실은 쌓였고 **"이 결정은 어느 층의 일인가"라는 배치 감각**이 다음 과제입니다.
- **2026-07-27 보강 완료**: 10항목 전부 반영됐습니다(다른 세션 수행, 커밋 `2198ea75`, 227→390줄, SVG 2→5장). 신규 SVG — `12-02-termination-vs-passthrough-visibility`(암호 해제 지점과 프록시가 볼 수 있는 것), `12-02-cert-host-mismatch-silent-failure`(생성은 성공하고 접속 시점에 브라우저가 거부), `12-02-ip-vs-cookie-affinity`(NAT 뒤 100명·모바일 IP 전환). `_todo-12-02-보강.md`는 삭제했습니다. **개념 자체의 재검증은 다음 복습 몫**입니다(문서가 고쳐진 것과 인출이 되는 것은 별개).
- **참고 챕터**: 12-02 §1(TLS)·§2(annotation·어피니티)·§3(IngressClass). "**termination=프록시가 풂(표준 `spec.tls`)/passthrough=파드가 풂(SNI만 보임→경로 라우팅 불가) / 구현마다 의미가 달라 보장 못 하는 설정은 annotation / `ingressClassName`은 컨트롤러가 아니라 IngressClass를 가리킨다**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) 두 TLS 방식의 **암호 해제 지점과 인증서 소유자**를 각각 적고 "프록시가 볼 수 있는 것"까지 대기, (2) "passthrough면 `pathType: Prefix` 규칙이 동작하나"에 예·아니오와 이유로 답하기, (3) 프록시별 세부 설정을 **어디에** 적는지 한 단어로 답하고 ConfigMap과의 차이 대기, (4) 인증서 host가 안 맞으면 **어느 시점에 무엇이** 실패하는지(생성/접속, 검증 주체) 답하기, (5) `ingressClassName`이 가리키는 대상과 **왜 컨트롤러를 직접 안 적는지**, (6) 클래스 미지정 시 두 분기와 기본 클래스가 둘일 때의 함정.

## 2026-07-27 — 결론은 맞고 근거가 다른 모델에서 나옴 (12-01 Phase 1)

- **자기 답**: 12-01 네 축을 예측·채점으로 돌린 결과. (1) "IP 하나로 왜 갈라낼 수 없나"에 **"애초에 매핑이 1대1이지 않은가?"** 로 답했습니다(path 라우팅·L4=ip:port는 자력). (2) 선언/집행 분리 구조는 스스로 봤으나 그 일을 **kube-proxy가 한다**고 답했습니다 — "노드포트·로드밸런서 때도 kube-proxy가 변환해줬으니 이번에도 동일하게 해야 하는 거 아닌가". (3) 프록시가 파드로 직접 보내는 이유는 "모르겠음"(미학습). (4) Prefix 매칭에서 `/foo/bar`·`/foo` 매칭, `/foobar` 불일치를 **맞혔으나** 근거를 "`/foo`로 시작하는 경우를 보는 거니깐"(문자열 접두사)으로 답했습니다. (5) Exact 우선을 **맞혔으나** 근거를 "규칙 중 먼저 만족하는 위에 선언된 것"(선언 순서)으로 답했습니다.
- **정답**: (1) 1대다는 L4에서도 됩니다(11-01의 cluster IP → 파드 다수). 문제는 **갈라지긴 하는데 기준을 정할 재료가 없다**는 것 — 커널이 파드를 고른 근거는 확률뿐이었습니다. 기준은 한 계층 위 페이로드(`GET /quote`, `Host:`)에 있고, **L4는 봉투만 보고 L7은 편지를 읽습니다.** DNAT은 헤더의 고정 위치 숫자를 덮어쓰는 일이고 L7 파싱은 TCP 스트림 재조립+구문 분석이라, 커널이 안 하고 사용자 공간 프로그램(Nginx·Envoy)이 합니다. (2) **kube-proxy는 Ingress를 감시조차 하지 않습니다**(Service·EndpointSlice만). 심을 수 있는 규칙이 "목적지 IP:port가 X면 Y로" 형태뿐이라 "경로가 /quote면"을 쓸 자리가 없습니다. 그 역할은 **Ingress 컨트롤러**(감시→`nginx.conf` 생성)와 **L7 프록시**(집행)가 맡습니다 — 구조는 11장과 같고 배우만 바뀝니다. (3) 컨트롤러가 **EndpointSlice를 감시**하므로 프록시는 파드 명단을 이미 압니다. 얻는 것은 DNAT 홉 제거 + **부하 분산의 주인이 커널의 확률에서 프록시의 L7 판단으로 이동**(쿠키 어피니티·재시도·정밀 카나리가 그래서 가능). cluster IP를 거치면 프록시가 애써 파싱한 판단이 커널의 확률에 버려집니다. (4) 기준은 문자열이 아니라 **`/`로 쪼갠 요소 단위** — `/foobar`는 `["foobar"]` 한 덩어리라 `foo`와 다른 요소입니다. 문자열 접두사로 보면 `/foobar`가 걸려야 하는데 실제로는 안 걸립니다. (5) 기준은 순서가 아니라 **구체성** — Prefix를 위로 올려도 Exact가 이깁니다. 순서 의존이면 §5의 Ingress 병합이 위험해지고, 구체성 우선이어야 "일반 규칙 + 예외" 패턴이 성립합니다.
- **원인 추정**: 11-03의 "규모를 안 곱함"과 **다른 결**입니다. 여기서는 **결론을 맞히면서 근거가 다른 모델에서 나옵니다.** (4)는 문자열 접두사 모델, (5)는 방화벽·`if-else` 체인 모델이 전이된 것 — 둘 다 일상적으로 익숙한 모델이라 답이 우연히 맞아떨어졌습니다. 답이 맞으니 그냥 넘어가기 쉬운데, 조건이 조금 바뀌면 무너집니다(순서를 뒤집은 반례가 증거). (1)(2)도 같은 결 — 11장에서 익힌 "cluster IP 1대1", "kube-proxy가 변환한다"를 **적용 범위를 확인하지 않고** 그대로 옮겼습니다. 반면 (2)의 선언/집행 분리 직관과 "파드 IP는 자주 바뀌는데 직접 통신하면 단점 아닌가"는 스스로 찾은 좋은 의문이었습니다(11장 원칙과의 충돌 감지 = 개념이 연결되는 신호).
- **2026-07-27 §5·§6 추가 인출**: (6) §5에서 "Ingress를 하나로 모으면 왜 IP가 하나인가"를 **자력 정답**("로드밸런서가 1개만 생겨서") — IP가 붙는 대상이 Ingress 오브젝트가 아니라 *그 때문에 생긴 LB*라는 것을 스스로 끌어냈습니다. 와일드카드는 오답 — `*.example.com`에 `foo.kiada.example.com`이 매칭될 것이라 봤으나, **`*`는 정확히 한 요소만 덮으므로** 4요소는 불일치입니다(`example.com`도 `*`가 덮을 요소가 없어 불일치). **경로는 `/`, 호스트는 `.`으로 쪼갠 요소 단위**라는 같은 원리이고, Prefix는 뒤에 요소가 더 붙어도 되지만 와일드카드는 개수가 고정이라는 대비가 핵심. (7) §6은 (a) 404·(b) 지정 서비스로 감을 자력 정답, (c) 규칙 없는 Ingress를 LB 대신 쓰는 이유는 "HTTP를 쓴다는 장점"까지만 — 정답은 **누가 HTTP를 이해하느냐**입니다(LB도 HTTP를 나르지만 L4라 봉투만 봄). 규칙이 없어도 **L7 중간자가 생기는 것 자체가 이득**이라 TLS 종료·인증·쿠키 어피니티·재작성·rate limit이 딸려 옵니다. 즉 §4·§5의 "결론 맞고 근거 틀림"이 §5 와일드카드에서 **세 번째로 반복**됐습니다(문자열 접두사 → 선언 순서 → 와일드카드 개수).
- **Phase 3 보류(생략 아님)**: 13장 환경 구축 시점으로 합칩니다. 이유 — ① **ingress-nginx 유지보수 종료**(공식 README 실측: 2026-03 이후 릴리스·버그픽스·보안 패치 없음, "새로 배포하지 말고 Gateway API 구현을 쓰라") ② 대체 컨트롤러(Traefik·Envoy 기반)는 Ingress API도 지원하므로 13장 준비가 12장 실습 환경을 겸함 ③ `cluster/kind-config.yaml`에 `extraPortMappings`가 없어 80·443 접근 불가 — 12·13장 공통 전제. ⛔ 단 **13장 실습으로 12장을 덮을 수는 없습니다** — `Ingress`와 `Gateway`+`HTTPRoute`는 다른 API이고 `defaultBackend`는 Gateway API에 대응물이 없습니다. 검증 목표 A~D와 전제는 `_todo-12-01-보강.md` §Phase 3 섹션에 기록.
- **2026-07-27 보강 완료**: 10항목 전부 반영됐습니다(다른 세션 수행, 268→432줄, SVG 4→9장). 커밋 `3ccdee86`(항목 1~8 — 계층·명단·매칭 3축 시각화 4종)과 `e17e6b71`(항목 9·10 — default backend 쓸모·와일드카드 요소 단위)입니다. 신규 SVG — `12-01-l4-vs-l7-visibility`, `12-01-lb-plus-proxy-stacking`(계층은 쌓인다), `12-01-proxy-to-pod-direct`, `12-01-endpoint-propagation-window`(전파 지연 창). `12-01-ingress-controller-role.svg`도 컨트롤러(설정 생성)/프록시(트래픽 처리) 분리가 보이게 재작도됐습니다. `_todo-12-01-보강.md`는 삭제했습니다. **개념 자체의 재검증은 다음 복습 몫**입니다(문서가 고쳐진 것과 인출이 되는 것은 별개).
- **참고 챕터**: 12-01 §1(세 요소·L4/L7·파드 직접)·§4(pathType)·§5(규칙 통합·와일드카드)·§6(default backend). 보강 TODO는 `_todo-12-01-보강.md`(10항목 중 8 완료 + Phase 3 전제). "**L4는 봉투만 보고 L7은 편지를 읽는다 / kube-proxy는 Ingress를 안 본다, 컨트롤러+프록시가 맡는다 / Prefix는 `/`로 쪼갠 요소 단위 / 겹치면 순서가 아니라 구체성으로 Exact 우선**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) 같은 요청 하나를 두고 L4가 보는 것과 L7이 보는 것을 각각 적기, (2) "Ingress 규칙을 kube-proxy가 처리하나"에 예·아니오와 *이유*로 답하기, (3) 프록시가 cluster IP를 건너뛰는 이유와 그래서 가능해지는 기능 3개 대기(미학습이라 첫 측정), (4) `/foo` Prefix에 `/foobar`가 왜 안 걸리는지 **요소로 쪼개서** 설명, (5) Exact/Prefix 순서를 뒤집은 YAML을 주고 어디로 가는지 답하기, (6) `*.example.com`에 `foo.kiada.example.com`·`example.com`이 걸리는지 답하고 **Prefix와 와일드카드의 차이**(뒤에 요소가 더 붙어도 되는가 vs 개수 고정)를 대비, (7) "규칙 없이 defaultBackend만 둔 Ingress를 LoadBalancer 대신 쓰는 이유"에 **L7 중간자** 축으로 답하기.

## 2026-07-27 — 메커니즘은 잡고 "규모·전파"를 놓침 (11-03 Phase 1)

- **자기 답**: 11-03 네 축을 예측·채점으로 돌린 결과. (1) EndpointSlice 도입 이유에서 "파드 100개면 IP 100개를 다 넣어둬서"까지는 자력이었으나 그것이 *노드 수만큼 곱해지는 것*을 못 봤고, 전송량을 "100곳에 **1개의 IP를 제거해달라**함"으로 답했습니다. "쪼개도 결국 100개인데 의미가 있나"로 막혔습니다. (2) `internalTrafficPolicy`는 "모르겠음"(미학습). (3) readiness 실패의 결과를 "읽기를 위한 대기"로만 답해 *엔드포인트에서 제거*를 못 짚었습니다. (4) 외부 의존 검사 함정에서 "**특정 파드에서만** 실패하면 제거되어 접속이 안 될 수 있다"고 답해 규모를 좁게 봤습니다. 자력 통과는 selector 없는 Service(자동으로 안 채워짐·직접 IP를 준다)와 headless(확률적 선택을 11-01에서 인출), readiness 없을 때 JVM 부팅 지연.
- **정답**: (1) 쿠버네티스 watch는 **델타가 아니라 오브젝트를 통째로** 보냅니다. 파드 1개 죽으면 각 수신자(노드별 kube-proxy 100곳)가 "IP 하나 삭제"가 아니라 **남은 99개가 전부 담긴 오브젝트**를 받아 9,900 IP분이 흐릅니다. 20개씩 5조각이면 바뀐 조각(19개)만 전송돼 1,900 IP분 — **저장량이 아니라 변경의 blast radius가 줄어드는 것**입니다. 최종 상태는 그대로 100개. (2) `internalTrafficPolicy`는 *클러스터 안 파드가 보낸* 트래픽에 걸리고, external과 달리 **앞단에 노드를 골라 주는 로드밸런서가 없어 불균등이 안 생깁니다**(11-02 불균등의 뿌리가 "LB는 노드 단위 균등, 노드마다 파드 수 상이"였으므로). 대신 구제 장치도 없어 DaemonSet 전제. (3) liveness=컨테이너 재시작 / **readiness=엔드포인트에서 제거**(프로세스는 그대로, 트래픽만 끊김). §1 명단에 파드를 넣고 빼는 스위치. (4) 외부 의존은 **모든 파드가 공유**하므로 Quote 1초 지연에 Kiada 파드 10개가 *동시에* 실패해 명단이 비고 전면 장애가 됩니다 — 특정 파드 문제가 아니라 공유 의존이라 필연. 의존 서비스 장애를 자기 장애로 **승격**시킨 것.
- **원인 추정**: 뿌리 하나입니다 — **사건의 메커니즘은 잡는데 "몇 곳에서 동시에 일어나는가"를 안 곱함.** (1)은 IP 개수를 봤지만 수신자 수를 안 곱했고, (4)는 명단 제거를 봤지만 파드 전원을 안 곱했습니다. 2026-07-27 11-02 오답의 "healthCheckNodePort를 켜면 균등해진다"(장치의 효과 *범위*를 좁게 봄)와 같은 결입니다. (2)는 이해 실패가 아니라 노출 부족. (3)은 이름("readiness")에서 취지는 추론했으나 이 편의 주제(엔드포인트)와 연결을 안 세운 것.
- **2026-07-27 보강 완료**: 8항목 전부 반영됐습니다(다른 세션 수행, 288→465줄). 신규 SVG 4장 — `11-03-endpointslice-blast-radius`(전파량 9,900→1,900), `11-03-headless-client-view`(커널 확률 선택 vs 앱 직접 순회), `11-03-internal-vs-external-traffic-policy`(대가 비대칭), `11-03-readiness-external-dependency-cascade`(파드 전원 동시 이탈). TODO에 없던 것도 들어갔습니다 — JVM DNS 캐싱 함정, Spring Cloud Kubernetes `DiscoveryClient` 비교. `_todo-11-03-보강.md`는 삭제했습니다. **개념 자체의 재검증은 다음 복습 몫**입니다(문서가 고쳐진 것과 인출이 되는 것은 별개).
- **참고 챕터**: 11-03 §1(EndpointSlice)·§3(internalTrafficPolicy)·§4(readiness). "**watch는 오브젝트를 통째로 보낸다 → 조각내면 blast radius가 줄어든다 / readiness 실패=재시작 아니라 명단 제거 / 외부 의존은 공유라 전원이 동시에 빠진다**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) 파드 N개·노드 M개를 임의로 주고 Endpoints vs EndpointSlice의 *변경 1회 전송량*을 계산, (2) `internalTrafficPolicy: Local`을 문서 안 보고 설명하고 external과 **대가가 왜 다른지** 답하기(미학습이라 첫 측정), (3) "readiness가 실패하면 컨테이너는 어떻게 되나"에 한 단어로 답하기, (4) 외부 의존 검사 시 **동시에 몇 개 파드가 빠지는지** 답하기.

## 2026-07-27 — namespace를 "격리 울타리"로 봄 (정의형 인출 실패)

- **자기 답**: 07-01 회차 1에서 namespace의 정의를 "자원과 권한을 분리하기 위한 쿠버네티스의 울타리"라 답했습니다. 동기를 물었을 때도 가시성 오염과 권한 경계만 나오고 *이름 충돌*이 두 번 연속 빠졌습니다. `kubectl get pods`가 보는 대상은 "현재 컨텍스트의 namespace"로 맞혔으나 `-n`·`-A`·`set-context` 세 명령을 인출하지 못했습니다. Terminating 진단은 conditions까지 갔으나 원인을 *finalizer*로 명명하지 못했습니다.
- **정답**: namespace의 1순위 축은 **이름의 스코프**입니다 — API 오브젝트를 겹치지 않는 그룹으로 조직하는 논리 장치이고, 그래서 팀 A와 팀 B가 둘 다 `kiada-ssl`이라는 Pod를 가질 수 있습니다(§1). 권한의 스코프는 그 위에 얹힌 **두 번째** 성질이고, 경계를 집행하는 것은 namespace가 아니라 **RBAC**입니다. 원문 §4는 "namespace는 이름과 권한을 나눌 뿐, 런타임과 네트워크는 나누지 못한다"고 명시합니다 — 같은 노드면 같은 OS 커널을 공유하고 네트워크는 기본 개방입니다. Terminating의 범인은 **finalizer**이며 `NamespaceFinalizersRemaining` condition이 지목합니다.
- **원인 추정**: 뿌리는 **"리눅스 네임스페이스의 격리 이미지가 쿠버네티스 namespace로 전이된 것"** 입니다. 원문 §진입이 두 개념의 혼동을 먼저 경고하는데, 격리 프레임을 쓰면 "울타리"가 먼저 나오고 이름 스코프가 뒤로 밀립니다. 다만 *적용* 문항에서는 "권한을 namespace별로 분리"를 스스로 도출했으므로 개념 자체는 있습니다 — **정의 형태의 인출 경로만 안 뚫린 상태**입니다(시나리오형 4점 / 정의형 2점). 명령 3종과 finalizer는 이해가 아니라 노출 부족입니다.
- **참고 챕터**: 07-01 §1(이름 충돌과 권한 경계)·§3(기본 namespace 전환)·§4(격리의 실체)·§5(finalizer). 원문 §빈칸 채우기가 `--all-namespaces`·`set-context`·`-o yaml`·`finalizers`를 그대로 묻습니다. "**namespace는 이름의 스코프가 1순위, 권한은 2순위, 런타임·네트워크는 아예 아님**" 한 줄.
- **재방문 트리거**: 2026-07-30 회차 2에서 (1) namespace를 문서 안 보고 한 문장으로 정의하고 리눅스 네임스페이스와의 차이를 덧붙이기, (2) §빈칸 채우기 4칸을 먼저 채우기, (3) "Terminating에 멈췄다"에 원인 한 단어로 답하기.

## 2026-07-27 — ClusterIP를 "정류장"으로 봄 + NodePort/LoadBalancer 타입이 노드 간 전달을 정한다고 봄

- **자기 답**: 11-02 재검증에서 세 번 연속 같은 지점에 걸렸습니다. (1) 목적지/출발지 변환을 물었는데 경로("PC → LB → 노드 → 파드")만 답하고 *어느 필드가 바뀌는지*를 안 나눴습니다. (2) 목적지가 "노드 → **클러스터**(ClusterIP)"로 바뀐다고 세 번 답했습니다 — 07-20 오답 노트의 "Cluster는 ClusterIP로 치환한다"와 같은 문장. (3) "NodePort 볼 때는 다른 노드로 전송을 못 해준 것 같은데 로드밸런서는 되나?"라고 물어 *타입*이 노드 간 전달을 정한다고 봤습니다. (4) 노드 3대(A:3/B:1/C:0)인데 로드밸런서 분배를 2대로 계산해 `50/3, 50`이라 답했습니다. (5) healthCheckNodePort를 켜면 "(a)처럼 균등하게" 된다고 답해 Local의 두 대가를 하나로 뭉쳤습니다.
- **정답**: **ClusterIP는 경로상의 정류장이 아니라 규칙표의 색인**입니다. 어느 인터페이스에도 안 붙은 가상 IP라 무언가를 경유시킬 실체가 없습니다. `ClusterIP:80`과 `노드IP:30080`은 같은 파드 목록을 가리키는 **두 입구**이고, node port로 들어온 패킷의 목적지는 `172.18.0.4:30080` → `10.244.2.9:8080`으로 **곧바로** 바뀝니다. 문서 §1의 "30080 → 80 → 8080" 표기는 매핑 관계이지 경유 경로가 아닙니다. **결정은 두 번** 내려집니다 — ① 로드밸런서가 *노드*를 고름(헬스체크+균등 분배, 파드는 안 봄) ② 도착한 노드 커널이 *파드*를 고름(후보 범위 = externalTrafficPolicy). 그래서 **다른 노드로 넘길 수 있는지는 정책이 정하고 타입이 정하지 않습니다** — Cluster면 NodePort도 넘기고, Local이면 LoadBalancer도 못 넘김. A:3/B:1/C:0에 100요청이면 Local은 노드 **셋 모두**에 33씩 → 파드당 11·11·11·33(3배 어긋남) + C의 33 유실. healthCheckNodePort를 켜면 C가 빠져 50씩 → 16.7·16.7·16.7·50 — **유실만 사라지고 3배 어긋남은 남습니다**. 불균등은 "LB는 노드 단위로 나누는데 노드마다 파드 수가 다르다"에서 나오므로 노드별 파드 수를 맞춰야(DaemonSet·topologySpreadConstraints) 줄어듭니다.
- **원인 추정**: 뿌리가 **"가상 IP를 실체 있는 경유지로 상상한 것"** 하나입니다. ClusterIP를 정류장으로 보면 목적지가 그리로 바뀐다고 답하게 되고, 07-20의 SNAT/DNAT 혼동도 여기서 재발했습니다. (3)의 타입 혼동은 다른 결 — 로드밸런서가 *노드를 고르는* 1단과 커널이 *파드를 고르는* 2단을 한 층으로 뭉쳐, 1단 장치(LB)의 유무로 2단 능력(노드 간 전달)을 설명하려 한 것입니다. (5)는 "장치가 있으면 문제가 다 풀린다"는 기대 — 대가가 둘이고 장치가 하나만 푼다는 비대칭을 안 세웠습니다.
- **참고 챕터**: 11-02 §3. 이번 세션에서 문서를 보강했습니다 — SVG 3장(`11-02-dnat-snat-two-fields`, `11-02-lb-picks-node-policy-picks-pod`, `11-02-external-traffic-policy` 3열 개정) + §1에 "ClusterIP는 색인" 교정 단락 + §3에 선언/집행 분리·2단 결정·SNAT 생략 기전·장치의 효과 범위. "**Cluster=NAT 2번(목적지→파드IP, 출발지→노드IP) / Local=NAT 1번(목적지만) + 대가 둘: 유실은 healthCheckNodePort로 막고 불균등은 못 막는다**" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) 문서 안 보고 목적지·출발지 두 줄을 채우고 각 변환의 주체와 "Local에서 사라지는 줄"을 적기, (2) 노드별 파드 수를 임의로 주고 Local·Local+healthCheck 두 경우의 파드당 요청 수를 계산해 *3배 비율이 안 바뀌는 것* 확인, (3) "ClusterIP는 정류장인가 색인인가"에 한 문장으로 답하기.

## 2026-07-24 — 이름 해석(DNS)과 주소 변환(DNAT)을 한 덩어리로 봄

- **2026-07-27 보강 완료**: `_todo-11-01-보강.md`의 세 항목을 모두 처리했습니다 — 항목 2·3(네 컴포넌트 시각화, 두 단계 주소 변환 시각화)은 `11-01-service-component-responsibilities.svg`·`11-01-dns-dnat-two-stage.svg`로 이미 반영돼 있었고, 항목 1(CoreDNS 정체)은 §5에 단락을 추가했습니다 — CoreDNS는 API 오브젝트가 아니라 `kube-system`의 Deployment 애드온이고 앞단 `kube-dns` Service의 ClusterIP가 파드 `nameserver`에 들어가므로 DNS 질의도 Service 트래픽과 같은 DNAT 경로를 지납니다 + 두 갈래 위임(클러스터 도메인은 자기가 답, 외부는 업스트림 forward). TODO 파일은 삭제했습니다. 개념 자체의 재검증은 다음 복습 몫입니다.
- **자기 답**: 11-01 복습에서 세 지점이 같은 뿌리로 막혔습니다. (1) `quiz`가 ClusterIP로 풀리는 과정에서 `/etc/resolv.conf`를 **누가** 채우는지 몰라 kube-proxy를 DNS 자리에 놓았습니다. (2) 네 컴포넌트 역할 분리(§3)에서 스케줄러만 정확했고, CNI가 `resolv.conf`를 채운다고 잘못 답했으며 kube-proxy는 "흐리게만 안다"고 했습니다. (3) 요청 한 번이 파드에 닿는 전 구간에서 "Service 오브젝트가 패킷을 받아 넘긴다", "라운드로빈으로 분산"이라 답했고 **주소가 두 번 바뀐다는 걸 못 떠올렸습니다.**
- **정답**: `quiz` → 파드IP까지 주소가 **두 번** 바뀝니다. **① 이름 해석(DNS)**: `quiz`를 CoreDNS가 ClusterIP(`10.96.136.190`)로 풀어줌 — `/etc/resolv.conf`는 **kubelet**이 파드 생성 시 씀(search 접미사 + ndots:5로 짧은 이름 판정). **② 주소 변환(DNAT)**: ClusterIP는 어느 인터페이스에도 안 붙은 **가상 IP**라, **보내는 쪽 노드 커널**이 목적지를 실제 파드IP(`10.244.2.9`)로 바꿔치기함. Service는 패킷을 받는 실체가 아님 — 받는 주체가 없고, kube-proxy가 심어둔 규칙을 근거로 커널이 변환. 파드 선택은 라운드로빈이 아니라 iptables random 모듈의 **확률적 선택**(실측 13/10/7). 네 컴포넌트: **스케줄러=노드 배정 / CNI=파드IP 발급(넷 중 IP를 유일하게 *만드는* 것) / EndpointSlice 컨트롤러=ready 파드IP를 명단에 옮겨 적기 / kube-proxy=명단으로 커널에 DNAT 규칙 심는 컨트롤러(패킷을 직접 나르지 않음)**.
- **원인 추정**: "이름→IP"와 "IP→IP"를 한 덩어리로 뭉뚱그려, 두 변환의 주체(CoreDNS vs 노드 커널)와 위치(DNS 단계 vs DNAT 단계)를 구분하지 못했습니다. kube-proxy를 DNS 자리에 놓은 것, Service가 패킷을 받아 넘긴다고 본 것이 전부 이 하나에서 나왔습니다. 2026-07-20 오답 노트의 공통 뿌리 "자동으로 채워지는 것의 내부 기전"과 같은 결입니다. A-1(expose selector)만 통과한 건 그게 라벨 매칭 문제라 이 축과 무관했기 때문입니다.
- **참고 챕터**: 11-01 §5(cluster DNS·NAT가 끼는 곳)·§3(네 컴포넌트 역할 분리). 깊은 기전은 `04-05.DNS와 CoreDNS`·`01-02.K8s 패킷 여정`. "이름→ClusterIP는 CoreDNS(DNS), ClusterIP→파드IP는 노드 커널(DNAT), 주소가 두 번 바뀐다" 한 줄. (보강 TODO는 `_todo-11-01-보강.md`)
- **재방문 트리거**: 다음 복습에서 (1) `quiz → ClusterIP → 파드IP` 두 화살표를 그리고 각 화살표의 주체(CoreDNS / 노드 커널)를 손으로 적기, (2) 네 컴포넌트를 "노드 배정·IP 발급·명단·길"로 나눠 문서 안 보고 나열.

## 2026-07-20 — externalTrafficPolicy Local: SNAT 정체 + "노드는 균등, 파드는 불균등"

- **자기 답**: 11-02 Phase 4 Q2에서 "Cluster는 ClusterIP로 치환한다"고 답해 *목적지(DNAT)와 출발지(SNAT)를 혼동*했습니다. Q5(Local의 두 번째 대가 = 불균등 분산 + healthCheckNodePort)는 "그게 있나?"로 막혀 설명이 필요했습니다.
- **정답**: 노드가 받은 요청을 다른 노드 파드로 넘길 때 두 NAT이 겹칩니다. **DNAT**(목적지 `노드IP:30080`→`파드IP:8080`)는 두 정책 다 함. **SNAT/masquerade**(출발지 `클라이언트IP`→`받은 노드IP`)는 **Cluster만** 함 — 응답이 반드시 받은 노드로 되돌아오게(비대칭 경로 방지) 출발지를 위조하는 대가로 파드가 보는 Client IP가 노드 IP가 됨. **Local은 SNAT을 안 해** 원본 IP 보존, 대신 "로컬 파드로만 라우팅"이라 파드 없는 노드는 타임아웃. 불균등: LB는 **노드 단위**로 균등 분배하는데 노드마다 파드 수가 달라(A:3, B:1) 파드당 부하가 어긋남(A 파드 11% vs B 파드 33%). 타임아웃 우회는 **healthCheckNodePort**(로컬 파드 있으면 200, 없으면 503) + LB 헬스체크로 파드 없는 노드를 분배에서 제외.
- **원인 추정**: (1) NAT을 "IP를 바꾸는 것" 하나로 뭉뚱그려 목적지/출발지 방향 구분을 안 세움. (2) Local의 대가를 "타임아웃" 하나만 알고 "불균등 분산"은 노드/파드 분배 단위가 다르다는 데서 나온다는 걸 못 떠올림. 오늘 3세션 공통 약점 "자동으로 채워지는 것의 내부 기전"과 같은 결(kube-proxy가 심는 SNAT 규칙).
- **참고 챕터**: 11-02 §3(externalTrafficPolicy). 뿌리는 `02_os/networking/01-02.K8s 패킷 여정` §1.2(POSTROUTING=SNAT)·§2(kube-proxy). "Cluster=SNAT함(Client IP=노드 IP), Local=SNAT안함(원본 보존)+로컬 파드만+healthCheckNodePort로 죽은 노드 제외" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) 한 요청이 다른 노드 파드로 갈 때 DNAT·SNAT이 각각 무엇을 바꾸는지 화살표로 그리기, (2) Local에서 노드별 파드 수를 임의로 주고 파드당 트래픽 비율을 계산.

## 2026-07-20 — DNS 짧은 이름 resolve: /etc/resolv.conf의 search와 ndots

- **자기 답**: 11-01 Phase 4 Q5(짧은 이름 `http://kiada`가 어떻게 완전한 이름으로 확장돼 resolve되나 + ndots:5 역할)에서 파일 경로(`/etc/resolv.conf`)만 근접하고 "왜 생기고 어떻게 하는지 모름"으로 막혔습니다.
- **정답**: kubelet이 파드 생성 시 `/etc/resolv.conf`를 자동으로 써넣습니다. `search default.svc.cluster.local svc.cluster.local cluster.local` 줄이 짧은 이름 뒤에 접미사를 차례로 붙여 매칭될 때까지 시도(→ `kiada.default.svc.cluster.local` 매칭). `options ndots:5`는 점이 5개 미만인 이름을 상대 이름으로 보고 search 접미사를 붙이게 함(k8s 최장 FQDN이 점 5개라 그 미만은 짧은 이름 취급). CoreDNS가 A 레코드로 ClusterIP를 반환.
- **원인 추정**: resolv.conf가 "자동으로 채워지는 것"이라는 것만 알고, search/ndots라는 확장 메커니즘 내부를 안 열어봤습니다. Q2(expose selector)와 함께 "자동으로 채워지는 것의 내부 동작"에서 공통으로 막힌 패턴.
- **참고 챕터**: 11-01 §5(cluster DNS). 실습 C-2에서 `http://kiada:8080`이 실제로 resolve된 것을 관찰. "search가 접미사 붙이고, ndots:5가 짧은 이름인지 판정" 한 줄.
- **재방문 트리거**: 다음 복습에서 파드 안 `cat /etc/resolv.conf`로 search·ndots를 직접 확인하고, `kiada`가 어느 접미사로 확장되는지 손으로 추적.

## 2026-07-20 — kubectl expose의 selector 함정: 파드 라벨을 전부 가져간다

- **2026-07-24 재검증**: 힌트 없이 자답 통과. SELECTOR `app=quiz,rel=stable`과 canary 0%(등록조차 안 됨)를 모두 인출했습니다. **해결**로 봅니다.
- **자기 답**: 11-01 Phase 4 Q2(`kubectl expose pod quiz`로 만든 Service의 selector 값 + canary 배포 시 문제)에서 selector가 `app=quiz` 하나일 거라 예측했고, canary 개념은 미학습이라 문제를 못 떠올렸습니다.
- **정답**: `kubectl expose pod`는 그 파드의 라벨을 *전부* selector로 복사합니다. 파드가 `app=quiz,rel=stable`이면 selector도 `app=quiz,rel=stable` 둘 다가 됩니다(실습 출력에서 `SELECTOR app=quiz,rel=stable` 확인). 나중에 canary(`app=quiz,rel=canary`)를 배포하면 `rel`이 stable이 아니라 selector에 안 맞아 **canary가 Service에 아예 등록조차 안 됩니다**(트래픽 0%). `kubectl set selector service quiz app=quiz`로 좁혀야 canary도 포함됩니다.
- **원인 추정**: "app=quiz 하나여야 한다"는 *목표 상태*는 직관했으나, expose가 그렇게 안 해주고 라벨을 전부 가져간다는 *실제 동작*을 몰랐습니다. Service 방식 자체는 멀쩡하고 expose의 selector 복사가 canary 시나리오와 어긋나는 것.
- **참고 챕터**: 11-01 §3(kubectl expose)·§4(set selector). "expose는 파드 라벨을 전부 selector로 가져가니, canary를 받으려면 app=quiz로 좁혀라" 한 줄. (canary = 신버전을 일부 트래픽에만 흘려 검증하는 배포. k8s 기본은 파드 개수 비율로 대략 조절, 정밀 %는 L7 필요.)
- **재방문 트리거**: 다음 복습에서 `kubectl expose pod`가 만든 selector를 예측하고 `kubectl get svc -o wide`로 실제 SELECTOR와 대조. canary 파드가 왜 트래픽을 못 받는지 설명.

## 2026-07-20 — 스냅샷 복원 시 dataSourceRef.apiGroup: 왜 스냅샷만 필수인가

- **자기 답**: Phase 4 Q4(PVC 소스 vs 스냅샷 소스에서 dataSourceRef가 딱 하나 달라지는 점)에서 "모르겠다"로 막혔습니다. 이어서 "core 그룹 API인 것과 아닌 것의 차이가 뭐냐"고 물었습니다 — API 그룹 구분 자체가 흐릿했습니다.
- **정답**: 스냅샷을 소스로 쓸 때만 `apiGroup: snapshot.storage.k8s.io`를 명시해야 합니다. PVC/PV는 **core 그룹**(`apiVersion: v1`, 그룹명이 빈 문자열 `""`)이라 `apiGroup`을 생략하면 core로 해석돼 찾아지지만, VolumeSnapshot은 **named 그룹**(`snapshot.storage.k8s.io/v1`)이라 생략하면 "core 그룹의 VolumeSnapshot"을 찾다 실패합니다. core 그룹은 Pod·Service·PVC·ConfigMap 등 초기 근본 오브젝트의 예약 공간(`/api/v1/...`)이고, 그 외는 전부 `apps/v1`·`snapshot.storage.k8s.io/v1`처럼 그룹명이 붙는 named 그룹(`/apis/<group>/...`)입니다.
- **원인 추정**: 오브젝트의 `apiVersion:` 접두어가 곧 API 그룹 소속이고, "생략 = core 그룹" 규칙이라 core에 없는 오브젝트는 그룹 명시가 필수라는 연결을 안 짚었습니다.
- **참고 챕터**: 10-03 §3(스냅샷에서 PV 복원). "apiGroup 생략 = core 그룹을 뒤져라, 스냅샷은 core에 없으니 그룹명 명시 필수" 한 줄.
- **재방문 트리거**: 다음 복습에서 (1) PVC 소스 복제 YAML과 스냅샷 소스 복원 YAML의 dataSourceRef를 나란히 쓰고 차이를 설명, (2) `kubectl api-resources` 출력에서 core(`v1`)와 named 그룹 오브젝트를 각각 두 개씩 골라내기.

## 2026-07-20 — ephemeral vs emptyDir: PV 기능 상속을 스스로 못 떠올림

- **자기 답**: Phase 4 Q5(emptyDir가 못 하는데 ephemeral은 되는 것 3가지+와 그 능력의 출처)에서 힌트 후에도 "모르겠다"로 막혔습니다. Phase 1에서도 4개 중 1개(data source)만 맞혔던 지점입니다.
- **정답**: ephemeral이 되는 것 — (1) 스냅샷·복원, (2) data source 초기화, (3) 고정 크기 제한(파드 초과 불가), (4) 리사이즈. 이 능력들은 전부 **ephemeral 볼륨으로 만들어지는 게 "정상 PV"이기 때문**에 나옵니다(실습에서 파드 생성 시 진짜 PVC·PV가 자동 생성돼 바인딩되는 것을 관찰). emptyDir는 노드 로컬 디렉터리라 이 PV 기능이 하나도 없습니다.
- **원인 추정**: 개별 사실(스냅샷·리사이즈)은 알지만, "ephemeral = 정상 PV → PV의 모든 기능을 물려받음"이라는 *상속(transfer)* 프레임을 못 세웠습니다. Q4(apiGroup)와 함께 "배운 것을 다른 맥락에 적용"에서 공통으로 막힌 패턴.
- **참고 챕터**: 10-03 §5. "ephemeral은 정상 PV라 스냅샷·data source·고정크기·리사이즈를 물려받고, emptyDir는 그냥 노드 디렉터리" 한 줄.
- **재방문 트리거**: 다음 복습에서 emptyDir와 ephemeral 각각 되는 것/안 되는 것을 표 없이 나열하고, "왜 ephemeral만 되나"를 한 문장(정상 PV)으로 답.

## 2026-07-14 — field selector와 fieldRef의 혼동: 같은 "field"인데 다른 메커니즘

- **자기 답**: Downward API의 `valueFrom.fieldRef.fieldPath`를 보고 "이게 07-03의 field selector 검색과 같은 것이냐"고 물었습니다 — 이름이 둘 다 field라 같은 개념으로 묶어 생각했습니다.
- **정답**: 완전히 다른 메커니즘입니다. **field selector**(07-03)는 클러스터 *바깥*에서 여러 오브젝트를 *필터링·조회*하는 검색(`kubectl get pods --field-selector spec.nodeName=X` → 조건 맞는 Pod 리스트)이고, **fieldRef**(08-03 Downward API)는 클러스터 *안*에서 한 Pod가 *자기 값 하나를 컨테이너에 주입*하는 참조(`valueFrom.fieldRef.fieldPath: spec.nodeName` → 그 값을 env/파일로)입니다. 방향이 정반대입니다 — 하나는 밖에서 안으로 걸러 보고, 하나는 안에서 자기 값을 꺼내 넣습니다.
- **원인 추정**: `spec.nodeName` 같은 필드 경로 문자열을 양쪽이 공유하는 것을 보고, "재료가 같으니 도구도 같다"고 묶었습니다. 재료(필드 경로)는 공유하지만 도구(검색 필터 vs 주입 참조)는 다릅니다.
- **참고 챕터**: 07-03 §1(field selector)·08-03 §5(fieldRef). "field selector = 밖에서 오브젝트 골라내는 검색, fieldRef = 안에서 자기 값 꺼내 넣는 주입" 한 줄로 정리함.
- **재방문 트리거**: 2026-07-16 복습에서 `spec.nodeName`을 field selector와 fieldRef 각각의 문법으로 쓰고, 두 결과(Pod 리스트 vs env 값 하나)가 어떻게 다른지 말로 설명.

## 2026-07-14 — Secret 주입: env vs 볼륨 트레이드오프를 스스로 못 떠올림

- **자기 답**: Phase 4 Q5(볼륨이 더 안전한데 env를 왜 안 없애나)에서 "모르겠다"로 막혔고, 힌트를 받은 뒤에야 "env가 코드상 편하고 Spring relaxed binding으로 자동 처리된다"에 도달했습니다.
- **정답**: env의 장점은 (1) 앱 코드가 단순(`getenv` 한 줄 vs 파일 열기·읽기·파싱), (2) 12-factor 관례로 프레임워크가 env를 기본 기대(Spring은 `SPRING_DATASOURCE_PASSWORD` env를 relaxed binding으로 자동 매핑)입니다. 판단 기준은 "비밀값이면 볼륨(유출 표면 회피), 비밀 아니면 env(편의)". 이는 Q3에서 본 "Downward API(POD_IP 등 공개 정보)는 env여도 되지만 Secret은 볼륨이 낫다"와 같은 판단 축입니다.
- **원인 추정**: env의 *위험*(Q3에서 답한 로그·자식 상속)은 알았지만, 그 반대편인 *편의*를 장점으로 뒤집어 세우지 못했습니다. 한 축(위험)만 보고 트레이드오프의 다른 축(편의)을 못 떠올린 것.
- **참고 챕터**: 08-03 §3(env 주입 경고)·Spring 관점(configtree). "편의는 env, 안전은 볼륨" 한 줄.
- **재방문 트리거**: 2026-07-16 복습에서 "DB 비밀번호"와 "로그 레벨"을 각각 어느 경로로 주입할지와 그 이유를 답. 두 값의 성격(비밀/공개)으로 갈리는 것을 설명.

## 2026-07-14 — ConfigMap 생성 방식: --from-file과 --from-env-file

- **자기 답**: 두 방식의 차이를 파일 원문과 값 치환의 차이라고 설명했습니다.
- **정답**: `--from-file`은 파일명을 키로, 파일 전체를 값으로 저장합니다. `--from-env-file`은 각 `KEY=value` 줄을 별도 엔트리로 만들며 어느 쪽도 값을 치환하지 않습니다.
- **원인 추정**: ConfigMap의 `data`가 key-value 맵이라는 구조보다 명령 옵션의 이름만 기억했습니다.
- **재방문 트리거**: 2026-07-17 복습에서 두 명령이 만드는 `data` 구조를 YAML 없이 말로 설명합니다.

## 2026-07-14 — ConfigMap 누락: Pod phase와 컨테이너 waiting reason

- **자기 답**: 필수 ConfigMap이 없으면 실행에 실패하고 optional이면 성공한다고만 설명했습니다.
- **정답**: 참조 컨테이너는 `state.waiting.reason=CreateContainerConfigError`이고 Pod phase는 `Pending`입니다. `optional: true`면 컨테이너는 실행되지만 해당 환경변수는 존재하지 않습니다.
- **원인 추정**: `kubectl get pod`의 STATUS 열을 Pod phase로 받아들여 상태 계층을 구분하지 못했습니다.
- **재방문 트리거**: 2026-07-17 복습에서 phase, container state, waiting reason을 세 줄로 구분합니다.

## 2026-07-14 — ConfigMap 볼륨: 일반 마운트와 subPath 갱신

- **자기 답**: ConfigMap을 수정했을 때 일반 볼륨과 `subPath`가 어떻게 달라지는지 설명하지 못했습니다.
- **정답**: 일반 ConfigMap 볼륨은 kubelet 동기화 후 갱신되지만 `subPath`로 마운트한 파일은 갱신되지 않습니다. 일반 파일이 바뀌어도 애플리케이션이 다시 읽어야 실제 동작이 바뀝니다.
- **원인 추정**: 볼륨의 파일 갱신과 애플리케이션 설정 재로딩을 하나의 동작으로 묶어 생각했습니다.
- **재방문 트리거**: 2026-07-17 복습에서 파일 갱신 주체와 애플리케이션 반영 주체를 각각 답합니다.

## 2026-07-14 — label selector: equality-based와 set-based의 구분

- **자기 답**: Phase 4 Q2(셀렉터 두 종류)와 Q3(nodeSelector vs nodeAffinity)에서 연달아 "모르겠다"로 막혔습니다. `in`·`OR` 조건이 된다는 것만 알았고, 이것이 두 종류 중 어디에 속하는지, AND(콤마)가 별도 종류인지 아닌지를 구분하지 못했습니다.
- **정답**: 셀렉터는 두 종류뿐입니다. **equality-based**는 등호로 끝나는 것(`=`, `!=`)이고, **set-based**는 등호로 안 되는 것 넷(`in`·`notin`·키 존재(`rel`)·키 부재(`!rel`))입니다. 콤마(`app=payment,rel=stable`)는 세 번째 종류가 아니라 두 종류를 잇는 AND 연결일 뿐입니다. `nodeSelector`는 equality-based만, `nodeAffinity`는 set-based까지 쓰는 확장이라, 둘의 차이도 결국 이 한 구분에서 나옵니다.
- **원인 추정**: `in`을 개별 기능으로만 외웠고 "등호로 되나 안 되나"라는 상위 분류 축을 잡지 못했습니다. Q2·Q3이 같은 뿌리에서 함께 막힌 것이 그 증거입니다.
- **참고 챕터**: 07-02 §4(셀렉터 두 종류)·§5(nodeSelector/nodeAffinity). 다지기 세션에서 "등호로 되면 equality, 안 되면 set-based, 콤마는 AND" 한 줄로 정리함.
- **재방문 트리거**: 2026-07-15 복습에서 다섯 요구(값 일치·여러 값 중 하나·키 존재·키 부재·값 불일치)를 셀렉터 문법으로 빈칸 채우기. set-based 4형제를 문서 안 보고 나열.

## 2026-07-13 — command·args와 환경변수: 미해결 참조와 미정의 변수 조회

- **자기 답**: 미정의 환경변수에 `printenv`를 실행하면 참조 표현이 그대로 남는다고 답했습니다.
- **정답**: Kubernetes가 확장하지 못한 `$(UNKNOWN)`은 문자열로 남지만, 실행 중 `printenv UNKNOWN`은 출력 없이 종료 코드 1을 반환합니다.
- **원인 추정**: Pod 스펙을 구성하는 시점과 컨테이너 안에서 명령을 실행하는 시점을 혼동했습니다.
- **재방문 트리거**: 2026-07-16 복습에서 두 상황을 나란히 비교합니다.

## 2026-07-13 — command·args와 환경변수: exec와 종료 신호

- **자기 답**: 셸이 PID 1이면 종료 문제가 생긴다는 방향은 알았지만 과정을 구체적으로 설명하지 못했습니다.
- **정답**: `exec`는 셸을 JVM으로 교체해 JVM이 PID 1이 되게 하며, JVM이 SIGTERM을 직접 받아 graceful shutdown을 수행할 수 있게 합니다.
- **원인 추정**: PID 1의 중요성을 결과로만 기억하고 프로세스 교체와 신호 전달 메커니즘을 연결하지 못했습니다.
- **재방문 트리거**: 2026-07-16 복습에서 `sh → java`와 `exec java`의 프로세스 트리를 말로 설명합니다.
