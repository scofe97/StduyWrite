---
title: Service 기초 — 파드 통신·ClusterIP·세션 어피니티
tags: [kubernetes, k8s, service, clusterip, dns, session-affinity, kubernetes-in-action]
status: draft
source:
  - 《Kubernetes in Action, Second Edition》(Marko Lukša, Manning, 2025) Ch11 §11.0~11.1
  - https://kubernetes.io/docs/concepts/services-networking/service/  # Service (2026-07-04 조회)
related:
  - ./11-02.외부 노출 — NodePort·LoadBalancer·트래픽 정책.md
  - ./11-03.엔드포인트·DNS·토폴로지·readiness.md
  - ./10-03.PV 관리 — 리사이즈·스냅샷·ephemeral.md
  - ../../kubernetes/04_networking/04-04.Service%EC%99%80%20EndpointSlice.md
  - ../../kubernetes/04_networking/04-05.DNS%EC%99%80%20CoreDNS.md
  - ./README.md
updated: 2026-08-12
---

# Service 기초 — 파드 통신·ClusterIP·세션 어피니티
---
> 파드는 언제든 지워지고 새 IP로 다시 뜨기 때문에, 파드 IP를 직접 붙잡으면 안 됩니다. **Service는 같은 일을 하는 파드 묶음 앞에 변하지 않는 단일 IP를 세워, 클라이언트가 그 IP 하나만 알면 되게 합니다.**



> 이 문서를 읽고 나면 파드끼리 NAT 없이 통신하는 이유를 설명하고, ClusterIP Service를 label selector로 만들어 클러스터 안에서 접근하며, 세션 어피니티가 왜 쿠키가 아니라 클라이언트 IP 기준인지 근거와 함께 말할 수 있습니다.

![11-01 전체 지도 — 변하는 파드 IP를 변하지 않는 하나의 주소로](_assets/11-01.chapter-overview.svg)



## 진입 — 왜 파드 IP를 직접 쓰면 안 되는가

> 오늘날은 한 파드가 아니라 여러 replica를 여러 노드에 흩어 돌립니다. 이때 클라이언트가 replica 하나하나의 IP를 추적하는 대신, 단일 주소로 묶어 접근하게 하는 것이 Service입니다.

이사를 자주 다니는 친구에게 편지를 보낸다고 해 봅시다. 집 주소를 외워 두면 이사할 때마다 편지가 반송됩니다. 대신 **평생 바뀌지 않는 사서함 번호** 하나를 받아 두면, 친구가 어디로 옮기든 편지는 도착합니다. 파드는 이사를 아주 자주 다니는 친구이고, Service가 그 사서함입니다.

이 편은 그 사서함이 어떻게 동작하는지를 따라갑니다.

- 누가 사서함 뒤에 설지 정합니다 — §2
- 사서함 번호를 받습니다 — §3
- 그 번호로 실제 파드까지 편지가 가는 길을 봅니다 — §5·§7

Kiada 애플리케이션은 세 서비스로 이뤄집니다 — Kiada 서비스가 Quote 서비스에서 책 인용구를, Quiz 서비스에서 퀴즈 문제를 받아 응답에 합칩니다. 각 서비스는 여러 파드 replica로 제공되므로, replica의 개수와 IP가 수시로 바뀝니다. Service 오브젝트가 이 변동을 흡수하는 안정적 접근점입니다.

파드 IP를 직접 쓰지 못하는 이유는 세 가지입니다. 

1. 파드는 ephemeral이라 노드 축출·노드 장애·스케일 축소 등으로 언제든 지워지고 대체됩니다. 
2. 파드는 노드에 배정될 때 비로소 IP를 받으므로 미리 알 수 없습니다. 
3. 수평 확장에서는 replica마다 IP가 달라, 하나의 IP나 DNS 이름 뒤에서 부하를 분산해 줄 무언가가 필요합니다.



## 1. 파드끼리의 통신 — flat 네트워크

> **클러스터의 모든 파드는 하나의 사설 flat 네트워크로 연결됩니다.** 파드가 다른 파드에 패킷을 보낼 때 SNAT도 DNAT도 일어나지 않아, 받는 쪽은 보낸 파드의 IP를 출발지로 그대로 봅니다.

먼저 용어 하나만 짚고 갑니다. **NAT**(Network Address Translation)는 패킷이 지나갈 때 주소를 바꿔치기하는 것입니다. 바꾸는 자리에 따라 이름이 갈립니다.

- **SNAT** (Source NAT) — 출발지 주소를 바꿉니다
- **DNAT** (Destination NAT) — 목적지 주소를 바꿉니다

우편물에 비유하면 SNAT는 보낸 사람 주소를, DNAT는 받는 사람 주소를 중간에 고쳐 적는 일입니다. 이 절의 요지는 **파드끼리는 둘 다 일어나지 않는다**는 것입니다.

각 파드는 자기 IP를 가진 네트워크 인터페이스를 갖습니다. 노드들이 여러 라우터를 사이에 두고 멀리 떨어져 있어도, **파드는 NAT가 필요 없는 자신들의 flat 네트워크 위에서 통신**합니다. 이 파드 네트워크는 대개 노드를 잇는 실제 네트워크 위에 얹힌 소프트웨어 정의 네트워크입니다.

파드가 다른 파드로 패킷을 보낼 때 출발지 IP·포트와 목적지 IP·포트는 바뀌지 않습니다. 보내는 파드가 받는 파드의 IP를 알면 그대로 패킷을 보낼 수 있고, 받는 파드는 보낸 쪽 IP를 출발지로 봅니다. 네트워크 플러그인은 여러 종류가 있지만 모두 이렇게 동작해야 하므로, 두 파드가 같은 노드에 있든 다른 지역의 노드에 있든 통신 방식은 같습니다. 애플리케이션 관점에서 노드 사이의 실제 토폴로지는 중요하지 않습니다.

![파드는 NAT 없는 flat 네트워크로 통신합니다 — 노드가 지리적으로 흩어져 있어도 파드끼리 IP를 그대로 주고받습니다](_assets/11-01-pod-flat-network.svg)

> **Spring 관점.** Boot 앱이 다른 Boot 앱을 `RestClient`로 호출할 때, 이 flat 네트워크 덕분에 상대 파드 IP만 알면 바로 닿습니다. 다만 그 IP가 불안정하다는 것이 이 장 전체의 문제의식이고, 그래서 IP 대신 Service 이름(`http://quote`)을 URL에 씁니다.



## 2. Service가 파드를 선택하는 방법 — label selector

> **Service는 label selector로 자기 뒤에 설 파드를 정합니다.** 파드에 라벨을 달고 Service에 selector를 적으면, selector에 맞는 라벨을 가진 파드가 그 Service의 일원이 됩니다.

Service는 여러 파드가 뒷받침할 수 있습니다. Service에 연결하면 그 연결은 뒷받침 파드 중 하나로 넘어갑니다. 어느 파드가 이 Service에 속하는지는 label selector가 정합니다. 파드 오브젝트에 라벨을 달고 Service 오브젝트에 selector를 지정하면, 라벨이 selector에 맞는 파드가 Service에 포함됩니다.

![label selector가 어느 파드를 Service에 넣을지 정합니다 — selector는 네임스페이스 안에서만 매칭합니다](_assets/11-01-service-label-selector.svg)

- Quote 서비스의 selector는 `app=quote`입니다. 이 selector는 stable이든 canary든 `app` 키에 `quote` 값을 가진 모든 quote 파드를 고릅니다. 
- 파드에 붙은 다른 라벨은 매칭에 영향을 주지 않습니다. 위 그림에서 `app: quiz` 파드는 selector에 맞지 않아 제외되고, 같은 라벨을 가졌어도 다른 네임스페이스에 있는 파드는 selector가 네임스페이스 안에서만 매칭하므로 포함되지 않습니다.



## 3. ClusterIP Service 만들기

> **타입을 지정하지 않으면 ClusterIP Service가 만들어집니다.** 이 타입은 **클러스터 안에서만 접근되며, 클러스터 내부 파드끼리 쓰는 Quiz·Quote 서비스에 알맞습니다.**

쿠버네티스 Service 타입은 ClusterIP·NodePort·LoadBalancer·ExternalName 넷입니다. ClusterIP는 클러스터 내부 전용이고, 타입을 적지 않으면 기본으로 이 타입이 됩니다. Kiada 서비스는 외부에도 노출돼야 하므로 ClusterIP만으로는 부족한데, 이 부분은 [11-02](./11-02.%EC%99%B8%EB%B6%80%20%EB%85%B8%EC%B6%9C%20%E2%80%94%20NodePort%C2%B7LoadBalancer%C2%B7%ED%8A%B8%EB%9E%98%ED%94%BD%20%EC%A0%95%EC%B1%85.md)에서 다룹니다.

quote Service의 최소 매니페스트입니다.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: quote
spec:
  type: ClusterIP        # 기본값 — 클러스터 안에서만 접근
  selector:
    app: quote           # 이 라벨을 가진 파드가 뒷받침
  ports:
  - name: http           # 포트가 하나여도 이름을 붙이는 게 좋습니다
    port: 80             # Service의 cluster IP에서 여는 포트
    targetPort: 80       # 파드에서 연결이 향하는 포트
    protocol: TCP
```

- 이 매니페스트는 `app=quote` 라벨을 가진 파드 중 하나를 무작위로 골라 80번 포트로 연결을 넘기는 ClusterIP Service를 정의합니다. 
- 포트가 여럿이면 각 포트에 이름을 반드시 붙여야 하고, 포트가 하나여도 붙이는 편이 낫습니다. `targetPort`에는 포트 번호 대신 파드 정의의 포트 이름을 적을 수도 있는데, 그러면 뒷받침 파드마다 포트 번호가 달라도 Service가 올바른 대상 포트를 씁니다.

### kubectl expose로 만들기

매니페스트를 `kubectl apply`하는 대신 `kubectl expose`로도 Service를 만들 수 있습니다.

```bash
kubectl expose pod quiz --name quiz
# service/quiz exposed
```

- 이 명령은 quiz 파드의 라벨을 읽어, 그 라벨 전부에 맞는 selector를 가진 Service를 만듭니다. 3장에서 Deployment를 expose했을 때는 Deployment의 selector를 가져와 그 파드 전부를 노출했습니다.

### 만든 Service 확인

Service를 만들면 클러스터 안 워크로드가 파드에 닿을 수 있는 내부 IP, 곧 cluster IP가 배정됩니다.

```bash
kubectl get svc -o wide

# NAME    TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)    AGE   SELECTOR
# quiz    ClusterIP   10.96.136.190   <none>        8080/TCP   15s   app=quiz,rel=stable
# quote   ClusterIP   10.96.74.151    <none>        80/TCP     23s   app=quote
```

- `kubectl expose`로 만든 quiz Service는 selector가 `app=quiz,rel=stable`입니다 — quiz 파드가 가진 라벨을 그대로 가져왔기 때문입니다. 
- 그런데 나중에 canary를 배포하면 트래픽이 canary에도 가야 하므로, selector가 stable에만 묶이는 건 대개 바라는 바가 아닙니다.

### 실제 확인 — kind에서 Endpoints 자동 등록 (03-02 배포 실습)

label selector 개념을 kind에서 직접 실증한 기록입니다. [03-02. 첫 애플리케이션 배포와 스케일링](./03-02.%EC%B2%AB%20%EC%95%A0%ED%94%8C%EB%A6%AC%EC%BC%80%EC%9D%B4%EC%85%98%20%EB%B0%B0%ED%8F%AC%EC%99%80%20%EC%8A%A4%EC%BC%80%EC%9D%BC%EB%A7%81.md)에서 배포한 kiada Deployment로 확인했습니다.

`kubectl expose`로 Service를 만들면 고정된 ClusterIP가 할당됩니다. Service는 `app=kiada` 라벨을 가진 Pod를 스스로 찾아 **Endpoints**에 등록합니다.

```bash
kubectl expose deployment kiada --port=8080 --target-port=8080
kubectl get svc kiada
#   kiada   ClusterIP   10.96.154.45   <none>   8080/TCP   ← 이 IP는 안 바뀜

kubectl get endpoints kiada
#   kiada   10.244.1.8:8080            ← Service가 물고 있는 실제 Pod
```

![Service → Endpoints — 라벨 셀렉터가 Pod를 자동 등록](_assets/03-02-service-endpoints-selector.svg)

핵심은 두 가지입니다.

- **ClusterIP(`10.96.154.45`)는 Pod가 죽고 새로 떠서 IP가 바뀌어도 변하지 않습니다** — 그래서 클라이언트는 이 IP 하나만 알면 됩니다.
- Service는 라벨 셀렉터로 대상 Pod를 **자동으로** 찾습니다.

Pod를 3개로 스케일하면, 내가 Service를 건드리지 않았는데도 새 Pod 두 개가 `app=kiada` 라벨을 갖자마자 Endpoints에 추가됩니다. 

- 목록이 세 개(`10.244.1.8`, `10.244.1.9`, `10.244.2.5`)로 늘어납니다. 이 중 `10.244.2.5`는 다른 노드(worker2)의 Pod였는데도 자동 등록됐습니다. 
- "Pod 집합이 바뀌어도 진입점은 고정"이라는 Service의 존재 이유가 여기서 실증됩니다.

### 누가 IP를 할당하고 누가 명단에 적나 — 네 컴포넌트의 역할 분리

"파드가 뜨면 IP가 붙고 Endpoints에 자동 등록된다"는 한 문장 뒤에는 서로 다른 네 컴포넌트가 각자 한 가지 일만 맡아 협업하는 구조가 있습니다. 이 넷을 섞으면 "누가 IP를 만드나"에서 혼란이 옵니다.

![스케줄러·CNI·EndpointSlice 컨트롤러·kube-proxy의 준비 단계와 실제 요청 경로](_assets/11-01-service-component-responsibilities.svg)

| 컴포넌트 | 맡는 일 | IP와의 관계 |
|----------|---------|------------|
| **스케줄러** | 파드를 어느 노드에 놓을지만 결정 | IP에 관여하지 않음 |
| **CNI 플러그인** (kindnet 등) | 파드가 그 노드에 뜰 때 실제 IP(`10.244.x.x`) 발급 | IP를 *만든다* |
| **EndpointSlice 컨트롤러** (kube-controller-manager) | selector ↔ 파드 라벨을 감시해 ready 파드 IP를 명단에 등록·삭제 | IP를 *가져다 적는다* |
| **kube-proxy** | 그 명단으로 각 노드의 ClusterIP→파드 전달 규칙 관리 | IP로 *길을 놓는다* |

- 한 줄로 외우면 이렇습니다 — **스케줄러=노드, CNI=IP 발급, EndpointSlice 컨트롤러=명단, kube-proxy=길**. IP를 실제로 만드는 건 CNI 하나뿐입니다. 
- `10.96.154.45`가 Service VIP(Virtual IP)입니다.
- 컨트롤러는 그 IP를 명단에 옮겨 적을 뿐입니다. kube-proxy는 그 명단으로 §5에서 본 DNAT 길을 놓습니다.

> **Endpoints는 deprecated, 지금은 EndpointSlice.** 
>
> EndpointSlice 컨트롤러는 같은 네임스페이스 안에서 Service selector와 Pod 라벨을 비교해, 해당 Service가 사용할 endpoint들을 EndpointSlice에 추가·갱신·삭제합니다. 위에서 쓴 `kubectl get endpoints`의 `Endpoints`(코어 v1) 오브젝트는 v1.33부터 deprecated이고, 표준은 `discovery.k8s.io/v1`의 **EndpointSlice**입니다. 
>
> v1.33 이상 클러스터에서 `kubectl get endpoints`를 쓰면 deprecation 경고가 뜹니다. 최신 명령은 `kubectl get endpointslices -l kubernetes.io/service-name=kiada`입니다. *왜* 바뀌었는지(대규모 Service 확장성)와 조각 상한 등 상세는 [11-03 § "Endpoints와 EndpointSlice"](./11-03.%EC%97%94%EB%93%9C%ED%8F%AC%EC%9D%B8%ED%8A%B8%C2%B7DNS%C2%B7%ED%86%A0%ED%8F%B4%EB%A1%9C%EC%A7%80%C2%B7readiness.md)가 정본입니다.



## 4. 만든 뒤에 Service 고치기

> selector와 포트는 만든 뒤에도 바꿀 수 있습니다. selector는 `kubectl set selector`로, 포트는 `kubectl edit`이나 매니페스트 재적용으로 고칩니다.

quiz Service의 selector를 `rel: stable` 없이 `app: quiz`만으로 좁히려면 이렇게 합니다.

```bash
kubectl set selector service quiz app=quiz
# service/quiz selector updated
```

- 이렇게 selector를 바꾸는 방식은 여러 버전을 배포하고 클라이언트를 한 버전에서 다른 버전으로 돌릴 때 유용합니다. 
- 포트를 바꾸려면 `kubectl edit svc quiz`로 `port`를 8080에서 80으로 고치되, quiz 파드가 실제로 듣는 포트는 8080이므로 `targetPort`는 8080으로 둡니다.

### Service spec의 기본 필드

Service 오브젝트 spec의 기본 필드입니다.

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | string | ClusterIP·NodePort·LoadBalancer·ExternalName 중 하나. 기본 ClusterIP |
| `clusterIP` | string | 클러스터 내부 IP. 보통 비워 두고 쿠버네티스가 배정. `None`이면 headless (11-03, StatefulSet 결합은 16-01) |
| `selector` | map | 이 Service가 트래픽을 넘길 파드가 가져야 할 라벨. 안 적으면 엔드포인트를 직접 관리 (11-03) |
| `ports` | []Object | 노출 포트 목록. 각 항목에 name·protocol·appProtocol·port·nodePort·targetPort |

### IPv4/IPv6 듀얼스택

쿠버네티스는 IPv4와 IPv6를 함께 지원합니다. `ipFamilyPolicy`로 single-stack(기본)·PreferDualStack·RequireDualStack을 고릅니다. 

- 만든 뒤 `spec.ipFamilies` 배열이 배정된 IP family(IPv4·IPv6)를 알려 줍니다. 듀얼스택 Service에서는 `spec.clusterIP`가 IP 하나만 담고, `spec.clusterIPs`가 IPv4·IPv6 둘 다를 `ipFamilies` 순서대로 담습니다.



## 5. 클러스터 안에서 Service 접근

> **ClusterIP Service는 클러스터 안에서만 접근됩니다.** 내 노트북에서는 닿지 않으므로, 노드에 ssh로 들어가거나 `kubectl exec`로 기존 파드 안에서 curl을 실행해 확인합니다.

quote-001 파드 안에서 셸을 열고 두 서비스에 접근해 봅니다.

```bash
kubectl exec -it quote-001 -c nginx -- sh

# / # curl http://10.96.136.190      # quiz의 cluster IP
# This is the quiz service running in pod quiz

# / # curl http://10.96.74.151       # quote의 cluster IP
# This is the quote service running in pod quote-canary
```

- 포트를 curl에 안 적은 이유는 Service 포트를 80으로 뒀고, 80이 HTTP 기본 포트이기 때문입니다. 
- quote 서비스에 여러 번 요청하면 매번 다른 파드가 처리하는 것을 볼 수 있습니다 — Service가 부하 분산기로서 뒤의 파드들에 요청을 흩뿌립니다.

> `kubectl port-forward svc/my-service`는 Service에 연결하는 것처럼 보이지만, 실제로는 Service를 이용해 파드 하나를 찾아 그 파드에 *직접* 연결합니다 — Service를 우회합니다.

### 실제 확인 — port-forward 우회 vs ClusterIP 분산 (kind)

바로 위 note("port-forward는 Service를 우회한다")를 kind에서 실측한 근거입니다. [03-02](./03-02.%EC%B2%AB%20%EC%95%A0%ED%94%8C%EB%A6%AC%EC%BC%80%EC%9D%B4%EC%85%98%20%EB%B0%B0%ED%8F%AC%EC%99%80%20%EC%8A%A4%EC%BC%80%EC%9D%BC%EB%A7%81.md)의 kiada를 3개로 스케일한 뒤, port-forward한 주소로 30번 요청하자 **응답이 전부 같은 Pod 하나**에서만 왔습니다.

```bash
kubectl scale deployment kiada --replicas=3
kubectl port-forward svc/kiada 18080:8080 &

for i in $(seq 1 30); do curl -s localhost:18080/ | grep -oE 'kiada-[a-z0-9-]+'; done | sort | uniq -c
#   30 kiada-7bc9cd6878-bhgc5     ← 한 Pod만! 분산이 안 됨
```

이름은 `svc/kiada`지만, `port-forward`는 Service의 endpoint 중 **Pod 하나를 골라 그 Pod에 직접 터널**을 뚫습니다. 한번 연결되면 그 Pod에 고정되어, ClusterIP의 Service 데이터패스를 거치지 않습니다.

로드밸런싱을 제대로 보려면 **클러스터 안에서 Service 이름(ClusterIP)으로** 요청해야 합니다. 임시 curl Pod를 클러스터 안에 띄워 확인했습니다.

```bash
kubectl run curl-test --image=curlimages/curl --rm -it --restart=Never -- \
  sh -c 'for i in $(seq 1 30); do curl -s http://kiada:8080/ | grep -oE "kiada-[a-z0-9-]+"; done | sort | uniq -c'
  
#   13 kiada-7bc9cd6878-2vm7s    (worker2)
#   10 kiada-7bc9cd6878-8phwt    (worker)
#    7 kiada-7bc9cd6878-bhgc5    (worker)
```

이번에는 30번 요청이 **세 Pod에 13/10/7로 분산**됐습니다. 그중 하나는 **다른 노드 worker2**에 있었으니, Service가 노드 경계를 넘어 분산한다는 증거입니다. 이 kind 환경의 kube-proxy는 iptables 모드였고, 설치된 규칙이 endpoint를 확률적으로 선택했습니다. 30회처럼 표본이 작으면 13/10/7 정도의 편차가 날 수 있습니다.

![로드밸런싱 — port-forward는 우회, 클러스터 내부 ClusterIP만 분산](_assets/03-02-loadbalancing-portforward-vs-clusterip.svg)

| 접속 방식 | 실제 경로 | 로드밸런싱 |
|-----------|----------|-----------|
| `kubectl port-forward svc/...` | Pod 하나에 직접 터널 | ❌ 한 Pod 고정 (30번 모두 같은 Pod) |
| 클러스터 내부에서 ClusterIP·Service 이름 | 출발 노드 커널 데이터패스가 endpoint 하나를 선택해 DNAT | ✅ 여러 Pod (13/10/7) |

- 즉 "Service로 요청했는데 왜 한 Pod만 응답하나"의 답은 대개 **port-forward로 접속했기 때문**입니다. 
- Service의 로드밸런싱을 보려면 클러스터 안에서 ClusterIP로 요청해야 합니다. 이 구분은 책의 클라우드(GKE External-IP) 실습에서는 드러나지 않는, 로컬 kind 특유의 관찰입니다.



## 6. 세션 어피니티

>  Service가 연결마다 다른 파드로 넘길지, 같은 클라이언트의 연결을 모두 같은 파드로 넘길지는 `spec.sessionAffinity`로 정합니다. 값은 **None**과 **ClientIP** 둘뿐입니다. 

**기본 None**은 어느 파드로 갈지 보장하지 않고, ClientIP는 같은 IP에서 온 연결을 모두 같은 파드로 넘깁니다. 지속 시간은 `sessionAffinityConfig.clientIP.timeoutSeconds`로 정하며 기본 3시간입니다.

쿠버네티스가 쿠키 기반 어피니티를 제공하지 않는 것이 뜻밖일 수 있는데, **Service는 OSI 전송 계층(UDP·TCP)에서 동작하지 애플리케이션 계층(HTTP)에서 동작하지 않으므로 HTTP 쿠키를 아예 이해하지 못하기 때문입니다.**

여기서 말하는 "계층"은 택배 상자에 비유하면 잡힙니다.

- **전송 계층**(흔히 **L4**) — 상자 겉면입니다. 받는 주소와 몇 번 창구로 갈지, 곧 IP와 포트가 적혀 있습니다.
- **애플리케이션 계층**(**L7**) — 상자 속입니다. 열어야 HTTP 헤더·쿠키·URL 경로가 보입니다.

Service는 상자를 뜯지 않고 겉면만 보고 분류하는 우체국입니다. 쿠키는 상자 안에 있으니 볼 수가 없습니다.

![같은 ClientIP의 첫 연결이 파드를 선택하고 이후 연결이 같은 파드를 재사용하는 세션 어피니티 흐름](_assets/11-01-osi-l4-l7-affinity.svg)

그림의 흐름은 다음과 같습니다.

1. 첫 연결이 들어오면 노드 데이터패스는 패킷의 출발지 IP를 ClientIP 어피니티 키로 사용합니다.
2. 아직 연결된 파드가 없으면 Service의 ready endpoint 중 하나를 고르고, `출발지 IP → 파드` 관계를 어피니티 상태로 기록합니다.
3. 같은 출발지 IP에서 새 연결이 들어오면 기록을 찾아 같은 파드로 보냅니다. 이 관계는 `sessionAffinityConfig.clientIP.timeoutSeconds` 동안 유지되며 기본값은 10,800초(3시간)입니다.
4. `sessionAffinity: None`이면 이 ClientIP 기록을 사용하지 않으므로 새 연결이 다른 ready endpoint로 갈 수 있습니다.

Service가 판단에 사용하는 정보는 L3의 출발지 IP와 L4의 프로토콜·포트입니다. HTTP 쿠키·경로·헤더는 L7 데이터라 Service 데이터패스가 읽지 않습니다. 그래서 쿠키 기반 어피니티가 필요하면 L7을 이해하는 계층을 앞에 둬야 합니다.

| 구분 | Service (L4) | Ingress (L7, 12장) |
|------|-------------|--------------------|
| 보는 계층 | 전송 계층 — IP·포트 | 애플리케이션 계층 — HTTP |
| 어피니티 기준 | 클라이언트 IP만 | 쿠키·경로·헤더 |
| 라우팅 판단 | 목적지 IP:포트 | URL 경로·호스트·쿠키 |

- ClientIP 어피니티에는 한계도 있습니다. 회사 NAT 뒤의 여러 사용자가 같은 출발 IP로 보이면, 그 사용자들의 연결이 전부 한 파드로 몰려 부하 분산이 깨집니다. 
- 쿠키라면 사용자별로 구분되겠지만, **L4에서는 그 정보가 보이지 않습니다. 쿠키·경로 기반 라우팅**은 [11-02](./11-02.%EC%99%B8%EB%B6%80%20%EB%85%B8%EC%B6%9C%20%E2%80%94%20NodePort%C2%B7LoadBalancer%C2%B7%ED%8A%B8%EB%9E%98%ED%94%BD%20%EC%A0%95%EC%B1%85.md) 이후 Ingress에서 다룹니다.



## 7. cluster DNS로 이름 resolve

> **클러스터는 대개 CoreDNS를 DNS 서버로 사용하므로, 파드는 cluster IP 대신 Service 이름으로 접근할 수 있습니다.** 

CoreDNS는 새로운 API 오브젝트 종류가 아니라 `kube-system` 네임스페이스에서 파드로 실행되는 애드온 Deployment입니다. 앞에는 하위 호환성을 위해 이름을 유지한 `kube-dns` Service가 있고, 파드는 이 Service의 ClusterIP를 DNS 서버 주소로 사용합니다.

```bash
# / # curl http://quiz              # cluster IP 대신 Service 이름
# This is the quiz service running in pod quiz
```

### 파드의 `/etc/resolv.conf`는 어떻게 채워지고 사용되나

`/etc/resolv.conf`는 컨테이너 이미지에 미리 들어 있는 클러스터 설정이 아닙니다. kubelet이 파드를 만들 때 `dnsPolicy`와 파드의 네임스페이스를 바탕으로 각 컨테이너에 DNS 설정을 구성합니다. `kiada` 네임스페이스의 일반 파드에서 확인하면 다음과 비슷합니다.

```bash
kubectl exec quote-001 -c nginx -- cat /etc/resolv.conf

# /etc/resolv.conf (내용)
search kiada.svc.cluster.local svc.cluster.local cluster.local
nameserver 10.96.0.10
options ndots:5
```

클러스터의 도메인·DNS 구성과 컨테이너 런타임에 따라 search 항목이나 options가 더 붙을 수 있지만, 세 줄의 기본 역할은 같습니다.

- `search`는 점이 적은 짧은 이름 뒤에 붙여 볼 접미사와 순서를 정합니다. 첫 항목에 파드의 네임스페이스인 `kiada`가 들어가므로 같은 네임스페이스의 Service를 이름만으로 찾을 수 있습니다.
- `nameserver`는 DNS 질의를 보낼 서버 주소입니다. 여기의 `10.96.0.10`은 CoreDNS 파드 IP가 아니라 앞단 `kube-dns` Service의 ClusterIP입니다.
- `options ndots:5`는 이름에 포함된 점이 5개보다 적으면 `search` 접미사를 붙인 후보를 먼저 조회하게 합니다. 조회가 모두 실패하면 접미사를 붙이지 않은 이름도 시도합니다.

그러면 `nameserver`가 가리키는 CoreDNS는 무엇인가요. **쿠버네티스 API 오브젝트가 아니라 애드온**입니다. Service나 Pod처럼 `kubectl get`으로 조회하는 리소스 종류가 아닙니다. `kube-system` 네임스페이스에 Deployment로 도는 평범한 파드 묶음입니다. 그 앞에 `kube-dns`라는 이름의 ClusterIP Service가 서 있고, 파드의 `nameserver`에 들어가는 `10.96.0.10`이 바로 그 Service의 ClusterIP입니다.

그래서 DNS 질의도 지금까지 본 Service 트래픽과 똑같은 경로를 지납니다. 파드가 `10.96.0.10:53`으로 질의를 보내면 노드 커널이 그 목적지를 실제 CoreDNS 파드 IP로 DNAT합니다. 클러스터의 이름 해석이 클러스터 자신의 Service 메커니즘 위에 올라가 있는 셈입니다.

```bash
kubectl get deploy,svc -n kube-system -l k8s-app=kube-dns
# NAME                      READY   UP-TO-DATE   AVAILABLE
# deployment.apps/coredns   2/2     2            2
#
# NAME               TYPE        CLUSTER-IP   PORT(S)
# service/kube-dns   ClusterIP   10.96.0.10   53/UDP,53/TCP,9153/TCP
```

CoreDNS는 받은 이름을 두 갈래로 처리합니다. `cluster.local` 같은 클러스터 도메인이면 자기가 아는 Service·파드 레코드로 직접 답합니다. 그 밖의 이름, 이를테면 `api.github.com` 같은 외부 도메인이면 **업스트림 DNS 서버로 넘깁니다**. 이 위임을 설정하는 것이 CoreDNS 설정의 `forward . /etc/resolv.conf` 줄입니다.

애플리케이션이 `curl http://quiz`를 실행하면 URL 파싱 뒤 운영체제 resolver에 호스트명 `quiz`를 넘깁니다. `quiz`에는 점이 없으므로 resolver는 먼저 `quiz.kiada.svc.cluster.local`을 만들고, `nameserver 10.96.0.10`의 53번 포트로 질의합니다. 이 이름이 존재하면 CoreDNS가 quiz Service의 ClusterIP를 응답하고 탐색을 멈춥니다.

첫 후보가 없으면 resolver는 `search` 순서에 따라 다음 후보를 차례로 시도합니다.

```pseudocode
quiz.kiada.svc.cluster.local
quiz.svc.cluster.local
quiz.cluster.local
quiz
```

DNS 응답으로 `10.96.136.190`을 받은 뒤에는 resolver의 역할이 끝납니다. `curl`이 `10.96.136.190:80`으로 연결을 시작하면, 그다음부터는 DNS가 아니라 Service 데이터패스가 목적지를 실제 파드 IP로 DNAT합니다.

파드는 같은 네임스페이스의 Service를 이름만으로 resolve합니다. 다른 네임스페이스면 `http://quiz.kiada/`처럼 네임스페이스를 붙입니다. 하나의 Service는 다음 네 이름 중 어느 것으로도 resolve됩니다.

- `<service>` — 같은 네임스페이스에서만
- `<service>.<namespace>`
- `<service>.<namespace>.svc`
- `<service>.<namespace>.svc.cluster.local` — FQDN

FQDN을 다 안 적어도 되는 이유는 파드의 `/etc/resolv.conf`에 있는 `search` 줄에 있습니다. 이 줄이 도메인 접미사를 차례로 붙여 매칭될 때까지 시도합니다.

`quiz` 한 단어가 실제 파드까지 이어지는 과정에는 성격이 다른 두 변환이 있습니다.

![Service 이름이 DNS로 ClusterIP가 된 뒤 DNAT로 실제 파드 IP가 되는 두 단계](_assets/11-01-dns-dnat-two-stage.svg)

1. 첫 번째는 **이름 해석**입니다. CoreDNS는 `quiz`가 클러스터 안의 일반 Service임을 확인하고 A 레코드로 ClusterIP를 돌려줍니다. 이 응답은 파드 IP가 아닙니다. 
2. 두 번째는 **주소 변환**입니다. 클라이언트가 ClusterIP로 연결을 시작하면 출발 노드의 커널 데이터패스가 목적지를 실제 파드 IP로 DNAT합니다.

앞서 본 `forward . /etc/resolv.conf`에서 한 가지를 짚어 둡니다. 이 줄이 가리키는 파일은 CoreDNS 컨테이너가 보는 것이고, 짧은 Service 이름을 확장하는 애플리케이션 파드의 것과 다른 파일입니다. 이름이 같아 헷갈리지만 역할이 갈립니다.

| 파일 | 누가 보나 | 정하는 것 |
|------|----------|----------|
| CoreDNS 컨테이너의 `/etc/resolv.conf` | CoreDNS | 외부로 나갈 때 물어볼 업스트림 |
| 애플리케이션 파드의 `/etc/resolv.conf` | 앱 파드 | 짧은 이름에 붙일 접미사 |

이 편은 이름이 ClusterIP가 되고 다시 파드 IP로 바뀌는 흐름까지만 다룹니다. DNS 레코드 유형과 CoreDNS의 Corefile·플러그인·업스트림 설정은 다음 정본이 자세히 다룹니다.

- [11-03 § "DNS 레코드"](./11-03.%EC%97%94%EB%93%9C%ED%8F%AC%EC%9D%B8%ED%8A%B8%C2%B7DNS%C2%B7%ED%86%A0%ED%8F%B4%EB%A1%9C%EC%A7%80%C2%B7readiness.md) — Service·headless의 레코드 유형
- [04-05. DNS와 CoreDNS](../../kubernetes/04_networking/04-05.DNS%EC%99%80%20CoreDNS.md) — CoreDNS 동작 통합 개념본

### 환경변수로 Service 발견 (옛 방식)

DNS가 없던 초창기에는 파드가 환경변수로 Service IP를 찾았고, 이 변수는 지금도 남아 있습니다. 컨테이너가 시작될 때 쿠버네티스는 그 파드 네임스페이스에 존재하는 각 Service마다 환경변수 묶음을 채웁니다.

```bash
# QUIZ_SERVICE_HOST=10.96.136.190
# QUIZ_SERVICE_PORT=80
# QUIZ_PORT=tcp://10.96.136.190:80
# ... (포트마다 _TCP_ADDR·_TCP_PORT·_TCP_PROTO 등)
```

이 변수는 Service보다 파드를 먼저 만들면 채워지지 않으므로, 확인하려면 컨테이너를 재시작해야 합니다(`kubectl exec quote-001 -c nginx -- kill 1`). Service 이름의 하이픈은 밑줄로 바뀌고 모두 대문자가 됩니다. 요즘은 DNS로 정보를 얻으므로 이 변수는 덜 쓰이고, 오히려 문제를 일으킬 수 있습니다 — 네임스페이스의 Service가 너무 많으면 새 파드가 `argument list too long` 오류로 시작에 실패합니다. 막으려면 파드 spec의 `enableServiceLinks`를 false로 둡니다.

### Service IP를 ping할 수 없는 이유

Service가 트래픽을 안 넘길 때 IP를 ping해 보고 싶겠지만, ping은 패킷이 하나도 통과하지 않습니다. Service IP는 가상이라 Service에 정의된 포트와 짝지을 때만 의미가 있어, IP 자체는 ping할 수 없습니다.

### 파드 IP 직접 vs ClusterIP 경유 — NAT가 끼는 곳

방금 "ClusterIP는 가상이라 ping이 안 된다"고 했는데, 왜 가상 IP인데도 curl은 되고 ping은 안 되는지가 이 절의 핵심입니다. 답은 목적지가 파드 IP냐 ClusterIP냐에 따라 **주소 변환(NAT)이 끼는지**가 갈리기 때문입니다.

§1에서 잡아 둔 용어를 그대로 씁니다 — SNAT는 출발지 주소를, DNAT는 목적지 주소를 바꿔치기하는 것입니다. 여기서 갈리는 것은 둘 중 어느 쪽이 언제 끼느냐입니다.

- **파드 IP로 직접** 보낼 때(`10.244.x.x`)는 §1에서 본 flat 네트워크라 SNAT도 DNAT도 없습니다. 출발지·목적지 주소가 끝까지 그대로 유지됩니다.
- **ClusterIP로** 보낼 때(`10.96.x.x`)는 다릅니다. ClusterIP는 실제 인터페이스에 붙지 않은 가상 IP라, 그대로는 아무 데도 못 갑니다. kube-proxy가 각 노드에 미리 심어 둔 규칙이 목적지를 실제 파드 IP로 바꿔치기하는 **DNAT**가 일어나야 비로소 파드에 닿습니다.

![파드 IP 직접 통신과 ClusterIP 경유 통신 — NAT가 끼는 곳](_assets/11-01-nat-dnat-paths.svg)

핵심은 ClusterIP(`10.96.74.151`)가 어느 네트워크 인터페이스에도 붙어 있지 않은 가상 IP라는 점입니다. kube-proxy는 EndpointSlice 명단을 바탕으로 각 노드의 데이터패스 규칙을 준비하고, 실제 요청이 오면 출발 노드의 커널이 목적지를 실제 파드 IP(`10.244.2.9`)로 DNAT합니다.

- **ping이 안 되는 이유**: DNAT 규칙은 "ClusterIP:포트" 조합에만 걸립니다. ping은 포트 없는 ICMP라 매칭되는 규칙이 없어 패킷이 그대로 버려집니다.
- **응답이 돌아오는 이유**: 커널은 변환한 연결을 추적해 응답 방향에서 주소를 복원합니다.

netfilter 훅, kube-proxy 모드별 규칙, conntrack의 양방향 장부와 실제 인터페이스 경로는 [01-02. K8s 패킷 여정](../../../02_os/networking/01-02.K8s%20%ED%8C%A8%ED%82%B7%20%EC%97%AC%EC%A0%95%20%E2%80%94%20netfilter%C2%B7conntrack%C2%B7%EB%9D%BC%EC%9A%B0%ED%8C%85.md)이 정본입니다.

### 파드에서 Service 쓰기

Kiada 파드는 두 서비스의 URL을 환경변수 `QUIZ_URL`·`QUOTE_URL`로 기대합니다. 이는 쿠버네티스가 자동으로 넣는 변수가 아니라, 애플리케이션이 두 서비스를 어디서 찾을지 알도록 직접 설정하는 변수입니다.

```yaml
env:
- name: QUOTE_URL
  value: http://quote/quote      # Service 이름이 곧 호스트명
- name: QUIZ_URL
  value: http://quiz
```

호스트명이 앞서 만든 Service 이름과 같습니다. 이렇게 두면 Kiada 파드가 cluster DNS로 두 서비스를 resolve해 통신합니다.

> **Spring 관점.** Boot 앱에서도 같은 패턴입니다 — 다른 서비스 URL을 `application.yml`이나 환경변수(`QUOTE_URL`)로 주입하고, 값에는 파드 IP가 아니라 Service 이름(`http://quote`)을 씁니다. 12-factor의 "backing service는 설정으로 붙인다" 원칙이 K8s에서는 Service 이름 주입으로 나타납니다.



## 면접에서 받을 만한 질문

> 아래 질문에 먼저 스스로 답해 본 뒤, 챕터 끝의 정답과 맞춰 봅니다.

1. 파드끼리 패킷을 주고받을 때 SNAT·DNAT가 일어나지 않는다는 것이 애플리케이션에 어떤 의미인가요? 이를 flat 네트워크라 부르는 이유는 무엇인가요?
2. Service가 어느 파드를 뒷받침으로 삼을지는 무엇이 정하나요? `kubectl expose pod`로 만든 Service의 selector가 나중에 문제가 되는 상황을 예로 드세요.
3. Service의 cluster IP를 ping하면 왜 응답이 없나요?
4. 세션 어피니티로 쿠키 기반을 못 쓰고 ClientIP만 되는 이유는 무엇인가요?
5. Service를 이름만으로 resolve할 수 있는 이유는 무엇인가요? `search` 줄과 `ndots`가 어떤 역할을 하나요?



## 정답 (자답 후 펼치기)

> 위 질문에 답해 본 다음 아래와 맞춰 봅니다. 순서는 질문과 1:1로 대응합니다.

### 정답 1 — flat 네트워크와 NAT 없음

파드가 다른 파드로 패킷을 보낼 때 출발지·목적지 IP와 포트가 그대로 유지됩니다. 받는 파드는 보낸 파드의 진짜 IP를 출발지로 보므로, 애플리케이션이 상대를 그대로 식별하고 접근 로그에 실제 IP를 남길 수 있습니다. flat이라 부르는 이유는 노드가 지리적으로 흩어져 여러 라우터를 거치더라도, 파드 관점에서는 하나의 스위치에 물린 LAN처럼 NAT 없이 서로 닿기 때문입니다.

### 정답 2 — label selector와 expose selector 함정

Service 오브젝트에 적은 label selector가 뒷받침 파드를 정합니다. selector에 맞는 라벨을 가진 파드가 자동으로 포함됩니다.

`kubectl expose pod quiz`는 그 파드의 라벨을 *전부* selector로 가져옵니다. 즉 `app=quiz,rel=stable` 둘 다입니다. 나중에 canary를 배포하면 그 라벨은 `rel: canary`라 stable과 다릅니다. 이 selector에 맞지 않으므로 트래픽이 canary로 가지 않습니다. `kubectl set selector`로 `app=quiz`만 남겨 좁혀야 합니다.

### 정답 3 — cluster IP를 ping 못 하는 이유

Service의 cluster IP는 실제 인터페이스에 붙은 IP가 아니라 가상 IP입니다. Service에 정의된 포트와 짝지어질 때만 의미를 갖고, iptables·IPVS 규칙이 그 IP:포트 조합을 파드로 리다이렉트합니다. ICMP(ping)에는 대응하는 포트 규칙이 없으므로 패킷이 어디로도 가지 못해 100% 손실이 납니다.

### 정답 4 — 쿠키 어피니티가 없는 이유

Service는 OSI 전송 계층(TCP·UDP)에서 동작합니다. HTTP 쿠키는 애플리케이션 계층 개념이라 전송 계층에서는 보이지 않습니다. 그래서 쿠버네티스 Service는 쿠키를 이해하지 못하고, 어피니티를 걸 수 있는 유일한 기준이 패킷에서 바로 읽히는 클라이언트 IP입니다. `sessionAffinity: ClientIP`로 걸며 기본 지속 시간은 3시간입니다.

### 정답 5 — 이름만으로 resolve되는 이유

파드의 `/etc/resolv.conf`에 있는 `search` 줄(`kiada.svc.cluster.local svc.cluster.local cluster.local …`)이 짧은 이름 뒤에 도메인 접미사를 차례로 붙여 매칭될 때까지 시도합니다. `options ndots:5`는 점이 5개 미만인 이름을 상대 이름으로 보고 search 접미사를 붙이게 합니다. 그래서 `quiz`만 적어도 `quiz.kiada.svc.cluster.local`로 확장돼 resolve됩니다.



## 관련 문서

> 이 편은 06장 파드 개념과 10장 지속 스토리지를 이어받아, 여러 파드 replica를 단일 주소로 묶는 Service의 기초(11-02·11-03)로 이어집니다.

- [03-02. 첫 애플리케이션 배포와 스케일링](./03-02.%EC%B2%AB%20%EC%95%A0%ED%94%8C%EB%A6%AC%EC%BC%80%EC%9D%B4%EC%85%98%20%EB%B0%B0%ED%8F%AC%EC%99%80%20%EC%8A%A4%EC%BC%80%EC%9D%BC%EB%A7%81.md) — 이 편 Service 개념(label selector·ClusterIP·port-forward 우회)을 kind에 처음 배포·스케일하며 실측한 3장 실습 편
- [11-02. 외부 노출 — NodePort·LoadBalancer·트래픽 정책](./11-02.%EC%99%B8%EB%B6%80%20%EB%85%B8%EC%B6%9C%20%E2%80%94%20NodePort%C2%B7LoadBalancer%C2%B7%ED%8A%B8%EB%9E%98%ED%94%BD%20%EC%A0%95%EC%B1%85.md) — ClusterIP를 넘어 클러스터 밖으로 노출하는 법
- [11-03. 엔드포인트·DNS·토폴로지·readiness](./11-03.%EC%97%94%EB%93%9C%ED%8F%AC%EC%9D%B8%ED%8A%B8%C2%B7DNS%C2%B7%ED%86%A0%ED%8F%B4%EB%A1%9C%EC%A7%80%C2%B7readiness.md) — Service 뒤 엔드포인트와 DNS 레코드
- [10-03. PV 관리 — 리사이즈·스냅샷·ephemeral](./10-03.PV%20%EA%B4%80%EB%A6%AC%20%E2%80%94%20%EB%A6%AC%EC%82%AC%EC%9D%B4%EC%A6%88%C2%B7%EC%8A%A4%EB%83%85%EC%83%B7%C2%B7ephemeral.md) — 앞 장(지속 스토리지)
- [02-04. Service와 EndpointSlice](../../kubernetes/04_networking/04-04.Service%EC%99%80%20EndpointSlice.md) — Service·ClusterIP 통합 개념본(책 비종속)
- [04-05. DNS와 CoreDNS](../../kubernetes/04_networking/04-05.DNS%EC%99%80%20CoreDNS.md) — 클러스터 DNS resolve 통합 개념본
- [01-02. K8s 패킷 여정 — netfilter·conntrack·라우팅](../../../02_os/networking/01-02.K8s%20%ED%8C%A8%ED%82%B7%20%EC%97%AC%EC%A0%95%20%E2%80%94%20netfilter%C2%B7conntrack%C2%B7%EB%9D%BC%EC%9A%B0%ED%8C%85.md) — ClusterIP가 실제 Pod IP로 바뀌는 순간의 커널 패킷 경로
- 원서: 《Kubernetes in Action, 2nd Ed.》 Ch11 §11.1 — [Manning](https://www.manning.com/books/kubernetes-in-action-second-edition), [예제 코드](https://github.com/luksa/kubernetes-in-action-2nd-edition/tree/master/Chapter11)
- 공식 문서: [Service](https://kubernetes.io/docs/concepts/services-networking/service/)
