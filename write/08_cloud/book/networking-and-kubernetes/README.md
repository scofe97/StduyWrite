---
title: Networking and Kubernetes — 정독 인덱스
tags: [moc, study-index, book, kubernetes, networking, linux-networking, cni, kube-proxy]
status: draft
source:
  - 《Networking and Kubernetes: A Layered Approach》(James Strong·Vallery Lancey, O'Reilly, 2021, ISBN 978-1492081654)
  - https://www.amazon.com/Networking-Kubernetes-Approach-James-Strong/dp/1492081655  # 서지 확인 (2026-07-16 조회)
related:
  - ../kubernetes-in-action/README.md
  - ../../kubernetes/README.md
  - ../../README.md
learning:
  topic: networking-and-kubernetes
  scope: durable
  level: 정상 — 03-01 에서 선행 개념 결손 둘이 나와 한 단계 올렸던 것을 되돌림
  last_verified: 2026-09-06
  blocked_count: 3
  next_lesson: "**재학습 우선(2026-09-06 등록)** — 01-02 Transport 계층 복습 quality 2, Q1(시퀀스 단위)·Q2(TIME_WAIT 위치)가 Phase 4 재발에 이어 2연속 실패. 연결 종료 4단계를 능동/수동 두 상태 계열로 나란히 그리는 것부터 다시. 그다음 4장 Phase 3 착수(2026-09-03). 실습편 04-04 신설·실행 전. 묶음 1부터 학습자가 직접 실행하며 예측표를 채운다. 사내 클러스터 조회는 write/_company/사업/미래에셋/ 의 대응 문서. 전부 끝나면 4장 전체 Phase 4"
updated: 2026-09-06
---

# Networking and Kubernetes — 정독 인덱스
---
> 이 폴더는 『Networking and Kubernetes: A Layered Approach』(James Strong·Vallery Lancey, O'Reilly, 2021)를 장 단위로 정독하며 정리하는 **책-종속 학습노트**입니다. 챕터 PDF를 하나씩 읽은 뒤 한 편씩 채워 넣습니다.

## 이 책을 여기 두는 이유

> `08_cloud` 안에서 이 책이 맡는 자리와, 이 책만 정독으로 읽는 이유를 적습니다.

`08_cloud`는 "클러스터 내부에서 어떻게 돌아가는가"를 다루는 카테고리입니다. 이 책은 그 질문을 네트워킹 축으로 파고듭니다 — TCP/IP와 Linux 네트워킹이라는 바닥에서 출발해, 컨테이너·CNI를 거쳐 kube-proxy·Service·Ingress, 마지막으로 클라우드 관리형 K8s의 네트워크까지 층(layer)을 하나씩 쌓아 올리는 구성입니다. 제목의 "A Layered Approach"가 곧 책의 목차 구조입니다.

같은 폴더의 [『Kubernetes in Action』 정독본](../kubernetes-in-action/README.md)이 오브젝트 중심이라면 이 책은 패킷 중심입니다. 저쪽이 무엇을 배포하는가를 다루고, 여기는 그 오브젝트 사이를 트래픽이 실제로 어떻게 흐르는가를 다룹니다.

그래서 Service·Ingress처럼 겹치는 주제가 나와도 관점이 다릅니다. 저쪽이 사용법이라면 여기는 구현 원리입니다. 개념이 겹치는 지점은 [`../../kubernetes/`](../../kubernetes/README.md) 개념 노트와 `../../service-mesh/` <!-- 링크 끊김(2026-08): ../../service-mesh/README.md -->로 링크를 걸어 넘깁니다. 이 폴더에는 이 책만의 계층 관점과 예제에서 새로 얻는 것만 남깁니다.



## 장별 목표

> 각 장 PDF 앞머리의 저자 선언("In this chapter, we will…")을 근거로 채웠습니다. 원문에 없는 목표를 추측해 넣지 않습니다.

| 장 | 제목 | 저자가 선언한 목표 | 주요 토픽 |
|----|------|------------------|----------|
| 1 | Networking Introduction | 네트워킹 기술·표준의 발전사를 훑고, 지배적 네트워킹 이론을 개관하며, 책 전체 예제의 기반이 될 Go 웹 서버를 소개 | 네트워킹 역사, OSI·TCP/IP 모델, Go 웹 서버 |
| 2 | Linux Networking | K8s 네트워크 스택 이해에 필요한 Linux 네트워킹 기초 — K8s에서 주목할 지점 중심의 스택 개요 | Linux 네트워크 스택, 인터페이스, 네트워크 관리 도구 |
| 3 | Container Networking Basics | 컨테이너의 역사와 실행 옵션·네트워킹 셋업을 살피고, Docker 네트워킹 모델과 CNI의 차이를 설명한 뒤 Docker 네트워킹 모드 예제로 마무리 | 컨테이너 역사, Docker 네트워킹 모델, CNI |
| 4 | Kubernetes Networking Introduction | Pod가 클러스터 내부·외부와 연결되는 방식과 K8s 내부 컴포넌트의 연결을 다룸 — K8s가 푸는 네트워킹 문제 4가지(컨테이너 간·Pod 간·Pod-Service·외부-Service) | Pod 네트워킹, kubelet·kube-proxy, CNI 플러그인 |
| 5 | Kubernetes Networking Abstractions | 서비스 디스커버리와 로드밸런싱 추상화 — 가장 가시적인 네트워크 스택인 Service와 Ingress의 동작 원리 | Service 유형, Endpoints, Ingress |
| 6 | Kubernetes and Cloud Networking | AWS·Azure·GCP의 네트워크 서비스가 그 클라우드 안에서 K8s 클러스터 운영에 필요한 네트워킹에 어떤 영향을 주는지, 각 프로바이더 CNI와 함께 탐구 | AWS·Azure·GCP 네트워크, 관리형 K8s, 클라우드 CNI |



## 작성된 정독 노트

> 원문을 정독해 편을 작성하는 대로 채웁니다. 아직 작성하지 않은 장은 상태만 "작성 예정"으로 표시하고, 본문 내용은 원문 도착 전까지 채우지 않습니다.

| 편 | 제목 | 상태 |
|----|------|------|
| [01-01](./01-01.%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%20%EC%97%AD%EC%82%AC%EC%99%80%20OSI%20%EB%AA%A8%EB%8D%B8%20%E2%80%94%20%EA%B3%84%EC%B8%B5%EC%9C%BC%EB%A1%9C%20%EB%82%98%EB%88%88%20%EC%9D%B4%EC%9C%A0.md) | 네트워킹 역사와 OSI 모델 — 계층으로 나눈 이유 (Ch1 역사·OSI·TCP/IP 개관) | 완료 |
| [01-02](./01-02.HTTP%EC%97%90%EC%84%9C%20TCP%C2%B7TLS%C2%B7UDP%EA%B9%8C%EC%A7%80%20%E2%80%94%20Transport%20%EA%B3%84%EC%B8%B5%20%ED%95%B4%EB%B6%80.md) | HTTP에서 TCP·TLS·UDP까지 — Transport 계층 해부 (Ch1 Application·Transport) | 완료 |
| [01-03](./01-03.IP%C2%B7%EB%9D%BC%EC%9A%B0%ED%8C%85%C2%B7Ethernet%20%E2%80%94%20%ED%8C%A8%ED%82%B7%EC%9D%B4%20%EA%B8%B8%EC%9D%84%20%EC%B0%BE%EB%8A%94%20%EB%B2%95.md) | IP·라우팅·Ethernet — 패킷이 길을 찾는 법 (Ch1 Network·Link·재조립) | 완료 |
| [01-04](./01-04.%ED%8C%A8%ED%82%B7%20%EC%BA%A1%EC%B2%98%20%EC%8B%A4%EC%8A%B5%20%E2%80%94%201%EC%9E%A5%20%EA%B0%9C%EB%85%90%EC%9D%84%20%EB%88%88%EC%9C%BC%EB%A1%9C%20%ED%99%95%EC%9D%B8%ED%95%98%EA%B8%B0.md) | 패킷 캡처 실습 — 1장 개념을 눈으로 확인하기 (Ch1 실습편) | 작성 완료·실행 전 |
| [02-01](./02-01.%EC%BB%A4%EB%84%90%EC%9D%B4%20%ED%8C%A8%ED%82%B7%EC%9D%84%20%EB%8B%A4%EB%A3%A8%EB%8A%94%20%EB%B2%95%20%E2%80%94%20%EC%86%8C%EC%BC%93%C2%B7Netfilter%C2%B7Conntrack%C2%B7%EB%9D%BC%EC%9A%B0%ED%8C%85.md) | 커널이 패킷을 다루는 법 — 소켓·Netfilter·Conntrack·라우팅 (Ch2 Basics·Kernel) | 완료 |
| [02-02](./02-02.iptables%C2%B7IPVS%C2%B7eBPF%20%E2%80%94%20kube-proxy%EB%A5%BC%20%EC%9D%B4%ED%95%B4%ED%95%98%EB%8A%94%20%EC%84%B8%20%EA%B8%B0%EC%88%A0.md) | iptables·IPVS·eBPF — kube-proxy를 이해하는 세 기술 (Ch2 High-Level Routing) | 완료 |
| [02-03](./02-03.Linux%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC%20%EC%A7%84%EB%8B%A8%20%EB%8F%84%EA%B5%AC%20%E2%80%94%20%EA%B3%84%EC%B8%B5%20%EC%88%9C%EC%84%9C%EB%8C%80%EB%A1%9C%20%EC%88%98%EC%82%AC%ED%95%98%EA%B8%B0.md) | Linux 네트워크 진단 도구 — 계층 순서대로 수사하기 (Ch2 Troubleshooting Tools) | 완료 |
| [02-04](./02-04.%EC%BB%A4%EB%84%90%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%20%EC%8B%A4%EC%8A%B5%20%E2%80%94%20veth%C2%B7%EB%B8%8C%EB%A6%AC%EC%A7%80%C2%B7%ED%8F%AC%EC%9B%8C%EB%94%A9%EC%9D%84%20%EC%86%90%EC%9C%BC%EB%A1%9C%20%EC%A7%93%EA%B8%B0.md) | 커널 네트워킹 실습 — veth·브리지·포워딩을 손으로 짓기 (Ch2 실습편) | 실행 완료·검증 전 |
| [02-05](./02-05.%EC%BB%A4%EB%84%90%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%20%EC%8B%A4%EC%8A%B5%202%20%E2%80%94%20%EB%A7%89%EA%B3%A0%2C%20%EC%A7%84%EB%8B%A8%ED%95%98%EA%B3%A0%2C%20%EB%82%98%EB%88%84%EA%B8%B0.md) | 커널 네트워킹 실습 2 — 막고, 진단하고, 나누기 (Ch2 실습편 2) | 실행 완료·검증 전 |
| [03-01](./03-01.%EC%BB%A8%ED%85%8C%EC%9D%B4%EB%84%88%EC%9D%98%20%ED%83%84%EC%83%9D%20%E2%80%94%20%EC%95%B1%20%EC%8B%A4%ED%96%89%EC%9D%98%20%EC%A7%84%ED%99%94%EC%99%80%20%EA%B2%A9%EB%A6%AC%20%ED%94%84%EB%A6%AC%EB%AF%B8%ED%8B%B0%EB%B8%8C.md) | 컨테이너의 탄생 — 앱 실행의 진화와 격리 프리미티브 (Ch3 Intro·Primitives·실습) | 완료 |
| [03-02](./03-02.%EC%BB%A8%ED%85%8C%EC%9D%B4%EB%84%88%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%20%EB%AA%A8%EB%93%9C%EC%99%80%20CNI%20%E2%80%94%20%EA%B2%A9%EB%A6%AC%EC%99%80%20%EC%97%B0%EA%B2%B0%EC%9D%98%20%EA%B1%B0%EB%9E%98.md) | 컨테이너 네트워킹 모드와 CNI — 격리와 연결의 거래 (Ch3 모드·CNM·VXLAN·CNI) | 완료 |
| [03-03](./03-03.%EC%BB%A8%ED%85%8C%EC%9D%B4%EB%84%88%20%EC%97%B0%EA%B2%B0%EA%B3%BC%20%ED%8F%AC%ED%8A%B8%20%EB%A7%A4%ED%95%91%20%E2%80%94%20%EA%B0%99%EC%9D%80%20%ED%98%B8%EC%8A%A4%ED%8A%B8%2C%20%EB%8B%A4%EB%A5%B8%20%ED%98%B8%EC%8A%A4%ED%8A%B8.md) | 컨테이너 연결과 포트 매핑 — 같은 호스트, 다른 호스트 (Ch3 Connectivity) | 완료 |
| [03-04](./03-04.%EC%BB%A8%ED%85%8C%EC%9D%B4%EB%84%88%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC%20%EC%8B%A4%EC%8A%B5%20%E2%80%94%20%EB%A7%A8%EC%86%90%20%EB%B0%B0%EC%84%A0%EC%97%90%EC%84%9C%20%ED%8F%AC%ED%8A%B8%20%EB%A7%A4%ED%95%91%EA%B9%8C%EC%A7%80.md) | 컨테이너 네트워크 실습 — 맨손 배선에서 포트 매핑까지 (Ch3 실습편) | 신설·실행 전 (묶음 구성 잠정) |
| [04-01](./04-01.Kubernetes%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%20%EB%AA%A8%EB%8D%B8%20%E2%80%94%20Pod%20IP%C2%B7%EB%A0%88%EC%9D%B4%EC%95%84%EC%9B%83%C2%B7Probe.md) | Kubernetes 네트워킹 모델 — Pod IP·레이아웃·Probe (Ch4 Model·Kubelet·Probes) | 완료 |
| [04-02](./04-02.CNI%EC%99%80%20kube-proxy%20%E2%80%94%20Pod%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC%EC%9D%98%20%EB%B0%B0%EC%84%A0%EA%B3%B5%EA%B3%BC%20%EB%A1%9C%EB%93%9C%EB%B0%B8%EB%9F%B0%EC%84%9C.md) | CNI와 kube-proxy — Pod 네트워크의 배선공과 로드밸런서 (Ch4 CNI·kube-proxy) | 완료 |
| [04-03](./04-03.NetworkPolicy%EC%99%80%20DNS%20%E2%80%94%20%ED%81%B4%EB%9F%AC%EC%8A%A4%ED%84%B0%20%EC%95%88%EC%9D%98%20%EB%B0%A9%ED%99%94%EB%B2%BD%EA%B3%BC%20%EC%9D%B4%EB%A6%84.md) | NetworkPolicy와 DNS — 클러스터 안의 방화벽과 이름 (Ch4 Policy·DNS·Dual Stack) | 완료 |
| [04-04](./04-04.Kubernetes%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC%20%EC%8B%A4%EC%8A%B5%20%E2%80%94%20CNI%20%EB%B6%80%EC%9E%AC%EB%B6%80%ED%84%B0%20%EC%A0%95%EC%B1%85%C2%B7DNS%EA%B9%8C%EC%A7%80.md) | Kubernetes 네트워크 실습 — CNI 부재부터 정책·DNS까지 (Ch4 실습편) | 신설·실행 전 |
| [05-01](./05-01.StatefulSet%C2%B7Endpoints%C2%B7EndpointSlices%20%E2%80%94%20%EC%84%9C%EB%B9%84%EC%8A%A4%EC%9D%98%20%EC%9E%AC%EB%A3%8C.md) | StatefulSet·Endpoints·EndpointSlices — 서비스의 재료 (Ch5 앞부분) | 완료 |
| [05-02](./05-02.Service%205%EC%9C%A0%ED%98%95%20%E2%80%94%20ClusterIP%EC%97%90%EC%84%9C%20LoadBalancer%EA%B9%8C%EC%A7%80.md) | Service 5유형 — ClusterIP에서 LoadBalancer까지 (Ch5 Services) | 완료 |
| [05-03](./05-03.Ingress%EC%99%80%20Service%20Mesh%20%E2%80%94%20L7%EC%9D%98%20%EB%91%90%20%EC%B8%B5.md) | Ingress와 Service Mesh — L7의 두 층 (Ch5 Ingress·Service Meshes) | 완료 |
| [06-01](./06-01.AWS%20%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%82%B9%EA%B3%BC%20EKS%20%E2%80%94%20VPC%20%EB%B6%80%ED%92%88%EC%9C%BC%EB%A1%9C%20%EC%A1%B0%EB%A6%BD%ED%95%98%EB%8A%94%20%ED%81%B4%EB%9F%AC%EC%8A%A4%ED%84%B0.md) | AWS 네트워킹과 EKS — VPC 부품으로 조립하는 클러스터 (Ch6 AWS) | 완료 |
| [06-02](./06-02.GCP%C2%B7Azure%EC%99%80%203%EC%82%AC%20%EB%B9%84%EA%B5%90%20%E2%80%94%20%EA%B0%99%EC%9D%80%20%EB%AC%B8%EC%A0%9C%2C%20%EB%8B%A4%EB%A5%B8%20%EA%B8%B0%EB%B3%B8%EA%B0%92.md) | GCP·Azure와 3사 비교 — 같은 문제, 다른 기본값 (Ch6 GCP·Azure·비교) | 완료 |
| [00-01](./00-01.%EC%9A%A9%EC%96%B4%EC%A7%91.md) | 용어집 — 핵심 용어 + 한 문장 정의 + 등장 편 (시리즈 산출물) | 완료 |
| [00-02](./00-02.%EA%B2%B0%EC%A0%95%20%EC%B9%98%ED%8A%B8%EC%8B%9C%ED%8A%B8.md) | 결정 치트시트 통합본 — 17편의 판단 규칙 모음 (시리즈 산출물) | 완료 |
| [00-03](./00-03.%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC%20%EC%84%A0%EC%88%98%20%EA%B0%9C%EB%85%90.md) | 네트워크 선수 개념 — 주소·인터페이스·장비 (본문이 설명 없이 쓰는 낱말) | 초안 · Phase 1 완료(2026-08-24) |



## 학습 상태

> 세션을 새로 열 때 이 표부터 읽습니다. 정독본의 STATE.md 대용입니다.

| 항목 | 현재 값 |
|------|--------|
| 진행률 | 6/6 장 완주 — 정독 노트 20편(2026-08-31 `03-04` 실습편 신설) + 시리즈 산출물 2편(용어집·결정 치트시트) |
| 난이도 레벨 | **한 단계 더 올린다** — 01-03 Phase 1 통과(5 절 전부). 근거 둘. (1) **뿌리 (A)·(B) 모두 재발 0 건.** (A)는 질문 13 에서 "다음 홉 IP 는 표에만 있고 헤더엔 안 들어간다"를 스스로 적용, (B)는 §4 에서 첫 답에 겉봉 2 칸을 맞혔습니다(§4 회차 되묻기 2 회 → §5 보강분 1 회 → 이번엔 표기 확인만). (2) **§5 다섯 단계를 유도 없이 순서·계층까지 자력 서술** — 이 편 학습 목표의 직접 증거. 막힘 12 건은 대부분 *설계를 의심해서 나온 물음*이고, 그중 하나(ARP 메시지 입출력 방향)는 AI 표기 오류를 사용자가 잡은 것입니다. 다음 편은 정상 난이도로 진행하되 IPv6·전파 타이밍 2 건만 표적 보완 |
| Phase 3 (2장 전체) | **2026-08-30 묶음 0·A·B·C·D·E 전부 완료 — 산출물 `02-04`(800줄, 도식 12장) + `02-05`(470줄, 도식 3장) 2편.** 묶음 D — `DROP` 5.062초 vs `REJECT` 0.036초(140배), 거절을 만든 쪽은 목적지가 아니라 중계 호스트(`tcpdump` 응답 출발지 `10.10.1.1`). **대화 중 제 표를 정정** — "RST=목적지 / ICMP=중간"은 짝이 아니고 진짜 축은 "커널 기본 동작이냐 규칙이냐"다(목적지에 REJECT 를 걸면 목적지가 ICMP 를 보내고, `--reject-with tcp-reset` 이면 방화벽이 RST 로 위장한다). 묶음 E — 고장을 심어 두고 사다리로 네 단 만에 원인을 바인딩 한 칸으로 좁힘. `getent` 성공 + `dig` 실패 = `/etc/hosts` 로만 붙는다, 밖 `closed` + 안 `LISTEN 127.0.0.1` 은 둘 다 참. `--bind 0.0.0.0` 으로 고치자 홈 디렉터리가 웹에 열린 것도 실물로 봄. 묶음 C — 확률 사다리 100회에 **27·22·30·21 = 100**, `policy 0 packets`. 마지막 규칙은 확률 당첨이 아니라 **뺄셈으로 남은 값**이라 확률을 붙이면 정책으로 샌다(규칙 하나만 걸렸을 때 실제로 `74 packets` 샘). **내 오독 1건 정정** — 그 74 가 "연결됐다"고 읽었으나 검증해 보니 전부 타임아웃이었다. 실습 환경은 원복 완료(`lo`·`eth0` 만 남음). **2026-08-28 묶음 0·A·B 완료, 산출물 `02-04` 신설 + 도식 4장.** 도식 타입 — §1 `dp-security-matrix`(두 목적지 값 대조) · §2 `type-architecture`(배선도) · §3 `type-swimlane`(세 주체를 가로지르는 경로) · §4 `type-layers`(진단 사다리). `type-data-flow`(조직 역할별 파이프라인 전용)와 `type-deployment`(버전·복제수 같은 배치 결정 필요)를 먼저 검토했으나 스펙상 맞지 않아 제외했다. **§5 는 소절마다 한 장씩 넷 더** — `type-state`(실험 3상태) · `type-flowchart`(세 줄 방화벽을 두 패킷이 다르게 지남) · `type-tree`(엔트리 한 줄 분해, **이 책 첫 사용**) · `type-sequence`(첫 패킷만 규칙을 훑는다). `type-timeline` 은 스펙이 시간 간격의 정직성을 요구하는데 실험 단계는 시간 축이 아니라 제외했다. **절 리드에 전체 흐름을 두는 요청으로 3장 더** — §3 `what-changes-where`(장치마다 바뀌는 필드가 다르다 — TTL 은 라우팅이, src IP 는 NAT 이, dst MAC 은 송신 직전 ARP 가 정하고 브리지 열은 통째로 빈다) · §4 `repair-journey`(swimlane, 커널이 문구를 던지고 내가 고치기를 세 번) · §5 `section5-path`(flowchart, 두 판단이 경로를 가른다). **총 11장.** 사용자 원칙 — *실습 문서는 입력 → 변화 → 결과와 상황 흐름이 보여야 한다.* 남은 빈틈은 §2 다: 명령(입력) → 장치 생성·이동(변화) → `ip link` 출력(결과)이 본문에 다 있으나 그 전이가 도식으로 없다(현재 도식은 완성된 배선만 보인다). **§2 에 `build-steps`(type-state) 추가로 총 12장** — 명령이 전이(입력), 상태가 변화, 상태 안의 값이 결과라 세 축이 그대로 선다. **중복 제거** — 02-01 §3 의 Docker netshoot 실습 블록을 02-04 §2 교차 참조로 대체하고 고아가 된 `veth-pair-lab.gif`·`tape` 는 git rm. 02-02·02-03 은 조사 결과 중복 없음(원문 예시와 ss 출력 판독이라 02-04 범위 밖). **주의 — `dp-security-matrix` 가 경고선(25%) 언저리다.** OrbStack VM 2대(ubuntu .208 / ubuntu2 .238)에서 netns 2 + veth 쌍 2 + br0 을 손으로 짓고 크로스호스트까지 통했다. **02-01 §3(veth·브리지)이 여기서 실측으로 닫혔다** — 예측만 남아 있던 마지막 절이다. 자력 확인 — 기본 netns 에 셋만 보이고 안쪽 끝은 안 보인다(예측 적중), `veth1@if3 ↔ veth1-br@if4` 상호 참조, 브리지는 홉이 아니라 TTL 64 유지, 크로스호스트는 63으로 감소, MASQUERADE 뒤 응답 방향 튜플만 바뀜, `pkts=1` 로 "규칙은 첫 패킷에만 발화" 증명. 되묻기 — `via` 가 붙는 조건(브리지는 스위치라 홉이 아님을 스스로 뒤집음), conntrack 이 켜지는 조건. **실패 3건이 각각 다른 계층을 가리켰고 그게 `02-04` §4 가 됐다** — `Network is unreachable`(default 없음) · `Destination Host Unreachable`(`br0` 주소를 `10.0.1.1` 로 오타 → ARP FAILED) · `conntrack -L` 0건(규칙 없으면 추적 안 켬). **남은 묶음 C·D·E** — 확률 사다리, DROP vs REJECT, 사다리 전체. 스크립트는 `networking-and-kubernetes-lab/netns-bridge/`(up·down·verify) |
| 02-03 Phase 1 | **2026-08-28 통과.** 자력 — 사다리 기준(아래 계층이 깨지면 위는 볼 필요 없다), ICMP 차단으로 인한 `ping` 거짓 실패, `netstat` 용도 추측, **"`curl` 이면 대개 끝난다"는 실무 감각**(사다리는 오르는 순서가 아니라 어디를 팔지 정하는 지도라는 결론으로 이어짐). 되묻기 필요 — ① `ss` 에서 **바인딩 주소**(`0.0.0.0` vs `127.0.0.1`)를 봐야 방화벽과 애플리케이션 설정이 갈린다는 것(한 층 위인 `PRE_ROUTING` 을 먼저 댐) ② **L4 연결과 L7 응답이 다른 사실**이라는 것(`nc -vz` 가 답해 준 계층을 L3 로 봄). 모른다고 명시해 설명 제공 — `netstat`/`nmap`/`telnet` 셋, `nc`, `ss`. **사용자 요청으로 이후 설명은 요즘 도구(`ss`·`ip`·`nc`)로 전환.** 실습 중 우연히 `ss -tulpn` 에 `127.0.0.1:5432` 가 잡혀 바인딩 함정이 실물로 나왔고, `nc -vz` 로 `Connection refused` 를 받아 DROP(타임아웃) vs REJECT(즉시 거절) 대조까지 실측 |
| 막힌 지점 (02-02 Phase 1) | **2026-08-28 Phase 1 통과 — 예측 라운드 막힘 4건, 문서 대조·보강은 다른 세션에서 처리.** ① **검문소 안 테이블 순서를 filter 먼저로 예측** — "버릴 건 일찍 버린다"는 최적화 논리를 정합성보다 앞세웠다. 되묻기 1회("filter 판정 직후 nat 가 주소를 바꾸면?")에 스스로 뒤집고 *"값을 바꾸는 놈이 먼저, 그 값으로 결정하는 놈이 나중"* 을 자기 말로 세웠다 — 01-03 의 "DNAT 가 라우팅보다 먼저인 이유"와 같은 모양임을 연결. ② **로드밸런싱을 카운터·라운드로빈으로 예측** — iptables 규칙이 상태를 못 든다는 점을 몰랐다. "기억 없이 매번 다른 값"이라는 힌트 한 단계에 랜덤까지 자력 도달했고, **확률 사다리 `1/3 · 1/2 · 마지막은 확률 없음`은 유도 없이 채웠다**(3/16 계산도 자력). ③ **Netfilter 와 nat 의 층위 혼동** — "nat 쪽에 Netfilter 가 있는 것 아니냐". 검문소(자리)와 규칙 묶음(테이블)이 다른 층이라는 것을 그림으로 다시 세움. 같은 응답에서 mangle→nat→filter 순서도 뒤집어 말해 ①이 아직 굳지 않았음이 드러남 — **재확인 대상**. ④ **eBPF 의 효용을 verifier 로 오인** — "검증한다"까지 갔으나 그게 무엇을 풀어 주는지 못 짚음. verifier 는 전제이지 해답이 아니라는 점을 설명으로 보충(학습자가 "모르겠다"고 명시). 자력 수확 — LOG 비종결 추론, PRE_ROUTING 에 걸면 중계 트래픽까지 막힌다(01-01 막힘 ②③ **재발 0**), IPVS 를 least-connection 으로 예측 |
| 막힌 지점 (02-02 Phase 2 · 읽기) | **2026-08-28 문서를 읽으며 막힌 5건 — 넷이 도식이었다.** ① **`INPUT` 이 `LOCAL_IN` 인가** — 본문이 "훅 5개에 그대로 대응된다"만 적고 어느 것이 어느 것인지는 전편에 두어, 도식의 `INPUT` 레인 앞에서 확인할 근거가 없었다. 다섯 쌍 목록을 §1 에 명시하고 도식 레인 머리에 훅 이름을 병기해 해소. **진단 — 막힘이 아니라 문서의 결손이다.** 학습자는 맞게 짚었고 근거가 문서에 없었다. ② **rule-token-gates 가 무엇을 설명하는지 모르겠다** — 도식이 규칙을 네 토막으로 쪼개 놓고 **원본 한 줄을 싣지 않았다.** "토큰을 왼쪽부터 읽으면"이라는 라벨의 지시 대상이 화면에 없었던 것. 실제 한 줄을 맨 위에 싣고 번호로 잇게 재작성. ③ **§2 서브체인 RETURN 설명이 안 읽힌다** — "ACCEPT 와 RETURN 은 자기 체인 안에서만 종결"이라는 한 문장에 종결의 두 층위가 겹쳐 있었다. "패킷의 운명이 정해지는 것"과 "이 체인만 끝나는 것" 두 갈래로 갈라 쓰고 `subchain-return.svg` 신설(복귀 화살표가 있고 없고로 대조). 같은 회차에 `JUMP`·`-g/--goto` 문단을 사용자 판단으로 제거. ④ **nat-table-traverse·conntrack-return-path 에서 실제 구조가 안 보인다** — 전자는 훅 둘과 점프 깊이와 다른 시나리오를 한 줄 여섯 칸에 뭉개 놓았고, 후자는 응답만 담아 **"역방향"의 원방향이 화면에 없었다.** 각각 훅 2레인+점프 계단, 왕복 6걸음으로 재작성. ⑤ **eBPF verifier 가 무슨 문제를 푸는가**(Phase 1 ④ 재발) — 본문에 verifier 가 **한 글자도 없었다.** "샌드박스된" 한 단어로 지나간 자리. `### verifier` 절 신설 — 검사 항목 넷 + "성능이 아니라 신뢰를 푼다" + 표현력이라는 대가. **회차 총평 — 다섯 중 넷이 도식 결함이고 하나가 본문 누락이다.** 학습자의 이해가 아니라 자료가 막고 있었다. MEMORY `lesson_diagram_is_verification_blindspot` 의 재확인 — 형식 게이트(overflow·lint·type-check)를 전부 통과한 도식 넷이 "무엇을 설명하는지 모르겠다"에 걸렸다. **기계 검사는 도식이 읽히는지를 보지 않는다.** |
| 막힌 지점 | **2026-08-26 01-02 Phase 4 로 8 일 전 막힘 6 건 전부 확인 완료 — 자력 3 / 재발 3.** 자력 정답: (1) 공개키 방향 · (2) 첫 메시지 암호화 전제 · (3) 누락 감지 계층(L4 즉답). 재발 후 되묻기로 해소: (4) 시퀀스 단위 · (5) TIME-WAIT 주체 · (6) 4-way 마지막 ACK. **재발 셋의 공통점 — 번호·순서·주체를 헷갈리는 계열이다.** 방향이나 개수 같은 세부 값이라 구조적 이해보다 먼저 흐려진다. 남은 셋 (1)(2)(3) 은 "누가 무엇을 맡는가"라 8 일 뒤에도 남았다. **다음 복습은 세부 값 계열을 우선한다.** 신규 정정 2 건 — TIME_WAIT 를 네트워크 이슈로 봄(실제로는 정상 종료, 버그 신호는 CLOSE_WAIT) · `Content-Length` 를 누락 탐지용으로 봄(실제로는 응답 경계). 01-03 Phase 1 12 건은 뿌리 없이 단발, (A)·(B) 재발 0 건 |
| 3장 실습 분리 | **2026-08-31 이론과 실습을 갈랐습니다.** 3장의 명령 블록은 네 곳뿐이었고 전부 책 예제 인용이었습니다 — `03-01 §5`(맨손 배선 11줄) · `03-02 §2`(netns 우회 3줄) · `03-03 §1`(빌드·조회) · `03-03 §2`(`docker run`). 이 넷을 신설한 `03-04` 로 옮기고 이론 세 편에는 개념 서술과 교차 참조만 남겼습니다. **multistage Dockerfile 블록은 03-03 에 유지**했습니다 — 그 절이 줄별로 해부하는 대상이라 옮기면 §1 이 설명 대상을 잃습니다. 2장 선례(`02-01 §3` → `02-04 §2` 교차 참조)와 같은 모양입니다. **같은 날 `03-03` 을 개명**했습니다 — 실행 명령이 떠나 "실습"이 남지 않아 `컨테이너 연결과 포트 매핑 — 같은 호스트, 다른 호스트` 로 바꾸고 참조 8곳을 repoint 했습니다. 부분 인코딩된 링크 3건이 문자열 치환을 비껴가 링크 센서에 잡혔고, 디코드 후 재인코딩으로 해소했습니다. `03-04` 는 실행 전이고 예측·관찰 칸이 비어 있으며, **묶음 다섯의 구성은 잠정안**입니다(학습자가 3장을 다 읽은 뒤 흐름을 다시 정합니다). 도식은 관찰이 생긴 뒤에 넣습니다. |
| 3장 전체 검증 | **2026-08-31 별개 컨텍스트 3개로 적대적 재검증 — 발견 24건, 교정 21건.** 원문 PDF 전수(`pdftotext -layout` 2,335줄)와 공식 문서 대조. **(A) 도식 결함 1건 — 본문만 고치고 SVG 를 안 연 자국.** `03-02.cnm-structure.svg` 가 sandbox 를 "호스트의 모든 컨테이너", endpoint 를 "네트워크 위의 호스트"로 표기하고 있었다. 2026-08-27 에 본문 136·137줄에서 교정한 바로 그 두 오역인데 도식만 옛 값으로 남았고, 본문 151줄이 "위 도식이 그 구조입니다"라며 그것을 보증하고 있었다. 생성기 라벨을 고쳐 재렌더(lint 0 · overflow 0 · Chrome 육안 확인). MEMORY `lesson_diagram_is_verification_blindspot` 의 재확인이다. **(B) 실습 분리 후유증 6건** — 03-01 이 "열 줄이 열 단계와 차례로 대응"이라 적었으나 7번(브리지 주소)에 명령이 없고 10번은 명령이 아니다. 줄 수도 "열 줄"·"열 줄 넘게"·"11줄"로 세 자리가 갈렸고(실측 11줄), "이 명령들은"·"그 열 줄"이 코드 블록 0개인 파일에서 대상을 잃었으며, 03-02 절 제목 "Docker 실측 — 눈으로 확인"과 학습목표·체크리스트의 "실측 출력으로"도 같은 자리였다. 03-03 은 Vagrant 전제가 03-04 의 OrbStack 전제와 충돌했고 "아래 표"가 가리킬 표가 없었다. **(C) 03-04 실행차단 4건 + 헛돌음 5건** — `echo 1 > /proc/...` 는 리다이렉트가 sudo 앞이라 실패(`sysctl -w` 로 교체), `master br0` 는 up 을 시키지 않아 `veth0 up` 이 빠져 있었다(책 예제의 여섯 번째 어긋남으로 기록), `/var/run/netns` 는 `ip netns add` 만 만들어 `ln -sfT` 가 ENOENT, **`jessie-dnsutils:1.3` 에 `curl`·`ping` 이 없다**(k8s 저장소 Dockerfile 실측 — `dnsutils` 한 패키지뿐). `%{http_code}` 는 거부·타임아웃 둘 다 `000` 이라 종료 코드(7/28)를 함께 찍게 고쳤고, `172.17.0.2` 하드코딩을 `docker inspect` 변수로 바꿨다. 이미지 레퍼런스 둘은 arm64 포함해 유효. **(D) 사용자 승인 후 사실 교정 2건.** ① 03-03 의 `No route to host` 인과가 뒤집혀 있었다 — "그 메시지가 나오려면 기본 경로가 없어야 한다"고 적었으나, 경로가 없으면 `ENETUNREACH`(Network is unreachable)이고 `EHOSTUNREACH`(No route to host)는 경로가 있어야 난다(온링크 ARP 실패 또는 게이트웨이의 ICMP 회신). 02-04 §4 의 실측이 같은 짝을 이미 기록하고 있었다. §3 에 `### 닿지 않음의 세 얼굴` 표를 신설하고 본문·비유·치트시트·면접 Q3·정답 3·체크리스트 여섯 자리를 한 묶음으로 훑었다. **도식에서 두 번째 사각지대가 나왔다** — `port-mapping-access-matrix.svg` 에 2026-08-27 에 본문에서 걷어낸 "사설 대역이라 밖에서 길이 없다"가 그대로 남아 있었다. 이번 회차에만 본문 교정이 도식에 반영되지 않은 사례가 둘이다(CNM 도식·이 도식). **본문을 고치면 그 절이 삽입하는 SVG 를 반드시 함께 연다** 를 다음 교정의 고정 절차로 둔다. ② 03-01 §3 의 Disk I/O — 책은 "device's cgroup subsystem" 이라 적고 우리는 `blkio`/`io` 로 교정했는데 그 차이가 frontmatter 에만 있었다. 본문에 원문 정오 인용을 신설하고 커널 문서를 링크했다. 교정 뒤 4편 센서 전부 통과(벽 단락 0 · 도식 배치·마크다운·문장 길이 exit 0 · dd-type-check 12/12 · 깨진 링크 0). |
| 03-01 Phase 1 | **2026-08-31 예측 라운드 완료 · 채점은 대화형으로 대체.** 다섯 질문 전부 예측을 받았고, 문서 전문 대조 대신 질문마다 그 자리에서 채점했습니다. **자력 정답 넷** — ① 빈 네트워크 네임스페이스에 없는 것이 라우팅 테이블(기본 경로)이라는 결론을 힌트 한 단계로 도달 ② containerd 재시작 시 고아가 되는 것은 shim 하나뿐이고 nginx 의 부모는 그대로라는 판정을 *괄호 안 자기 정정*으로 스스로 뒤집음 ③ namespace 가 자원 경계를 못 나눈다는 사실 ④ "K8s `resources.limits` 가 이것과 같은 것 아니냐"를 먼저 물어 cgroup 과 상위 표현을 스스로 이었습니다. **막힘 6건** — ① 하이퍼바이저가 못 넘긴 자리(라이브러리·패키징·배포)를 못 댐, 유도 후 "필요한 정보가 VM OS 에 있어서 환경이 바뀔 때마다 깨진다"로 자력 도달 ② **전제 오류** — "베어메탈이든 하이퍼바이저든 결국 커널 하나"로 요약(VM 은 커널이 VM 수만큼) ③ **전제 결손** — 프로세스 부모-자식·stdout 파이프·`wait()` 종료 코드 수거를 배운 적 없음 ④ runC 와 containerd 의 관계를 모름(프로세스 트리로 설명) ⑤ §2 용어 8개 배치 — 고유명사라 회상 대상이 아니어서 그냥 제공 ⑥ 컨테이너 안 root 안전성을 "최상위 경로로 못 가서"로 답(Mount namespace 는 맞지만 User namespace 미도달). **난이도는 정상으로 되돌립니다** — 3건 이상 + 전제 오류라 규칙상 선행 개념 복귀 구간인데, 원인의 절반이 학습자가 아니라 *출제 보폭*이었습니다(아래 행) |
| 출제 실패 박제 (03-01) | **2026-08-31 사용자 지적 — "학습이 1 2 3 이게 아니라 3 5 9 이렇게 생략해서 설명하는 것 같은데 수정해줘".** containerd-shim 의 존재 이유를 물으면서 프로세스 부모-자식 관계를 전제로 깔았고, 그 전제를 배운 적 없는 학습자가 "그 컨테이너가 스스로 보고하는 거 아닌가?"로 되물었습니다. **질문이 아니라 답이 안 나올 퀴즈였습니다.** 스킬 정본 두 곳에 박제 — `learning-method/references/01-4-phase-learning.md` 의 `### Phase 1 보충: 보폭` 신설, `runners/4phase.md` 에 한 문단. 메모리는 `feedback_learning_step_size.md`. 규칙 요지는 "질문 쓰기 전 이 답에 필요한 선행 개념을 확인했는가" 자문 + 하위 질문 3개 묶음은 보폭이 넓다는 신호 + **전제를 설명하되 마지막 한 칸은 학습자에게 남긴다** |
| 03-01 보강 (2026-08-31) | **§2·§5 소절 분해 + §3 도식 신설 + cri-dockerd 갱신.** §2 를 넷(low/high-level·계보와 OCI·Docker 분해와 shim·CRI), §5 를 넷(풀려는 문제·열 단계·배울 세 가지·런타임의 몫)으로 나눴습니다. §5 의 "첫째·둘째·셋째" 산문 나열은 번호 리스트로 전환(02 규칙 2 위반이었습니다). §3 은 도식이 없던 절이라 `03-01.cgroup-subsystems.svg` 신설 — `type-dp-security-matrix`, 행이 자원 4종이고 열이 v1/v2 대조라 마지막 열을 판정 축(focal_col)으로 세웠습니다. 자원별 v2 운명이 통합(`cpu+cpuacct`→`cpu`)·유지(`memory`)·개명(`blkio`→`io`)·소멸(`net_cls` 없음, eBPF 대체)로 갈리는 것이 요점입니다. **사실 갱신 1건** — 심화 학습에 `cri-dockerd` 신설. Docker Engine 이 아직 K8s 지원 런타임 목록에 있는 것은 Mirantis·Docker 가 K8s 밖에서 유지보수하는 CRI shim 덕이고, 1.26 부터는 CRI v1 만 동작합니다(공식 문서 2건 대조). 결정 치트시트의 CRI 구현 목록도 같이 맞췄습니다 — *한 자리만 고치는* 되풀이 실패를 피하려고 |
| 03-02 Phase 1 | **2026-08-31 통과 — 자력 8 / 막힘 6, 잘못 알던 인과는 2건뿐.** 이 편은 막힘이 전부 *몰랐던 사실*(고유명사·전제)이었고 인과를 틀린 곳은 끝의 둘뿐이라, 03-01 과 성격이 다릅니다. **자력 정답 여덟** — ① Host 모드가 격리를 잃는다 → 스스로 "베어메탈로 돌아가는 것"으로 연결 ② 그 증상을 포트 충돌로 구체화 ③ Host 의 이득을 "거치는 게 없으니 빠르다"로 ④ 남은 배선을 `ip link`·`ip route` 로 확인 ⑤ 노드 둘의 `docker0` 대역이 겹치면 라우팅이 불가하고 "노드 간 규약"이 필요하다 ⑥ 그 규약을 유지하려면 "50대를 다 아는 중계 지점"이 있어야 한다(= 공유 저장소) ⑦ 호스트 경계 넘기를 "이중으로 감싼다"로, 바깥 봉투에 호스트 실주소를 적는다까지 ⑧ CNM 걸림돌 둘 중 하나(Docker 데몬 종속)를 힌트 없이. **막힘 여섯** — ① "`docker run` 에 네트워크 모드를 붙인다"는 말 자체(전제 결손, **또 출제 보폭 문제**) ② 모드 7종(고유명사, 제공) ③ `ip netns list` 에 컨테이너 netns 가 안 보이는 이유 ④ 드라이버 개념과 Overlay ⑤ VXLAN 구체(캡슐화 형태까지는 자력) ⑥ CNM 걸림돌 첫 갈래는 힌트 요청. **인과 정정 2건** — ⓐ "CNM 이 CNI 로 수정됐다"(둘은 별개 표준이고 공존. K8s 가 채택하지 않았을 뿐) ⓑ "Cilium 부터 외부 코드"(넷 다 외부 코드이고 K8s 내장 CNI 는 없음 — 그래서 `kubeadm init` 뒤 CNI 설치가 별도 단계). **난이도 유지** — 막힘 6건이지만 전제 오류가 아니라 미지 사실이라 복귀 구간으로 보지 않습니다 |
| 출제 보폭 재발 (03-02) | **같은 세션에서 두 번째 지적을 받았습니다.** 03-01 에서 박제한 뒤에도 질문 1 에서 "`docker run` 에 붙일 네트워크 모드가 일곱 개입니다"로 시작해, **모드가 무엇인지·어떻게 붙이는지를 안 깔고** 물었습니다. 사용자 지적 — "docker run에서 네트워크 모드를 붙인다는게 정확히 무슨말이지? 또 1 2 3 4 이렇게 아니라 3 5 8 이렇게 설명하는거 같음". **박제만으로는 안 고쳐졌다는 것이 이 세션의 발견입니다.** 규칙을 쓴 직후에도 같은 실패가 나왔으므로, 다음 세션은 질문을 내기 전 *그 질문에 등장하는 명사 하나하나가 이 세션에서 정의됐는지* 를 세는 편이 낫습니다 — "선행 개념을 확인했는가"라는 추상 자문은 통과시켜 버립니다 |
| 03-03 Phase 1 | **2026-08-31 통과 — 이 편이 3장 세 편 중 가장 좋았습니다.** 자력 3 / 힌트 후 도달 2 / 인과 정정 1. **자력** — (1) 호스트에서 `127.0.0.1:80` 은 되고 `:8080` 은 거부되는 이유를 "호스트에 8080 을 잡은 프로세스가 없다"로 정확히 (2) 컨테이너끼리는 8080 만 통한다는 결론 (3) **마지막 질문을 유도 없이** — 다른 호스트에서 컨테이너 IP 로 두드리면 "도달도 못해서 타임아웃, 거절 아님"이라고 답해 *거부와 닿지 않음의 구분*을 스스로 세웠습니다. 이 편의 결론에 해당하는 자리입니다. **힌트 후 도달** — (1) 두 컨테이너 간 `localhost` 불통(첫 답은 "8080 은 될 것", 되묻기 한 번에 "컨테이너1에 8080 이 있는 거지 2 에는 없구나"로 자기 정정) (2) host-2 에서 호스트 포트 80 은 된다(같은 랜이라 도달 자체는 문제없다는 힌트 한 단계). **인과 정정 1건** — "브리지가 8080→80 변환을 해 준다"고 답했으나 브리지는 L2 라 포트를 볼 줄 모르고, 포트 매핑은 호스트 쪽 iptables DNAT 이며 컨테이너 간 통신은 그것을 아예 안 지납니다. 결론은 맞고 이유가 뒤집힌 사례라 그 자리에서 교정했습니다. **Pod 연결 성립** — 컨테이너마다 스택이 따로라 `localhost` 가 안 통하는데 Pod 안에서는 통한다는 대비를 짚었고, "Pod 는 기본값을 일부러 깬 구조"까지 이었습니다 |
| 원문자 금지 재발 | **2026-08-31 사용자 지적 — "내가 동그라미 내부에 숫자있는 글자 쓰지말라했지? (안보인다고)".** 대화 응답에 `①②③` 을 계속 썼습니다. 원인은 규칙의 적용 범위였습니다 — `writing-method` 의 `02-prose-rules §규칙 7` 과 `markdown-compat-check.py` 가 원문자를 잡지만 그건 **`write/` 문서에만** 걸리고, 사용자가 매 순간 읽는 **대화 응답에는 가드가 없었습니다.** 메모리 `feedback_no_circled_numbers.md` 로 박제. 대안은 `1.` 리스트 또는 `(1)` |
| 02-02 도식 신설 | **2026-08-31 `02-02.table-order-value-change.svg`(type-state) 신설.** 2026-08-28 Phase 1 에서 학습자가 스스로 세운 "값을 바꾸는 놈이 먼저, 그 값으로 결정하는 놈이 나중"을 패킷 필드의 실제 변화로 보인 것이다. 기존 `chain-then-table` 은 어느 칸이 있고 없는지만 보이고 값은 담지 않았다. OUTPUT 체인을 고른 근거는 본문 표다 — mangle·nat·filter 셋을 다 가진 체인은 INPUT 과 OUTPUT 뿐이다. 주소는 frontmatter `allow` 에 이미 있는 값(10.96.192.224 · 10.244.1.66)만 재사용해 새 IP 를 들이지 않았다. **타입 선택은 분포를 보고 정했다** — 저장소 상위 3개(dp-security-matrix 19% · architecture 18% · process 12%)가 49% 를 먹고 있어, 2% 인 `type-state` 로 갔다. **기계 검사 통과 뒤 눈으로 두 번 고쳤다** — `ddx.band` 인자를 잘못 넘겨 하단 밴드가 텍스트를 관통했고(lint·overflow 는 통과), 바뀐 필드가 색으로 구분되지 않아 독자가 값을 눈으로 diff 해야 했다. `lesson_overflow_checker_misses_overlap` 의 재확인이다. |
| 3장 Phase 3 (03-04) | **2026-09-01 묶음 2·3·4·5 완료, 묶음 1 제외.** 사용자가 모든 명령을 직접 실행했고 매 단계 예측을 먼저 받았습니다. **빗나간 예측이 여섯**이고 그게 이 실습의 수확입니다 — ① `docker0` 가 `DOWN` 인 이유(절전이 아니라 캐리어 없음. 손으로 `up` 해도 안 올라감) ② multistage 를 썼으니 작아질 것(1.08GB, 오히려 10MB 큼 — 실행 스테이지 베이스가 그대로였다) ③ 캐시는 `RUN` 부터 깨질 것(`COPY` 부터. COPY 만 파일 내용을 캐시 키에 넣는다) ④ conntrack 한 줄에 방향이 하나일 것(둘. 응답 튜플은 뒤집기가 아니라 NAT 적용 후 모양) ⑤ `nsenter -n` 에서 `curl` 이 안 될 것(실행됨. `-n` 은 네트워크만 들어가고 파일시스템은 호스트 것) ⑥ 호스트 자기 랜 주소도 루프백처럼 DNAT 을 안 탈 것(탐. 카운터 0→1). **자력으로 맞힌 것도 여럿** — Host 모드 포트 충돌, `go-web` 이 `.4` 를 받는다, 게이트웨이는 이 대역이 누구 것인지 모른다, 컨테이너 IP 는 타임아웃. **새 사실 발견 둘** — `127.0.0.1:80` 은 DNAT 이 아니라 `docker-proxy` 가 처리한다(OUTPUT 점프 조건의 `!127.0.0.0/8` 탓에 DOCKER 체인 진입 자체가 없다), `/proc/net` 은 mount 가 아니라 네트워크 네임스페이스를 따라간다 |
| 3장 Phase 3 환경·산출물 | **환경** — OrbStack VM 두 대(`ubuntu` 192.168.139.208 · `ubuntu2` 192.168.139.238), 인터페이스는 `eth0`(책의 `enp0s8` 아님), Docker 는 **VM 안에 별도 설치**(Mac 의 OrbStack Docker 로는 호스트에 `docker0` 가 안 남아 성립하지 않음). 상대 컨테이너는 책의 `jessie-dnsutils` 를 유지하되 `curl` 이 없고 jessie EOL 로 설치도 실패해, `nsenter -t <PID> -n` 으로 그 netns 안에서 호스트 `curl` 을 실행하는 방식으로 풀었습니다. **산출물** — `03-04` 에 도식 4장 신설(`docker-run-leaves` type-nested · `build-stage-choice` type-flowchart · `six-probes` type-swimlane · `three-addresses` type-sequence), 예측표 관찰 칸 15개, 신설 `###` 7개. 도식이 한 장도 없던 문서였습니다. **묶음 1(맨손 배선) 제외** — 02-04 와 겹치고 새로 더하는 것은 호스트 NIC 를 브리지에 넣는 단계뿐인데 VM 네트워크를 끊을 위험이 대가였습니다 |
| 3장 보강 완료 (2026-09-02) | **Phase 4 중 나온 요청 여섯 건을 같은 세션에서 처리했습니다.** (1) **심화 학습 → 본문 병합** — 03-01·03-02·03-03 의 항목 13개를 관련 절로 옮기고 절을 지웠습니다(03-04 에는 원래 없음). 시점 표기는 문장 안에 남겼습니다("책이 여섯을 열거하지만 현재 커널은 여덟입니다"). 가장 값한 이동은 03-01 의 namespace 종 수 — 2026-08-27 재검증에서 잡혔던 불일치가 심화 학습에만 갱신이 들어가 생긴 것이었고 이제 표 바로 아래에 붙어 다시 갈라질 수 없습니다. (2) **Kubernetes 접점** — `03-03.pod-shares-the-stack.svg`(type-nested) 신설 + `### 링의 개수가 전부다` 소절. 왼쪽 링 둘 / 오른쪽 링 하나의 대비로 사이드카 패턴의 근거까지 잇습니다. (3) **VXLAN 도식 재제작** — VTEP 를 주체로 세우고 컨테이너·브리지가 매달린 배치를 그렸습니다. 옛 판은 단계 사슬이라 VTEP 가 머리글로만 있었고 **원문자(①~④)를 쓰고 있었습니다**. (4) **ICMP·PMTU** — `01-03` 에 `### Path MTU Discovery` 신설 + `01-03.pmtu-blackhole.svg`(type-sequence). 같은 왕복이 ICMP 유무로 갈리는 두 묶음이며, 03-02 §4 에 오버레이에서의 발현을 한 단락 두고 교차 참조했습니다. (5) **CNM 드라이버** — `03-02.cnm-driver-split.svg`(type-flowchart). 점선이 CNM 경계이고 global 갈래만 그 밖으로 나갑니다. (6) **CNI 구현체** — `03-02.cni-implementations.svg`(type-dp-security-matrix). 판정 축은 오버레이 사용 여부. **CrossSubnet 도 여기서 해소** — 6장으로 미루려던 것을 §5 Calico 행 옆에 캡슐화 종류·적용 범위로 함께 적었습니다. 도식 5장 전부 생성기와 함께 `_gen/` 에 두고 렌더해 눈으로 확인했으며, 기계 검사가 못 잡은 겹침 2건(칩이 VTEP 상자를 덮음, 갈래 라벨이 상자 모서리에 걸침)과 사실 오류 2건(§2 도식의 DNAT 대상 IP 불일치, Pod 도식의 두 컨테이너 주소 중복·화살표 방향)을 눈으로 잡아 고쳤습니다. 병합이 만든 형식 위반 6건도 그 자리에서 교정했습니다 |
| 보강 대기 (04-01 §2 레이아웃 도식) | **2026-09-02 Phase 1 중 학습자 질문에서 나왔습니다 — "Isolated 와 Island 가 뭐가 다른지".** 본문 표와 `_assets/04-01.cluster-network-layouts.svg` 는 둘 다 *얻는 것·내주는 것* 축으로만 갈라 놓아서, 실제 구분선인 **트래픽 방향**(Pod 가 밖으로 나갈 수 있는가 / 밖에서 Pod 로 들어올 수 있는가)이 어디에도 없습니다. 대화에서는 방향 2열 표로 설명해 해소했고, 그 표를 본문 §2 에 넣고 도식에도 egress·ingress 축을 더하는 것이 보강 방향입니다. Isolated 는 양방향 차단, Island 는 나가는 방향만 SNAT 으로 허용, Flat 은 양방향 개방입니다 |
| 보강 대기 (readiness 전파 경로 도식) | **2026-09-02 Phase 1 중 학습자 질문 — "이 흐름 도식화 되어 있는가".** readiness 실패가 iptables 규칙까지 내려가는 경로(kubelet 프로브 실패 → Pod status Ready:False → EndpointSlice 컨트롤러 → kube-proxy watch → `KUBE-SVC-` 체인 재작성 → 확률 재계산)는 **세 편에 조각으로 흩어져 있고 도식이 없습니다.** `04-01 §5` 는 결과만 적고("Endpoints 에서 제외"), 주체인 EndpointSlice 컨트롤러는 `05-01` 에서야 이름이 나오며, `04-02` 의 `kube-proxy-iptables-chains.svg` 는 체인 *구조*를 그린 것이라 전파 *경로*가 아닙니다. 대화에서는 10단계로 풀어 설명했습니다. 보강 방향은 이 경로를 `type-sequence` 한 장으로 그려 `04-01 §5` 에 넣고 `04-02`·`05-01` 에서 교차 참조하는 것입니다. 3장 실습의 확률 사다리 실측(27·22·30·21)이 9단계와 같은 구조라 연결점이 됩니다 |
| 4장 실습 분리 (04-04 예정) | **2026-09-02 학습자 지시 — "실습절은 나중에 실습문서에 모두 이관하는거 잊지마".** 3장의 `03-04` 선례와 같은 모양으로 4장 실습편 `04-04` 를 신설하고 아래를 옮깁니다. **이관 대상 둘** — `04-02 §3`(KIND 위에 Cilium 배포, `disableDefaultCNI: true` 로 노드가 NotReady 에 머무는 장면) · `04-03 §4`(Cilium L3→L7 정책 실습). 이론 편에는 개념 서술과 교차 참조만 남깁니다. **실습으로 추가할 것 둘** — (1) `iptables-save | grep KUBE-SVC` 로 규칙을 직접 꺼내 보고 Pod 하나를 unready 로 만들어 줄이 사라지는지 확인(Phase 1 질문 13 에서 나온 요청, readiness 전파 경로를 눈으로 보는 자리) (2) NetworkPolicy 를 걸기 전후로 같은 연결을 시도해 "선택되는 순간 잠긴다"를 실측. 3장 선례대로 `04-03 §3` 의 YAML 두 블록처럼 *해부 대상인* 코드는 이론 편에 남깁니다 |
| 4장 학습 계획 | **2026-09-02 착수 — 학습자 지정 순서.** 4장 세 편(04-01·04-02·04-03)을 한 범위로 묶어 Phase 1 을 먼저 끝내고, Phase 2 는 편마다 하나씩 돌립니다. Phase 3 은 3장 선례(`03-04`)처럼 실습편 `04-04` 를 신설해 실습 흐름을 한 번에 진행하고, 전부 끝나면 4장 전체로 Phase 4 를 칩니다 |
| 4장 Phase 1 | **2026-09-02 세 편 한 범위로 완료 — 16문항, 예측→그 자리 채점.** **자력 정답 열둘** — 포트 충돌의 동적 관리 붕괴, NAT 이 식별을 지운다는 인과, 자기 광고 주소가 안 닿으면 통신 실패, Flat 은 라우터가 경로를 관리해야 함, `/16` 을 `/24` 로 잘라 노드 256대·노드당 256개 계산, `Pending` 은 미배치(PVC 미바인딩 예시까지), 502 의 답이 Probe, liveness 오용 시 재시작 반복과 DB 무관, CNI 설정에 브리지·대역이 필요, VXLAN 의 대가는 MTU, **Service 5천·Pod 5만 규모에서 iptables 선형 평가 비용**(이번 회차 최고 답변), Dual Stack 은 v4·v6 CIDR 을 둘 다. **잘못 알던 인과 아홉** — (1) 관리 대상을 노드별 컨테이너 대역으로 봄(실제는 호스트 포트) (2) Island 의 대가를 오버헤드로 봄(실제는 IP 기반 식별 상실) (3) `ContainerCreating` 을 이미지 수신 완료 이후로 봄(이미지 pull·볼륨·CNI 가 전부 그 구간) (4) **readiness 를 "Pod IP 라우팅을 끊는다"로 봄** — 끊으면 그 Pod 가 DB 에 못 붙어 초기화를 못 끝내는 자기모순. 실제는 다른 노드 iptables 에서 그 Pod 를 가리키던 줄이 빠지는 것 (5) Flat 의 대가를 IP 비용만으로 봄(장비 협조·BGP 가 진짜 값) (6) **NetworkPolicy 를 포트 단위 차단으로 봄** — 선택되는 순간 그 방향 전체가 기본 거부, 포트가 달라도 막힘 (7) 정책에 안 나온 Pod 도 잠긴다고 봄(선택 안 되면 무영향, 다만 잠긴 Pod 로 가는 호출은 막힘) (8) DNS `search` 순서를 "원래 이름 먼저, 실패하면 접미사"로 봄 — 실제는 `ndots:5` 탓에 접미사가 먼저이고 절대 이름이 마지막 (9) 제 서술을 "ClusterIP 는 NAT 을 안 쓴다"로 오독(쓰는 쪽). **몰랐던 사실(제공)** — netns 를 CNI 에 넘긴다는 것, `statistic` 모듈, IPAM, VTEP, Geneve, NodeLocal DNSCache, `search`·`ndots`, EndpointSlice 컨트롤러. **난이도 유지** — 인과 오류 9건이지만 전제 결손이 거의 없었고, 3장 실습 실측을 스스로 끌어와 잇는 힘이 좋았습니다(확률 사다리 100회 → kube-proxy 백엔드 확률 분기). `last_verified` 는 갱신하지 않습니다 — Phase 1 은 검증 회차가 아닙니다 |
| 출제 보폭 재발 (4장 Phase 1) | **세 번째 지적입니다.** "EndpointSlice 가 리눅스에서 어떻게 규칙 명단을 보고 관리하는지 1~10 순서로 설명해야지 3 5 7 이렇게 설명하지 말아줘". 명단에서 빠진다는 *결과*만 말하고 kubelet 프로브 실패 → Pod status `Ready:False` → EndpointSlice 컨트롤러 → kube-proxy watch → `KUBE-SVC-` 체인 재작성 사이를 통째로 건너뛴 것이 원인입니다. 10단계로 다시 풀어 해소했고, 그 경로에 도식이 없다는 사실도 함께 드러나 아래 보강 대기 행이 됐습니다. 3장에서 두 번(03-01·03-02) 받은 것과 같은 실패 양식이라, **결과를 말하기 전에 중간 주체를 세는** 것을 다음 세션 규칙으로 둡니다 |
| 04-01 Phase 2 | **2026-09-02 완료 — Phase 1 에서 드러난 결손만 채웠습니다. 추가만 하고 기존 서술은 바꾸지 않았습니다.** 네 곳입니다. (1) **§2 방향 축 표** — Isolated 와 Island 가 얻는 것·내주는 것 축에서는 같아 보였습니다. 실제 구분선인 egress·ingress 를 2열 표로 넣었습니다. (2) **§3 두 소절 신설** — Pod 대역과 ClusterIP 대역이 겹치면 안 되는 이유(인터페이스에 붙은 진짜 주소 대 규칙이 가로채는 가짜 주소), 그리고 `/24` 의 256 은 주소 상한이고 kubelet `--max-pods` 기본 110 이 먼저 걸린다는 것. (3) **§4 진단 표** — `Pending` 과 `ContainerCreating` 이 서로 다른 주체의 자리이고 네트워크는 뒤쪽에만 있다는 것. (4) **§5 전파 경로 10단계 + 도식 신설** — `_assets/04-01.readiness-propagation.svg`, 도식 6장에서 7장이 됐습니다. **검증** — §16 재통과(합니다체 88 · 한다체 0 · AI강조어 0 · 365→378줄 · 원문자 0), C-11 신규 2건 정리, 벽단락·절도입·도식배치·markdown-compat 전부 통과, 도식은 Chrome 렌더 후 눈으로 확인했습니다. dd-lint advice 6건은 위양성이었습니다 — 오른쪽 상자 글자를 왼쪽 상자에 귀속시켜 폭을 계산했습니다. **미수정 1건** — 기존 요약 위계 위반(L48 핵심 요약이 인용 2문단)은 보강 범위 밖이라 손대지 않았습니다 |
| 04-01 Phase 2 다음 단계 | **학습자가 04-01 을 읽고 "수정하면 좋을 부분"을 전달하는 차례입니다.** Phase 2 진행 방식을 학습자가 이렇게 정했습니다 — 문서 하나씩 보면서 질의응답에서 부족했던 부분을 먼저 보충하고, 그다음 학습자가 읽은 뒤 수정 요청을 전달해 보강합니다. 앞 절반은 끝났고 뒤 절반이 남았습니다 |
| 04-01 Phase 2 · 학습자 읽기 피드백 | **2026-09-02 9건 전부 반영.** (1) §1 제목·요약의 "세 가지"를 "3가지"로 — 세와 새가 헷갈린다는 지적. (2) 요구사항 3 문장을 "자기 주소를 조회했을 때 나오는 IP 와 남이 부를 때 쓰는 IP 가 같아야 한다"로 고쳐 쓰고 광고 주소(`advertised.listeners`) 예를 붙였습니다. (3) 그 자리에 도식 `same-address-both-sides` 신설 — Docker 는 어긋나고 Kubernetes 는 같다는 대비. (4) §2 에 도식 `layout-packet-paths` 신설 — 노드 넘는 Pod 통신은 셋이 같고 밖으로 나갈 때만 출발지가 갈린다는 것을 실제 경로로. (5) `### 셋 중 기본값은 무엇인가` 신설 — Kubernetes 자체엔 기본이 없고, CNI 가 masquerade 를 기본으로 켜 사실상 Island 이며, 클라우드 VPC CNI 는 Flat. (6) L2 인접성 문단에 도식 `l2-adjacency` 신설 + 같은 노드와 다른 노드를 나란히 놓는 서술로 보강 — "글만 읽어선 모르겠다"는 지적. (7) §3 `### 이 값이 실제로 적히는 자리` 신설 — kubeadm ClusterConfiguration → 정적 Pod 매니페스트 → `kubectl get node -o jsonpath='{.spec.podCIDR}'` 로 확인까지. (8) §4 진단 표를 축약하고 phase·condition 층위는 kubernetes-in-action 06-01 로 링크. **`ContainerCreating` 은 phase 가 아니라 대기 이유**라는 사실도 이때 바로잡았습니다. (9) §5 10단계를 네 주체 서술로 줄이고 프로브 설정값은 kubernetes-in-action 11-03 §4 로 링크. **도식 6장에서 10장으로 늘었고 전부 Chrome 렌더 후 눈으로 확인했습니다** — `l2-adjacency` 는 첫 렌더에서 브리지 상자 글자 넘침과 라우팅 상자가 노드 상자를 관통하는 겹침이 실제로 있어 좌표를 고쳤습니다. 나머지 lint advice 는 위양성이었습니다 |
| 사고와 복구 (04-01 Phase 2) | **본문 §1~§4 가 편집 중 잘렸다가 복구됐습니다.** 원인은 앵커 충돌입니다. 앞선 보강에서 프론트매터 review 주석에 신설 소제목 이름(`### Pending 과 ContainerCreating 은 다른 구간입니다`)을 그대로 적어 뒀는데, 다음 편집이 그 문자열을 본문에서 찾으려다 **프론트매터 주석에 먼저 걸렸고** 그 지점부터 §4 끝까지를 잘라냈습니다. 복구는 `git show HEAD:` 원본과 잔존본을 합치고 모든 보강을 재적용하는 방식으로 했고, HEAD 대비 사라진 8줄이 전부 의도한 교체인지 기계로 대조해 확인했습니다. **재발 방지 두 가지** — 프론트매터 주석에 소제목 이름을 백틱째 적지 않는다, 그리고 본문 편집 앵커는 반드시 본문 문장에 건다. 다른 세션이 같은 파일에 넣어 둔 `03-03` 개명 반영도 복구 과정에서 보존했습니다 |
| 04-01 Phase 2 · 읽기 피드백 2차 | **2026-09-02 2건 반영.** (1) **도식 안 표기 누락** — 본문의 "세 가지"만 숫자로 바꾸고 SVG 안 텍스트를 놔뒀습니다. 학습자가 `chapter-overview` 에서 잡았고, 그 도식 4곳과 `nat-free-promise` 1곳을 "3가지 약속"으로 고쳤습니다. **교훈은 수사 표기를 바꿀 때 SVG 내부 텍스트까지 grep 해야 한다는 것입니다.** (2) **`same-address-both-sides` 전면 재작성** — "실제 주소와 외부에서 조회한 주소가 뭔지 모르겠다, 하단 글이 너무 산문이다". 두 주소를 추상 라벨에서 조회 명령과 실제 값으로 바꿨습니다. 왼쪽은 `ip addr → 172.17.0.5`, 오른쪽은 `192.168.1.20:31000` 입니다. 산문 캡션은 번호 3단계로 교체했습니다. 사람이 적은 주소로 붙어 성공, 앱이 스스로 말한 주소를 받음, 그 주소로 붙어 타임아웃입니다. 본문 산문도 도식과 겹치는 서술을 덜어 네 문단으로 줄였습니다. 최종 도식 10장, 447줄, 벽 단락 0 |
| 04-01 Phase 2 · 읽기 피드백 3차 | **2026-09-02 학습자 질문 둘이 노트의 결손을 짚었습니다.** (1) "매번 외부에서 조회한 IP 쓰면 문제 없는 거 아니야?" — 노트가 광고 주소 사고를 적으면서 **왜 클라이언트가 처음 붙은 주소를 계속 쓸 수 없는지**를 빠뜨렸습니다. 메타데이터 응답에 브로커 여러 대의 주소가 담기고 그 값이 각 브로커의 자기 보고라는 것, 손수 적는 처방이 매핑 포트와 노드 이동으로 깨진다는 것을 §1 에 두 문단 넣었습니다. (2) "쿠버네티스에서 어떻게 가상 IP 직접 통신이 되나?" — 학습자가 **Pod IP 를 가상 주소로 오해**했고 노트에 그 구분이 없었습니다. Pod IP 는 `eth0` 에 붙은 진짜 주소이고 가상 주소는 ClusterIP 쪽이라는 인용 블록을 넣었습니다. (3) **같은 오해가 제 도식에도 있었습니다.** `same-address-both-sides` 가 "밖에서 닿는 주소"라고 적었는데 이 약속의 범위는 클러스터 안이고 Island 레이아웃에서는 밖에서 Pod IP 로 안 닿습니다. "상대가 닿는 주소"로 고치고 부제에 상대의 범위를 명시했습니다. (4) §1 산문 정리 — 동기 문단을 둘로 쪼개고 함정 둘을 표로 바꿨습니다. 최종 473줄, 도식 10장, 벽 단락 0 |
| 04-01 Phase 2 · 읽기 피드백 4차 | **2026-09-02 2건 반영.** (1) **"Service 는 S/D NAT 이 다 동작하고 확률 로드밸런싱인데 IP-per-Pod 강조와 헷갈린다"** — 노트가 §1 의 약속과 서비스의 NAT 을 잇지 않아 모순처럼 읽혔습니다. §1 에 `### 서비스를 거치면 NAT 이 생기는데 약속은 어떻게 되나` 를 신설했습니다. 요지는 셋입니다. 서비스는 약속의 예외가 아니라 **약속 위에 얹은 층**이고 DNAT 의 목적지가 곧 Pod IP 라 IP-per-Pod 를 전제로 씁니다. 출발지는 보통 보존되며 노드 발신·헤어핀·NodePort 경유에서만 바뀝니다. iptables 모드의 확률 분배는 무작위이되 연결 단위로 고정됩니다. (2) **"이미지를 2개로 나눠서 배치하면 더 좋아 보임, 각 장은 좀 더 상세하게 흐름도가 보이게"** — 통합본 `same-address-both-sides` 를 걷어내고 흐름도 둘로 갈랐습니다. `advertised-address-docker` 는 포트 매핑과 컨테이너 위상 위에 3단계 화살표를 얹고 3단계를 X 로 끊습니다. `advertised-address-k8s` 는 노드 2 의 클라이언트 Pod 에서 노드 1 의 브로커 Pod 로 가는 같은 3단계가 전부 성공하는 모습입니다. 주소 상자 나열이 아니라 실제 경로가 보이게 그렸습니다. 본문도 그에 맞춰 갈라 도식마다 해설을 붙였습니다. 최종 511줄, 도식 11장, 벽 단락 0 |
| 04-01 Phase 2 · 읽기 피드백 5차 | **2026-09-02 §1 소제목 분할.** 보강을 거치며 §1 이 98줄로 커졌는데 소제목이 `### 풀려는 문제` 하나뿐이라 한 덩어리로 읽혔습니다. 다섯을 신설해 일곱으로 갈랐습니다 — 풀려는 문제 · 3가지 요구사항 · 세 번째 요구사항이 왜 따로 필요한가 · IP 를 Pod 마다 주는 동기와 대가 · 이 모델에 따라오는 함정 둘 · 서비스를 거치면 NAT 이 생기는데 약속은 어떻게 되나 · CNI 는 함께 오지 않는다. **본문 문장은 하나도 바꾸지 않았고 소제목만 넣었습니다.** 최종 522줄, 도식 11장, 벽 단락 0, 절 도입 위반 0 |
| 04-02 Phase 2 | **2026-09-02 앞 절반 완료 — Phase 1 결손 넷을 채웠습니다. 겹치는 주제는 형제 편으로 넘겼습니다.** (1) **§1 `### 호출 한 번에 무엇이 넘어가는가` 신설 + 도식 `cni-invocation` 신설** — 노트가 "설정 JSON 을 stdin 으로"까지만 적고 **호출마다 달라지는 환경변수 넷을 빠뜨렸습니다.** 학습자가 Phase 1 질문 11 에서 브리지·대역(stdin 설정)만 답하고 "어느 Pod 인지" 축을 못 댄 자리가 정확히 여기입니다. `CNI_NETNS` 를 02-04 실습의 `ip link set … netns` 와 이었습니다. (2) §1 IPAM 보강 — `host-local` 구현체와, 그 재고가 04-01 §3 의 노드 `spec.podCIDR` 이라는 연결. (3) **§2 `### 캡슐화와 라우팅 광고는 각각 무엇을 낸다` 신설** — 표가 Flannel 은 오버레이, Calico 는 BGP 라고만 적고 각각의 대가가 없었습니다. 캡슐화는 MTU 를 내주고 라우팅 광고는 남의 장비 협조를 요구합니다. VXLAN·VTEP 상세는 03-02 §4 로 링크. (4) **§4 `### 규모가 커지면 무엇이 먼저 무너지는가` 신설** — 노트가 연결 편향만 다루고 규칙 수와 갱신 비용을 빠뜨렸습니다. 서비스 5천에 Pod 10이면 엔드포인트 줄이 5만이라는 계산, 한 줄 갱신에 테이블 전체 재작성이라는 축을 적고 메커니즘은 02-02 로 넘겼습니다. 학습자 Phase 1 최고 답변이 이 주제였는데 노트에 근거가 없던 자리입니다. 그 밖에 `statistic` 매치 모듈 이름을 명시하고 EndpointSlice watch 문단에서 04-01 §5 전파 경로로 교차 참조했습니다. **§3 실습절은 04-04 이관 예정이라 손대지 않았습니다.** 검증 — 합니다체 75 · 한다체 0 · 도식 3장에서 4장 · 335줄 · 벽 단락 0 · 절 도입 0 · 깨진 링크 0 |
| 04-02 Phase 2 · 읽기 피드백 | **2026-09-02 6건 반영. 도식 4장에서 7장으로 늘었습니다.** (1) **04-01 프로브 비유 재작성** — 한 문단에 줄표로 이어 붙인 네 문장을 인용 블록 안 다섯 문단으로 갈랐습니다. (2) 04-02 학습 목표 1 을 "4연산"에서 "4가지 연산(`ADD`·`DEL`·`CHECK`·`VERSION`)"으로 고쳐 이름을 명시했습니다. (3) **`cni-add-flow` 신설** — "런타임에서 추가되는 흐름이 도식으로 궁금하다"는 요청. Kubelet·컨테이너 런타임·CNI 플러그인·IPAM 네 주체가 6단계로 일하는 흐름이고, **네임스페이스가 먼저 생겨야 `CNI_NETNS` 가 존재한다**는 순서를 명시했습니다. (4) **`four-cni-compare` 신설** — §2 의 4행 표를 대체. NetworkPolicy 관문에서 Flannel 이 걸리는 것과 Weave 가 아카이브인 것을 색으로 갈랐습니다. (5) **`kube-proxy-modes` 신설** — §4 의 5모드 리스트를 대체. "무엇으로 찾나" 축으로 갈랐고 iptables 사용 여부를 별도 열로 뒀습니다. (6) **산문 정리** — §1 에 소제목 넷, §4 에 소제목 다섯을 신설하고 산문 3문단을 표 2개로 전환했습니다. 검증 — 합니다체 81 · 한다체 0 · 383줄 · 벽 단락 0 · 절 도입 0 · 깨진 링크 0. 도식 셋 다 Chrome 렌더 후 눈으로 확인했습니다 |
| 04-02 읽기 피드백 3차 · 원문자 일괄 정리 | **2026-09-03 3건 반영, 그 과정에서 전 편 결함 하나를 찾았습니다.** (1) **`ipam-split` 도식 신설** — 재고가 `--cluster-cidr` 에서 `spec.podCIDR` 을 거쳐 설정 JSON 의 ipam 칸으로 내려오는 길과, 배선 플러그인과 IPAM 이 갈라진 구조를 한 장에. (2) **`connection-bias` 도식 신설** — §4 하단에서 글로만 설명하던 편향 시나리오를 BEFORE·ROLLING UPDATE·AFTER 3단으로. X 와 Y 가 연결을 반씩 쥔 상태에서 X 가 Z 로 바뀌면 Y 가 75 를 갖습니다. (3) **"이 모드가 어디서 나온 모드인가"** — kube-proxy 자신의 실행 모드이고 `--proxy-mode` 로 고른다는 것이 노트에 없었습니다. 명시했습니다. (4) **"체인은 이전 장에서 다룬 것 같은데 또 적은 이유"** — 학습자 지적이 맞습니다. 확률 산식과 규칙 평가가 02-02 §3 과 겹쳤습니다. 이 편의 관심은 서비스 오브젝트가 무엇으로 번역되는가라, 진입·서비스·엔드포인트 3층 표만 남기고 나머지는 02-02 로 넘겼습니다. (5) **원문자 33자를 SVG 8장에서 걷어냈습니다.** 마크다운 센서가 `.md` 만 보는 탓에 도식 안에 그대로 남아 있었습니다. 03-01 2장·04-01·04-02·04-03·05-03·06-01 2장이 대상이었습니다. **표기 규칙을 바꿀 때 SVG 내부까지 grep 한다**를 두 번째로 확인한 셈입니다. 최종 412줄, 도식 9장, 벽 단락 0 |
| 04-02 `connection-bias` 재작성 | **2026-09-03 "흐름이 잘 보이게" 요청으로 다시 그렸습니다.** 앞 판은 BEFORE·UPDATE·AFTER 세 띠를 쌓기만 해서 연결이 어디로 옮겨 가는지가 안 보였습니다. 새 판은 **클라이언트를 왼쪽에 세로로 두고 세 단을 화살표로 이었습니다.** 핵심은 갈림입니다. 끊긴 50 은 빨간 화살표로 확률 규칙 상자를 지나 25씩 나뉘고, Y 가 쥔 50 은 **파란 점선으로 규칙을 우회해** 곧장 3단으로 내려갑니다. 3단 막대 길이를 75 대 25 로 실제 비율에 맞춰, 확률은 균등했는데 결과가 3 대 1 인 것이 눈으로 잡힙니다. 본문 해설도 도식을 단 단위로 따라가는 세 문단으로 다시 썼습니다. 첫 렌더에서 3단 라벨이 막대에 붙고 파란 점선이 막대 사이 빈틈을 가리켜 좌표를 보정했습니다 |
| 용어 결손 (GA) | **2026-09-03 학습자 질문 "GA 가 되었다는 게 무슨 말인가".** GA 가 8편에 걸쳐 20회 넘게 나오는데 **어디에도 뜻풀이가 없었습니다.** 용어집(`00-01`)에도 Alpha·Beta·GA 항목이 없었습니다. 두 곳을 채웠습니다. 용어집 4장 절에 `Alpha / Beta / GA` 항목을 신설했고, 04-02 §4 의 첫 실질 사용 자리에 3단계 표와 뜻풀이를 넣고 용어집으로 링크했습니다. 같은 김에 용어집의 kube-proxy 정의에 빠져 있던 `nftables` 모드도 더했습니다. **교훈은 약어가 여러 편에 흩어져 반복되면 정의된 적 없이도 익숙해 보인다는 것입니다.** 다음 회차에 다른 약어들(CRI·CNM·VTEP 등)도 용어집 등재 여부를 훑어야 합니다 |
| 04-02 §4 기본값 근거 + 스킬 규약화 | **2026-09-03 학습자 질문 "문제가 있는데 왜 iptables 가 기본값이냐, nftables 나 ipvs 가 더 좋은 것 아니냐".** 노트가 iptables 의 한계만 적고 **왜 그래도 기본값인지를 안 적었습니다.** §4 에 `### 그런데 왜 iptables 가 여전히 기본값인가` 를 신설했습니다. 이유 셋은 다수 규모에서는 문제가 안 된다, 전제 없이 어디서나 돈다, 바꿔도 안 풀리는 문제가 있다입니다. 3모드 비교 표도 함께 넣었고 **마지막 두 행이 요점**입니다. IPVS 로 옮겨도 masquerade 는 iptables 를 계속 써서 두 스택을 다 알아야 하고, 연결 단위 쏠림은 어느 모드로도 안 풀립니다. **같은 실패를 막으려고 스킬에 규약을 넣었습니다** — `writing-method/references/06-exemplars.md` 에 패턴 10 "선택지가 여럿이면 비교하고 기본값의 근거를 댄다"와 절 `### 선택지를 나열했으면 비교와 기본값 근거까지 간다`, 안티패턴 "선택지를 나열만 한다"를 신설했습니다. 비교 축 최소 골격은 메커니즘·전제·잔존 비용·공통 한계 넷이고, 이 노트의 kube-proxy 절을 적용 사례로 걸었습니다 |
| 04-03 Phase 2 | **2026-09-03 완료. Phase 1 결손 넷 + 읽기 피드백 여섯을 반영해 도식이 4장에서 8장이 됐습니다.** **Phase 1 결손** — (1) §1 `### 커널은 라벨을 모르는데 어떻게 강제되나` + 도식 `policy-enforcement` 신설. 노트가 "강제는 CNI 가 한다"까지만 적고 방법을 안 적었습니다. 학습자 질문 "라벨은 L4 에서 모를 텐데"가 원인이고, Calico 의 ipset 과 Cilium 의 identity 를 대비했습니다. (2) 3규칙 뒤에 오해 둘 명시 — 포트 단위 차단이 아니라는 것과, 정책은 자기 Pod 관점이라 선택 안 된 Pod 는 무영향이라는 것. Phase 1 질문 14 에서 둘 다 어긋난 자리입니다. (3) §5 dnsPolicy 표에 "언제 고르나" 열과 기본값 표시 — **`Default` 가 기본값이 아니라 `ClusterFirst` 가 기본**이라는 함정을 명시. (4) §6 ipFamilyPolicy 를 표로 전환하고 기본값 근거 추가. (3)(4)는 방금 만든 `06-exemplars` 패턴 10 의 첫 적용입니다. **읽기 피드백** — `policy-three-steps` 도식과 이해용 YAML 두 벌 신설, `dns-search-path` 전면 재작성(안 이름과 밖 이름 대비로 흐름화, 처방은 분리), `dns-remedies` 신설(고치는 자리 축으로 앱·Pod·서버 셋), `dual-stack` 신설(정책 3종 × 클러스터 2종 매트릭스), §5 소제목 셋 신설과 산문 정리. 검증 — 454줄 · 도식 8 · 한다체 0 · 원문자 0 · 벽 단락 0 · 절 도입 0 · 도식 배치 통과 · 깨진 링크 0. 미수정은 기존 핵심 요약 인용 문단 수 1건 |
| 4장 Phase 3 착수 | **2026-09-03 실습편 `04-04` 신설, 실행 전입니다.** 3장의 `03-04` 가 맡던 자리를 4장에서 맡습니다. **이관 완료 둘** — `04-02 §3`(KIND 위 Cilium 배포)과 `04-03 §4`(Cilium L3→L7)에서 절차와 명령을 빼고 개념 서술만 남긴 뒤 04-04 로 교차 참조했습니다. 04-02 §3 은 `### CNI 를 갈아 끼울 수 있다는 것의 실물`, 04-03 §4 는 `### L7 정책 — 표준 문법이 못 적는 것` 으로 제목도 이론 편에 맞게 바꿨습니다. **묶음 일곱** — CNI 없는 클러스터, 주소 재고가 내려온 자리, 서비스가 만드는 규칙 줄, readiness 를 잃으면 어느 줄이 사라지는가, 선택되는 순간 잠긴다, 이름 하나가 질의 몇 번, Cilium L7. Phase 1 에서 나온 요청 둘(`iptables-save` 로 `KUBE-SVC` 직접 보기, 정책 전후 실측)이 3번과 5번입니다. 예측 질문 24개를 두고 관찰 칸은 비워 뒀습니다. **사내 클러스터는 조회 전용으로 갈랐습니다** — `write/08_cloud/` 는 GitHub Pages 로 공개 배포되므로 사내 노드 이름과 대역을 적을 수 없습니다. 관측 문서는 `write/_company/사업/미래에셋/` 에 두었고 git 추적 제외를 확인했습니다. 컨텍스트는 `bd-tps-3.0.5.1` 이고 접속 정보는 `business-dev-infra` 스킬이 정본입니다. 그 클러스터는 OpenShift 라 kube-proxy 유무부터 예측 대상입니다 |
| 보강 대기 (04-01 §4 진단 표) | **2026-09-03 Phase 3 묶음 1 실측에서 드러났습니다.** 04-01 §4 의 `### Pod 가 안 뜰 때 어디를 보는가` 표가 **CNI 실패를 `ContainerCreating` 한 경우로만 적고 있습니다.** 실제로는 둘로 갈립니다. (1) **CNI 플러그인이 아예 없으면** kubelet 이 노드를 `Ready: False`(`cni plugin not initialized`)로 보고하고, 그 노드에 `node.kubernetes.io/not-ready` taint 가 붙어 Pod 는 **`Pending`** 에 머뭅니다. 스케줄 자체가 안 됩니다. (2) **CNI 는 있는데 ADD 가 실패하면** 노드는 Ready 이고 Pod 가 `ContainerCreating` 에서 멈춥니다. 실측 증거 — kind 클러스터를 `disableDefaultCNI: true` 로 세우자 노드 셋이 NotReady 이고 CoreDNS 는 `Pending` 이며 이벤트가 `0/3 nodes are available: 3 node(s) had untolerated taint(s)` 였습니다. **CoreDNS 의 Tolerations 가 `not-ready:NoExecute` 만 견디고 `NoSchedule` 은 안 견딘다**는 것이 그 이유입니다. 표에 행을 하나 더하고 노드 상태 열을 붙이는 것이 보강 방향입니다 |
| 4장 Phase 3 묶음 2·3 | **2026-09-03 완료. 사용자가 모든 명령을 직접 실행했고 매 단계 예측을 먼저 받았습니다.** **묶음 2(주소 재고)** — podCIDR 은 `10.244.0.0/24`(control-plane) · `10.244.2.0/24`(worker) · `10.244.1.0/24`(worker2), `--cluster-cidr=10.244.0.0/16`, `--service-cluster-ip-range=10.96.0.0/16`, `--node-cidr-mask-size` 는 **부재**(기본 24 라 안 적힌 것). `/etc/cni/net.d/` 에는 `05-cilium.conflist` 하나뿐이고 `ipam` 칸이 **없습니다** — 명세가 IPAM 분리를 허용할 뿐 강제하지 않고 Cilium 은 자기 안에서 처리한다는 실물 증거입니다. **묶음 3(서비스 규칙 줄)** — 예측 4개 중 2개 정답, 1개 절반, 1개 반증. 반증된 것이 수확입니다: "마스터는 Pod 를 안 받으니 규칙도 몰라도 되나" → 세 노드 **모두 정확히 11줄**. taint 가 막는 것은 스케줄링이지 호출이 아니고, CoreDNS 가 실제로 control-plane 에 떠 있습니다. 절반 맞은 것은 ping 실패 이유 — ICMP 도 맞지만 더 근본은 규칙 조건이 `-p tcp --dport 80` 이라 **체인 진입 자체가 없다**는 것. **덤프에서 부수적으로 확정된 둘** — `default/kubernetes:https` 와 `hubble-peer` 의 백엔드가 Pod IP 가 아니라 노드 IP(`172.18.0.3` · `172.18.0.4`) 라서 hostNetwork 추측이 이름이 아닌 **엔드포인트 주소로** 증명됐고, nat 테이블에 **FORWARD 가 없다**는 것(테이블은 다섯 훅 중 쓸 것만 붙는다) |
| 04-03 §1 보강 (생략된 필드의 뜻) | **2026-09-05 묶음 5 에서 드러났습니다.** 노트는 "`ports` 를 적었다고 그 포트만 통제되는 게 아니다"까지만 적고, **생략했을 때** 어떻게 되는지를 안 적었습니다. 학습자가 `ports` 를 지우면 전부 막힐 것으로 봤는데 열렸습니다. `ingress: []`(허용 0개) · `[{from: X}]`(모든 포트) · `[{ports: P}]`(누구나) · `[{}]`(전면 개방)을 표로 넣었습니다. 마지막 줄이 실무 함정입니다 — 중괄호 하나짜리 규칙은 적혀 있어 보이는데 아무것도 안 막습니다 |
| 보강 대기 (04-02 §4 — SVC/SEP 를 왜 나눴는가) | **2026-09-03 Phase 3 묶음 3 질의에서 드러났습니다.** 학습자가 "KUBE-SVC 와 SEP 의 차이가 뭔가"를 물었는데, 04-02 §4 의 3층 표와 02-02 §3 둘 다 **무엇을 하는지만** 적고 **왜 둘이어야 하는지**를 안 적었습니다. 답은 확률 판정의 성질입니다 — `-m statistic` 은 그 규칙 **한 줄에서 한 번만** 굴러갑니다. 뽑힌 뒤 할 일은 두 줄(헤어핀 마킹 · DNAT)이라, 같은 확률을 두 줄에 각각 적으면 따로 굴러 서로 다른 백엔드가 뽑힙니다. **한 번의 판정을 여러 동작으로 잇는 유일한 방법이 점프**이고 그래서 엔드포인트 체인이 존재합니다. 지금은 `04-04` 묶음 3 의 `### 확인된 것` 에만 적혀 있으니 정본(02-02 §3)으로 올려야 합니다 |
| 4장 Phase 3 이론 보강 · 도식 4장 신설 | **2026-09-03. 실습 중 학습자가 "몰랐다"고 말한 자리를 도식·산문으로 메웠습니다.** 실습 문서에 도식이 0장이었던 것도 함께 고쳤습니다. **(1) `02-02.hook-slots-and-cuts`(type-dp-security-matrix)** — 질문 "nat 은 4개, filter 는 FORWARD 가 있고 PREROUTING 이 없다, 무슨 차이지". 본문 표(02-02 §1)는 어느 칸이 비었는지만 보이고 **왜** 비었는지를 안 적었습니다. 답은 한 축입니다 — nat 은 주소를 바꾸니 결정 전이거나 나가기 직전이라야 하고, filter 는 통과 여부를 정하니 운명이 정해진 뒤라야 합니다. 남은 셋(INPUT·FORWARD·OUTPUT)이 정확히 그 운명 세 가지라 filter 가 셋인 것이 필연입니다. **(2) `02-02.chain-comes-alive`(type-process)** — 질문 "-A 에 PREROUTING 같은 방향만 들어오는 줄 알았다 / 커스텀이 가능한가". 내장 체인은 커널이 부르지만 사용자 체인은 선언·규칙·호출 세 줄이 다 있어야 살고, 가장 자주 빠지는 것이 호출입니다. `iptables-save` 의 콜론 줄 정책 자리(`ACCEPT` vs `-`)가 둘을 가르는 표식이라는 것도 넣었습니다. **(3) `02-02.why-two-chains`(type-flowchart)** — 질문 "SVC 와 SEP 의 차이가 뭔가". 표는 무엇을 하는지만 적고 **왜 둘이어야 하는지**를 안 적었습니다. `-m statistic` 은 붙은 규칙 한 줄에서만 굴러가는데 뽑힌 뒤 할 일이 두 줄(헤어핀 마킹·DNAT)이라, 합치면 두 번 굴러 A 를 표시하고 B 로 보내는 사고가 납니다. 합친 설계와 실제 설계를 짝 추적으로 나란히 놓았습니다. **(4) `04-04.lab-state-progression`(type-state)** — 실습편에 도식이 한 장도 없어 묶음이 무관한 명령 더미로 읽혔습니다. 묶음을 클러스터의 상태 전이로 놓고, 실행해서 값을 본 상태(묶음 1~3)와 아직 실행 전(묶음 4~5)을 실선·점선으로 갈랐습니다. **타입 선택 근거** — 이 책의 타입 분포가 `dp-security-matrix` 32 로 쏠려 있어 0회인 `timeline`·`journey` 를 먼저 봤지만 둘 다 스펙이 막았습니다(timeline 은 "시간 간격이 정직해야 한다", journey 는 "감정 곡선이 없으면 process 를 쓰라"). 쏠림보다 적합이 우선이라는 계약을 따랐습니다 |
| 04-02 §1 · 04-01 §4 산문 보강 | **2026-09-03 실습 관측이 노트의 결손 둘을 드러냈습니다.** **(1) 04-02 §1 `런타임과의 접점`** — 학습자 질문 "conflist 이게 뭐지". 노트는 두 경로(`/etc/cni/net.d` · `/opt/cni/bin`)를 표로만 적고 **둘이 무엇으로 이어지는지**를 안 적었습니다. `type` 문자열이 곧 바이너리 파일 이름이라는 것, `.conf` 와 `.conflist` 의 차이, 그리고 **디렉토리에 파일이 여럿이면 사전순 첫 번째 하나만 쓴다**는 것(Cilium 이 `05-`, Calico 가 `10-` 인 이유)을 세 문단 추가. IPAM 절에는 실물 반증을 붙였습니다 — Cilium 의 conflist 에는 `ipam` 칸이 **아예 없고** `/opt/cni/bin/host-local` 은 있어도 실행되지 않습니다. 명세가 분리를 허용할 뿐 강제하지 않는다는 증거입니다. **(2) 04-01 §4 진단 표** — 기존 보강 대기 항목을 해소했습니다. 표가 CNI 실패(`ContainerCreating`)만 담고 CNI **부재**를 안 담았고, 본문은 "CNI 를 아직 깔지 않았을 때 모든 Pod 가 여기(ContainerCreating) 머문다"고 **틀리게** 적고 있었습니다. 실측은 반대입니다 — 부재면 노드가 `NotReady` 가 되고 `not-ready` taint 가 붙어 스케줄러가 배치를 안 하므로 `Pending` 입니다. 두 줄 모두에 CNI 를 넣고, 가르는 질문("노드가 Ready 인가")을 본문에 명시 |
| 도식 생성기 부채 (4장) | **2026-09-03 실측 — `_assets/` 의 SVG 19장에 생성기가 없습니다.** 그중 15장이 이번 4장 Phase 2 회차에 만든 것입니다(04-01 다섯 · 04-02 여섯 · 04-03 넷). 러너 규약은 "생성기가 없는 SVG 는 만들지 않는다"인데 그 회차에 지키지 못했습니다. 스타일은 계약을 따랐으나 선언이 SSOT 라는 원칙이 깨져 재생성이 불가능합니다. **이번 신규 4장은 전부 생성기와 함께 넣었습니다.** 파이프라인은 복원해 뒀습니다 — 이 편 `_assets/` 에 `dd.py` 가 없어 생성기가 `import dd` 에서 죽는데(알려진 결손), 스킬 정본 `dd-primitives.py` 를 `dd.py` 로, `_gen/ddx.py` 를 함께 스크래치에 복사하면 돕니다. 기존 생성기를 그렇게 돌려 커밋본과 **바이트 동일**한 SVG 가 나오는 것으로 확인했습니다. 백필은 별도 회차 |
| 고아 SVG 해소 | **2026-09-03 `_assets/01-02.handshake-why-3-and-4.svg` 를 01-02 §3 에 편입했습니다. 지우지 않은 이유가 있습니다.** `git log` 로 보니 두 번 커밋된 정상 산출물이고 생성기(`gen-01-02.handshake-why-3-and-4.py`)도 있으며, 돌려 보니 커밋본과 바이트 동일했습니다. 그리고 **중복이 아니라 누락이었습니다** — 본문은 3-way 도식(L429)과 4-way 도식(L462)을 각각 갖고 있으면서 *왜 열 때는 셋이고 닫을 때는 넷인지*는 어디서도 답하지 않았습니다. 도식의 답은 "확인할 것은 양쪽 다 넷이고 갈린 것은 2·3번이 한 패킷에 실리느냐뿐"입니다. 그 자리에 `### 왜 열 때는 셋이고 닫을 때는 넷인가` 를 신설해 붙였습니다. **정확도 교정 1건** — 도식 범례가 "닫을 때는 못 합침"이었는데 앱이 이미 닫혀 있으면 FIN·ACK 가 한 패킷에 실려 3-way 로 끝나기도 하므로 "대개 못 합침"으로 고치고, 본문에도 4-way 가 일반형이고 3-way 가 축약이라는 것을 명시했습니다. 이로써 이 책의 고아 SVG 는 0장입니다 |
| 4장 Phase 3 묶음 4 | **2026-09-04 완료. 예측 5개 중 3개 정답, 1개 부분, 1개 반증.** **정답 셋** — (4-2) 셋에서 둘이 되면 `--probability 0.50000000000` 한 줄과 확률 없는 줄 하나가 남는다. (4-3) 떨어진 Pod 도 밖으로 나간다. (4-4) 재시작되지 않는다 — `0/1 Running · RESTARTS 0 · AGE 16h` 가 16시간짜리 증거였습니다. **부분** — (4-1) "확인 간격 때문"까지는 맞았으나 `failureThreshold: 2` 를 셈에 안 넣었습니다. 실측 **8초**, 범위는 5~10초. **반증** — (4-5) "낙하와 복귀가 같다"고 봤는데 복귀는 **1~4초**로 절반 이하입니다. `successThreshold` 가 **1** 이라 한 번 성공하면 뒤집힙니다. **새로 확정된 사실 둘** — (1) 명단과 규칙의 표현이 다릅니다. EndpointSlice 는 주소를 남기고 깃발만 `false` 로 내리는데 kube-proxy 는 그 주소의 `KUBE-SEP` 체인을 **아예 만들지 않습니다**. (2) `readinessProbe` 를 셋만 적었는데 스펙에는 다섯이 들어 있습니다 — `successThreshold`·`timeoutSeconds` 를 admission 이 채웁니다. CoreDNS toleration 때와 같은 자리입니다. **측정 실패 1건에서 배운 것** — 앞 회차의 `rm` 이 남아 있는 상태에서 다시 `rm` 을 쳐 `No such file` 이 났고, 루프가 **이미 내려가 있던 상태**를 `+1s` 로 읽었습니다. 시간을 재려면 출발선을 코드로 보장해야 합니다 |
| 4장 Phase 3 묶음 5 | **2026-09-05 완료. 예측 6개 중 4개 정답, 1개 결론만 정답, 1개 반증.** **반증 둘이 수확입니다.** (5-6) "나중 정책이 앞 것을 덮어쓸까" → **합쳐집니다.** `demo-db-lockdown` 을 지웠는데 세 결과가 한 글자도 안 바뀌었습니다. 그 정책의 허용 목록이 빈 집합이라 합집합에 기여가 없었던 것이고, **잠근 것은 그 정책이 아니라 "어느 정책에든 선택되었다"는 사실**이었습니다. (5-5 뒷부분) "`ports` 를 지우면 둘 다 막힌다" → **demo 는 열립니다.** 비어 있음이 층마다 반대입니다 — `ingress` 목록이 비면 허용 0개지만, 항목 안에서 `ports` 를 생략하면 "포트를 제한하지 않는다"입니다. **결론만 맞은 것** (5-3) — "Ingress 가 요청이면 Egress 는 응답"으로 보셨는데 둘은 **방향**이고 누가 연결을 시작했느냐로 갈립니다. 3단계에서 `egress` 가 빈 채로 `demo→db 200` 이 돌아온 것이 그 반증입니다. **관측으로 새로 잡은 것 둘** — (1) 같은 정책인데 실패 모양이 갈립니다. 들어오는 쪽은 `000 5.00s` 타임아웃이고 나가는 쪽은 `bad address` 즉시 실패입니다. DNS 질의부터 나가는 연결이라 이름 푸는 단계에서 죽습니다. (2) **출발선을 재 둔 덕에 오진을 면했습니다** — 정책 전인데 `db` 의 `nslookup` 이 이미 실패했고, 원인은 DNS 가 아니라 도구였습니다. busybox `nslookup` 은 `search` 줄을 안 쓰고 같은 컨테이너의 `wget` 은 씁니다 |
| 4장 Phase 3 묶음 6 | **2026-09-05 완료. 예측 4개 전부 정답.** 시간을 재는 대신 `tcpdump` 로 CoreDNS 로 나가는 질의를 직접 셌습니다. 시간은 캐시에 흔들리지만 질의 수에는 규칙이 그대로 드러납니다. **관측** — `demo-db` 5개 · `github.com` 8개 · `github.com.` 2개 · `demo-db.polylab.svc.cluster.local` 8개. **두 가지가 직관과 반대입니다** — (1) **긴 이름이 더 비쌉니다.** 완전한 클러스터 이름을 다 적으면 8인데 짧게 쓰면 5입니다. 점이 4개라 문턱 5를 못 넘어 꼬리표를 다 거칩니다. (2) **줄이는 것은 끝의 점 하나**입니다. 8에서 2로 떨어집니다. **홀수 5가 이 묶음의 수확입니다** — A 와 AAAA 가 독립으로 검색 경로를 돕니다. A 는 첫 꼬리표에서 끝났는데 AAAA 는 답이 없어 목록을 소진했습니다. 이 클러스터가 `SingleStack`·`["IPv4"]` 라 AAAA 레코드가 아예 없기 때문입니다. **노트 보강** — 04-03 §3 이 "각 시도가 A·AAAA 두 질의를 내므로 패킷은 두 배"라고만 적고 있었습니다. 두 줄기가 독립이라 한쪽이 먼저 멈추면 두 배가 아니게 된다는 것을 세 문단 추가 |
| 4장 Phase 3 묶음 7 | **2026-09-06 완료. 학습자가 7-2·7-3 을 "모름"이라 해 채점 전에 먼저 설명하고 7-1 만 물었습니다**(`feedback_explain_unknown_before_grading` 적용). 7-1 은 정답 — "연결이 이미 맺어졌으니 `000` 이 아니라 예외 코드"까지 추론했고 실측 `403 · 0.006초`. **한 화면에 서명 셋** — `200`(nginx 까지) · `403`(프록시까지) · `000 3.00s`(아무 데도). `cart` 가 `000` 인 것이 순서를 보여 줍니다. 출발지 판정이 먼저라 허용 목록에 없으면 커널 단에서 버려지고 HTTP 를 읽을 일이 없습니다. **L7 은 문을 더 여는 게 아니라 이미 열린 문을 좁히는 것**입니다. **학습자 질문에서 나온 실험** — "404 가 서버에서 없는 건지 프록시가 막은 건지 구분되나". 정책에 `/nope` 를 허용으로 더하니 같은 "없는 경로"인데 `/health` 403(본문 `Access denied`, 15바이트) · `/nope` 404(본문에 `nginx/1.31.5`)로 갈렸습니다. **제 판정 기준 1건 정정** — `Server` 헤더를 기준으로 쓰자고 했는데 못 씁니다. nginx 가 만든 404 에도 `server: envoy` 가 붙습니다. Envoy 가 지나가는 응답의 그 헤더를 덮어씁니다. **7-4(추가 출제)** — 넓은 표준 정책을 옆에 두니 L7 제약이 **무력화**됐습니다(403→404). 묶음 5 의 합집합이 계층을 넘어서도 적용됩니다. 그런데 응답에는 여전히 `server: envoy` 가 붙습니다 — **연결은 프록시를 계속 거치는데 거기서 아무것도 안 막는 상태**입니다. 보안은 없고 지연만 남습니다. **Envoy 의 출처도 확인** — 학습자가 "설치한 기억이 없다"고 해 조회했습니다. `cilium-envoy` 별도 DaemonSet 이 노드마다 하나씩(주소 `172.18.0.x` = hostNetwork), 생성 시각이 `cilium` DaemonSet 과 **초 단위로 동일**(2026-09-03T05:39:26Z). `enable-l7-proxy: true` · `external-envoy-proxy: true` 가 그 결과입니다. 묶음 2 의 "helm 한 줄이 무엇을 남기는지는 세어 봐야 안다"가 반복됐습니다 |
| 4장 Phase 3 묶음 8 · Phase 3 완료 | **2026-09-06. 학습자 요청으로 실행자가 직접 돌렸습니다**("이거 너가 진행하고 문서 기록한 다음 세션 마무리"). 예측 없이 관측만 기록했고 그 사실을 표에 명시했습니다. **레이아웃은 클러스터의 속성이 아니라 주변이 그 대역을 아느냐에 달린 상태입니다.** VM 라우팅 테이블에 `ip route add 10.244.2.0/24 via 172.18.0.4` 한 줄을 넣자 `000 4.00s` 가 `200 0.03s` 로 바뀌었습니다. Pod·노드·Cilium 설정은 아무것도 안 건드렸습니다. **한 줄은 한 노드만 엽니다** — 같은 노드의 두 번째 Pod 는 함께 열렸는데 다른 노드의 Pod(`10.244.1.35`)는 그대로 `000` 이었습니다. 04-01 §2 가 Flat 의 값으로 적은 "네트워크 장비의 협조"가 이 늘어나는 줄이고, 실제로는 BGP 로 광고합니다. **결론은 방향입니다** — 들어오는 쪽은 Flat 이 됐는데 나가는 쪽은 Island 그대로였습니다. Pod `10.244.2.153` 이 VM 을 부르자 VM 로그에 `172.18.0.4` 가 찍혔고, 원인은 그 노드의 `CILIUM_POST_nat` masquerade 한 줄입니다. **이 세션이 세 번째로 만난 방향 축입니다** — readiness(들어오는 길만 끊김) · NetworkPolicy(Ingress·Egress) · 레이아웃. **원복까지 실행했습니다.** 경로를 빼고 다시 두드려 `000 4.00s` 가 재현되는 것을 확인했습니다. VM 은 원래 상태입니다 |
| 4장 Phase 3 총평 | **8묶음 전부 완료 (2026-09-03 ~ 09-06).** 학습자가 예측한 것은 묶음 2~7 의 26문항이고 그중 **반증·부분이 8건**입니다. 반증이 이 실습의 산출물입니다 — (1) readiness 복귀가 낙하와 같은 시간일 것(실제 절반 이하, `successThreshold:1`) (2) 나중 정책이 앞 정책을 덮어쓸 것(합집합) (3) `ports` 를 지우면 전부 막힐 것(열림) (4) Ingress·Egress 가 요청·응답일 것(방향) (5) ClusterIP ping 실패가 ICMP 때문일 것(규칙 조건이 `-p tcp` 라 진입 자체가 없음) (6) 마스터는 규칙을 몰라도 될 것(세 노드 모두 11줄) 외 2건. **노트 결손 5건을 실습이 찾아냈습니다** — 04-01 §4 진단 표(CNI 부재는 `Pending` 인데 `ContainerCreating` 이라 **틀리게** 적혀 있었음) · 04-02 §1(conflist 와 바이너리를 잇는 `type`, 사전순 첫 파일만 쓴다) · 04-03 §1(생략된 필드의 뜻) · 04-03 §3(A·AAAA 가 독립이라 "두 배"가 어림값) · 02-02 §3(SVC·SEP 를 왜 나눴는가). **도식 5장 신설** — `02-02.hook-slots-and-cuts` · `02-02.chain-comes-alive` · `02-02.why-two-chains` · `04-04.lab-state-progression` · `01-02.handshake-why-3-and-4` 편입. 다음은 **4장 전체 Phase 4** 입니다 |
| 다음 레슨 후보 | **3장 Phase 1 재개 (2026-08-31 미착수).** 이번 세션은 문서 정리·검증으로 다 갔고 Phase 1 은 질문 1(베어메탈→하이퍼바이저→컨테이너의 포트 충돌을 스택 수로 설명)을 낸 지점에서 멈췄습니다. 답을 받지 못했으므로 `last_verified` 는 갱신하지 않고 `blocked_count` 도 비워 둡니다 — 원문 대조일로 대신 채우지 않습니다. 재개 시 그 질문부터 이어가고, 03-01 §1~§5 → 03-02 §1~§5 순으로 예측→채점을 돌린 뒤 Phase 2, 그다음 3장 전체 Phase 3 로 갑니다. **Phase 3 진입 전에 정할 것 둘** — ① 03-04 의 묶음 다섯은 잠정안이라 3장을 다 읽은 뒤 흐름을 다시 정합니다(학습자 요청). ② 상대 컨테이너 이미지: 책의 `jessie-dnsutils:1.3` 에 `curl`·`ping` 이 없어 그대로는 §4 의 컨테이너 쪽 세 방향을 못 돌립니다. `nicolaka/netshoot` 이 후보이며 실행 직전에 승인받습니다. 난이도는 02-03 통과 시점의 정상 수준을 유지합니다. **2장에서 넘어온 미완 요청은 2026-08-31 에 닫았습니다** — `mangle → nat → filter` 변환 흐름 도식을 `02-02` §1 에 `### 값을 바꾸는 테이블이 먼저인 이유` 와 함께 신설했습니다. `_review/2026-08-26/` 회차 1(00-03·01-03)과 연체 복습 2편(kubernetes-in-action 08-01·08-02)도 미진행입니다 |
| 최근 검증 결과 | **2026-08-27 02-01 Phase 1 통과 (연장) — 기존 자료 예측→채점, 6개 절 중 5개(§1 소켓·§2 loopback·§4 Netfilter 5훅·§5 Conntrack·§6 라우팅) 완료, §3(veth·브리지)은 예측만.** 막힘 총 8건 — ①L2를 "라우팅"이라 호칭(용어) ②③방화벽 자리를 ①PREROUTING으로(자리 범위, SSH는 ②INPUT·포워딩은 ③FORWARD가 맞음) ④"중계기 커널에 방화벽을 심을 수 있나" 전제 오해(다섯 지점이 한 기계 안임) ⑤⑥응답 패킷을 낱개로 판정(**낱개 vs 연결이 이 편의 뿌리** — Conntrack은 분류만, 통과는 규칙이 정함) ⑦veth(랜선)를 건너뛰고 브리지·게이트웨이로 직행 ⑧loopback 바인딩=외부불가 가정. **자력 도달이 값졌다** — end-to-end vs hop-by-hop 유도, "구현이 계층을 가로지른다" 통찰, 5-튜플, Conntrack 수명·고갈 증상, "뒤집힘만으로는 방향을 모른다"(문서에도 없는 반문), netns 필요성. **OrbStack VM 2대(ubuntu 192.168.139.208 / ubuntu2 .238)로 CVE-2020-8558 실측 재현·원복** — route_localnet=1 + PREROUTING DNAT로 루프백 전용 DB가 외부에서 뚫림을 확인. 이전 검증은 2026-08-26 01-02 Phase 4. 세부값 계열(01-02 취약점) 이번엔 재발 0. |
| 이전 검증 (01-02) | **2026-08-26 01-02 Phase 4 통과 — 5 축 + 잔여 2 문항, 막힘 6 건 전수 확인.** 축별 결론 — 왜(TCP 가 막는 세 증상을 재전송·순서 재조립·흐름 제어에 짝지음) · 원리(4-way `FIN→ACK→FIN→ACK`, TIME-WAIT 는 **역할이 아니라 순서가 정한다**) · 대안(UDP, 되묻기 0) · 함정(**버그 신호는 CLOSE_WAIT** — 타이머가 없어 `close` 전까지 영구, TIME_WAIT 는 2MSL 뒤 자동 소멸) · 트레이드오프(HTTPS 첫 요청 = TCP 1 왕복 + TLS 1.2 2 왕복 = **3 왕복 450ms**). 잔여 2 문항 — **L4 는 빠짐 없음을, L7 은 경계를 맡는다**(`Content-Length` 는 누락 탐지가 아니라 어디서 끊어 읽을지. TCP 는 "안 오는 것"과 "끝난 것"을 구별해 주지 않는다) · **시퀀스는 바이트를 센다**(경로 중간에서 쪼개질 수 있어 패킷 경계는 신뢰 불가, 다음 seq = 이전 seq + 보낸 바이트 수). 부수 수확 — `FIN-ACK` 는 플래그 두 개를 한 패킷에 켠 것이라 **4-way 가 조건부로 3 패킷** · TCP 연결 식별은 4-튜플(IP·포트, MAC 아님) · MSL 2 배 근거(ACK 유실 1MSL + FIN 재전송 1MSL) · ISN 이 임의값인 것은 보안. 이전 회차는 2026-08-24 01-03 Phase 1 통과 |
| 전편 사실검증 (02~06장) | **2026-08-27 정독 노트 14편 전수 적대적 재검증 — 발견 44건, 교정 39건.** 편당 발견 수: 02-01 6 · 02-02 3 · 02-03 3 · 03-01 5 · 03-02 3 · 03-03 6 · 04-01 4 · 04-02 13 · 04-03 4 · 05-01 3 · 05-02 3 · 05-03 4 · 06-01 3 · 06-02 1. 미교정 5건은 서술 구조 변경이 필요한 것들(02-01 conntrack 튜플 L3·zone, 02-02 mermaid `NAT (SNAT)` 라벨 자체, 02-03 진단 사다리에서 ping 의 위치, 05-02 sessionAffinity·allocateLoadBalancerNodePorts 누락, 05-01 volumeClaimTemplates 계열 누락). **14편 모두 `status: final` 이었고 02-01 은 앞서 적대적 검증을 3회 거쳤는데도 신규 사실오류가 나왔다 — `final` 이 사실 정확성의 보증이 아니라는 뜻이다.** |
| 검증에서 드러난 실패 양식 | **되풀이되는 것은 "고친 판단이 한 자리에만 반영되는 것"이다.** 12편에서 같은 모양이 나왔다 — 심화 학습·갱신 인용에서 사실을 고쳐 놓고 **핵심 요약·결정 치트시트·면접 질문·정답·체크리스트·frontmatter 요약행**은 옛 값을 유지한다. 최악은 04-02(kube-proxy "4모드"가 네 자리에 남아 있었고 심화 학습만 5모드), 02-03("ping 항상 실패"가 네 자리), 03-01(namespace 6종이 세 자리). **다음 편 검증·수정 시 본문 한 곳을 고치면 그 6자리를 같은 묶음으로 훑는다.** 두 번째 양식은 **책 서술을 충실히 옮긴 것이 곧 오류가 되는 경우** — 04-01 "Pod 는 MAC 이 없다", 04-02 "모드 전부 iptables 의존", 03-02 CNM sandbox·endpoint 오역, 03-03 "사설 대역이라 경로 없음"이 그렇다. 원문 충실성과 사실 정확성이 갈릴 때는 정오 인용으로 갈라 적는다. |
| 02장 사실검증 | **2026-08-27 02-01·02-02·02-03 적대적 재검증 — 14건 발견, A(사실오류 6)+B(내부 불일치 3) 총 10건 교정 완료, C(과잉단정 5)는 2건만 반영.** 세 편 모두 `status: final` 이고 02-01 은 앞서 적대적 검증을 3회 거쳤는데도 신규 사실오류가 나왔다. **되풀이되는 실패 양식은 "고친 판단이 한 자리에만 반영되는 것"이다** — 02-03 의 "ping 항상 실패"는 §2 갱신 인용에서 이미 "판단하지 말라"로 고쳐 놓고 핵심 요약·치트시트·면접 Q3·정답 3 네 곳이 옛 표현으로 남아 있었다. 02-01 도 정답 3 이 raw `NOTRACK` 을 요구하는데 본문에 raw 설명과 UNTRACKED 상태가 없었다. **다음 편 검증 시 요약·치트시트·면접·정답 4개 자리를 본문 수정과 한 묶음으로 훑는다.** 미반영 C 3건 — 02-01 conntrack 튜플에 L3 프로토콜·zone 누락, 02-02 mermaid `NAT (SNAT)` 라벨(단서만 추가), 02-03 ping 이 L3 게이트로 쓰인 진단 사다리 구조 |
| 도식 생성기 부채 | **2026-08-28 실측 — 02-01 의 SVG 중 2장이 생성기 없는 손 SVG 였다.** 러너 규약("생성기가 없는 SVG 는 만들지 않는다")상 타입 선택 절차를 통째로 건너뛴 것이고, `conntrack-states` 가 혼자 표 모양이었던 원인이 이것이다. `conntrack-states` 는 이번에 생성기와 함께 재제작해 해소. **남은 것 둘** — `02-01.socket-syscall.svg`·`02-01.socket-accept-loop.svg` 는 아직 생성기가 없다(둘 다 미커밋 손 SVG). 관련해 `gen-02-01.socket-syscall-order.py` 는 삭제했다 — 출력 SVG 가 이미 git rm 된 고아인 데다, 내용이 교정 전 값("넘치면 새 연결 거절")이라 돌리면 교정된 사실이 되살아나는 상태였다. 책 전체로는 생성기 26개가 타입 선언이 없다(01-01·03·04·05·06 편, 이번 범위 밖) **2026-08-31 덧붙임 — `_assets/_gen/` 에 `dd.py` 가 없어 이 폴더에서는 생성기를 그대로 못 돌린다.** `ddx.py` 만 있고 `dd.py` 가 빠져 `ModuleNotFoundError` 가 난다. 정본은 `writing-method/assets/scripts/dd-primitives.py` 이고, 평면 스크래치 디렉터리에 그것을 `dd.py` 로 복사한 뒤 `ddx.py` 와 생성기를 같이 두고 돌린 다음 SVG 만 되가져오는 것이 지금의 우회로다. `02-05` 의 도식 3장도 그 경로로 만들었다. |
| 복습카드 공백 | **Phase 4 통과 편에 복습카드가 없다 — 대상이 뒤집혀 있다.** `_review/` 4장 중 이 책은 00-03·01-03 둘(둘 다 2026-08-26 회차 1 미진행)이고, 정작 Phase 4 를 통과한 01-01·01-02 는 카드가 없다. 카드를 "Phase 1 막힘 표적"으로 뽑았기 때문인데, 간격 반복이 필요한 쪽은 8일 만에 세부 값 계열 3건이 재발한 01-02 다. **01-02 카드 신설이 복습 우선순위 1번.** **2026-08-31 해소 — `01-02` 카드를 `_review/2026-08-31/` 에 신설했다.** 이 행이 지목한 "복습 우선순위 1번"이 그것이었다. 문항은 Phase 4 에서 *재발한 3건* 에 겨눴다 — 시퀀스가 바이트를 센다 · TIME_WAIT 은 역할이 아니라 순서가 정한다 · FIN-ACK 합쳐짐으로 4-way 가 3패킷. `01-01` 도 같은 날 신설해 **이 행이 지목한 결손 둘을 다 닫았다** — 문항은 2026-08-15 Phase 4 에서 드러난 설명 빈틈 셋에 겨눴다(라우터가 Internet 계층까지만 구현하는 이유 · TCP/IP 계층은 번호가 아닌 이름 · **NCP 몰락과 OSI 패배를 한 사건으로 묶어 "OSI 는 안 쪼갠 쪽"으로 오해**). |
| 보강 산출물 | 전체 요약 도식 13장 신설(`*.chapter-overview.svg`) · H2 요약 인용 전 절 삽입 · 원문자 전수 제거 · 기존 SVG 15장 가독성 정합화(대비 경고 100→8) · 2026-08-15 `01-01.encapsulation-roundtrip` 신설 + 01-01 §3·§4 빈틈 3곳 · 2026-08-16~17 01-02 archify 도식 6장 + `tls-handshake.sequence` 신설, **mermaid 3 → 0**, §3·§5 중복 정리, §4 캡처 줄별 주석, 비유 절 2개 · **2026-08-24 00-03 Phase 1 막힘 7건 전수 보강** — `00-03-unicast-vs-broadcast.svg` 신설, `###` 5개, 배치 위반 5→0 · **2026-08-24 세션 후속 3건** — 00-03 §5 를 유니캐스트/브로드캐스트 + ARP 두 하위절로 분리, `00-03-arp-message-fields.svg`(빈 칸→채워짐 대조로 입력=IP·출력=MAC 명시), `01-03.underlay-overlay-knowledge.svg` + 01-03 §4 `#### 하부 네트워크와 오버레이`(BGP/VXLAN 판단축은 "클라우드냐"가 아니라 "경로 주입 수단이 있는가") · **2026-08-26 01-02 Phase 4 후속 3건** — (a) **사실 정정** §3 TIME-WAIT 행 "아무도 안 기다립니다"→"올지 안 올지 모르는 상대의 FIN 재전송"(같은 절 비유 문단과 어긋나 있었고 왜 2MSL 인지 설명 불가였다, 사용자 승인 후 수정) (b) `01-02.time-wait-2msl.svg` + `#### 2MSL이 왜 하필 두 배인가` 신설 — 두 구간 타임라인 · RST 갈래 · TTL 연결 (c) `01-02.tcp-header-layout.svg` 신설 — 32비트 격자 배치로 Data offset 의 필요성과 플래그 9비트가 SYN-ACK·FIN-ACK 를 만드는 근거. 전 건 4축 센서 통과 · **2026-08-28 §4·§5 도식 보강 6건** — (a) `02-01.hook-path-decision.svg` 신설(`type-flowchart`): 두 질문 → 네 갈래, 훅 5개 전부 등장. `LOCAL_` 두 훅만 주소 한쪽이 고정된다는 축을 색으로, 로컬→로컬의 재진입을 focal 로. (b) **사실 정정** `netfilter-hooks-flow` 의 목적지가 외부 출발지 + Service ClusterIP 라는 성립 불가 조합이었다 — NodePort 30080 으로 교체하고 장면 한 줄을 도식에 명시, 본문에 정정 인용. (c) **사실 정정** §4 가 Martian 검사와 rp_filter 를 한 문단에 이어 붙여 하나로 읽혔다 — 표로 분리하고 둘 다 Netfilter 훅이 아니라 라우팅 코드에서 발화한다는 점, `route_localnet` 이 목적지 쪽 짝이라는 점 추가. (d) 반환값 5종에 용례 + verdict(커널) vs 타깃(iptables) 층 구분 + `NF_STOP` Deprecated + `NF_DROP_ERR`/`NF_QUEUE_NR` 상위 비트 — 커널 헤더 실측(`/usr/include/linux/netfilter.h`, 7.0.14). DROP vs REJECT 도 여기서 갈림. (e) `conntrack-states` 를 표 → 상태 기계(`type-state`) 재제작: 본문이 "주 경로·두 단계·그보다 뒤"로 순서를 말하는데 도식이 순서 없는 표였다. 곁가지 셋은 전이로 잇지 않고 따로 모음. (f) `02-01.conntrack-nat-tuples.svg` 신설(`type-sequence`): §5 본문 절반이 NAT 인데 도식에 NAT 가 없었다 — '뒤집기라면' 줄을 끼워 세 줄 대조, DNAT 가 응답 src 를·MASQUERADE 가 응답 dst 를 정함을 보임. §5 에 `### 풀려는 문제`·`### NAT가 걸리면…` 신설(이 문서에서 유일하게 `###` 이 없던 절). 벽 단락 3곳·배치 위반 1건 교정 후 0곳 · **2026-08-28 02-02 학습자 피드백 보강 5건** — (a) §1 체인↔훅 다섯 쌍 명시 + `chain-then-table` 레인 머리에 `NF_IP_PRE_ROUTING`·`NF_IP_LOCAL_IN` 병기. (b) `rule-token-gates` 재작성 — 원본 규칙 한 줄을 도식에 싣고 번호 칩으로 토막과 칸을 연결, dst 전/후 대조 추가. (c) `subchain-return.svg` 신설(`type-sequence`) + `### 종결은 어디까지인가`·`### 규칙을 운용할 때 걸리는 것들` 신설, `JUMP`·`-g/--goto` 문단 제거(사용자 판단). (d) `nat-table-traverse` 재작성 (`type-process`→`type-swimlane`, 훅 2레인 + `-j` 점프 계단 + 라우팅 판단 handoff)·`conntrack-return-path` 재작성 (`type-process`→`type-sequence`, 요청 1~3 + 응답 4~6 왕복 + conntrack 항목 띠). (e) §3 `### MASQUERADE` 신설, §5 `### verifier` 신설 + 커널 문서 각주 + 면접 Q6·정답 6·체크리스트 2줄. 도식 5장 전부 Chrome 렌더 육안 확인, dd-lint error 0 · dd-type-check 9/9 · 벽 단락 0곳 · **2026-08-28 02-03 명령 현대화** — 심화 학습 표에만 있던 net-tools→iproute2 전환을 본문·도식·회상 요소로 밀어 넣었다. §4 를 `netstat` → `ss` 로 옮기고 제목·H2 요약·핵심 요약·학습 목표·수사 순서 캡션·비유·결정 치트시트·정답 2·정답 4·빈칸 정답·체크리스트까지 **한 묶음으로 훑었다**(이 표 아래 '검증에서 드러난 실패 양식' 대응). 출력은 OrbStack Ubuntu 26.04(iproute2 6.19.0)에서 리스너 넷을 띄워 실측하고 각주로 환경을 남겼다. **실측이 사실 하나를 정정했다** — `ss` 는 듀얼 스택 와일드카드를 `*`, IPv6 전용을 `[::]` 로 갈라 적는데 `netstat` 은 둘 다 `:::` 로 찍는다. 앞 판의 "와일드카드는 IPv4 에서 `0.0.0.0`, IPv6 에서 `[::]`" 이분법은 형태를 하나 빠뜨리고 있었고 네 형태로 고쳤다. §5 에 `nc -zv -w 2` 갱신 인용 신설(telnet 과 대체가 아니라 용도가 갈림). 도식 둘 — `triage-ladder` 관문 3 명령을 `ss -ltnp`·읽을 자리를 `Local Address:Port` 열로, `tool-layer-map` 은 `netstat` 열을 빼고 `dig` 를 넣고(관문 1 이 그림에 아예 없었다) `arp`→`ip neigh`, 도구 일련번호를 사다리의 관문 번호로 통일해 **두 도식이 같은 번호 축**을 쓰게 했다. **부수 발견 — 02-03 frontmatter YAML 이 깨져 있었다.** 직전 회차의 method 문단이 `source:` 아래에 들어가 파싱이 실패하는 상태였고 이번에 수리했다. dd-lint error 0 · dd-type-check 2/2 · 벽 단락 0곳 |
| 복습 회차 | **회차 1 준비 완료 (2026-08-26) — `_review/2026-08-26/` 에 00-03·01-03 두 편.** 아직 미진행(quality 미입력)이며, 각 5문항(정의·동기·메커니즘·적용·함정)은 이 편들의 Phase 1 막힘을 표적으로 뽑았다 — 00-03 은 IP=배달 주체 뿌리(Q1·Q3)와 허브 무판단성(Q5), 01-03 은 세부 값 계열(Q3 경로 선택 기준·Q5 underlay 통제권). learning-session 진행 4 편 — 01-01(Phase 1·4), 01-02(Phase 1 + Phase 4 통과, 막힘 6 전수 확인), 00-03(Phase 1), 01-03(Phase 1) **2026-08-31 덧붙임 — 2장 다섯 편을 복습 대기 큐에 등록했다.** `write/_review/README.md` 의 회차 통계 표에 행을 추가하고, 그 아래 §복습 대기 큐에 편마다 *회차 1 에서 물을 것* 을 적어 뒀다. 회차를 시작할 때 그 목록에서 꺼내 `review-template.md` 로 파일을 만든다. `_review/2026-08-31/` 에 다섯 파일을 만들었다(`round=1`·`quality: null`). 정답은 `<details>` 로 접혀 있어 미리 읽어도 회상 효과가 깎이지 않는다. `learning-status.mjs` 가 `quality 미입력 — 회차 미완` 으로 잡는 것을 확인했다(`today_folder.rounds = 5`). 회차를 돌면 각 파일의 `next_round_date` 를 채워야 다음 회차가 잡힌다. 미실시로 남은 2026-08-26 의 00-03·01-03 도 같이 처리 대상이다. |
| 02-02 복습 (즉석 13문항) | **2026-09-06 즉석 모드로 13문항 진행, 채점된 9문항 평균 2.7.** 회차 파일은 만들지 않았다(사용자가 승격 대신 기록만 요청). **축이 뚜렷하게 갈렸다** — "왜"를 묻는 질문에 강하고(Q2 갱신이 느린 이유 4, Q7 DROP vs REJECT 4), **"무엇이 없는가"에서 무너진다**(Q1′ filter 가 PREROUTING 에 없다 2, Q5 ip6tables 미적용 0). 표에서 O 가 찍힌 칸은 읽히는데 빈칸은 안 읽히는 패턴이고, 이번 회차의 진짜 약점이다. 그 밖 3점 넷 — Q3 확률 fan-out(후보 감소는 짚었으나 곱해서 1/n 이 되는 계산과 마지막 규칙에 확률이 없는 이유 누락), Q6 기본 정책 순서(메커니즘은 맞고 '내 SSH 가 끊긴다'는 결론과 허용 규칙 선행 순서 누락), Q8 규칙 한 줄 읽기(`-j DNAT` 는 맞혔으나 `-m` 을 코멘트로 일반화), Q10 스케줄러(예시임은 맞고 화이트리스트 부재 메커니즘 누락). Q9 는 문서를 직접 찾아본 뒤라 점수 없이, Q11·Q12(eBPF)는 학습자가 "생소하고 이 편에서 깊게 다루기 애매하다"고 해 전체 리뷰로 대체. Q13 미답. **다음 회차 대상은 부재 축 둘(Q1′·Q5) + Q13 + 3점 넷.** 평균으로는 +3일 구간이지만 부재 축만 보면 +1일 즉시 재학습 구간이다. 다만 채점은 첫 인출만 잰 값이고, 대화에서는 테이블 분리 이유·두 층 순서·훅 우선순위·NFQUEUE·eBPF 전체까지 끌고 갔다 — 읽어서 아는 것과 인출은 별개 근육이라 재시도에 의미가 있다. |
| 02-02·02-01 보강 (복습 중 파생) | **2026-09-06 복습 세션에서 도식 3건·본문 2건이 나왔다.** (1) **§1 도식 5장 → 4장.** 학습자가 중복을 지적해 전수 렌더 대조했더니 `chain-then-table`(swimlane)과 이 세션에서 만든 `two-layer-order-trace`(swimlane)가 같은 문법·같은 메시지였다. 후자를 전자에 접었다 — 레인을 넷(PREROUTING·INPUT·FORWARD·POSTROUTING)으로 늘리고 칸을 열로 정렬해 **NAT 열이 위아래로 두 번 켜지는 것**이 보이게 했다. INPUT 레인을 남긴 것이 핵심 제약이다. 원서 단서 상자가 "위 도식의 INPUT 레인에 있는 NAT 칸"을 지목하고 있어 그 레인을 없애면 지시어가 고아가 된다. (2) **`three-levels-nested`(type-nested) 신설** — 체인 ⊃ 테이블 ⊃ 규칙 3계층. 학습자 질문 "테이블·체인·규칙이 어떻게 연계되는가"에서 나왔고, 유일하게 **규칙 층**을 담는 도식이다. 처음엔 순서 화살표를 테이블 다섯에 다 그었다가 INPUT 에 없는 Raw·Security 가 순서에 낀 것처럼 읽혀 셋 사이로 줄였다. (3) **`ebpf-verifier-gate`(type-swimlane) 신설** — 학습자가 eBPF 를 모른다고 해 §5 에 넣었다. 레인이 유저스페이스·커널이고 **경계를 넘는 곳이 둘뿐**(로드 시 `bpf()`, 이후 맵)이라는 것이 논지다. (4) **§3 gRPC 쏠림 해법을 04-02 §4 로 위임.** 두 편의 등장 자리를 세니 02-02 3곳 · 04-02 5곳이었고 도식(`connection-bias`)과 3모드 비교표가 04-02 에 있다. 02-02 에는 "DNAT 결과가 conntrack 에 박혀 연결 수명 동안 유지된다"는 메커니즘까지만 남겼다. (5) **02-01 §4 에 두 절 신설** — 「한 훅에 여럿이 등록된다」와 「iptables 말고 이 프레임워크를 쓰는 것들」. 학습자가 준 블로그는 얇아(구성요소·우선순위·흐름·conntrack 위치 전부 부재) **커널 헤더를 1차 자료로 썼다** — OrbStack 컨테이너에서 `enum nf_ip_hook_priorities` 를 직접 읽고 netfilter 위키 값과 대조했다. **이 표가 Raw→Mangle→NAT→Filter 순서를 설명한다** — iptables 가 정한 규칙이 아니라 -300·-150·-100·0 이라는 우선순위 숫자의 결과다. raw 가 conntrack 이전인 것도 -300 대 -200 의 차이다. `iptables -V` 실측(alpine 1.8.13 · ubuntu 24.04 1.8.10, 둘 다 `nf_tables`)과 `iptables -A PREROUTING`(filter 에 그 체인이 없어 `No chain/target/match by that name`)도 컨테이너에서 확인했다. |



## 번호 체계와 작성 규약

> 파일명·번호·작성 규약을 한곳에 모읍니다. 새 편을 더하기 전에 여기를 먼저 봅니다.

파일명은 `NN-MM.제목.md` 형식입니다. `NN`은 책의 장 번호, `MM`은 그 장을 여러 편으로 나눌 때의 편 순번입니다. 모든 장이 45~64쪽으로 길기 때문에 장마다 PDF 목차를 보고 분할안을 정한 뒤 작성합니다. 정밀 도식은 `_assets/`에 SVG로 두고, 흐름·관계는 Mermaid로 본문에 직접 그립니다.

작성 규약은 writing 스킬의 책 요약 템플릿과 정독 노트 세션 규약을 따릅니다. 다만 한 가지를 이 책에 맞게 바꿨습니다. "Spring 앱 개발 관점" 섹션은 매 장 필수가 아니라 **Spring 접점이 자연스러운 장에만 선별 적용**합니다(합의 2026-07-16).

이 책은 TCP/IP·Linux·CNI 같은 인프라 계층이 본체입니다. 순수 네트워킹 장에 Spring을 붙이면 연결이 억지스러워지기 때문입니다.



## 관련 문서

- [『Kubernetes in Action, 2판』 정독본](../kubernetes-in-action/README.md) — 같은 `book/` 영역, 오브젝트 중심 관점의 짝
- [08_cloud/kubernetes — 개념 노트](../../kubernetes/README.md) — 개념 중복을 링크로 위임하는 대상
- `08_cloud/service-mesh` <!-- 링크 끊김(2026-08): ../../service-mesh/README.md --> — 이 책 이후 계층(L7 프록시·mTLS)을 다루는 이웃 카테고리
