---
title: Linux 네트워크 진단 도구 — 계층 순서대로 수사하기
tags: [linux, troubleshooting, ping, traceroute, dig, netstat, nmap, netcat, openssl, curl, networking-and-kubernetes]
status: final
review:
  status: approved
  reviewed_at: 2026-07-19
  method: >
    2026-08-19 시각화 검증 — 도식 3 장을 JSON 원본과 본문으로 대조했다. 셋 다 정적 QA 31개는
    통과 중이었고, 게이트가 못 보는 두 가지가 나왔다.
    `tool-layer-map` 은 **논지를 글로만 말하고 있었다.** 제목이 "수사 순서는 계층 순서가 아니다"
    이고 본문도 "응용 → 네트워크로 내려갔다가 → 전송으로 → 다시 응용으로 올라온다" 고 적는데,
    정작 계층 노드 넷이 전부 `y=320` 한 줄에 평평하게 놓여 오르내림이 그림에 없었다. 대신
    `계층으로는 위`·`계층으로는 아래`·`다시 위로` 같은 tag 글자로 때워 두었다. 계층 노드의 y 를
    실제 계층 높이로 어긋나게 놓아(L7 280 · L4 400 · L3 520) 도구에서 내려오는 세로선의 길이가
    달라지게 했다 — `ping` 선이 가장 길고 `dig`·`cURL` 선이 짧다. 라벨도 `L7 응용`·`L3 네트워크`·
    `L4 전송` 으로 계층 번호를 앞세웠고, 마지막이 첫 번째와 같은 `L7 응용` 인 것은 의도다.
    노드는 `118×86` 에서 `140×100` 으로 키웠다(`네트워크 계층` 라벨이 빡빡했다). 배치가 세로로
    퍼져 `horizontal` 에서 `balanced` 로 분류가 바뀌었고 점유율 0.94/0.93 으로 통과했다.
    본문 설명은 오르내림을 나열하던 문장을 걷어내고 *왜* 그 순서인지만 남겼다 — 그림이 지는 것을
    글이 되풀이하지 않게 했다.
    `chapter-overview` 는 **`triage-ladder` 와 같은 사다리를 두 번 그리고 있어 폐기했다.**
    둘이 `## 수사 순서 한눈에` 한 절에 연달아 있었고, 뒤엣것이 관문·치는 명령·읽을 자리까지 담은
    상위집합이라 앞엣것이 더할 게 없었다. 게다가 dataflow 평면 4패널이라 세로가 비고 노드에 tag 가
    없어 두 줄뿐이었다. 참조는 본문 이미지 1줄뿐이었고(전수 확인), `## 학습 목표` 에 도식이 없어
    1부 규칙은 `triage-ladder` 가 충족한다. 01-03 ARP 중복·01-04 ICMP 표 중복을 걷어낸 것과 같은
    판단이다. §1 보안 전제는 사다리에 안 들어가는 별개 축이라 도식화하지 않고 산문으로 두었다.
    `triage-ladder` 는 관문 ↔ 명령·읽을 자리를 두 boundary 로 짝지은 구조로 `02-01.socket-syscall-order`
    와 같은 형이라 손대지 않았다. 도식 3 장 → 2 장. 정적 QA 31개 통과, PNG 눈 검사 완료.
    §18 적대적 사실검증(별개 에이전트 + 책 PDF 2챕터 직접 대조 + dig/traceroute man page) — ping/traceroute/dig/netstat/nmap/telnet/openssl/cURL 명령·플래그·계층 전부 CONFIRMED, 사실 오류 0.
    노트의 "원문 정오"(책이 dig `-b`를 대상주소로 설명, 실제는 출발지 IP) 지적이 man page 기준 타당함을 확인.
    보강: A-2′ 정답 분리, 벽 단락 정리, `### 0.`→비번호 헤딩 승격, status final.
    2026-08-08 보강 러너 1·2축 — 원문자(U+2460~) 8곳 제거해 `(1)` 표기로 통일, `##` 앞 빈 줄 3개 규칙 위반 13건 교정,
    모든 절에 H2 요약 인용 8개 신설(1부 규칙 1). 전체 요약 도식은 기존 `## 수사 순서 한눈에` Mermaid 흐름도가
    같은 역할을 하므로 중복 신설하지 않음. 사실 변경 없음. 4축 센서 전수 재통과.
    2026-08-15 적대적 재검증(별개 세션 + 원서 PDF 1839~2613 대조) — 14건 발견. 앞선 "사실 오류 0" 기록이 성립하지 않음.
    검증 통과 확인: 원서의 진단 도구 9종을 노트가 전부 다룸(wget 만 누락, 이번에 보강).
    "Service 는 ping 이 안 된다"는 서술은 원서 근거 있음(TCP·UDP 만 지원, telnet·cURL 로 대체, Pod 는 예외까지 전재).
    setuid·보안 경고 내용도 원서와 일치.
    사실 정정 2: (1) 리스닝 주소를 "0.0.0.0 인지 127.0.0.1 인지"로 이분했으나 자기 예시가 `[::]:3000` 이라 어긋남
    — 와일드카드 대 루프백으로 일반화하고 IPv6 표기를 함께 적음. (2) "계층 순서"라는 명명이 실제 순서(L7→L3→L4→L7)와 불일치.
    최신화 2: Service 지원 프로토콜에 SCTP 추가, IPVS 모드에서는 ClusterIP 가 더미 인터페이스에 바인딩돼 ping 이 응답할 수 있음을 인용으로 보강.
    현대 배포판의 ping 은 setuid 대신 CAP_NET_RAW·ping_group_range 를 쓴다는 갱신 인용 추가.
    프레임 출처 명시(사용자 결정) — 수사 순서는 원서 주장이 아니라 편집자가 엮은 것이고, 원서는 오히려 도구 기능이 겹쳐
    선택이 선호·친숙도·가용성의 문제라고 적는다는 사실을 본문 인용으로 박음. 제목·파일명은 유지.
    시각화 신설 3 장 — chapter-overview(형제 편과 형식 통일, 이 편에는 전체 지도가 아예 없었음),
    triage-ladder(mermaid 의 arch 상세판), tool-layer-map(수사 순서가 계층 순서가 아님을 그림으로 드러냄). mermaid 는 유지.
    전부 showcase QA 통과.
    형식: `$` 프롬프트 1건 제거하고 명령·출력 블록 분리, 150자 초과 문장 3건 분리.
    2026-08-16 도식 축척 3단 체계 도입 — 지금까지 지도(dataflow)와 나머지 두 단뿐이라 축척이 다른 도식이
    같은 모양으로 그려져 있었다. L1 지도(dataflow 129x100, 개념어) / L2 관통(architecture 118x86, 경계 없음, 절 여럿을 잇는 흐름)
    / L3 해부(architecture 250~270x100, 점선 경계, 실측값)로 나누고 모양만으로 배율이 읽히게 함.
    L3 승격 1장 + mermaid 1개 삭제 — triage-ladder 를 관문별 실제 명령과 읽을 자리(dig 의 status·ANSWER,
    netstat 의 Local Address 열 등)까지 담게 재설계. 같은 내용을 그리던 `## 수사 순서 한눈에` 의 mermaid 결정 트리는
    arch 가 더 자세해졌으므로 삭제(사용자 방침: 동일하면 자세한 쪽만 남긴다).
    앞선 기록의 "전체 요약 도식은 mermaid 가 대신하므로 신설하지 않음"은 이제 성립하지 않는다 —
    chapter-overview 를 신설했고 mermaid 는 없앴다. L2 유지: tool-layer-map.
source:
  - 《Networking and Kubernetes: A Layered Approach》(James Strong·Vallery Lancey, O'Reilly, 2021) Ch2. Linux Networking — Network Troubleshooting Tools
  - https://curl.se/docs/  # cURL 공식 문서 (2026-07-16 조회)
related:
  - ./02-01.커널이 패킷을 다루는 법 — 소켓·Netfilter·Conntrack·라우팅.md
  - ./02-02.iptables·IPVS·eBPF — kube-proxy를 이해하는 세 기술.md
  - ./README.md
updated: 2026-07-16
---

# Linux 네트워크 진단 도구 — 계층 순서대로 수사하기

---

## 핵심 요약
> 이 편 전체가 매달린 하나의 생각은 "네트워크 진단은 계층 순서대로 좁혀 가는 수사"입니다.
>
> 이름이 풀리는가(dig), 도달하는가(ping·traceroute), 포트가 열려 있고 누가 듣는가(netstat·nmap), 프로토콜 대화가 되는가(telnet·netcat·openssl·cURL) — 이 순서로 도구를 골라 쓰면 "어디까지는 되고 어디서부터 안 되는지"가 체계적으로 드러납니다. 단, Kubernetes Service는 ICMP를 지원하지 않으므로 ping이 항상 실패한다는 함정 하나는 미리 알아야 합니다.



## 학습 목표
> 도구 이름을 외우는 대신, 증상을 보고 어느 계층부터 의심할지 정하고 그 순서로 도구를 고르는 상태를 목표로 합니다.

이 내용을 읽고 나면:

1. 증상(연결·DNS·포트·TLS·HTTP)별로 첫 번째로 꺼낼 도구를 고를 수 있습니다.
2. traceroute가 TTL을 조작해 경로를 알아내는 원리를 설명할 수 있습니다.
3. Kubernetes Service에 ping이 실패하는 이유와 대안을 말할 수 있습니다.
4. netstat로 특정 포트에서 듣는 프로세스를 찾을 수 있습니다.
5. openssl s_client와 cURL로 TLS 문제를 진단하는 절차를 수행할 수 있습니다.



## 수사 순서 한눈에
> 아래 흐름도가 이 편 전체의 지도입니다. 앞 관문이 막히면 뒤를 볼 필요가 없으므로, 순서대로 좁히면 실패 지점이 빠르게 격리됩니다.

이 편은 관문 넷을 순서대로 통과하는 수사입니다. 각 관문에서 실제로 무엇을 치고 어디를 읽는지까지 붙이면 이렇게 됩니다.

![수사 사다리 — 관문마다 무엇을 치고 어디를 읽는가](_assets/02-03.triage-ladder.svg)

왼쪽이 통과해야 할 관문이고 오른쪽이 그 자리에서 치는 명령과 읽을 지점입니다. 명령을 아는 것보다 **어느 열을 보느냐**가 실무에서 갈립니다. `netstat`에서 Local Address가 와일드카드인지 루프백인지가 그 예입니다.

> 이 순서는 원서의 주장이 아닙니다. 책은 도구를 기능별로 소개할 뿐이고, 오히려 "도구끼리 기능이 겹치는 경우가 많아 선택은 선호·친숙도·가용성의 문제"라고 적습니다. 위 사다리는 그 도구들을 실무에서 쓰기 좋게 편집자가 엮은 순서이며, 각 도구의 설명 자체는 원서를 따릅니다.

이름이 "계층 순서"이지만 실제로 밟는 계층은 오르내립니다. 도구별로 무엇을 보는지 겹쳐 보면 그 점이 드러납니다.

![도구가 보는 계층 — 수사 순서는 계층 순서가 아니다](_assets/02-03.tool-layer-map.svg)

내려오는 선의 길이가 제각각인 것이 요점입니다. 계층을 위에서 아래로 훑는 것이 아니라 **끊길 가능성이 큰 곳부터 싼 검사로 짚어 나가는 순서**이기 때문입니다.

K8s 함정 하나를 먼저 박아 둡니다 — 위 흐름에서 대상이 Kubernetes Service라면 ping 단계를 건너뛰어야 합니다. 이유는 §2에서 설명합니다.



## 1. 시작 전 보안 경고 — 진단 도구는 공격 도구다
> 여기 나오는 도구는 전부 공격자의 정찰 도구이기도 합니다. 머신에 적게 깔아 두는 것이 모범 관행인 이유입니다.

여기 나오는 모든 도구는 공격자가 시스템을 탐색하고 접근을 넓히는 데 그대로 쓸 수 있습니다. 저자들은 머신에 네트워킹 도구를 가능한 한 적게 설치해 두는 것을 모범 관행으로 꼽습니다. 공격자가 직접 바이너리를 내려받을 수는 있지만 최소한 마찰은 늘어납니다.

setuid 비트가 그 근거 중 하나입니다. 파일에 setuid가 설정되면 실행한 사용자가 아니라 파일 소유자의 권한으로 실행됩니다. 권한 표기에서 `x` 자리에 `s`가 오는 것이 그 표시입니다. ping·nmap 같은 도구는 raw 패킷을 보내야 해서 일부 시스템에서 setuid를 씁니다. 공격자가 자기 복사본을 내려받아도 root 권한이 없으면 시스템 설치본만큼 할 수 있는 일이 줄어듭니다.

> 원서 대비 갱신: 요즘 배포판의 `ping`은 setuid 대신 `CAP_NET_RAW` 능력(capability)을 쓰거나 `net.ipv4.ping_group_range` 설정으로 일반 사용자에게 허용합니다. setuid 바이너리를 줄이는 방향이라 원서 서술과 다를 수 있으며, `ls -l $(which ping)`으로 그 환경의 실제 방식을 확인할 수 있습니다.



## 2. 도달 확인 — ping과 traceroute
> 도달성은 ping으로 보고 끊긴 지점은 traceroute로 특정합니다. 단, Kubernetes Service는 ICMP를 받지 않으므로 여기서 ping을 믿으면 안 됩니다.

ping은 ICMP ECHO_REQUEST 패킷을 보내 호스트 간 연결을 확인하는 가장 단순한 도구입니다. `ping <주소>` 형태로 쓰고 `-c <횟수>`로 전송 수를 제한합니다.

Kubernetes 사용자에게 중요한 함정이 하나 있습니다. Kubernetes Service는 ICMP를 지원하지 않으므로 Service 주소로 보낸 ping은 응답을 받지 못합니다. 연결 확인은 telnet이나 cURL 같은 상위 도구로 해야 하며, 개별 Pod는 네트워크 구성에 따라 ping이 닿을 수도 있습니다.

> 원서 대비 갱신 둘. 책은 Service가 "TCP와 UDP만" 지원한다고 적지만 SCTP가 추가돼 지금은 셋입니다. 또 kube-proxy가 IPVS 모드로 도는 클러스터에서는 ClusterIP가 노드의 더미 인터페이스에 실제로 바인딩되므로 ping이 응답하기도 합니다. "항상 실패"보다는 "ping 결과로 서비스 정상 여부를 판단하지 말라"가 안전한 규칙입니다. 방화벽·라우터가 ICMP를 걸러내도록 설정될 수 있다는 점도 함께 기억해 둘 부분입니다.

traceroute는 한 호스트에서 다른 호스트까지의 경로를 홉 단위로 보여 줍니다. 원리가 재치 있습니다 — 1장에서 본 TTL을 역이용합니다. TTL을 1로 시작해 1씩 올려 가며 패킷을 보내면, TTL을 0으로 깎은 각 라우터가 TIME_EXCEEDED 패킷을 자기 주소로 회신하므로 경로상의 호스트가 한 줄씩 드러납니다. 응답이 없으면 `*`로 표시되는데, TIME_EXCEEDED 회신을 거부하는 호스트나 중간 방화벽이 원인일 수 있습니다. 기본 프로토콜은 UDP이고 `-P`로 TCP·ICMP를 고를 수 있습니다.



## 3. 이름 확인 — dig
> 어느 DNS가 무엇이라고 답했는지를 확정하는 단계입니다. status와 ANSWER SECTION, 응답한 서버 셋을 함께 읽습니다.

dig는 명령줄 DNS 조회 도구입니다. `dig <도메인>`이 기본형이고 CNAME·A·AAAA 레코드를 보여 줍니다. 가장 흔한 사용법은 레코드 타입 지정으로, `dig <도메인> <타입>` 형식입니다(예: `dig kubernetes.io TXT`).

응답에서는 status(`NOERROR`·`NXDOMAIN` 등), ANSWER SECTION, 응답한 서버(SERVER)를 읽으면 "어느 DNS가 뭐라고 답했는지"가 확정됩니다. `-p`로 비표준 포트를 지정할 수 있고, 특정 DNS 서버에 물으려면 `dig @서버 <도메인>` 표기를 씁니다. `@서버` 표기와 `NXDOMAIN` 판독은 책의 옵션 표에 없고 dig 매뉴얼에서 보탠 것입니다.

> 원문 정오: 책의 표 2-15는 `-b`를 "질의를 보낼 대상 주소"로 설명하지만, dig 매뉴얼 기준 `-b`는 질의의 출발지(source) IP 주소를 지정하는 옵션입니다. 대상 서버 지정은 `@서버` 표기가 맞습니다.



## 4. 포트와 소켓 — netstat와 nmap
> 안에서 누가 듣는지는 netstat, 밖에서 무엇이 보이는지는 nmap입니다. 리스닝 주소가 `0.0.0.0`인지 `127.0.0.1`인지가 곧 노출 여부입니다.

netstat는 머신의 네트워크 스택과 연결 정보를 폭넓게 보여 줍니다. 인자 없이 실행하면 연결된 소켓 전부를, `-a`는 리스닝 소켓까지 전부를, `-l`은 리스닝만 보여 줍니다. 실무에서 가장 잦은 용법은 "이 포트에서 누가 듣고 있나"입니다.

```bash
# 3000 번 포트에서 누가 듣고 있는지 봅니다
sudo netstat -lp | grep 3000
```

```bash
tcp6    0    0 [::]:3000    [::]:*    LISTEN    613/grafana-server
```

`-l`(listening)과 `-p`(program)의 조합이며, 프로세스 정보를 다 보려면 sudo가 필요할 수 있습니다. 출력의 로컬 주소가 와일드카드인지 루프백인지로 "밖에서 접근 가능한가"도 함께 판별됩니다. 와일드카드는 IPv4에서 `0.0.0.0`, IPv6에서 `[::]`로 찍히고, 루프백은 `127.0.0.1` 또는 `[::1]`입니다. 위 예시의 `[::]:3000`은 모든 인터페이스에서 듣는다는 뜻이라 밖에서 접근할 수 있습니다. `-t`/`-u`로 TCP/UDP만, `-s`로 프로토콜 통계를 봅니다.

nmap은 포트 스캐너입니다. `nmap <대상>`(도메인·IP·CIDR)으로 열린 포트와 추정 서비스를 빠르게 요약합니다. 원격에서 어떤 서비스가 보이는지 즉시 알려 주므로 노출되면 안 되는 서비스를 찾는 손쉬운 방법이고, 같은 이유로 공격자가 애용하는 도구이기도 합니다. `-A`는 OS·버전 탐지까지 켭니다.



## 5. 프로토콜 대화 — telnet·netcat·openssl·cURL
> 연결이 되는데도 안 될 때 마지막으로 남는 단계입니다. 실제로 프로토콜을 말해 보고 어느 응답이 어긋나는지 확인합니다.

여기서부터는 연결 위에서 실제 프로토콜을 말해 보는 단계입니다.

telnet은 `telnet <주소> <포트>`로 연결해 대화형 입력을 제공합니다. 텍스트 기반 프로토콜 서버를 디버깅하는 고전으로, HTTP/1 서버에 `HEAD / HTTP/1.1`을 직접 타이핑해 응답 헤더를 받아볼 수 있습니다. 도구를 제대로 쓰려면 해당 애플리케이션 프로토콜을 알아야 한다는 전제가 붙습니다.

netcat(`nc`)은 연결·데이터 전송·리스닝을 다 하는 다목적 도구입니다. telnet과 비슷하게 서버에 붙을 수 있지만 stdin 파이프를 받으므로 요청을 파일로 만들어 흘려 넣는 식의 자동화가 쉽습니다. 서버나 클라이언트를 "수동으로" 돌려 무슨 일이 일어나는지 들여다볼 때 유용합니다.

openssl은 TLS 진단의 종결자입니다. 여러 도구가 TLS 오류를 잡아내지만 디버깅 정보의 깊이는 openssl이 가장 풍부합니다. 핵심 용법은 `openssl s_client -connect <호스트>:443`으로, 인증서 체인·발급자·협상된 프로토콜과 cipher(예: TLSv1.3, TLS_AES_256_GCM_SHA384)·검증 결과(Verify return code)를 전부 보여 줍니다. 자체 서명 CA는 `-CAfile <경로>`로 지정해 검증할 수 있습니다.

cURL은 HTTP·HTTPS를 비롯한 여러 프로토콜의 데이터 전송 도구입니다. 기본 동작은 GET이고 자주 쓰는 옵션은 다음과 같습니다.

- `-L` — 리다이렉트 따라가기 (기본은 따라가지 않음 — `curl kubernetes.io`가 본문 대신 리다이렉트 안내만 돌려주는 이유)
- `-X <verb>` — 특정 HTTP 메서드 (예: `-X DELETE`)
- `-d <데이터>` — 본문 전송. URL 인코딩, JSON, `@파일` 모두 가능
- `-H <헤더>` — 명시적 헤더 추가
- `-v` — 상세 출력. TCP 연결부터 TLS 핸드셰이크 단계, 요청·응답 헤더까지 보여 줘 L7 디버깅에 특히 값짐

cURL은 브라우저처럼 인증서 체인을 호스트의 CA 목록과 대조하므로 TLS 문제도 드러냅니다. 만료된 인증서 사이트에 붙으면 `curl: (60) SSL certificate problem: certificate has expired`로 실패합니다. `-v`를 붙이면 어느 핸드셰이크 단계에서 알림이 났는지까지 보입니다.

책은 cURL 옆에 wget을 짧게 덧붙입니다. 둘 다 HTTP로 자원을 가져오지만 wget은 파일 내려받기에 기울어 있고 재귀 다운로드가 되며, cURL은 프로토콜 폭이 넓고 헤더·본문을 직접 다루기 좋아 디버깅에 더 자주 쓰입니다.

> 💬 **비유**: 진단 도구 세트는 병원의 검사 순서와 비슷합니다. ping은 맥박 확인이고 traceroute는 혈관 조영술입니다 — 어디까지 피가 도는지 봅니다. dig는 환자 차트 조회(이름↔번호 대조)이고, netstat·nmap은 청진기로 어느 장기(포트)가 활동 중인지 듣는 단계입니다. telnet·openssl·cURL은 문진입니다 — 실제로 대화(프로토콜)를 걸어 보고 대답의 어디가 이상한지 확인합니다. 맥박도 안 뛰는데 문진부터 시작하면 시간만 잃습니다.



## 심화 학습
> 책 밖에서 보강한 내용입니다. 출처를 함께 남깁니다.

- netstat는 net-tools 패키지 소속이고, 현대 배포판의 표준 대체는 iproute2의 [`ss`](https://man7.org/linux/man-pages/man8/ss.8.html)입니다(`ss -lntp`가 `netstat -lntp`에 대응). 이 책의 netstat 용법은 ss에 거의 그대로 이식됩니다.
- 컨테이너에는 이런 도구가 없는 경우가 대부분입니다. 최소 이미지 관행 때문이며 이 편 §1의 보안 경고와 같은 이유입니다. 표준 우회는 Kubernetes 1.25에서 GA가 된 임시(ephemeral) 컨테이너입니다. `kubectl debug --image=nicolaka/netshoot`처럼 진단 도구를 주입해 붙습니다. 절차는 [kubectl debug 공식 문서](https://kubernetes.io/docs/tasks/debug/debug-application/debug-running-pod/)에 있습니다.
- 이 편의 도구를 K8s에서 쓰는 순서는 형제 개념 노트 축과 이어집니다 — 클러스터 쪽 진단은 [`08_cloud/kubernetes`](../../kubernetes/README.md) 운영 문서들이 SSOT입니다.



## 결정 치트시트
> 원문 표 2-12의 케이스별 도구 선택에 K8s 함정 하나를 더했습니다.

| 확인하려는 것 | 도구 |
|--------------|------|
| 연결성 | traceroute, ping, telnet, netcat |
| 포트 스캔 | nmap |
| DNS 레코드 | dig (+ 연결성 도구들) |
| HTTP/1 | cURL, telnet, netcat |
| HTTPS | openssl, cURL |
| 듣고 있는 프로그램 | netstat (`sudo netstat -lp`) |
| Kubernetes Service 연결성 | telnet·cURL — ping은 ICMP라 항상 실패 |



## 면접에서 받을 만한 질문

> 먼저 스스로 답을 소리 내어 말해 본 뒤, 아래 §정답 섹션을 펼쳐 맞춰 봅니다.

1. traceroute를 한 문장으로 정의하면? TTL과 TIME_EXCEEDED로 어떻게 경로를 알아냅니까?
2. 네트워크 진단은 왜 계층 순서로 좁혀야 합니까? 그 순서는?
3. Kubernetes Service에 ping이 항상 실패하는 이유는? 그럼 무엇으로 확인합니까?
4. 서비스가 안 붙을 때 어떤 순서로 도구를 씁니까?
5. TLS 오류가 의심될 때 cURL과 openssl 중 무엇을 씁니까? traceroute의 `* * *`는 경로 단절입니까?

### 빈칸 채우기 — 진단 계층 순서

진단은 좁혀 가는 순서입니다. 이름은 `(1)____`, 도달은 `(2)____`·traceroute, 포트는 `(3)____`·nmap, 프로토콜은 telnet·openssl·`(4)____`로 확인합니다.



## 정답 (자답 후 펼치기)

> 위 §면접에서 받을 만한 질문의 5개에 *먼저 자답한 뒤* 아래를 읽으세요. 자답 없이 먼저 읽으면 학습 효과가 0입니다.

### 정답 1 — traceroute 정의

traceroute는 TTL을 1부터 올려 보내며 각 라우터의 TIME_EXCEEDED 회신으로 경로상 홉을 한 줄씩 알아내는 경로 진단 도구입니다. TTL이 0이 된 지점의 라우터가 발신자에게 TIME_EXCEEDED를 돌려주므로, TTL을 1·2·3…으로 늘리면 첫 홉부터 목적지까지 한 홉씩 드러납니다.

### 정답 2 — 계층 순서로 진단

다음 순서로 좁혀야 "어디서부터 안 되는지"가 나옵니다.

1. 이름 — dig
2. 도달 — ping·traceroute
3. 포트 — netstat·nmap
4. 프로토콜 — telnet·openssl·cURL

위 계층이 안 되면 아래는 볼 필요가 없으므로, 순서대로 가면 실패 지점을 빠르게 격리할 수 있습니다.

### 정답 3 — K8s Service와 ping

Kubernetes Service는 TCP·UDP만 지원하므로 ping(ICMP)이 항상 실패합니다. Service가 죽어서가 아니라 프로토콜을 안 받는 것이라, Service 연결 확인은 telnet·cURL 같은 TCP 기반 도구로 합니다. 개별 Pod IP로는 ping이 될 수 있습니다.

### 정답 4 — 서비스 진단 순서

먼저 이름이 풀리는지 dig로, 호스트에 도달하는지 ping·traceroute로 확인합니다. 도달하면 netstat로 대상 포트에서 프로세스가 실제로 듣는지(그리고 `0.0.0.0`인지 `127.0.0.1`인지) 보고, 마지막으로 telnet·cURL로 프로토콜 수준 응답을 확인합니다.

### 정답 5 — cURL vs openssl, `* * *`

빠른 확인은 `curl -v`로 충분합니다. 인증서 체인·cipher 협상까지 파고들려면 `openssl s_client -connect`가 가장 상세합니다. traceroute의 `* * *`는 경로 단절이 아닐 수 있습니다. 해당 홉이 TIME_EXCEEDED 회신을 거부하거나 방화벽이 회신을 막았을 뿐일 수 있고, 그 뒤 홉이 정상 표시되면 트래픽은 통과하고 있다는 뜻입니다.

### 정답 빈칸 — (1)`dig` (2)`ping` (3)`netstat` (4)`cURL`

이름은 dig, 도달은 ping·traceroute, 포트는 netstat·nmap, 프로토콜은 telnet·openssl·cURL로 확인합니다.



## 핵심 개념 체크리스트
> 다섯 항목을 막힘없이 답할 수 있으면 3장으로 넘어가도 좋습니다.
- [ ] 증상별 첫 도구 선택 표를 안 보고 재구성할 수 있는가?
- [ ] traceroute의 TTL 트릭을 TIME_EXCEEDED와 함께 설명할 수 있는가?
- [ ] K8s Service에 ping이 실패하는 이유를 프로토콜 수준에서 말할 수 있는가?
- [ ] `netstat -lp` 출력에서 리스닝 주소의 보안 함의를 읽을 수 있는가?
- [ ] `openssl s_client` 출력에서 확인할 세 가지(체인·cipher·검증 코드)를 아는가?



## 참고 자료
- 《Networking and Kubernetes: A Layered Approach》(James Strong·Vallery Lancey, O'Reilly, 2021, ISBN 978-1492081654) Ch2
- [cURL 공식 문서](https://curl.se/docs/)
- [kubectl debug — 실행 중인 Pod 디버깅](https://kubernetes.io/docs/tasks/debug/debug-application/debug-running-pod/)
- 이전 편: [02-02. iptables·IPVS·eBPF](./02-02.iptables%C2%B7IPVS%C2%B7eBPF%20%E2%80%94%20kube-proxy%EB%A5%BC%20%EC%9D%B4%ED%95%B4%ED%95%98%EB%8A%94%20%EC%84%B8%20%EA%B8%B0%EC%88%A0.md)
