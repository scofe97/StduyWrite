---
title: 개념 노트 — 문항에서 갈라져 나온 이론
tags: [moc, troubleshooting, concepts]
status: draft
source:
  - experience
related:
  - ../README.md
  - ../_drill/README.md
updated: 2026-09-22
---

# 개념 노트

---

> 문항을 풀다 갈라져 나온 이론을 주제별로 모읍니다. 같은 개념이 여러 문항에 걸리므로, 문항 파일마다 복제하지 않고 여기 한 번 적고 링크합니다.

## 왜 문항에서 떼어 냈나

> 개념은 문항보다 오래 살고, 문항보다 넓게 쓰입니다.

문항 파일 하나는 **한 사건의 기록** 입니다. 2026-09-08 에 어떤 증상을 만나 무엇으로 갈랐는지가 거기 남습니다. 그런데 그 과정에서 나온 개념 — conntrack 이 무엇이고 백로그와 어떻게 다른지 — 은 그 사건만의 것이 아닙니다. 다음 회차의 다른 증상에서 또 나옵니다.

같은 설명을 문항마다 복제하면 셋이 어긋납니다. 하나를 고쳐도 나머지가 안 고쳐지고, 어느 것이 최신인지 알 수 없게 되고, 문항 파일이 사건 기록이 아니라 교과서가 됩니다.

그래서 나눕니다. 문항 파일에는 **그 사건에서 어떻게 접근했고 실제와 어디서 갈렸는지**를 남기고, 개념 노트에는 **여러 문항이 공통으로 쓰는 이론**을 둡니다.

| | 문항 파일 | 개념 노트 |
|---|---|---|
| 담는 것 | 증상 · 배제 논리 · 접근 대조 · 그 사례의 수치 | 자료구조 · 명령 · 헷갈리는 쌍 |
| 수명 | 그 사건 한 번 | 여러 문항에 걸침 |
| 없으면 | 무엇을 어떻게 틀렸는지 못 봄 | 같은 설명이 문항마다 복제됨 |

문항의 `## 나온 개념` 절에는 링크와 그 문항에서 걸린 지점만 한두 줄 남깁니다. `## 내 접근과 실제가 갈린 자리` 는 그 사건 고유의 기록이라 문항에 남습니다.

## 무엇이 여기 오나

들어오는 것은 **두 번 이상 쓰일 이론** 입니다.

- 커널·런타임의 자료구조와 그 한도 (conntrack, 백로그, 힙 등)
- 진단 도구의 사용법과 출력 읽는 법
- 자주 헷갈리는 개념 쌍의 구분

안 들어오는 것은 **그 사건에만 해당하는 것** 입니다. 그 회사의 구성, 그날의 수치, 그 문항의 배제 논리는 문항 파일에 남습니다.

## 규약

파일 하나가 주제 하나입니다. 이름은 `{계층}-{주제}.md` 이고, 계층 자리에는 문항 폴더 이름(`os`·`kubernetes`·`mesh`·`cloud`·`runtime`)을 씁니다. 파일 목록만 봐도 어느 계층의 개념인지 보이게 하려는 것입니다.

두 계층 이상의 문항에서 쓰이는 개념은 `common-` 을 붙입니다. 처음 나온 계층 하나로 몰면 분류가 내용과 어긋나기 때문입니다. 흐름을 가르는 다섯 값은 B 문항에서 처음 나왔지만 내용은 호스트 네트워크라 `kubernetes-` 로 두면 거꾸로 읽힙니다. 한 계층에서만 쓰이던 노트가 다른 계층 문항에도 쓰이게 되면 `common-` 으로 옮기고 링크를 함께 고칩니다. 아래 표의 "나온 문항" 칸이 그 판단의 근거입니다.

빈칸을 남겨도 됩니다. 아직 안 배운 자리는 `*(미정)*` 으로 두고, 그 자리가 다음 회차 출제 지점이 됩니다. 채운 척하지 않는 것이 이 폴더의 규칙입니다.

근거에는 등급을 답니다. 1차(공식 문서·man·소스)인지 2차(포스트모템)인지 가설인지 표시하고, 확인 안 한 수치는 확인 안 했다고 적습니다.

## 문서

| 계층 | 주제 | 무엇을 다루나 | 나온 문항 |
|------|------|-------------|----------|
| os | [패킷이 사라지는 네 자리](./os-패킷이-사라지는-네-자리.md) | 네 자리의 범위와 진단 명령, 패킷이 그 자리를 지나는 순서, conntrack 의 조회와 등록 두 시점 | A 2026-09-08 |
| os | [한 노드의 라우팅 테이블을 둘이 만질 때](./os-한-노드의-라우팅-테이블을-둘이-만질-때.md) | 한 표에 줄을 넣는 주체들, 누가 넣었는지를 적는 `proto` 칸, networkd 가 남의 경로를 지우는 기본값, 부팅과 재시작의 순서 차이, 막는 법 세 층 | A 2026-09-21 |
| os | [파이프라인 밖에서 노드를 바꾸는 장치](./os-파이프라인-밖에서-노드를-바꾸는-장치.md) | 배포 파이프라인과 OS 자동 업데이트가 네 물음에 정반대로 답하는 이유, 타이머가 부르는 패키지 관리자, 같은 이미지가 공유하는 기본 창, 불변 인프라와 그 대가 | A 2026-09-21 |
| kubernetes | [노드 간 경로를 누가 퍼뜨리는가](./kubernetes-노드-간-경로를-누가-퍼뜨리는가.md) | 노드마다 다른 Pod 대역, 풀메시와 경로 반사기의 세션 수, 반사기를 고르는 라벨 셀렉터가 빈 집합이 될 때, 세션과 경로를 노드에서 읽는 명령 | B 2026-09-14 |
| runtime | [가상 스레드가 내려오지 못할 때](./runtime-가상-스레드가-내려오지-못할-때.md) | pinning 의 기전과 최소 재현, 교착까지 가는 경로, 풀이 보상하지 않는 이유, 덤프·JFR 이 갈리는 자리 | E 2026-09-10 |
| runtime | [힙 밖에서 쌓이는 메모리](./runtime-힙-밖에서-쌓이는-메모리.md) | 가상 메모리와 RSS, JVM 이 세는 여섯 칸과 세지 않는 몫, arena 가 반납되지 않고 쌓이는 기전, 스레드 수와 arena 수의 연결, NMT·pmap 로 갈리는 자리 | E 2026-09-14 |
| common | [한도는 어디서 오는가](./common-한도는-어디서-오는가.md) | 상한의 네 출처와 물어볼 것 셋, 응급과 재발 방지가 갈리는 자리 | A · B 2026-09-08 |
| common | [무엇을 보려면 무엇을 치는가](./common-무엇을-보려면-무엇을-치는가.md) | 단위가 도구를 정한다. 도구별로 보는 것과 못 보는 것, 누적과 현재의 구분, 증상에서 첫 명령으로 | A 2026-09-10 · B 2026-09-12 |
| common | [흐름을 가르는 다섯 값](./common-흐름을-가르는-다섯-값.md) | 소켓 조회·conntrack·ECMP 가 흐름을 알아보는 값, 바깥 헤더에 포트가 없는 ICMP 오류와 조각, 인용을 읽는 쪽과 안 읽는 쪽, Packet Too Big 이 연결 없는 서버에 떨어지는 여섯 걸음과 완화책 셋 | B 2026-09-12 · A 2026-09-14 |

## 관련 문서

- [troubleshooting 진입점](../README.md) — 계층 지도
- [드릴 진입점](../_drill/README.md) — 루틴과 채점 규약
