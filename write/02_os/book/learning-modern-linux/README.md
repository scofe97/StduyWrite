---
title: Learning Modern Linux 정독 인덱스
tags: [moc, linux, kernel, shell, filesystem, networking, observability, container]
status: draft
source:
  - 《Learning Modern Linux》(Michael Hausenblas, O'Reilly, 2022)
related:
  - ../../README.md
  - ../systems-performance/README.md
  - ../linux-kernel-programming/README.md
  - ../../../network-roadmap.md
learning:
  topic: learning-modern-linux
  scope: durable
  level: 기본
  last_verified: 2026-09-10   # 07-03 복습 회차 3(quality 3). 문서 검증일과는 다른 축이라 학습 상태 표에 따로 적습니다
  blocked_count: 2   # 07-03 복습 회차 3 에서 quality 3(2→3 상승). 남은 학습자 축은 「코드별 다음 명령」 하나이고, accept 큐 축은 노트 결손이라 학습자 축에서 뺐습니다
  next_lesson: "**07-03 복습 회차 3 완료(2026-09-10, quality 3 — 회차 2 의 2 에서 상승).** 세 회차 만에 처음 오른 회차이고, 오른 자리는 반복 인출이 먹은 자리입니다 — 회차 2 에서 0 점이던 `curl -I` 함정이 4 점으로, 2 점이던 종료 코드가 3 점으로. **다음 회차 2026-09-24(회차 4)에 물을 것은 둘입니다** — Q1 의 뒷절반(코드 6·7·28 각각에 이어지는 다음 명령)과 Q3 처방의 구체형(`-s -o /dev/null -w '%{http_code}'`). accept 큐 축은 노트 결손이라 보강 뒤로 미룹니다. **07-01·07-02 는 2026-09-11 회차 2 가 예정돼 있습니다**(둘 다 회차 1 quality 3). 회차 2 는 회차 1 문항을 재탕하지 말고 2026-09-07 보강이 만든 새 자리에서 뽑습니다. **보강 작업은 위 학습 상태 표의 `보강 후보` 행이 정본입니다** — `07-03` §3 의 종료 코드 사다리에 TLS·앱 응답 층 추가, 6 번이 세 원인을 뭉갠다는 것, accept 큐 관측 명령. 셋 다 `man` 축자 대조가 선행 조건입니다."
updated: 2026-09-07
---

# Learning Modern Linux 정독 인덱스

---

> 클라우드 네이티브 환경에서 리눅스를 쓰는 사람을 독자로 잡은 개론서입니다. 커널부터 셸·접근 제어·파일시스템·네트워크·관측까지를 한 권으로 훑고, 컨테이너를 특별한 주제가 아니라 리눅스 기능의 조합으로 다룹니다.

## 이 책을 여기 두는 이유

> `02_os` 는 언어가 아니라 실행 환경의 공통 기반을 모으는 자리입니다. 이 책은 그 기반을 한 바퀴 도는 개론이라 카테고리의 입구에 놓습니다.

같은 폴더의 다른 두 책과는 깊이와 시점이 다릅니다. [linux-kernel-programming](../linux-kernel-programming/README.md)은 커널 개발자의 시선으로 모듈을 짜고 커널을 빌드하는 책입니다. [systems-performance](../systems-performance/README.md)는 성능 분석가의 시선으로 방법론과 도구를 다룹니다. 이 책은 그 둘보다 얕은 대신 넓으며, 무엇보다 **컨테이너 환경을 전제로** 각 주제를 고릅니다.

읽는 이유는 하나 더 있습니다. [Kubernetes 네트워크 학습 로드맵](../../../network-roadmap.md)의 0단계가 이 책 7장 Networking 을 지정합니다. 그 7장을 읽으려면 네임스페이스와 커널의 어휘가 먼저 서 있어야 하고, 1장과 2장이 그 어휘를 만듭니다.



## 파트 구조

> 원서는 파트로 나뉘어 있지 않고 장 아홉과 부록 둘로 이어집니다. 성격이 갈리는 지점만 표시합니다.

| 구간 | 장 | 성격 |
|------|-----|------|
| 무대와 커널 | 1~2장 | 운영체제와 커널의 자리를 세우는 예비 구간 |
| 일상의 인터페이스 | 3~5장 | 셸·접근 제어·파일시스템. 손으로 매일 만지는 층 |
| 앱과 컨테이너 | 6장 | 부팅·패키지 관리·컨테이너. 이 책의 중심 |
| 네트워크와 관측 | 7~8장 | 로드맵 0단계가 지정한 7장이 여기 |
| 선택 구간 | 9장 · 부록 A·B | 저자가 선택 사항이라 밝힌 고급 주제와 레시피·도구 목록 |



## 장별 목표

> 각 장 앞머리에서 저자가 직접 적은 범위를 옮긴 것입니다. 추측으로 채운 칸은 없습니다.

| 장 | 제목 | 저자가 밝힌 범위 |
|:--:|------|------------------|
| 1 | Introduction to Linux | modern 의 뜻, 30년 약사, 운영체제의 역할, 배포판, 자원 가시성 |
| 2 | The Linux Kernel | 리눅스 아키텍처와 커널의 자리, CPU 계열, 커널 구성요소, 시스템 콜, 커널 확장 |
| 3 | Shells and Scripting | 셸 기본과 용어, Fish 같은 모던 셸, 설정과 일상 작업, 터미널 멀티플렉서, 안전하고 이식성 있는 스크립트와 린트·테스트 |
| 4 | Access Control | 사용자·프로세스·파일의 접근 관계, 샌드박싱과 접근 제어 유형, 사용자 정의와 관리, 권한, capability·seccomp·ACL 과 보안 관례 |
| 5 | Filesystems | 용어 정의, "모든 것은 파일" 추상의 구현, 커널이 정보를 드러내는 특수 목적 파일시스템, 일반 파일시스템 비교와 공통 연산 |
| 6 | Applications, Package Management, and Containers | 애플리케이션과 패키지의 정의, 부팅 과정과 systemd, 앱 공급망과 배포판별 패키지 관리, 컨테이너의 구성 요소와 도구, 모던 패키지 관리 |
| 7 | Networking | 하드웨어부터 HTTP·SSH 까지의 용어, 네트워크 스택과 프로토콜과 인터페이스, DNS, 애플리케이션 계층 프로토콜과 도구, 고급 주제 |
| 8 | Observability | 신호 유형(로그·메트릭·트레이스), 문제 해결과 성능 측정, 로그, 자원별 모니터링과 도구, 종단 간 구성 |
| 9 | Advanced Topics | IPC(시그널·네임드 파이프·유닉스 도메인 소켓), 가상 머신, 모던 배포판, Kerberos 와 PAM, 아직 주류가 아닌 쓰임새 |
| A | Helpful Recipes | 부록 |
| B | Modern Linux Tools | 부록 |



## 작성된 정독 노트

> 장 하나에 노트 하나가 원칙이고, 장이 길면 절로 나눕니다.

| 노트 | 장 | 한 줄 핵심 |
|------|:--:|-----------|
| [01-01. 하드웨어를 가리고 나면 무엇이 보일지를 정해야 한다](./01-01.%ED%95%98%EB%93%9C%EC%9B%A8%EC%96%B4%EB%A5%BC%20%EA%B0%80%EB%A6%AC%EA%B3%A0%20%EB%82%98%EB%A9%B4%20%EB%AC%B4%EC%97%87%EC%9D%B4%20%EB%B3%B4%EC%9D%BC%EC%A7%80%EB%A5%BC%20%EC%A0%95%ED%95%B4%EC%95%BC%20%ED%95%9C%EB%8B%A4.md) | 1 | 운영체제가 하드웨어를 가려 준 뒤에도 어느 자원이 어디까지 보이는지는 namespace 와 cgroups 가 따로 정합니다 |
| [02-01. 커널은 전부를 하지만 운영체제는 아니다](./02-01.%EC%BB%A4%EB%84%90%EC%9D%80%20%EC%A0%84%EB%B6%80%EB%A5%BC%20%ED%95%98%EC%A7%80%EB%A7%8C%20%EC%9A%B4%EC%98%81%EC%B2%B4%EC%A0%9C%EB%8A%94%20%EC%95%84%EB%8B%88%EB%8B%A4.md) | 2 | 셸도 `ps` 도 커널이 아니며, 커널이 기능을 드러내는 통로는 시스템 콜 하나입니다 |
| [03-01. 셸의 실체는 스트림과 변수와 종료 상태다](./03-01.%EC%85%B8%EC%9D%98%20%EC%8B%A4%EC%B2%B4%EB%8A%94%20%EC%8A%A4%ED%8A%B8%EB%A6%BC%EA%B3%BC%20%EB%B3%80%EC%88%98%EC%99%80%20%EC%A2%85%EB%A3%8C%20%EC%83%81%ED%83%9C%EB%8B%A4.md) | 3 | 셸이 실제로 다루는 것은 스트림·변수·종료 상태 셋이고, 원서 실습 두 곳이 의도대로 동작하지 않습니다 |
| [03-02. 자주 치는 것일수록 짧아야 한다](./03-02.%EC%9E%90%EC%A3%BC%20%EC%B9%98%EB%8A%94%20%EA%B2%83%EC%9D%BC%EC%88%98%EB%A1%9D%20%EC%A7%A7%EC%95%84%EC%95%BC%20%ED%95%9C%EB%8B%A4.md) | 3 | 손이 덜 가게 만드는 법이 별칭·셸 교체·멀티플렉서 세 층으로 쌓입니다 |
| [03-03. bash 는 조용히 실패하므로 시끄럽게 만들어야 한다](./03-03.bash%20%EB%8A%94%20%EC%A1%B0%EC%9A%A9%ED%9E%88%20%EC%8B%A4%ED%8C%A8%ED%95%98%EB%AF%80%EB%A1%9C%20%EC%8B%9C%EB%81%84%EB%9F%BD%EA%B2%8C%20%EB%A7%8C%EB%93%A4%EC%96%B4%EC%95%BC%20%ED%95%9C%EB%8B%A4.md) | 3 | bash 가 기본으로 조용히 실패하므로 스켈레톤 세 줄이 그 침묵을 깹니다 |
| [04-01. 전부 아니면 전무에서 잘게 쪼개는 쪽으로](./04-01.%EC%A0%84%EB%B6%80%20%EC%95%84%EB%8B%88%EB%A9%B4%20%EC%A0%84%EB%AC%B4%EC%97%90%EC%84%9C%20%EC%9E%98%EA%B2%8C%20%EC%AA%BC%EA%B0%9C%EB%8A%94%20%EC%AA%BD%EC%9C%BC%EB%A1%9C.md) | 4 | root 냐 아니냐라는 이분법이 capability·seccomp·ACL 로 쪼개져 왔습니다 |
| [05-01. 모든 것이 파일이라는 말은 손잡이가 하나라는 뜻이다](./05-01.%EB%AA%A8%EB%93%A0%20%EA%B2%83%EC%9D%B4%20%ED%8C%8C%EC%9D%BC%EC%9D%B4%EB%9D%BC%EB%8A%94%20%EB%A7%90%EC%9D%80%20%EC%86%90%EC%9E%A1%EC%9D%B4%EA%B0%80%20%ED%95%98%EB%82%98%EB%9D%BC%EB%8A%94%20%EB%9C%BB%EC%9D%B4%EB%8B%A4.md) | 5 | 파일시스템이 주는 것은 저장 공간이 아니라 균일한 손잡이이고, VFS 가 그 손잡이를 블록 장치 밖으로 뻗습니다 |
| [06-01. 먼저 켜지는 것 하나가 나머지 전부를 켠다](./06-01.%EB%A8%BC%EC%A0%80%20%EC%BC%9C%EC%A7%80%EB%8A%94%20%EA%B2%83%20%ED%95%98%EB%82%98%EA%B0%80%20%EB%82%98%EB%A8%B8%EC%A7%80%20%EC%A0%84%EB%B6%80%EB%A5%BC%20%EC%BC%A0%EB%8B%A4.md) | 6 | PID 1 이 나머지를 띄우고, 공급망의 세 자리가 그 앱을 날라 옵니다 |
| [06-02. 컨테이너의 새로움은 재료가 아니라 조합에 있다](./06-02.%EC%BB%A8%ED%85%8C%EC%9D%B4%EB%84%88%EC%9D%98%20%EC%83%88%EB%A1%9C%EC%9B%80%EC%9D%80%20%EC%9E%AC%EB%A3%8C%EA%B0%80%20%EC%95%84%EB%8B%88%EB%9D%BC%20%EC%A1%B0%ED%95%A9%EC%97%90%20%EC%9E%88%EB%8B%A4.md) | 6 | 네임스페이스와 cgroups 와 CoW 는 Docker 이전에도 있었고, 새로운 것은 그것을 감싼 방식입니다 |
| [07-01. 결국은 선과 공기를 타고 다니는 비트다](./07-01.%EA%B2%B0%EA%B5%AD%EC%9D%80%20%EC%84%A0%EA%B3%BC%20%EA%B3%B5%EA%B8%B0%EB%A5%BC%20%ED%83%80%EA%B3%A0%20%EB%8B%A4%EB%8B%88%EB%8A%94%20%EB%B9%84%ED%8A%B8%EB%8B%A4.md) | 7 | 추상이 원격을 로컬처럼 보이게 하지만 아래에는 링크 계층과 인터넷 계층이 있습니다 |
| [07-02. 포트가 있어야 서비스를 가리킬 수 있고 이름이 있어야 사람이 기억한다](./07-02.%ED%8F%AC%ED%8A%B8%EA%B0%80%20%EC%9E%88%EC%96%B4%EC%95%BC%20%EC%84%9C%EB%B9%84%EC%8A%A4%EB%A5%BC%20%EA%B0%80%EB%A6%AC%ED%82%AC%20%EC%88%98%20%EC%9E%88%EA%B3%A0%20%EC%9D%B4%EB%A6%84%EC%9D%B4%20%EC%9E%88%EC%96%B4%EC%95%BC%20%EC%82%AC%EB%9E%8C%EC%9D%B4%20%EA%B8%B0%EC%96%B5%ED%95%9C%EB%8B%A4.md) | 7 | 주소는 기계까지만 데려다주므로 포트가 서비스를 가리키고 DNS 가 이름을 맡습니다 |
| [07-03. 주고받는 일은 결국 몇 개의 명령으로 줄어든다](./07-03.%EC%A3%BC%EA%B3%A0%EB%B0%9B%EB%8A%94%20%EC%9D%BC%EC%9D%80%20%EA%B2%B0%EA%B5%AD%20%EB%AA%87%20%EA%B0%9C%EC%9D%98%20%EB%AA%85%EB%A0%B9%EC%9C%BC%EB%A1%9C%20%EC%A4%84%EC%96%B4%EB%93%A0%EB%8B%A4.md) | 7 | 웹의 세 축 위에서 curl 과 ssh 와 rsync 가 일하고, tshark 한 줄에 네 층이 다 보입니다 |
| [08-01. 재는 것은 바깥이고 알고 싶은 것은 안이다](./08-01.%EC%9E%AC%EB%8A%94%20%EA%B2%83%EC%9D%80%20%EB%B0%94%EA%B9%A5%EC%9D%B4%EA%B3%A0%20%EC%95%8C%EA%B3%A0%20%EC%8B%B6%EC%9D%80%20%EA%B2%83%EC%9D%80%20%EC%95%88%EC%9D%B4%EB%8B%A4.md) | 8 | 관측은 바깥을 재서 안을 판정하는 일이고, 신호 셋 중 로그만 사람을 위한 것입니다 |
| [08-02. 자원마다 창이 따로 나 있어 어느 창을 여느냐가 곧 진단이다](./08-02.%EC%9E%90%EC%9B%90%EB%A7%88%EB%8B%A4%20%EC%B0%BD%EC%9D%B4%20%EB%94%B0%EB%A1%9C%20%EB%82%98%20%EC%9E%88%EC%96%B4%20%EC%96%B4%EB%8A%90%20%EC%B0%BD%EC%9D%84%20%EC%97%AC%EB%8A%90%EB%83%90%EA%B0%80%20%EA%B3%A7%20%EC%A7%84%EB%8B%A8%EC%9D%B4%EB%8B%A4.md) | 8 | 명령마다 비추는 자원이 다르고, 원서의 해석 여섯 곳을 man page 와 실측이 정정합니다 |
| [09-01. 이 장을 묶는 것은 주제가 아니라 필요해지는 조건이다](./09-01.%EC%9D%B4%20%EC%9E%A5%EC%9D%84%20%EB%AC%B6%EB%8A%94%20%EA%B2%83%EC%9D%80%20%EC%A3%BC%EC%A0%9C%EA%B0%80%20%EC%95%84%EB%8B%88%EB%9D%BC%20%ED%95%84%EC%9A%94%ED%95%B4%EC%A7%80%EB%8A%94%20%EC%A1%B0%EA%B1%B4%EC%9D%B4%EB%8B%A4.md) | 9 | IPC·VM·현대 배포판·인증·미래 주제를 잇는 것은 내용이 아니라 각각이 필요해지는 조건입니다 |
| [00-03. 레시피와 현대 도구 참조 시트](./00-03.%EB%A0%88%EC%8B%9C%ED%94%BC%EC%99%80%20%ED%98%84%EB%8C%80%20%EB%8F%84%EA%B5%AC%20%EC%B0%B8%EC%A1%B0%20%EC%8B%9C%ED%8A%B8.md) | 부록 A · B | 부록 둘을 한 장으로 합친 조회용 시트. 명령마다 본문 어느 장이 설명했는지를 붙였습니다 |



## 학습 상태

> 다음 세션이 콜드 스타트로 시작하지 않도록 진입 조건을 적어 둡니다.

| 항목 | 값 |
|------|-----|
| 난이도 레벨 | 기본 |
| 진행률 | **11 / 11 — 완주** · 본문 아홉 장은 노트 12편으로, 부록 둘은 참조 시트 한 편으로 합침 |
| 최근 검증 | **2026-09-07 Phase 3 실습과 Phase 4 자답이 남긴 결손 열 자리를 세 편에 걸쳐 메웠습니다.** 이번에는 학습자가 막힌 것이 아니라 **노트에 없는 것을 물어본** 회차라, 실측값을 그대로 옮기고 재현 명령을 함께 적었습니다. `07-01` 에는 IP 헤더 체크섬을 20바이트에서 손으로 세는 절(쪼개 더하기·자리올림 되접기·뒤집기 — TTL 64 → 합 `0xE2FB` → 체크섬 `0x1D04`, 검산은 다 더해 `0xFFFF`), 라우터가 매 홉 다시 세지 않고 `0x1D04 + 0x0100 = 0x1E04` 로 끝내는 증분 갱신, IPv6 에 헤더 체크섬이 없는 이유(대신 UDP 체크섬을 필수로 만들어 메움), **층을 나눈 대가 셋째**(고장 났을 때 어느 겹인지 찾아야 하고 겹마다 캐시가 어긋난다 — DNS TTL·ARP·NFS 속성 캐시를 한 표로 묶음)를 더했습니다. `07-02` 에는 캡처에 찍힌 `ECE`·`CWR` 의 정체와 ECN 협상, **소켓은 층이 아니라 API 추상**이라는 것(패킷을 떠도 소켓 헤더가 없다), A 레코드 다건이 곧 부하 분산이라는 것, CA 가 도메인 소유를 확인하는 HTTP-01·DNS-01 을 더했고 마지막 절은 `07-03` 의 SSH TOFU 와 교차 참조했습니다. `07-03` 에는 `curl` 종료 코드로 부류를 가르는 절(6·7·28 실측 — **`0 ms` 와 `6005 ms` 가 갈라 준다**)과 진단 순서 넷, TOFU 말고 CA 로 호스트 키에 서명하는 길을 더했습니다. 도식은 `07-03.curl-exit-triage.svg` 하나를 세웠습니다(type-process, 요청이 어디까지 갔는지로 실패를 늘어놓음). 회상 문항도 늘렸습니다 — 지금은 07-01 이 학습 목표 11·면접 11·체크리스트 17, 07-02 가 12·13·17, 07-03 이 11·11·19 입니다. 근거는 RFC 1624·3168·8200·8555 원문이고 각주 열하나를 새로 달았으며, 실측한 DNS 응답(`_acme-challenge.example.com` 의 토큰과 `google.com` 의 소유 확인 TXT)은 재현 명령과 함께 실었습니다. **복습 회차 1 파일 셋(`write/_review/2026-09-07/`)의 문항은 바꾸지 않았습니다** — 5축 고정 구조이고 아직 한 자도 안 채운 상태라, 회차 사이 난이도 비교가 깨지지 않게 원본이 두꺼워졌다는 사실만 머리말에 한 줄 적어 회차 2 가 새 자리에서도 뽑도록 했습니다. 앞선 회차 기록: **2026-09-07 `07-03` 을 같은 방식으로 보강해 7장 세 편이 모두 같은 밀도가 됐습니다.** 학습자가 결손을 직접 실측해 표로 가져온 회차라 그 넷을 그대로 메웠습니다 — §4 SSH 가 982자에 서버 인증 축이 통째로 없던 것(`###` 「누가 누구를 믿는가」로 호스트 키·TOFU·대역 밖 지문 확인, `###` 「명령 넷을 언제 쓰는가」로 `ssh-keygen`·`ssh-copy-id`·`ssh-agent`·`~/.ssh/config` 의 `ProxyJump`), §6 마운트가 901자에 도식 0장이던 것(`###` 둘로 세 모델의 사본 위치와 close-to-open·속성 캐시 3~60초·락은 권고일 뿐), §7 의 도구 열 개가 `###` 없이 평평하던 것(하는 일로 넷으로 묶음), §2 의 프래그먼트가 아홉 번 나오는데 「서버로 전송되지 않는다」가 0건이던 것. §5 에는 FTP 가 `scp`·`rsync` 와 **별개 프로토콜**이라는 것과 SFTP≠FTP 를 더했습니다. 도식은 셋을 세웠습니다 — §4 `07-03.ssh-handshake.svg`(type-sequence, 서버 인증과 클라이언트 인증이 서로 다른 걸음), §5 `07-03.transfer-second-run.svg`(type-swimlane, 두 번째 전송에서 갈리는 자리), §6 `07-03.copy-sync-mount.svg`(type-architecture, 사본이 어디에 있는가). 회상 문항도 늘렸습니다(학습 목표 5→9, 면접 문항 5→9, 체크리스트 8→15). 근거는 RFC 4251·959·3986·4217·8881 원문과 `rsync(1)`·`nfs(5)`·`sftp(1)`·`dhcpdump(8)` man page 이고 각주 15개를 새로 달았습니다. **앞선 회차가 남긴 도식 결함 둘도 이때 잡았습니다** — `07-03.advanced-tools.svg` 의 10.5px 한글 5개와 `07-03.file-transfer.svg` 의 2개를 올렸고, 그 file-transfer 는 **점선 상자가 424~504 인데 글자가 518 에 앉아 상자 밖으로 나가 있던 것**을 렌더해서 눈으로 찾아 고쳤습니다(HEAD 에도 있던 결함이고, 그 상자는 raw rect 라 오버플로 검사기가 텍스트와 짝짓지 못합니다). 지금은 07-03 도식 7장이 dd-lint·오버플로·겹침·가독성 error 0 이고 타입 선언 7/7 에 타입도 7종이며, 본문은 벽 단락 0 · 요약 위계 0 · 도식 배치 0 입니다. **이 편만 코드 블록 관례가 다릅니다** — 모든 명령 블록 첫 줄에 한 줄 주석을 달았고, 명령과 결과는 다른 편과 같이 블록을 나눠 두었습니다. 앞선 회차 기록: **2026-09-07 `07-02` 를 앞 편과 같은 방식으로 보강했습니다.** 2026-09-06 학습 세션에서 막힌 자리와 노트 결손이 겹친 일곱 자리에 산문을 넣고, 학습자가 직접 요청한 도식 둘을 세웠습니다 — §2 「헤더에 IP 주소가 없는데 체크섬은 IP 를 봅니다」(의사 헤더)와 RFC 793→9293 현행 병기, §1 `nmap` 이 무슨 도구인지, §4 `ss` 가 대체한 것은 `lsof` 가 아니라 `netstat` 이라는 세대 교체·「소켓이 read 와 write 로 다뤄지는 이유」(파일 디스크립터)·두 도구의 축 구분, §7 「같은 TTL 인데 하나는 초이고 하나는 홉입니다」, §6 「CNAME 이 사는 이유」. 도식은 §6 `07-02.cname-chain.svg` 를 새로 세우고(앞 편 `07-01.layer-swap.svg` 와 같은 논지라 캔버스·칩 문법을 그대로 가져왔습니다) §7 `07-02.dns-lookup.svg` 를 다시 그렸습니다 — 앞 회차가 「루트에서부터 반복 질의」 한 줄로 뭉쳐 둔 것을 리졸버→루트→TLD→권한 다섯 레인으로 펴고 걸음마다 TTL 이 어디 붙는지를 표시했습니다. 회상 문항도 함께 늘렸습니다(학습 목표 5→8, 면접 문항 5→9, 체크리스트 8→12). 근거는 RFC 9293·1034·1035·791·3022 원문과 `socket(2)`·`open(2)`·`stdin(3)`·`proc_pid_fd(5)`·`errno(3)`·`accept(2)`·`ss(8)`·`lsof(8)`·`netstat(8)` man page 이고, 각주 인용 22건을 원문과 글자 단위로 다시 대조했습니다. **앞선 회차가 남긴 결함 셋도 이때 잡았습니다** — 워킹 트리에서 `## 참고 자료` 절과 교차 링크 3개가 지워져 있던 것을 HEAD 원본으로 되돌렸고, `07-02.dns-tree.svg` 의 10.5px 한글 8개와 `07-02.tcp-udp-header.svg` 의 4개를 올렸으며, 그 tcp-udp-header 는 **TCP 7행이 414 까지 내려오는데 UDP 블록을 400 에 두어 겹쳐 있던 것**을 렌더해서 눈으로 찾아 고쳤습니다(겹침 검사기는 글자끼리만 보므로 상자 겹침은 놓칩니다). 지금은 07-02 도식 6장이 dd-lint·오버플로·겹침·가독성 error 0 이고 타입 선언 6/6·타입 6종이며, 본문은 벽 단락 0 · 요약 위계 0 · 도식 배치 0 · 상대 링크 11개 전부 유효입니다. 앞선 회차 기록: **2026-09-06 학습 세션이 드러낸 결손을 `07-01` 에 보강했습니다.** 학습자가 막힌 자리와 노트 결손이 겹쳐, 절 넷과 문단 셋과 도식 둘을 더했습니다 — §2 「서로를 모르기 때문에 갈아 끼울 수 있습니다」(도식 `07-01.layer-swap.svg`), §8 「여러 줄에 걸리면 무엇이 이기는가」(최장 일치, 도식 `07-01.longest-prefix.svg`), §8 「2021년 10월 4일, Facebook 이 사라진 날」, §6 브로드캐스트와 멀티캐스트 구분, §8 traceroute 의 TTL 원리, §7 ICMP 에 포트가 없다는 것, §3 OUI 풀어쓰기. 회상 문항도 함께 늘렸습니다(학습 목표 5→7, 면접 문항 5→7, 체크리스트 8→12). 근거는 RFC 1812·RFC 3022 원문과 Meta Engineering 사후 보고이고 전부 노트의 읽기로 표시했습니다. **앞선 회차가 놓친 도식 결함 둘도 이때 잡았습니다** — `07-01.tcp-ip-stack.svg` 의 9.5px 한글 8개와 `07-01.link-layer.svg` 의 10.5px 3개로, 7장 회차 기록의 「전수 11px 이상」이 이 둘에는 걸리지 않았습니다. 지금은 7장 도식 8장이 dd-lint·오버플로·겹침·가독성 error 0 이고 타입 선언 8/8 이며, 본문은 벽 단락 0 · 요약 위계 0 입니다. 앞선 회차 기록: 2026-09-06 · 전 문서 형식 센서 전수(도식 87장 error 0) + 적대적 내용 검증 전수. 9장은 지적 12건 중 11건 반영 — 셋에만 적힌 것을 넷으로 일반화한 자리, 표시 없이 섞인 노트의 추론 2건, `lsmod` 열 오독을 고치고, 재현되지 않는 1회성 관측을 불변 사실로 적은 FIFO 실험 서술을 다시 썼습니다. 00-03 은 「본문」 열 여섯 칸이 근거 없이 채워진 것이 드러나 아홉 장 노트를 grep 해 전부 다시 세었고, 눅여 두었던 정오 2건을 승격했습니다 |
| 막힌 지점 | **Phase 3·4 가 남긴 것은 막힘이 아니라 노트 결손 열 건이었고 2026-09-07 보강으로 전부 닫혔습니다** — 세 편 전체에서 0건임을 학습자가 직접 실측해 온 것이라 학습자 축이 아닙니다. 앞선 회차 기록: **2026-09-07 Phase 4 통합 자답은 여섯 축을 다 지났습니다** — 원리(홉마다 바뀌는 것과 안 바뀌는 것), 함정(대역 계산과 최장 일치), 트레이드오프(TOFU 대 CA), 실습 연결(무응답 대 RST 진단), 왜·대안(한 겹 더 두기의 값과 대가). TTL 은 이름을 준 뒤에야 회상했고 나머지는 스스로 답했습니다. **복습 회차 1 을 `_review/2026-09-07/` 에 세 편 등록했습니다.** **2026-09-07 `07-03` Phase 1(§1~§7)과 Phase 3 실습에서 셋.** `Host` 헤더가 필요한 이유(경로로 가르면 된다고 답함), 응답 첫 줄의 구성(요청 조각이 그대로 온다고 답함), 3-way 의 셋째 걸음(`ACK` 를 `SYN` 이라 답함)입니다. **Phase 3 은 게이트를 통과했습니다.** rsync 의 mtime 대 체크섬 비교를 예측하고 직접 반증했고(`touch` 만으로 전송 대상이 되고 `-c` 를 주면 빠지는 것), `tshark` 캡처에서 3-way·요청 첫 줄·상태 코드·FIN 을 개념과 짝지었습니다. **의도하지 않은 관측도 스스로 읽었습니다** — 같은 캡처에 잡힌 `SYN` 재전송을 보고 「응답이 없다」와 「거절당했다」가 다른 지문이라는 데까지 갔습니다. 첫날 개념으로 말한 「목적지 IP 는 안 바뀌고 MAC 만 홉마다 다시 쓰인다」를 `ip.dst` 와 `eth.dst` 대조로 실측 확정했습니다. **「방화벽 개념이 라우팅 자리로 샌다」 패턴은 두 회차 연속 0회입니다.** 같은 날의 결손 보강 기록: **같은 날 학습자가 실측해 온 `07-03` 노트 결손 넷은 2026-09-07 보강으로 닫혔습니다** — §4 SSH 의 서버 인증 축(known_hosts·호스트 키·TOFU 각 0건), §6 마운트의 도식·`sshfs`·캐시 일관성 0건, §7 의 `###` 0개, §2 프래그먼트의 「서버로 전송되지 않는다」 0건. 학습자 축은 따로입니다. **2026-09-07 `07-03` Phase 1(§1~§3)에서 둘.** 가상 호스팅에서 `Host` 헤더가 필요한 이유(경로로 가르면 된다고 답함)와 응답 첫 줄의 구성(요청 조각이 그대로 돌아온다고 답함)입니다. **막힘이 줄고 성격도 달라졌습니다** — 앞 회차의 「방화벽 개념이 라우팅 자리로 샌다」 패턴이 이번엔 한 번도 안 나왔습니다. 강점도 드러났습니다. 「배달에는 이름이 필요 없는데 통신에 왜 필요한가」를 스스로 물어 계층 경계를 정확히 짚었고, 프래그먼트는 「스프링에서 본 적이 없다」는 자기 경험으로 성질을 맞혔습니다. **`07-03` 결손 1건** — 프래그먼트가 9번 나오는데 「서버로 전송되지 않는다」는 한 줄이 없어 학습자가 못 찾았습니다. 앞선 회차 기록: **2026-09-06 `07-02` Phase 1(§1~§7)에서 여섯.** 다만 성격이 갈립니다 — 학습자 축은 셋(포트 칸 폭을 스스로 못 도출, 출발지 포트의 이유를 또 방화벽으로 답함, DNS 의 둘째 문제를 보안으로 답함)이고, 나머지 셋(파일 디스크립터·`netstat`·DNS TTL 대 IP TTL)은 **노트에 아예 없어서** 막힌 것이라 학습자 탓이 아닙니다. **방화벽 개념이 새는 패턴은 4회차에서 멎었습니다** — 출발지 포트에서 한 번 더 나왔지만 학습자가 스스로 잡아냈고 그 뒤로는 안 나왔습니다. conntrack 도 5-tuple 이라는 제 자리를 찾았습니다. **그 셋을 포함한 산문 일곱과 도식 둘을 2026-09-07 에 보강해 노트 결손 축은 닫혔습니다** — 의사 헤더·DNS TTL 구분·nmap·ss↔netstat·파일 디스크립터·lsof 역할 분담·RFC 9293 과 DNS 조회 흐름·CNAME 갈아 끼우기입니다. 다음 회차에는 학습자 축 셋만 다시 물어 남았는지 보면 됩니다. 앞선 회차 기록: **`07-01` Phase 1(§1~§8)에서 다섯.** 층 규칙의 귀결 축에 셋 — 「각 층은 바로 위아래만 안다」는 규칙을 §2 에서 스스로 못 찾음, 방화벽이 포트를 읽는 것을 「IP 층이 규칙을 어긴 것」으로 읽음, 이더넷이 IP 주소를 쓰면 불투명성이 깨진다는 귀결을 두 번 시도에도 못 도출. 용어가 새는 축에 둘 — ARP 캐시 자리에 conntrack 을, Longest Prefix Match 자리에 iptables 순차 매칭을 놓음. **둘 다 방화벽·netfilter 개념이 라우팅 자리로 새는 같은 패턴이라 07-02 에서 표적 보완합니다.** |
| 다음 레슨 후보 | 복습 회차 1 자답(`write/_review/2026-09-07/` 셋). 7장 Phase 1·3·4 는 모두 마쳤습니다. 시리즈 마감 산출물(`00-01.용어집`·`00-02.결정 치트시트`)은 그 뒤로 미룹니다 |
| 보강 후보 | **`07-03` §3 의 `curl` 종료 코드 사다리가 셋(6·7·28)에서 끊깁니다.** 2026-09-10 복습 회차 3 에서 학습자가 「이 셋 말고 더 기억할 코드가 있는가」를 물어 드러난 결손입니다. 지금 사다리는 `이름 → 포트 → 무응답` 까지만 가고 **TLS 층과 앱 응답 층이 통째로 비어 있습니다** — TCP 는 붙었는데 인증서에서 깨진 경우, 응답이 왔는데 내용이 문제인 경우가 갈 자리가 없습니다. 후보 축은 셋입니다: TLS(연결·인증서 검증 실패), 앱 응답(`-f` 를 켰을 때의 4xx·5xx 와 빈 응답), 리다이렉트 한도(`-L` 옵션 표와 짝인데 실패 쪽이 비어 있음). ⚠️ **번호는 반드시 `man curl` 의 EXIT CODES 절을 열어 축자 대조하고 실측한 뒤 적습니다** — 진단 사다리는 번호가 틀리면 통째로 틀어지므로 기억으로 채우지 않습니다. 표를 늘리면 `07-03.curl-exit-triage.svg` 도 같이 손봐야 합니다(층이 늘어나므로) **같은 §3 에 결손이 둘 더 있습니다.** 첫째 — **종료 코드 6 이 세 원인을 하나로 뭉갭니다.** `curl` 은 리졸버가 실패했다는 사실만 받으므로 `NXDOMAIN`(이름이 없다)·`SERVFAIL`(서버가 조회를 못 했다)·무응답(서버에 못 닿는다)이 전부 6 으로 옵니다. 노트가 「다음에 볼 것: `dig`」 라고 적은 이유가 바로 이것인데 **그 셋이 갈린다는 서술이 본문에 없어**, 학습자가 2026-09-10 회차 3 에서 정확히 이 자리를 되물었습니다. `dig` 출력의 `status:` 로 셋을 가르는 한 문단이 필요합니다. 둘째 — **사다리 맨 끝 칸에 관측 명령이 없습니다.** §3 은 「accept 큐 포화를 의심합니다」 한 문장으로 끝나고 **그것을 무엇으로 보는지**를 안 적습니다. `ss -tln` 의 `LISTEN` 행에서 `Recv-Q` 는 accept 큐에 지금 쌓인 수, `Send-Q` 는 backlog 최대치이고 둘이 같으면 포화라는 것, 큐가 SYN 큐와 accept 큐 **둘**이라는 것, `listen()` 의 backlog 는 accept 큐의 한도이고 SYN 큐는 `net.ipv4.tcp_max_syn_backlog` 가 따로 정한다는 것, 그리고 **3-way 가 끝나는 순간 이미 `ESTABLISHED` 라 `accept()` 는 상태를 바꾸지 않고 fd 만 건넨다**(그래서 앱이 멈춰도 클라이언트에는 연결이 성공한 것으로 보입니다)는 것이 들어가야 합니다. ⚠️ 이 대목은 `man 2 listen`·`man 2 accept`·`man 7 tcp` 로 축자 대조하고 실측한 뒤 적습니다. **`_review/2026-09-09` 회차 2 정답 4 가 `Recv-Q`/`Send-Q` 표에 「원본 §3」 을 단 것은 잘못된 출처 표기였습니다** — 보강이 들어가면 그 표기가 사실이 되지만, 그 전까지는 원문 밖입니다. |
| 발견한 원문 정오 | 39건 — 1장 1(CPU 모델명) · 2장 4(단위 목록 문구·eBPF 최소 버전·`mallocopt` 표기·시스템 콜 명령어) · 3장 4(`set` 으로 변수 만들기·`unset $VAR`·Ctrl+Z 서술·날짜 예제 입력값) · 4장 1(`/proc/$pid` 로 실제 UID 조회) · 5장 5(`mount -a`·`/proc/self/net/arp`·`devfs` 이름·`var` 행 복붙·AUFS 시제) · 6장 5(`initd`·Ruby 칸 Rails·`ENTRYPOINT` vs `CMD`·`prune -all`·`ARGS`) · 7장 5(멀티캐스트 `/24`·인프라 TLD 예·SRV 를 TTL 로 읽음·URI 순서·`rsync` 목적지 오타) · 8장 7(RFC 5424 `VER` 을 생략 가능이라 적음 · `2&>` 리다이렉션 · 부하 평균이 정규화됐다는 서술 · `sys` 를 대기 시간이라 부름 · `top` 의 `m` 을 정렬 키라 적음 · Prometheus UI 를 `localhost:9000` 이라 적음 · Grafana 데이터소스를 `9100` 이라 적음) · 9장 1(시그널 핸들러가 기본 동작 *전에* 실행된다고 적음) · 부록 A 6(`cat` 으로 문자열 넘김 · 지역 시각에 `Z` 를 붙임 · Fish 문법의 `git log` · `git diff --color-moved` 설명 · `head -c 450m` 을 450,000,000 바이트라 함 · `ls -l | wc -l` 을 파일 수 세기라 함) |

원서 PDF 는 Google Drive 의 `내 드라이브/book/Learning Modern Linux/` 에 장별로 나뉘어 있습니다. 1장은 2026-09-05 에 추가됐고, 나머지는 2026-07-16 에 함께 받은 파일입니다.



## 출처와 톤

- 원서: 《Learning Modern Linux》 Michael Hausenblas, O'Reilly, 2022년
- 노트는 합니다체로 통일합니다. 같은 폴더의 기존 두 책 노트가 다른 톤이어도 따라가지 않습니다.
- 사실·수치·코드는 원서 PDF 에서 추출한 것만 씁니다. 책 밖 보강은 절을 나누고 공식 1차 자료 링크를 각주로 남깁니다.
- 원문의 오류는 조용히 고치지 않고 `원문 정오` 인용 블록으로 병기합니다. 원서를 다시 폈을 때 혼동하지 않게 하는 것이 목적입니다.
- **코드 블록 관례 예외 하나.** `07-03` 한 편만 모든 명령 블록의 첫 줄에 그 명령이 무엇을 하는지 한 줄 주석을 답니다. 2026-09-07 학습자 요청으로 이 편에서만 시험해 보는 것이라 다른 편에는 퍼뜨리지 않습니다. 명령과 결과를 블록으로 나누는 것은 폴더 전체가 같습니다.



## 관련 문서

- [02_os — OS 공통 기반](../../README.md) — 이 카테고리의 MOC
- [systems-performance](../systems-performance/README.md) — 같은 주제를 성능 분석 관점에서 깊게
- [linux-kernel-programming](../linux-kernel-programming/README.md) — 같은 주제를 커널 개발자 관점에서 깊게
- [Kubernetes 네트워크 학습 로드맵](../../../network-roadmap.md) — 이 책 7장이 0단계로 지정된 자리
