# 03-05 §3 BBR — 전송 중인 양이 대역폭·지연 곱에 이르면 파이프가 딱 차고, 그 이상은 큐만 만든다.
# 본문 근거(§3 「BBR — 파이프를 꽉 채우되 큐는 안 만듭니다」): 전송 중 양이 대역폭·지연 곱에 이르면 파이프가 찬다 ·
#   더 넣으면 지속적인 큐가 생겨 처리량은 안 늘고 RTT 만 는다 · 긴 주기로 파이프를 비워 더 낮은 RTT 를 살피고,
#   짧은 주기로 가속·순항·감속을 돈다. 두 곡선의 꺾임은 그 논리를 그린 개략이다.
# 타입 스펙: type-line — 전송 중 양에 대한 RTT 와 처리량 두 판. 대역폭·지연 곱이 둘의 공통 꺾임점.
import sys; sys.path.insert(0, ".")
from _cc_common import Chart
from dd import D, ACC, MUTED, SOFT, INK, OK, WARN, BAD, INFO, RULE, KR, MONO

W, H = 1000, 600
d = D(W, H, "COMPUTER NETWORKING TOP-DOWN · 03-05 §3",
      "BBR — 파이프가 딱 차는 지점을 숫자로 잡습니다",
      "전송 중 데이터 양을 늘리면 처리량은 대역폭·지연 곱까지만 오르고, 그 뒤로는 RTT 만 는다. 손실 기반은 오른쪽 끝 버퍼가 넘칠 때야 물러서고, BBR 은 꺾임점에 머문다.",
      "두 판의 가로축이 같음. 세로 점선이 대역폭·지연 곱, 파이프가 딱 차는 자리입니다")

BDP = 0.45
xt = [(0, "0"), (BDP, "대역폭·지연 곱"), (1, "버퍼 넘침")]
c1 = Chart(d, 24, 110, 470, 360, "전송 중인 데이터의 양", "왕복 시간 RTT", xt, [(0, ""), (0.3, "전파 시간"), (1, "")])
c1.series([(0, 0.3), (0.15, 0.3), (0.3, 0.3), (BDP, 0.3), (0.6, 0.5), (0.8, 0.77), (1.0, 1.0)], WARN, focal=True)
d.line(c1.px(BDP), c1.T, c1.px(BDP), c1.B, ACC, 0.9, "3 4")
c1.note(0.04, 0.42, "여유 — RTT 는 전파 시간 그대로", MUTED, "start")
c1.note(0.48, 0.86, "큐가 쌓여 RTT 만 늠", WARN, "start")

c2 = Chart(d, 506, 110, 470, 360, "전송 중인 데이터의 양", "처리량", xt, [(0, ""), (0.78, "병목 대역폭"), (1, "")])
c2.series([(0, 0), (0.15, 0.26), (0.3, 0.52), (BDP, 0.78), (0.6, 0.78), (0.8, 0.78), (1.0, 0.78)], INFO, focal=True)
d.line(c2.px(BDP), c2.T, c2.px(BDP), c2.B, ACC, 0.9, "3 4")
c2.note(0.04, 0.9, "더 넣을수록 더 나감", MUTED, "start")
c2.note(0.48, 0.62, "더 넣어도 처리량은 그대로", INFO, "start")
c2.note(0.98, 0.10, "손실 기반은 여기서야 물러섬", BAD, "end")

d.t(24, 504, "BBR 은 RTT 와 병목 대역폭을 직접 재서 이 꺾임점을 추정하고, 전송 중인 양을 거기에 맞춤", 11, MUTED, KR, "start")
d.t(24, 524, "짧은 주기로 가속·순항·감속을 돌고, 긴 주기로 파이프를 비워 더 낮은 RTT 가 있는지 살핌", 11, MUTED, KR, "start")
d.legend(H - 44, [("RTT", WARN), ("처리량", INFO), ("대역폭·지연 곱 — BBR 이 머무는 자리", ACC), ("손실 기반이 물러서는 자리", BAD)])
d.save("03-05.bbr-bdp.svg")
print("ok bbr-bdp")
