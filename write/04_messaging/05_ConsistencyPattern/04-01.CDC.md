---
title: 04-01.CDC
tags: [cdc, debezium, kafka-connect, binlog, wal, logical-replication]
status: final
source:
  - https://debezium.io/documentation/reference/stable/index.html
  - https://www.confluent.io/blog/cdc-and-streaming-analytics-using-debezium-kafka/
  - https://github.com/debezium/debezium
related:
  - 02-01.Outbox.md
  - 02-02.Outbox 스케일링.md
  - ../03_TopicDesign/01-01.토픽 디자인.md
  - ../04_BrokerArchitecture/01-01.메시지 큐 아키텍처.md
updated: 2026-05-16
---

# Debezium CDC 설정

---

> CDC(Change Data Capture)는 DB의 *binlog·WAL*을 따라가며 row 변경을 이벤트로 흘려보내는 패턴이다. Debezium은 그 대표 구현이고, Kafka Connect 위에서 동작한다. Outbox 폴링의 대안으로 ms 단위 지연을 달성하지만 운영 인프라가 커진다.



## 학습 목표

> CDC가 *폴링 Outbox의 대안*으로 어떤 이점과 운영 비용을 만드는지 이해한다.

이 장을 다 읽고 다음 다섯 가지에 자신 있게 답할 수 있으면 학습이 완료된다.

1. binlog(MySQL)·WAL(PostgreSQL)을 따라가는 logical replication 모델을 설명할 수 있다.
2. Debezium이 Kafka Connect 위에서 동작하는 구조와 connector 설정의 핵심을 설명할 수 있다.
3. CDC가 폴링 Outbox 대비 지연·처리량에서 어떤 이점을 만드는지 설명할 수 있다.
4. CDC의 운영 비용(별도 인프라, 권한, 스키마 변경 영향)을 비교할 수 있다.
5. Outbox 테이블을 CDC가 읽는 패턴과 비즈니스 테이블을 직접 읽는 패턴의 차이를 설명할 수 있다.

# Debezium CDC 설정

---

> Debezium은 Redpanda의 기술도 DB에 내장된 기술도 아닙니다. Red Hat이 2015년에 만든 독립 오픈소스 CDC 플랫폼으로, Apache License 2.0이므로 개인/기업 모두가 무료로 사용할 수 있습니다.

Debezium은 DB의 변경 로그(WAL, binlog)를 읽어서 이벤트로 변환하는 역할을 수행하며, DB자체가 CDC를 제공하는게 아닌, DB가 내부적으로 기록하는 변경 로그를 Debezium이 외부에서 읽는 구조입니다.

| DB             | 변경 감지                           | 커넥터                          | DB 설정             | 비고                                                  |
| -------------- | ----------------------------------- | ------------------------------- | ------------------- | ----------------------------------------------------- |
| **PostgreSQL** | WAL + Logical Decoding (`pgoutput`) | `debezium-connector-postgresql` | `wal_level=logical` | Replication Slot 필수, PK 없는 테이블은 이벤트 미발행 |
| **MySQL**      | Binlog                              | `debezium-connector-mysql`      | `binlog_format=ROW` | DDL 변경도 자동 추적                                  |
| **MariaDB**    | Binlog                              | `debezium-connector-mariadb`    | `binlog_format=ROW` | Debezium 2.7부터 MySQL과 별도 커넥터로 분리           |

- 이 문서 기준은 PostgreSQL기준입니다. 다른DB에서는 wal_level대신 binlog_format=ROW 설정만 다르고, 나머지 Debezium 커넥터 등록 흐름은 동일합니다.

### Docker-compose

```yaml
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: orderdb
      POSTGRES_USER: orderuser
      POSTGRES_PASSWORD: orderpass
    command:
      - postgres
      - -c
      - wal_level=logical        # CDC 필수
      - -c
      - max_wal_senders=10
      - -c
      - max_replication_slots=10
    ports:
      - "5432:5432"

  redpanda:
    image: docker.redpanda.com/vectorized/redpanda:v25.3.1
    command:
      - redpanda start --smp 1 --memory 1G
      - --kafka-addr internal://0.0.0.0:9092,external://0.0.0.0:19092
    ports:
      - "19092:19092"

  kafka-connect:
    image: quay.io/debezium/connect:2.7
    depends_on: [redpanda, postgres]
    ports:
      - "8083:8083"
    environment:
      BOOTSTRAP_SERVERS: redpanda:9092
      GROUP_ID: "debezium-cluster"
      CONFIG_STORAGE_TOPIC: "debezium-config"
      OFFSET_STORAGE_TOPIC: "debezium-offset"
      STATUS_STORAGE_TOPIC: "debezium-status"
```

- WAL을 logical 수준으로 등록해야 Debezium이 행 단위 변경사항(INSERT, UPDATE, DELETE)를 읽을 수 있습니다. 기본값 replica로는 물리적 블록 수준만 읽힙니다.

### Debezium Connector 생성

```bash
curl -X POST http://localhost:8083/connectors \
  -H "Content-Type: application/json" \
  -d '{
    "name": "order-outbox-connector",
    "config": {
      "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
      "database.hostname": "postgres",
      "database.port": "5432",
      "database.user": "orderuser",
      "database.password": "orderpass",
      "database.dbname": "orderdb",
      "database.server.name": "orderdb",
      "table.include.list": "public.outbox", # 스키마 
      "plugin.name": "pgoutput",

      "transforms": "outbox",
      "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
      "transforms.outbox.table.field.event.key": "aggregate_id",
      "transforms.outbox.table.field.event.type": "event_type",
      "transforms.outbox.table.field.event.payload": "payload",
      "transforms.outbox.route.topic.replacement": "${routedByValue}-events"
    }
  }'
```

| 설정                      | 값                        | 설명                                         |
| ------------------------- | ------------------------- | -------------------------------------------- |
| `table.include.list`      | `public.outbox`           | Outbox 테이블만 모니터링                     |
| `transforms.outbox.type`  | `EventRouter`             | Outbox 전용 SMT                              |
| `route.topic.replacement` | `${routedByValue}-events` | `aggregate_type=Order` → `order-events` 토픽 |



## 1. EventRouter 토픽 라우팅 전략

### 1-1. Aggregate 단위 토픽 (권장)

aggregate_type으로 라우팅한다. Order의 모든 이벤트(생성/취소/수정)이 하나의 order-events 토픽에 들어가고, Consumer가 event_type 헤더로 필터링 합니다.

```json
{
  "transforms.outbox.route.by.field": "aggregate_type",
  "transforms.outbox.route.topic.replacement": "${routedByValue}-events"
}
```

```bash
outbox 행: aggregate_type=Order, event_type=order-created    → order-events 토픽
outbox 행: aggregate_type=Order, event_type=order-cancelled  → order-events 토픽
outbox 행: aggregate_type=Payment, event_type=payment-completed → payment-events 토픽
```

- 커넥터 설정의 transforms.outbox.table.field.event.type 속성이 outbox 테이블의 event_type 컬럼 값을 Kafka 메시지 헤더 eventType에 매핑한다.

```java
@KafkaListener(topics = "order-events")
public void handle(ConsumerRecord<String, String> record) {
    String eventType = new String(
        record.headers().lastHeader("eventType").value());

    switch (eventType) {
        case "order-created"   -> handleCreated(record);
        case "order-cancelled" -> handleCancelled(record);
    }
}
```

- 단일 토픽방식이 권장되는 이유는 같은 Aggregate의 이벤트가 같은 토픽에 있으므로 aggregate_id 기준으로 파티셔닝이 만들어져 순서가 보장된다.
- 토픽 수가 적어 운영부담이 낮고, 새로운 이벤트 타입을 추가해도 토픽을 새로 추가할 필요가 없다.

### 1-2. Event Type 단위 토픽

event_type으로 라우팅한다. 이벤트 타입마다 전용 토픽이 생긴다.

```json
{
  "transforms.outbox.route.by.field": "event_type",
  "transforms.outbox.route.topic.replacement": "${routedByValue}"
}
```

```bash
outbox 행: event_type=order-created    → order-created 토픽
outbox 행: event_type=order-cancelled  → order-cancelled 토픽
outbox 행: event_type=payment-completed → payment-completed 토픽
```

- Consumer가 필요한 이벤트만 구독할 수 있어 필터링 로직이 불필요하다.

```java
@KafkaListener(topics = "order-created")
public void handleOrderCreated(ConsumerRecord<String, String> record) {
    // order-created 이벤트만 수신 — 필터링 불필요
}
```

- 별도의 필터링 로직은 필요없으나, 이벤트 타입이 늘어날수록 토픽 수가 늘어나며, 토픽마다 파티션/레플리카 관리가 필요해집니다.
- 같은 Order에 대한 created, cancelled 이벤트가 서로 다른 토픽에 있으므로, 하나의 Consumer에서 순서를 맞추려면 별도 조정이 필요할 수 있습니다.

### 1-3. 스키마 전략과의 관계

Schema Registry의 기본 네이밍 전략(TopicNameStrategy)은 토픽당 하나의 스키마만 허용합니다. 전략 1처럼 하나의 토픽에 여러 이벤트가 섞인다면 하나의 Avro 스키마로는 표현할 수 없습니다. 

이 충돌을 해결하는 방법은 3가지가 있습니다.

| 방식                              | 설명                                                         |              |
| --------------------------------- | ------------------------------------------------------------ | ------------ |
| **Event Type 단위 토픽 (전략 2)** | 이벤트 타입별 토픽 분리. 토픽 = 스키마 1:1 자연스럽게 유지   | 1:1          |
| **TopicRecordNameStrategy**       | SR 네이밍 전략 변경. 스키마 subject가 `{토픽}-{레코드명}`이 되어 같은 토픽에 여러 Avro 스키마 공존 | 1:N          |
| **Envelope 스키마**               | 외부 구조를 통일하고(`id`, `event_type`, `payload`), payload를 JSON 문자열로 저장. 토픽 스키마는 하나지만 payload 내부는 자유 | 1:1 (껍데기) |

- Outbox 패턴에서는 Envelope 방식이 가장 자연스럽습니다. 
- 토픽 1 = 스키마 1을 엄격하게 유지하면서 이벤트 타입별 Avro 스키마 검증까지 원한다면 전략2가 가장 명확한 선택입니다.



## 2. CDC 이벤트 구조

> Debezium이 발행하는 이벤트에는 변경 전(before), 변경 후(after) 상태가 모두 담겨 있어, 다운스트림 서비스가 정확히 무엇이 바뀌었는지 판단할 수 있습니다.

```json
{
  // 변경 전
  "before": null,
  
  // 변경후
  "after": {
    "id": "ord-123",
    "customer_id": "cust-456",
    "total": 59900,
    "status": "PENDING"
  },
  
  // 변경 유형(c, u, d, r)
  "op": "c",
  "ts_ms": 1708420000000,
  "source": {
    "db": "shop",
    "table": "orders",
    "lsn": 12345678 // WAL 위치(Debezium 재시작 시 이 위치부터 재개)
  }
}
```

### 복제 슬롯 모니터링(PostgreSQL 이슈)

복제 슬롯은 PostgreSQL이 소비자가 WAL을 어디까지 읽었는지 기억하는 북마크입니다.

Debezium이 커넥터를 생성하면 PostgreSQL에 복제 슬롯이 하나 만들어지며, 2가지 역할을 수행합니다.

1. Debezium 재시작 시 슬롯에 기록된 LSN(Log Sequence Number) 위치부터 재개하여 이벤트 유실 방지
2. 슬롯의 소비자가 아직 읽지 않은 WAL을 PostgreSQL이 삭제하지 않고 보존한다.

문제는 2번째 역할에 대해서, Debezium이 장시간 중단되면 WAL을 계속 보유하려 해서 디스크가 고갈될 수 있습니다 운영환경에서 반드시 모니터링 해야합니다.

```sql
-- 복제 슬롯 WAL 누적량 확인
SELECT slot_name,
       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS wal_behind
FROM pg_replication_slots;

-- wal_behind가 수 GB를 넘으면 Debezium 커넥터 상태를 즉시 확인
```

### MySQL/MariaDB 비교

MySQL/MariaDB는 반대로 소비자와 무관하게 binlog_expire_logs_seconds 설정에 따라 자동 삭제되며, Debezium이 장시간 중단되면 문제가 반대로 발생한다.

| DB                | Debezium 중단 시 동작                                     | 위험                               | 대응                                                         |
| ----------------- | --------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------ |
| **PostgreSQL**    | 복제 슬롯이 WAL 삭제를 막음 → WAL 누적                    | 디스크 고갈                        | 슬롯 WAL 누적량 모니터링, 임계치 초과 시 알림                |
| **MySQL/MariaDB** | binlog가 보관 기한 후 자동 삭제 → Debezium 읽기 위치 유실 | 재개 불가, 전체 스냅샷 재수행 필요 | binlog 보관 기한을 충분히 설정 (기본 30일), Debezium 상태 모니터링 |



---

> **TPS 적용 사례** — `okestro/tps-gitlab2` (CDC 미사용)
>
> - **상태**: TPS는 Debezium 등 CDC 도구 대신 폴링 기반 Outbox를 채택. `OutboxPoller`가 `TB_TRB_OX_001`을 직접 SELECT한다.
> - **선택 이유**: Kafka Connect + Debezium 별도 운영 인프라 회피, 트래픽 규모가 폴링으로 충분한 수준, DBA 권한 분리 환경에서 logical replication 권한 확보 비용 회피.
> - **CDC 도입 검토 시**: `08_advanced/01_variants/01-01.Outbox 변종 비교`에서 폴링 vs Debezium 트레이드오프 매트릭스를 본다. 트리거 시그널은 (a) 폴러가 단일 인스턴스 한계에 가까워질 때, (b) 지연이 ms 단위로 요구될 때.



## 면접 대비 Q&A

> 면접에서 자주 나오는 형태로 5개. 답을 보지 않고 먼저 입으로 답해 본 뒤 비교한다.

### Q1. CDC가 binlog/WAL을 따라간다는 의미는?

DB는 commit 직전에 *변경 사항을 로그 파일에 먼저 기록*한다(MySQL은 binlog, PostgreSQL은 WAL). CDC 도구는 그 로그 파일을 *replica처럼 구독*해 변경을 이벤트로 흘려보낸다. 결과적으로 *DB가 알고 있는 모든 변경*을 누락 없이, ms 단위 지연으로 캡처한다. 애플리케이션 코드 변경 없이 동작한다는 점이 가장 큰 이점이다.

### Q2. Debezium이 Kafka Connect 위에서 동작하는 구조는?

Debezium은 *Source Connector* 플러그인이다. Kafka Connect 워커가 Debezium 플러그인을 로드하고, DB 측에서 변경을 받아 Kafka 토픽에 발행한다. connector 설정은 `database.hostname`, `database.user`, `table.include.list`, `topic.prefix` 같은 키로 구성된다. Kafka Connect의 *분산 모드*로 다중 워커를 띄우면 connector가 자동 분산된다.

### Q3. CDC가 폴링 Outbox 대비 만드는 이득은?

세 가지다. (1) *지연*: ms 단위. binlog 갱신과 거의 동시에 이벤트가 발행된다. (2) *애플리케이션 코드 무관*: `EventPublisher.publish()` 같은 코드를 없애도 DB 변경이 자동 캡처된다. 단 도메인 의미 표현이 빈약해진다. (3) *순서 보장*: binlog 자체가 순차적이므로 같은 row의 변경 순서가 자연스럽게 보존된다.

### Q4. CDC의 운영 비용은?

(1) 별도 인프라: Kafka Connect 클러스터를 띄우고 운영해야 한다. (2) DB 권한: logical replication 또는 binlog 읽기 권한이 필요해 *DBA와의 협의*가 따라온다. (3) 스키마 변경 영향: DB 컬럼 추가/삭제가 토픽 스키마에 즉시 반영되어 Consumer 코드를 같이 바꿔야 한다. (4) replication lag 모니터링: CDC 자체가 늦으면 *DB 사이즈가 무한 증가*하는 위험이 있다.

### Q5. CDC가 *비즈니스 테이블*을 직접 읽는 패턴이 위험한 이유는?

도메인 *의도*가 사라진다. "주문 생성됨"이라는 비즈니스 이벤트가 토픽에는 `orders.INSERT { ... }` 같은 *DB 변경 이벤트*로만 도달한다. Consumer는 그걸 다시 도메인 이벤트로 해석해야 하고, *컬럼 추가가 곧 토픽 스키마 변경*이 되어 결합이 강해진다. 그래서 CDC를 쓸 때도 *Outbox 테이블을 CDC가 읽는* 패턴이 더 깔끔하다. 도메인 의도는 Outbox row에 명시되고, CDC는 단순 전달자가 된다.



## 관련 문서

- [02-01.Outbox](02-01.Outbox.md) — Outbox + CDC 결합 패턴
- [02-02.Outbox 스케일링](02-02.Outbox%20스케일링.md) — 폴링 한계와 CDC 전환 트리거
- [01-01.토픽 디자인](../03_TopicDesign/01-01.토픽%20디자인.md) — CDC 토픽 명명 규칙
- [01-01.메시지 큐 아키텍처](../04_BrokerArchitecture/01-01.메시지%20큐%20아키텍처.md) — CDC가 발행하는 토픽의 처리량 계획
