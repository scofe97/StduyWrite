---
title: 실전 — 바이트코드 치환과 실행
tags: [jvm, bytecode-manipulation, constant-pool, hacksystem, instrumentation, classloader]
status: draft
source:
  - 《JVM 밑바닥까지 파헤치기》(저우즈밍, 위키북스) §9.3.3~§9.4
  - https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-4.html#jvms-4.4.7   # CONSTANT_Utf8_info
related:
  - ./04-03.실전 — 원격 실행 기능 설계.md
  - ./04-01.톰캣의 클래스 로더 아키텍처.md
  - ../ch06_class-file/01-01.클래스 파일 구조.md
updated: 2026-06-10
---

# 실전 — 바이트코드 치환과 실행
---
> §9.3.3~§9.4를 한 줄로 압축하면 — **상수 풀의 `System` 문자열을 `HackSystem`으로 바꾸려면 단순 치환이 아니라 *길이 필드까지 함께 고치는* 바이트 조작이 필요하며, 이를 `ByteUtils`가 처리하고 `JavaClassExecuter`가 전체를 묶어 실행합니다.** 핵심은 "상수 풀의 Utf8 항목은 앞 2바이트가 길이라, 문자열을 줄이면 길이 필드도 16→10으로 고쳐야 한다"는 디테일이며, 이 실습이 3부(7·8·9장) 전체를 종합합니다.

이 글을 읽고 나면 상수 풀 문자열 치환에서 길이 보정이 왜 필요한지 설명하고, `JavaClassExecuter`가 치환·적재·실행을 어떻게 묶는지 말하며, 이 기술이 Java Agent·바이트코드 계측의 토대가 되는 이유를 그림 없이 짚을 수 있습니다.


## 진입 — 설계를 코드로 완성하기

> [앞 글](./04-03.실전%20—%20원격%20실행%20기능%20설계.md)이 "상수 풀의 System을 HackSystem으로 바꾼다"는 아이디어였다면, 이 글은 그 치환을 *바이트 단위로 어떻게 하는가*입니다.

앞 글에서 `System`을 `HackSystem`으로 갈아끼우는 설계를 봤습니다. 이제 그 치환을 실제 바이트로 옮깁니다. 막상 해 보면 *길이 차이*라는 함정이 있습니다. `"java/lang/System"`은 16바이트, `"HackSystem"`은 10바이트라, 단순히 문자열만 바꾸면 클래스 파일이 깨집니다.

![바이트코드 치환과 실행](_assets/04-04-bytecode-swap.svg)


## 1. 상수 풀 Utf8 항목과 길이 보정

> 상수 풀의 Utf8 항목은 *앞 2바이트가 길이*입니다. 문자열을 16자에서 10자로 줄이면 그 길이 필드도 16→10으로 고치고, 줄어든 만큼 뒤 바이트를 당겨야 합니다.

`System`의 클래스 이름은 상수 풀에 `CONSTANT_Utf8_info` 항목으로 들어 있습니다. [클래스 파일 구조](../ch06_class-file/01-01.클래스%20파일%20구조.md)에서 본 이 항목의 포맷은 *앞 2바이트가 문자열 길이(`length`)*, 그 뒤가 실제 바이트입니다.

```
CONSTANT_Utf8_info {
    u1 tag;            // 1 (Utf8 태그)
    u2 length;         // 문자열 바이트 길이  ← 여기를 함께 고쳐야 함
    u1 bytes[length];  // 실제 UTF-8 바이트
}
```

`"java/lang/System"`(16바이트)을 `"HackSystem"`(10바이트)으로 바꾸면, `length` 필드도 `16`에서 `10`으로 고쳐야 합니다. 그리고 6바이트가 줄었으니, 그 항목 뒤의 모든 바이트를 *6바이트 앞으로 당겨* 붙여야 합니다. 단순 문자열 치환이 아니라 *길이 필드 + 실제 바이트 + 뒤따르는 데이터 이동*을 함께 처리해야 합니다.

이 바이트 조작을 `ByteUtils`가 맡습니다. 2바이트 정수(길이)를 읽고 쓰는 `bytes2Int`·`int2Bytes`, 그리고 바이트 배열의 일부를 다른 길이로 교체하는 `bytesReplace` 같은 도구를 제공합니다.

```java
public class ByteUtils {
    // 2바이트(또는 N바이트)를 정수로 — 상수 풀 length 필드 읽기
    public static int bytes2Int(byte[] b, int start, int len) {
        int sum = 0;
        for (int i = start; i < start + len; i++) {
            int n = b[i] & 0xff;          // 부호 확장 방지 — byte 는 부호 있음
            n <<= (--len) * 8;            // 상위 바이트일수록 더 왼쪽으로 시프트
            sum = n + sum;
        }
        return sum;
    }

    // 정수를 N바이트로 — 새 length 필드 쓰기
    public static byte[] int2Bytes(int value, int len) {
        byte[] b = new byte[len];
        for (int i = 0; i < len; i++) {
            // 하위 바이트부터 끝에서 채움
            b[len - 1 - i] = (byte) ((value >> (8 * i)) & 0xff);
        }
        return b;
    }
}
```

`n & 0xff`로 부호 확장을 막는 것이 핵심입니다. 자바 `byte`는 부호가 있어, `0x80` 이상의 바이트를 `int`로 올리면 음수로 확장됩니다. `& 0xff`로 하위 8비트만 남겨 부호 없는 값으로 다룹니다.


## 2. JavaClassExecuter — 전체를 묶는 입구

> `JavaClassExecuter`가 치환·적재·실행을 한 메서드로 묶습니다. 외부 바이트를 받아 `HackSystem`으로 치환하고, 새 로더로 적재해 리플렉션으로 실행한 뒤 버퍼의 결과를 반환합니다.

`JavaClassExecuter`는 앞에서 만든 컴포넌트를 묶는 입구입니다.

```java
public class JavaClassExecuter {
    public static String execute(byte[] classByte) {
        // 1. HackSystem 버퍼 초기화
        HackSystem.clearBuffer();
        // 2. 상수 풀의 System 을 HackSystem 으로 치환
        ClassModifier cm = new ClassModifier(classByte);
        byte[] modiBytes = cm.modifyUTF8Constant("java/lang/System", "HackSystem");
        // 3. 새 HotSwapClassLoader 인스턴스로 적재 (핫스왑 — 04-03 참조)
        HotSwapClassLoader loader = new HotSwapClassLoader();
        Class<?> clazz = loader.loadByte(modiBytes);
        try {
            // 4. 리플렉션으로 main(String[]) 실행
            Method method = clazz.getMethod("main", new Class[]{String[].class});
            method.invoke(null, new String[]{null});
        } catch (Throwable e) {
            e.printStackTrace(HackSystem.out);   // 예외도 버퍼로
        }
        // 5. HackSystem 버퍼에 쌓인 출력을 문자열로 반환
        return HackSystem.getBufferString();
    }
}
```

흐름은 다섯 단계입니다. 버퍼를 비우고(1), 상수 풀을 치환하고(2), 새 로더로 적재하고(3), 리플렉션으로 `main`을 실행하고(4), 버퍼의 결과를 반환합니다(5). 실행 중 클래스가 호출하는 `System.out`은 치환 덕에 `HackSystem.out`이 되어 버퍼에 쌓이고, 그 내용이 호출자에게 돌아갑니다.

마지막으로 JSP 페이지가 이 입구를 호출하면 웹에서 코드를 실행할 수 있습니다. 브라우저에서 클래스 바이트를 업로드하면 `JavaClassExecuter.execute(bytes)`가 실행해 결과 문자열을 화면에 출력합니다. 온라인 코드 실행기가 완성됩니다.


## 3. 종합 — 3부 전체가 이 실습에 모인다

> 이 한 실습에 클래스 파일 구조·클래스 로더·바이트코드 조작·동적 적재가 모두 동원됩니다. "바꿀 수 없는 것을 바이트 한 곳으로 우회한다"가 관통하는 발상입니다.

9.4 마치며는 3부 전체를 닫습니다. 이 원격 실행 실습 하나에 7·8·9장이 모두 모입니다.

1. [클래스 파일 구조](../ch06_class-file/01-01.클래스%20파일%20구조.md)의 상수 풀 — 치환의 대상이자 길이 보정의 근거.
2. [클래스 로더](./04-01.톰캣의%20클래스%20로더%20아키텍처.md)의 동일성과 핫스왑 — 매번 새 로더로 반복 실행.
3. 바이트코드 조작 — `System`을 `HackSystem`으로 갈아끼우는 치환.
4. 동적 적재(`defineClass`) — 만든 바이트를 실행 가능한 클래스로.

관통하는 발상은 *"바꿀 수 없는 것을 바이트 한 곳으로 우회한다"*입니다. `System`의 동작은 못 바꾸지만, 클래스가 그것을 참조하는 *상수 풀 문자열*은 바꿀 수 있습니다.

실무로 이어 보면, 이 발상이 Java Agent·APM(Application Performance Monitoring)·바이트코드 계측(instrumentation)·핫 리로드의 토대입니다. 실행 중인 클래스의 바이트코드를 가로채 변형하는 기술이 모두 같은 뿌리에서 나옵니다. 다만 운영 환경에서 임의 바이트코드를 받아 실행하는 것은 *심각한 보안 위험*이므로, 이 실습은 학습·격리 환경에서만 다뤄야 합니다.


## 4. 면접 대비 요약

> 핵심은 "Utf8 길이 필드까지 보정", "JavaClassExecuter가 치환·적재·실행 묶음", "이 발상이 Java Agent의 토대"입니다.

### 한 줄 정의

바이트코드 치환 실행이란 상수 풀의 Utf8 항목에서 `System` 문자열을 `HackSystem`으로 바꾸되 길이 필드까지 보정하고, 새 로더로 적재해 실행한 뒤 가로챈 출력을 돌려주는 기술을 말합니다.

### 핵심 포인트 3가지

1. 상수 풀 Utf8 항목은 앞 2바이트가 길이라, 문자열을 줄이면 길이 필드(16→10)도 고치고 뒤 바이트를 당겨야 합니다.
2. `ByteUtils`는 `& 0xff`로 부호 확장을 막으며 바이트와 정수를 변환해 길이 보정을 처리합니다.
3. `JavaClassExecuter`가 버퍼 초기화·치환·새 로더 적재·리플렉션 실행·버퍼 반환을 한 메서드로 묶으며, 이 발상이 Java Agent·바이트코드 계측의 토대입니다.

### 면접에서 받을 만한 질문

1. 상수 풀 문자열을 줄일 때 왜 길이 필드를 함께 고쳐야 합니까?
2. `ByteUtils`에서 `& 0xff`가 필요한 이유는 무엇입니까?
3. 이 바이트코드 조작 기술이 실무의 어디에 쓰입니까?

> 세 질문에 *먼저 자답한 뒤* 아래 §정답으로 내려갑니다.


## 정답 (자답 후 펼치기)

> 위 §면접에서 받을 만한 질문의 3개에 *먼저 자답한 뒤* 아래를 읽으세요.

### 정답 1 — 길이 필드 보정

상수 풀의 `CONSTANT_Utf8_info`는 앞 2바이트가 문자열 길이(`length`)이고 그 뒤가 실제 바이트이기 때문입니다. `"java/lang/System"`(16바이트)을 `"HackSystem"`(10바이트)으로 바꾸면 `length`도 16→10으로 고치고, 6바이트 줄어든 만큼 그 뒤 모든 바이트를 앞으로 당겨야 합니다. 길이 필드를 안 고치면 JVM이 항목 경계를 잘못 읽어 클래스 파일이 깨집니다.

### 정답 2 — & 0xff의 이유

자바 `byte`는 부호가 있어, `0x80` 이상의 바이트를 `int`로 올리면 음수로 부호 확장되기 때문입니다. `& 0xff`로 하위 8비트만 남겨 부호 없는 값으로 다뤄야 길이 같은 수치를 올바르게 계산할 수 있습니다.

### 정답 3 — 실무 활용처

Java Agent·APM·바이트코드 계측(instrumentation)·핫 리로드입니다. 실행 중인 클래스의 바이트코드를 가로채 변형하는 기술이 모두 이 발상에서 나옵니다. 다만 임의 바이트코드를 받아 실행하는 것은 심각한 보안 위험이라, 운영이 아닌 학습·격리 환경에서만 다뤄야 합니다.


## 핵심 개념 체크리스트

- [ ] Utf8 항목의 길이 필드 보정이 왜 필요한지 설명할 수 있는가?
- [ ] `& 0xff`로 부호 확장을 막는 이유를 아는가?
- [ ] `JavaClassExecuter`의 다섯 단계 흐름을 말할 수 있는가?
- [ ] 이 실습이 3부의 어떤 지식들을 종합하는지 아는가?
- [ ] 바이트코드 조작이 Java Agent의 토대임과 그 보안 위험을 아는가?


## 관련 문서

> 이 글로 9장과 3부 전체가 마무리됩니다. 실습이 종합한 클래스 파일 구조·클래스 로더가 앞을 받칩니다.

- [04-03. 실전 — 원격 실행 기능 설계](./04-03.실전%20—%20원격%20실행%20기능%20설계.md) — 이 치환의 설계와 아이디어
- [04-01. 톰캣의 클래스 로더 아키텍처](./04-01.톰캣의%20클래스%20로더%20아키텍처.md) — 핫스왑의 토대가 되는 로더 동일성
- [클래스 파일 구조](../ch06_class-file/01-01.클래스%20파일%20구조.md) § "상수 풀" — Utf8 항목 포맷의 정본
