---
title: 07-점검.핵심 질문과 답
tags: [jenkins, review, qna]
status: draft
related:
  - ../README.md
updated: 2026-05-28
---

# 7단계 점검 — Pipeline 내구성과 가용성 핵심 질문

---

> 이 단계를 마쳤다면 다음 질문에 답할 수 있어야 한다.
> 다루는 문서: 07-01.Pipeline 내구성과 재기동, 07-02.가용성 테스트 시나리오

## Q1: CPS(Continuation-Passing Style)란 무엇이며, Pipeline resume에서 어떤 역할을 하는가?

CPS는 함수가 반환값을 직접 돌려주는 대신 "다음에 실행할 함수(continuation)"를 인자로 받아 호출하는 방식이다.

- **일반 Groovy**: JVM 스택에 실행 상태가 존재 → 재시작 시 스택이 사라지면 resume 불가
- **CPS 변환된 코드**: 실행 중간 상태를 FlowNode 단위로 직렬화 → 디스크에 저장 → 역직렬화 후 이전 지점부터 재개

Jenkins Pipeline이 CPS를 사용하는 이유가 여기에 있다. Controller가 재시작되어도 각 FlowNode의 상태를 복원해 이전 실행 지점부터 재개할 수 있다.

### 심화

`@NonCPS`를 붙인 메서드에서 `sh` step을 호출하면 어떻게 되는가?

## Q2: Resumable step과 Nonresumable step의 차이는?

| 구분 | 대표 step | 동작 |
|------|-----------|------|
| Resumable | `sh`, `input` | Controller 재시작 후 Agent 프로세스가 살아있으면 재연결해 이어간다 |
| Nonresumable | `checkout`, `junit` | 재시작 중 실행 중이었다면 해당 step을 처음부터 다시 실행한다 |

- `sh`: durable-task 플러그인이 Agent의 실제 프로세스를 추적 파일로 관리한다.
- `input`: 사람이 승인을 기다리는 단계이므로 재시작과 무관하게 대기 상태를 유지한다.
- `checkout`, `junit`: 빠르게 완료되거나 원자적으로 재실행하는 것이 더 안전하다고 설계됐다.

### 심화

`sh` step이 Resumable임에도 불구하고 Agent가 오프라인이 된 경우에는 어떻게 되는가?

## Q3: `disableResume()`은 언제 사용하며, 사용하지 않으면 어떤 위험이 있는가?

배포, 결제 API 호출, 외부 시스템 상태 변경처럼 **멱등성을 보장할 수 없는** 파이프라인에서 사용한다.

```groovy
pipeline {
    options { disableResume() }
    // ...
}
```

- **위험**: Controller 재시작 후 resume이 발생하면 이미 실행된 배포가 다시 실행될 수 있다.
- **효과**: `disableResume()` 선언 시 Controller 재시작 후 해당 파이프라인은 resume을 시도하지 않고 실패 상태로 종료된다. 재실행은 사람이 직접 판단한다.

### 심화

`disableResume()`과 `disableRestartFromStage()`의 차이는?

## Q4: Durability 세 레벨의 트레이드오프를 설명하라.

| 레벨 | 동작 | 트레이드오프 |
|------|------|-------------|
| `MAX_SURVIVABILITY` | 모든 FlowNode를 즉시 동기 쓰기 | 최대 복구 가능, I/O 부하 최대 |
| `SURVIVABLE_NONATOMIC` | 기본값. 대부분 복구 가능 | 성능과 내구성의 균형. 극히 드문 타이밍에 일부 상태 유실 가능 |
| `PERFORMANCE_OPTIMIZED` | 디스크 쓰기 최소화 | 빌드 속도 최대, Controller crash 후 해당 파이프라인 복구 불가 |

- `PERFORMANCE_OPTIMIZED`는 빌드 속도가 중요하고 재실행이 가능한 CI 빌드에 적합하다.

### 심화

파이프라인별로 Durability를 다르게 설정하려면 어떻게 하는가?

## Q5: Controller 장애 시 실행 중인 빌드는 어떻게 되는가?

| 상태 | 결과 |
|------|------|
| Agent에서 실행 중인 빌드 | 즉시 죽지 않지만, 결과를 Controller에 보고할 수 없어 로그와 아티팩트가 유실된다 |
| 빌드 큐의 대기 빌드 | Controller 메모리에 존재하므로 대기 중인 빌드 전부 소실된다 |

복구 절차는 다음과 같다:

1. `JENKINS_HOME` 상태 확인
2. 정상이면 재시작 → Durability 설정에 따라 미완료 빌드 resume 시도
3. 손상이면 백업 복원
4. 미완료 빌드는 수동 재트리거

### 심화

`JENKINS_HOME/jobs/{job}/builds/{n}/log` 파일이 존재하는데 빌드가 ABORTED 상태로 남은 경우, 이 빌드를 정상 완료로 표시하려면 어떻게 하는가?

## Q6: "Restart from Stage"와 자동 resume의 차이는?

| 구분 | 자동 resume | Restart from Stage |
|------|-------------|-------------------|
| 트리거 | Controller 재시작 후 자동 | 사람이 UI에서 수동 선택 |
| 빌드 번호 | 동일 유지 | 새 빌드 번호 생성 |
| 시작 지점 | 중단된 지점 | 선택한 stage부터 |
| 이전 데이터 | 복원 | 이전 빌드의 환경 변수와 stash 재사용 |
| 사용 범위 | 모든 파이프라인 | Declarative Pipeline 전용 |

자동 resume은 **장애 복구**, Restart from Stage는 **부분 재실행**이다.

### 심화

Scripted Pipeline에서 Declarative Pipeline의 "Restart from Stage"와 동등한 기능을 구현하려면 어떻게 접근해야 하는가?

## Q7: stash/unstash 패턴은 언제 사용하며 한계는 무엇인가?

K8s 환경에서 stage 간 agent Pod가 교체될 수 있으므로 workspace가 보존되지 않는다. `stash`/`unstash`는 이 문제를 해결한다.

- `stash`: 지정한 파일을 Controller에 저장한다.
- `unstash`: 새 Pod에서 Controller로부터 파일을 복원한다.
- 한계: Controller에 저장되므로 대용량 파일(수백 MB 이상)에는 부적합하다. 대안으로 Nexus, S3 같은 외부 아티팩트 저장소를 사용한다.

### 심화

`stash`에 저장된 데이터는 빌드가 삭제되면 함께 사라지는가? 빌드 보존 정책과 stash 수명의 관계는?
